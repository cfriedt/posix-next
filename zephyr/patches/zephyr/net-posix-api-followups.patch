diff --git a/subsys/logging/backends/log_backend_net.c b/subsys/logging/backends/log_backend_net.c
index 1c14971936b..0d90e655604 100644
--- a/subsys/logging/backends/log_backend_net.c
+++ b/subsys/logging/backends/log_backend_net.c
@@ -32,7 +32,7 @@ static char dev_hostname[MAX_HOSTNAME_LEN + 1];

 static uint8_t output_buf[CONFIG_LOG_BACKEND_NET_MAX_BUF_SIZE];
 static bool net_init_done;
-struct sockaddr server_addr;
+static struct zsock_sockaddr server_addr;
 static bool panic_mode;
 static uint32_t log_format_current = CONFIG_LOG_BACKEND_NET_OUTPUT_DEFAULT;

@@ -46,8 +46,8 @@ static struct log_backend_net_ctx {
 static int line_out(uint8_t *data, size_t length, void *output_ctx)
 {
 	struct log_backend_net_ctx *ctx = (struct log_backend_net_ctx *)output_ctx;
-	struct msghdr msg = { 0 };
-	struct iovec io_vector[2];
+	struct zsock_msghdr msg = { 0 };
+	struct zsock_iovec io_vector[2];
 	int pos = 0;
 	int ret;

@@ -91,15 +91,15 @@ LOG_OUTPUT_DEFINE(log_output_net, line_out, output_buf, sizeof(output_buf));

 static int do_net_init(struct log_backend_net_ctx *ctx)
 {
-	socklen_t server_addr_len = 0;
-	int ret, proto = IPPROTO_UDP, type = SOCK_DGRAM;
+	zsock_socklen_t server_addr_len = 0;
+	int ret, proto = ZSOCK_IPPROTO_UDP, type = ZSOCK_SOCK_DGRAM;

-	if (IS_ENABLED(CONFIG_NET_IPV4) && server_addr.sa_family == AF_INET) {
-		server_addr_len = sizeof(struct sockaddr_in);
+	if (IS_ENABLED(CONFIG_NET_IPV4) && server_addr.sa_family == ZSOCK_AF_INET) {
+		server_addr_len = sizeof(struct zsock_sockaddr_in);
 	}

-	if (IS_ENABLED(CONFIG_NET_IPV6) && server_addr.sa_family == AF_INET6) {
-		server_addr_len = sizeof(struct sockaddr_in6);
+	if (IS_ENABLED(CONFIG_NET_IPV6) && server_addr.sa_family == ZSOCK_AF_INET6) {
+		server_addr_len = sizeof(struct zsock_sockaddr_in6);
 	}

 	if (server_addr_len == 0) {
@@ -108,8 +108,8 @@ static int do_net_init(struct log_backend_net_ctx *ctx)
 	}

 	if (ctx->is_tcp) {
-		proto = IPPROTO_TCP;
-		type = SOCK_STREAM;
+		proto = ZSOCK_IPPROTO_TCP;
+		type = ZSOCK_SOCK_STREAM;
 	}

 	ret = zsock_socket(server_addr.sa_family, type, proto);
@@ -219,7 +219,7 @@ bool log_backend_net_set_addr(const char *addr)
 		return ret;
 	}

-	net_sin(&server_addr)->sin_port = htons(514);
+	net_sin(&server_addr)->sin_port = zsock_htons(514);

 	ret = net_ipaddr_parse(addr, strlen(addr), &server_addr);
 	if (!ret) {
@@ -230,7 +230,7 @@ bool log_backend_net_set_addr(const char *addr)
 	return ret;
 }

-bool log_backend_net_set_ip(const struct sockaddr *addr)
+bool log_backend_net_set_ip(const struct zsock_sockaddr *addr)
 {
 	bool ret = check_net_init_done();

@@ -238,8 +238,8 @@ bool log_backend_net_set_ip(const struct sockaddr *addr)
 		return ret;
 	}

-	if ((IS_ENABLED(CONFIG_NET_IPV4) && addr->sa_family == AF_INET) ||
-	    (IS_ENABLED(CONFIG_NET_IPV6) && addr->sa_family == AF_INET6)) {
+	if ((IS_ENABLED(CONFIG_NET_IPV4) && addr->sa_family == ZSOCK_AF_INET) ||
+	    (IS_ENABLED(CONFIG_NET_IPV6) && addr->sa_family == ZSOCK_AF_INET6)) {
 		memcpy(&server_addr, addr, sizeof(server_addr));

 		net_port_set_default(&server_addr, 514);
diff --git a/subsys/tracing/ctf/ctf_top.c b/subsys/tracing/ctf/ctf_top.c
index 65c41062cfe..2b3f5d78297 100644
--- a/subsys/tracing/ctf/ctf_top.c
+++ b/subsys/tracing/ctf/ctf_top.c
@@ -965,14 +965,14 @@ void sys_trace_socket_shutdown_exit(int sock, int ret)
 	ctf_top_socket_shutdown_exit(sock, ret);
 }

-void sys_trace_socket_bind_enter(int sock, const struct sockaddr *addr, size_t addrlen)
+void sys_trace_socket_bind_enter(int sock, const struct zsock_sockaddr *addr, size_t addrlen)
 {
 	ctf_net_bounded_string_t addr_str;

 	(void)net_addr_ntop(addr->sa_family, &net_sin(addr)->sin_addr, addr_str.buf,
 			    sizeof(addr_str.buf));

-	ctf_top_socket_bind_enter(sock, addr_str, addrlen, ntohs(net_sin(addr)->sin_port));
+	ctf_top_socket_bind_enter(sock, addr_str, addrlen, zsock_ntohs(net_sin(addr)->sin_port));
 }

 void sys_trace_socket_bind_exit(int sock, int ret)
@@ -980,7 +980,7 @@ void sys_trace_socket_bind_exit(int sock, int ret)
 	ctf_top_socket_bind_exit(sock, ret);
 }

-void sys_trace_socket_connect_enter(int sock, const struct sockaddr *addr, size_t addrlen)
+void sys_trace_socket_connect_enter(int sock, const struct zsock_sockaddr *addr, size_t addrlen)
 {
 	ctf_net_bounded_string_t addr_str;

@@ -1010,7 +1010,7 @@ void sys_trace_socket_accept_enter(int sock)
 	ctf_top_socket_accept_enter(sock);
 }

-void sys_trace_socket_accept_exit(int sock, const struct sockaddr *addr, const uint32_t *addrlen,
+void sys_trace_socket_accept_exit(int sock, const struct zsock_sockaddr *addr, const uint32_t *addrlen,
 				  int ret)
 {
 	ctf_net_bounded_string_t addr_str = {"unknown"};
@@ -1031,7 +1031,7 @@ void sys_trace_socket_accept_exit(int sock, const struct sockaddr *addr, const u
 }

 void sys_trace_socket_sendto_enter(int sock, int len, int flags,
-				   const struct sockaddr *dest_addr, uint32_t addrlen)
+				   const struct zsock_sockaddr *dest_addr, uint32_t addrlen)
 {
 	ctf_net_bounded_string_t addr_str = {"unknown"};

@@ -1048,7 +1048,7 @@ void sys_trace_socket_sendto_exit(int sock, int ret)
 	ctf_top_socket_sendto_exit(sock, ret);
 }

-void sys_trace_socket_sendmsg_enter(int sock, const struct msghdr *msg, int flags)
+void sys_trace_socket_sendmsg_enter(int sock, const struct zsock_msghdr *msg, int flags)
 {
 	ctf_net_bounded_string_t addr = {"unknown"};
 	uint32_t len = 0;
@@ -1058,8 +1058,8 @@ void sys_trace_socket_sendmsg_enter(int sock, const struct msghdr *msg, int flag
 	}

 	if (msg->msg_name != NULL) {
-		(void)net_addr_ntop(((struct sockaddr *)msg->msg_name)->sa_family,
-				    &net_sin((struct sockaddr *)msg->msg_name)->sin_addr, addr.buf,
+		(void)net_addr_ntop(((struct zsock_sockaddr *)msg->msg_name)->sa_family,
+				    &net_sin((struct zsock_sockaddr *)msg->msg_name)->sin_addr, addr.buf,
 				    sizeof(addr.buf));
 	}

@@ -1072,13 +1072,13 @@ void sys_trace_socket_sendmsg_exit(int sock, int ret)
 }

 void sys_trace_socket_recvfrom_enter(int sock, int max_len, int flags,
-				     struct sockaddr *addr, uint32_t *addrlen)
+				     struct zsock_sockaddr *addr, uint32_t *addrlen)
 {
 	ctf_top_socket_recvfrom_enter(sock, max_len, flags, (uint32_t)(uintptr_t)addr,
 				      (uint32_t)(uintptr_t)addrlen);
 }

-void sys_trace_socket_recvfrom_exit(int sock, const struct sockaddr *src_addr,
+void sys_trace_socket_recvfrom_exit(int sock, const struct zsock_sockaddr *src_addr,
 				    const uint32_t *addrlen, int ret)
 {
 	ctf_net_bounded_string_t addr_str = {"unknown"};
@@ -1096,7 +1096,7 @@ void sys_trace_socket_recvfrom_exit(int sock, const struct sockaddr *src_addr,
 	ctf_top_socket_recvfrom_exit(sock, addr_str, len, ret);
 }

-void sys_trace_socket_recvmsg_enter(int sock, const struct msghdr *msg, int flags)
+void sys_trace_socket_recvmsg_enter(int sock, const struct zsock_msghdr *msg, int flags)
 {
 	uint32_t max_len = 0;

@@ -1107,7 +1107,7 @@ void sys_trace_socket_recvmsg_enter(int sock, const struct msghdr *msg, int flag
 	ctf_top_socket_recvmsg_enter(sock, (uint32_t)(uintptr_t)msg, max_len, flags);
 }

-void sys_trace_socket_recvmsg_exit(int sock, const struct msghdr *msg, int ret)
+void sys_trace_socket_recvmsg_exit(int sock, const struct zsock_msghdr *msg, int ret)
 {
 	uint32_t len = 0;
 	ctf_net_bounded_string_t addr = {"unknown"};
@@ -1117,8 +1117,8 @@ void sys_trace_socket_recvmsg_exit(int sock, const struct msghdr *msg, int ret)
 	}

 	if (msg->msg_name != NULL) {
-		(void)net_addr_ntop(((struct sockaddr *)msg->msg_name)->sa_family,
-				    &net_sin((struct sockaddr *)msg->msg_name)->sin_addr, addr.buf,
+		(void)net_addr_ntop(((struct zsock_sockaddr *)msg->msg_name)->sa_family,
+				    &net_sin((struct zsock_sockaddr *)msg->msg_name)->sin_addr, addr.buf,
 				    sizeof(addr.buf));
 	}

@@ -1196,7 +1196,7 @@ void sys_trace_socket_getpeername_enter(int sock)
 	ctf_top_socket_getpeername_enter(sock);
 }

-void sys_trace_socket_getpeername_exit(int sock,  struct sockaddr *addr,
+void sys_trace_socket_getpeername_exit(int sock,  struct zsock_sockaddr *addr,
 				       const uint32_t *addrlen, int ret)
 {
 	ctf_net_bounded_string_t addr_str;
@@ -1212,7 +1212,7 @@ void sys_trace_socket_getsockname_enter(int sock)
 	ctf_top_socket_getsockname_enter(sock);
 }

-void sys_trace_socket_getsockname_exit(int sock, const struct sockaddr *addr,
+void sys_trace_socket_getsockname_exit(int sock, const struct zsock_sockaddr *addr,
 				       const uint32_t *addrlen, int ret)
 {
 	ctf_net_bounded_string_t addr_str;
diff --git a/subsys/tracing/ctf/ctf_top.h b/subsys/tracing/ctf/ctf_top.h
index 325bd711b01..43f37a210d3 100644
--- a/subsys/tracing/ctf/ctf_top.h
+++ b/subsys/tracing/ctf/ctf_top.h
@@ -18,7 +18,7 @@

 /* Increase the string length to be able to store IPv4/6 address */
 #if defined(CONFIG_NET_IPV6)
-#define CTF_NET_MAX_STRING_LEN INET6_ADDRSTRLEN
+#define CTF_NET_MAX_STRING_LEN ZSOCK_INET6_ADDRSTRLEN
 #else
 #define CTF_NET_MAX_STRING_LEN CTF_MAX_STRING_LEN
 #endif
diff --git a/tests/net/dhcpv4/server/src/main.c b/tests/net/dhcpv4/server/src/main.c
index 49478c991d2..bac42ff6be6 100644
--- a/tests/net/dhcpv4/server/src/main.c
+++ b/tests/net/dhcpv4/server/src/main.c
@@ -21,12 +21,12 @@
 static uint8_t server_mac_addr[] = { 0x00, 0x00, 0x5E, 0x00, 0x53, 0x01 };
 static uint8_t client_mac_addr[] = { 0x00, 0x00, 0x5E, 0x00, 0x53, 0x02 };

-static struct in_addr server_addr = { { { 192, 0, 2, 1 } } };
-static struct in_addr netmask = { { { 255, 255, 255, 0 } } };
-static struct in_addr test_base_addr = { { { 192, 0, 2, 10 } } };
+static struct zsock_in_addr server_addr = { { { 192, 0, 2, 1 } } };
+static struct zsock_in_addr netmask = { { { 255, 255, 255, 0 } } };
+static struct zsock_in_addr test_base_addr = { { { 192, 0, 2, 10 } } };

 /* Only to test Inform. */
-static struct in_addr client_addr_static = { { { 192, 0, 2, 2 } } };
+static struct zsock_in_addr client_addr_static = { { { 192, 0, 2, 2 } } };

 typedef void (*test_dhcpv4_server_fn_t)(struct net_if *iface,
 					struct net_pkt *pkt);
@@ -36,8 +36,8 @@ static struct test_dhcpv4_server_ctx {
 	struct net_if *iface;
 	struct k_sem test_proceed;
 	struct net_pkt *pkt;
-	struct in_addr assigned_ip;
-	struct in_addr declined_ip;
+	struct zsock_in_addr assigned_ip;
+	struct zsock_in_addr declined_ip;

 	/* Request params */
 	const char *client_id;
@@ -78,11 +78,11 @@ static void send_icmp_echo_reply(struct net_pkt *pkt,
 			     NET_ICMPH_LEN;

 	reply = net_pkt_alloc_with_buffer(net_pkt_iface(pkt), payload_len,
-					  AF_INET, IPPROTO_ICMP, K_FOREVER);
+					  ZSOCK_AF_INET, ZSOCK_IPPROTO_ICMP, K_FOREVER);
 	zassert_not_null(reply, "Failed to allocate echo reply");

-	zassert_ok(net_ipv4_create(reply, (struct in_addr *)ipv4_hdr->dst,
-				   (struct in_addr *)ipv4_hdr->src),
+	zassert_ok(net_ipv4_create(reply, (struct zsock_in_addr *)ipv4_hdr->dst,
+				   (struct zsock_in_addr *)ipv4_hdr->src),
 		   "Failed to create IPv4 header");

 	zassert_ok(net_icmpv4_create(reply, NET_ICMPV4_ECHO_REPLY, 0),
@@ -91,7 +91,7 @@ static void send_icmp_echo_reply(struct net_pkt *pkt,
 		   "Failed to copy payload");

 	net_pkt_cursor_init(reply);
-	net_ipv4_finalize(reply, IPPROTO_ICMP);
+	net_ipv4_finalize(reply, ZSOCK_IPPROTO_ICMP);

 	zassert_ok(net_recv_data(test_ctx.iface, reply), "Failed to receive data");
 }
@@ -104,11 +104,11 @@ static int server_send(const struct device *dev, struct net_pkt *pkt)
 	ipv4_hdr = (struct net_ipv4_hdr *)net_pkt_get_data(pkt, &ipv4_access);
 	zassert_not_null(ipv4_hdr, "Failed to access IPv4 header.");

-	if (ipv4_hdr->proto == IPPROTO_ICMP) {
+	if (ipv4_hdr->proto == ZSOCK_IPPROTO_ICMP) {
 		if (test_ctx.send_echo_reply) {
 			test_ctx.send_echo_reply = false;
 			memcpy(&test_ctx.declined_ip, ipv4_hdr->dst,
-			       sizeof(struct in_addr));
+			       sizeof(struct zsock_in_addr));
 			send_icmp_echo_reply(pkt, ipv4_hdr);
 		}

@@ -143,32 +143,32 @@ static void test_pkt_free(void)
 }

 static void client_prepare_test_msg(
-	const struct in_addr *src_addr, const struct in_addr *dst_addr,
-	enum net_dhcpv4_msg_type type, const struct in_addr *server_id,
-	const struct in_addr *requested_ip, const struct in_addr *ciaddr)
+	const struct zsock_in_addr *src_addr, const struct zsock_in_addr *dst_addr,
+	enum net_dhcpv4_msg_type type, const struct zsock_in_addr *server_id,
+	const struct zsock_in_addr *requested_ip, const struct zsock_in_addr *ciaddr)
 {
 	struct dhcp_msg msg = { 0 };
 	uint8_t empty_buf[SIZE_OF_FILE] = { 0 };
 	struct net_pkt *pkt;

-	pkt = net_pkt_alloc_with_buffer(test_ctx.iface, NET_IPV4_MTU, AF_INET,
-					IPPROTO_UDP, K_FOREVER);
+	pkt = net_pkt_alloc_with_buffer(test_ctx.iface, NET_IPV4_MTU, ZSOCK_AF_INET,
+					ZSOCK_IPPROTO_UDP, K_FOREVER);
 	zassert_not_null(pkt, "Failed to allocate packet");

 	net_pkt_set_ipv4_ttl(pkt, 1);

 	zassert_ok(net_ipv4_create(pkt, src_addr, dst_addr),
 		   "Failed to create IPv4 header");
-	zassert_ok(net_udp_create(pkt, htons(DHCPV4_CLIENT_PORT),
-				  htons(DHCPV4_SERVER_PORT)),
+	zassert_ok(net_udp_create(pkt, zsock_htons(DHCPV4_CLIENT_PORT),
+				  zsock_htons(DHCPV4_SERVER_PORT)),
 		   "Failed to create UDP header");

 	msg.op = DHCPV4_MSG_BOOT_REQUEST;
 	msg.htype = HARDWARE_ETHERNET_TYPE;
 	msg.hlen = sizeof(client_mac_addr);
-	msg.xid = htonl(TEST_XID);
+	msg.xid = zsock_htonl(TEST_XID);
 	if (test_ctx.broadcast) {
-		msg.flags = htons(DHCPV4_MSG_BROADCAST);
+		msg.flags = zsock_htons(DHCPV4_MSG_BROADCAST);
 	}

 	if (ciaddr) {
@@ -223,7 +223,7 @@ static void client_prepare_test_msg(
 	net_pkt_write_u8(pkt, DHCPV4_OPTIONS_END);

 	net_pkt_cursor_init(pkt);
-	net_ipv4_finalize(pkt, IPPROTO_UDP);
+	net_ipv4_finalize(pkt, ZSOCK_IPPROTO_UDP);

 	zassert_ok(net_recv_data(test_ctx.iface, pkt), "Failed to receive data");
 }
@@ -372,14 +372,14 @@ static void get_reserved_cb(struct net_if *iface,
 			    struct dhcpv4_addr_slot *lease,
 			    void *user_data)
 {
-	struct in_addr *reserved = user_data;
+	struct zsock_in_addr *reserved = user_data;

 	if (lease->state == DHCPV4_SERVER_ADDR_RESERVED) {
 		reserved->s_addr = lease->addr.s_addr;
 	}
 }

-static void get_reserved_address(struct in_addr *reserved)
+static void get_reserved_address(struct zsock_in_addr *reserved)
 {
 	int ret;

@@ -471,7 +471,7 @@ static void verify_option(struct net_pkt *pkt, uint8_t opt_type,
 static void verify_option_uint32(struct net_pkt *pkt, uint8_t opt_type,
 				 uint32_t optval)
 {
-	optval = htonl(optval);
+	optval = zsock_htonl(optval);

 	verify_option(pkt, opt_type, &optval, sizeof(optval));
 }
@@ -508,22 +508,22 @@ static void verify_offer(bool broadcast)

 	/* IPv4 */
 	zassert_mem_equal(ipv4_hdr->src, server_addr.s4_addr,
-			  sizeof(struct in_addr), "Incorrect source address");
+			  sizeof(struct zsock_in_addr), "Incorrect source address");
 	if (broadcast) {
 		zassert_mem_equal(ipv4_hdr->dst, net_ipv4_broadcast_address(),
-				  sizeof(struct in_addr),
+				  sizeof(struct zsock_in_addr),
 				  "Destination should be broadcast");
 	} else {
 		zassert_mem_equal(ipv4_hdr->dst, msg->yiaddr,
-				  sizeof(struct in_addr),
+				  sizeof(struct zsock_in_addr),
 				  "Destination should match address lease");
 	}
-	zassert_equal(ipv4_hdr->proto, IPPROTO_UDP, "Wrong protocol");
+	zassert_equal(ipv4_hdr->proto, ZSOCK_IPPROTO_UDP, "Wrong protocol");

 	/* UDP */
-	zassert_equal(udp_hdr->src_port, htons(DHCPV4_SERVER_PORT),
+	zassert_equal(udp_hdr->src_port, zsock_htons(DHCPV4_SERVER_PORT),
 		      "Wrong source port");
-	zassert_equal(udp_hdr->dst_port, htons(DHCPV4_CLIENT_PORT),
+	zassert_equal(udp_hdr->dst_port, zsock_htons(DHCPV4_CLIENT_PORT),
 		      "Wrong client port");

 	/* DHCPv4 */
@@ -531,7 +531,7 @@ static void verify_offer(bool broadcast)
 	zassert_equal(msg->htype, HARDWARE_ETHERNET_TYPE, "Incorrect %s value", "htype");
 	zassert_equal(msg->hlen, sizeof(client_mac_addr), "Incorrect %s value", "hlen");
 	zassert_equal(msg->hops, 0, "Incorrect %s value", "hops");
-	zassert_equal(msg->xid, htonl(TEST_XID), "Incorrect %s value", "xid");
+	zassert_equal(msg->xid, zsock_htonl(TEST_XID), "Incorrect %s value", "xid");
 	zassert_equal(msg->secs, 0, "Incorrect %s value", "secs");
 	zassert_equal(sys_get_be32(msg->ciaddr), 0, "Incorrect %s value", "ciaddr");
 	zassert_true((sys_get_be32(msg->yiaddr) >=
@@ -542,7 +542,7 @@ static void verify_offer(bool broadcast)
 		     "Assigned DHCP address outside of address pool");
 	zassert_equal(sys_get_be32(msg->siaddr), 0, "Incorrect %s value", "siaddr");
 	if (broadcast) {
-		zassert_equal(msg->flags, htons(DHCPV4_MSG_BROADCAST),
+		zassert_equal(msg->flags, zsock_htons(DHCPV4_MSG_BROADCAST),
 			      "Incorrect %s value", "flags");
 	} else {
 		zassert_equal(msg->flags, 0, "Incorrect %s value", "flags");
@@ -551,7 +551,7 @@ static void verify_offer(bool broadcast)
 	zassert_mem_equal(msg->chaddr, client_mac_addr, sizeof(client_mac_addr),
 			  "Incorrect %s value", "chaddr");

-	memcpy(&test_ctx.assigned_ip, msg->yiaddr, sizeof(struct in_addr));
+	memcpy(&test_ctx.assigned_ip, msg->yiaddr, sizeof(struct zsock_in_addr));

 	ret = net_pkt_skip(pkt, SIZE_OF_SNAME + SIZE_OF_FILE);
 	zassert_ok(ret, "DHCP Offer too short");
@@ -566,11 +566,11 @@ static void verify_offer(bool broadcast)
 	verify_option_uint8(pkt, DHCPV4_OPTIONS_MSG_TYPE,
 			    NET_DHCPV4_MSG_TYPE_OFFER);
 	verify_option(pkt, DHCPV4_OPTIONS_SERVER_ID, server_addr.s4_addr,
-		      sizeof(struct in_addr));
+		      sizeof(struct zsock_in_addr));
 	verify_option(pkt, DHCPV4_OPTIONS_CLIENT_ID, test_ctx.client_id,
 		      strlen(test_ctx.client_id));
 	verify_option(pkt, DHCPV4_OPTIONS_SUBNET_MASK, netmask.s4_addr,
-		      sizeof(struct in_addr));
+		      sizeof(struct zsock_in_addr));
 	verify_no_option(pkt, DHCPV4_OPTIONS_REQ_IPADDR);
 	verify_no_option(pkt, DHCPV4_OPTIONS_REQ_LIST);
 }
@@ -579,7 +579,7 @@ static void reserved_address_cb(struct net_if *iface,
 				struct dhcpv4_addr_slot *lease,
 				void *user_data)
 {
-	struct in_addr *reserved = user_data;
+	struct zsock_in_addr *reserved = user_data;

 	zassert_equal(lease->state, DHCPV4_SERVER_ADDR_RESERVED,
 		      "Wrong lease state");
@@ -587,7 +587,7 @@ static void reserved_address_cb(struct net_if *iface,
 		      "Reserved wrong address");
 }

-static void verify_reserved_address(struct in_addr *reserved)
+static void verify_reserved_address(struct zsock_in_addr *reserved)
 {
 	int ret;

@@ -613,7 +613,7 @@ ZTEST(dhcpv4_server_tests, test_discover)
  */
 ZTEST(dhcpv4_server_tests, test_discover_repeat)
 {
-	struct in_addr first_addr;
+	struct zsock_in_addr first_addr;

 	client_send_discover();
 	verify_offer(false);
@@ -682,21 +682,21 @@ static void verify_ack(bool inform, bool renew)

 	/* IPv4 */
 	zassert_mem_equal(ipv4_hdr->src, server_addr.s4_addr,
-			  sizeof(struct in_addr), "Incorrect source address");
+			  sizeof(struct zsock_in_addr), "Incorrect source address");
 	if (inform || renew) {
-		zassert_mem_equal(ipv4_hdr->dst, msg->ciaddr, sizeof(struct in_addr),
+		zassert_mem_equal(ipv4_hdr->dst, msg->ciaddr, sizeof(struct zsock_in_addr),
 				  "Destination should match client address");
 	} else {
-		zassert_mem_equal(ipv4_hdr->dst, msg->yiaddr, sizeof(struct in_addr),
+		zassert_mem_equal(ipv4_hdr->dst, msg->yiaddr, sizeof(struct zsock_in_addr),
 				  "Destination should match client address");
 	}

-	zassert_equal(ipv4_hdr->proto, IPPROTO_UDP, "Wrong protocol");
+	zassert_equal(ipv4_hdr->proto, ZSOCK_IPPROTO_UDP, "Wrong protocol");

 	/* UDP */
-	zassert_equal(udp_hdr->src_port, htons(DHCPV4_SERVER_PORT),
+	zassert_equal(udp_hdr->src_port, zsock_htons(DHCPV4_SERVER_PORT),
 		      "Wrong source port");
-	zassert_equal(udp_hdr->dst_port, htons(DHCPV4_CLIENT_PORT),
+	zassert_equal(udp_hdr->dst_port, zsock_htons(DHCPV4_CLIENT_PORT),
 		      "Wrong client port");

 	/* DHCPv4 */
@@ -704,16 +704,16 @@ static void verify_ack(bool inform, bool renew)
 	zassert_equal(msg->htype, HARDWARE_ETHERNET_TYPE, "Incorrect %s value", "htype");
 	zassert_equal(msg->hlen, sizeof(client_mac_addr), "Incorrect %s value", "hlen");
 	zassert_equal(msg->hops, 0, "Incorrect %s value", "hops");
-	zassert_equal(msg->xid, htonl(TEST_XID), "Incorrect %s value", "xid");
+	zassert_equal(msg->xid, zsock_htonl(TEST_XID), "Incorrect %s value", "xid");
 	zassert_equal(msg->secs, 0, "Incorrect %s value", "secs");

 	if (inform) {
 		zassert_mem_equal(msg->ciaddr, client_addr_static.s4_addr,
-				  sizeof(struct in_addr),
+				  sizeof(struct zsock_in_addr),
 				  "Incorrect %s value", "ciaddr");
 	} else if (renew) {
 		zassert_mem_equal(msg->ciaddr, test_ctx.assigned_ip.s4_addr,
-				  sizeof(struct in_addr),
+				  sizeof(struct zsock_in_addr),
 				  "Incorrect %s value", "ciaddr");
 	} else {
 		zassert_equal(sys_get_be32(msg->ciaddr), 0, "Incorrect %s value", "ciaddr");
@@ -723,7 +723,7 @@ static void verify_ack(bool inform, bool renew)
 		zassert_equal(sys_get_be32(msg->yiaddr), 0, "Incorrect %s value", "yiaddr");
 	} else {
 		zassert_mem_equal(msg->yiaddr, test_ctx.assigned_ip.s4_addr,
-				  sizeof(struct in_addr), "Incorrect %s value", "yiaddr");
+				  sizeof(struct zsock_in_addr), "Incorrect %s value", "yiaddr");
 	}

 	zassert_equal(sys_get_be32(msg->siaddr), 0, "Incorrect %s value", "siaddr");
@@ -733,7 +733,7 @@ static void verify_ack(bool inform, bool renew)
 			  "Incorrect %s value", "chaddr");

 	if (!inform) {
-		memcpy(&test_ctx.assigned_ip, msg->yiaddr, sizeof(struct in_addr));
+		memcpy(&test_ctx.assigned_ip, msg->yiaddr, sizeof(struct zsock_in_addr));
 	}

 	ret = net_pkt_skip(pkt, SIZE_OF_SNAME + SIZE_OF_FILE);
@@ -754,7 +754,7 @@ static void verify_ack(bool inform, bool renew)
 	verify_option_uint8(pkt, DHCPV4_OPTIONS_MSG_TYPE,
 			    NET_DHCPV4_MSG_TYPE_ACK);
 	verify_option(pkt, DHCPV4_OPTIONS_SERVER_ID, server_addr.s4_addr,
-		      sizeof(struct in_addr));
+		      sizeof(struct zsock_in_addr));
 	if (inform) {
 		verify_no_option(pkt, DHCPV4_OPTIONS_CLIENT_ID);
 	} else {
@@ -762,7 +762,7 @@ static void verify_ack(bool inform, bool renew)
 			      strlen(test_ctx.client_id));
 	}
 	verify_option(pkt, DHCPV4_OPTIONS_SUBNET_MASK, netmask.s4_addr,
-		      sizeof(struct in_addr));
+		      sizeof(struct zsock_in_addr));
 	verify_no_option(pkt, DHCPV4_OPTIONS_REQ_IPADDR);
 	verify_no_option(pkt, DHCPV4_OPTIONS_REQ_LIST);
 }
@@ -771,7 +771,7 @@ static void allocated_address_cb(struct net_if *iface,
 				 struct dhcpv4_addr_slot *lease,
 				 void *user_data)
 {
-	struct in_addr *allocated = user_data;
+	struct zsock_in_addr *allocated = user_data;

 	zassert_equal(lease->state, DHCPV4_SERVER_ADDR_ALLOCATED,
 		      "Wrong lease state");
@@ -779,7 +779,7 @@ static void allocated_address_cb(struct net_if *iface,
 		      "Reserved wrong address");
 }

-static void verify_allocated_address(struct in_addr *allocated)
+static void verify_allocated_address(struct zsock_in_addr *allocated)
 {
 	int ret;

@@ -856,7 +856,7 @@ static void declined_address_cb(struct net_if *iface,
 				struct dhcpv4_addr_slot *lease,
 				void *user_data)
 {
-	struct in_addr *declined = user_data;
+	struct zsock_in_addr *declined = user_data;

 	zassert_equal(lease->state, DHCPV4_SERVER_ADDR_DECLINED,
 		      "Wrong lease state");
@@ -864,7 +864,7 @@ static void declined_address_cb(struct net_if *iface,
 		      "Declined wrong address");
 }

-static void verify_declined_address(struct in_addr *declined)
+static void verify_declined_address(struct zsock_in_addr *declined)
 {
 	int ret;

@@ -896,7 +896,7 @@ ZTEST(dhcpv4_server_tests, test_decline)
  */
 ZTEST(dhcpv4_server_tests, test_declined_reuse)
 {
-	struct in_addr oldest_addr;
+	struct zsock_in_addr oldest_addr;

 	for (int i = 0; i < CONFIG_NET_DHCPV4_SERVER_ADDR_COUNT; i++) {
 		client_get_lease(false);
@@ -983,8 +983,8 @@ ZTEST(dhcpv4_server_tests, test_icmp_probe)
 /* Verify that the DHCP server can start and validate input properly. */
 ZTEST(dhcpv4_server_tests_no_init, test_initialization)
 {
-	struct in_addr base_addr_wrong_subnet = { { { 192, 0, 3, 10 } } };
-	struct in_addr base_addr_overlap = { { { 192, 0, 2, 1 } } };
+	struct zsock_in_addr base_addr_wrong_subnet = { { { 192, 0, 3, 10 } } };
+	struct zsock_in_addr base_addr_overlap = { { { 192, 0, 2, 1 } } };
 	int ret;

 	ret = net_dhcpv4_server_start(test_ctx.iface, &base_addr_wrong_subnet);
diff --git a/tests/net/lib/http_server/core/src/main.c b/tests/net/lib/http_server/core/src/main.c
index 6514f9d85ac..c9a90ea967b 100644
--- a/tests/net/lib/http_server/core/src/main.c
+++ b/tests/net/lib/http_server/core/src/main.c
@@ -11,8 +11,12 @@
 #include <strings.h>

 #include <zephyr/net/http/service.h>
-#include <zephyr/net/socket.h>
 #include <sys/eventfd.h>
+#include <arpa/inet.h>
+#include <netinet/in.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
 #include <zephyr/ztest.h>

 #define BUFFER_SIZE                    1024
@@ -552,7 +556,7 @@ static void test_read_data(size_t *offset, size_t need)
 	int ret;

 	while (*offset < need) {
-		ret = zsock_recv(client_fd, buf + *offset, sizeof(buf) - *offset, 0);
+		ret = recv(client_fd, buf + *offset, sizeof(buf) - *offset, 0);
 		zassert_not_equal(ret, -1, "recv() failed (%d)", errno);
 		*offset += ret;
 		if (ret == 0) {
@@ -733,7 +737,7 @@ ZTEST(server_function_tests, test_http2_get_concurrent_streams)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request_get_2_streams,
+	ret = send(client_fd, request_get_2_streams,
 			 sizeof(request_get_2_streams), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -763,7 +767,7 @@ ZTEST(server_function_tests, test_http2_static_get)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request_get_static_simple,
+	ret = send(client_fd, request_get_static_simple,
 			 sizeof(request_get_static_simple), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -791,7 +795,7 @@ ZTEST(server_function_tests, test_http1_static_upgrade_get)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -825,7 +829,7 @@ ZTEST(server_function_tests, test_http1_static_get)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -842,7 +846,7 @@ static void common_verify_http2_dynamic_post_request(const uint8_t *request,
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request, request_len, 0);
+	ret = send(client_fd, request, request_len, 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -890,10 +894,10 @@ static void common_verify_http1_dynamic_upgrade_post(const uint8_t *method)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, method, strlen(method), 0);
+	ret = send(client_fd, method, strlen(method), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -936,10 +940,10 @@ static void common_verify_http1_dynamic_post(const uint8_t *method)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, method, strlen(method), 0);
+	ret = send(client_fd, method, strlen(method), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -968,7 +972,7 @@ static void common_verify_http2_dynamic_get_request(const uint8_t *request,
 	dynamic_payload_len = strlen(TEST_DYNAMIC_GET_PAYLOAD);
 	memcpy(dynamic_payload, TEST_DYNAMIC_GET_PAYLOAD, dynamic_payload_len);

-	ret = zsock_send(client_fd, request, request_len, 0);
+	ret = send(client_fd, request, request_len, 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1012,7 +1016,7 @@ ZTEST(server_function_tests, test_http1_dynamic_upgrade_get)
 	dynamic_payload_len = strlen(TEST_DYNAMIC_GET_PAYLOAD);
 	memcpy(dynamic_payload, TEST_DYNAMIC_GET_PAYLOAD, dynamic_payload_len);

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1048,7 +1052,7 @@ ZTEST(server_function_tests, test_http1_dynamic_get)
 	dynamic_payload_len = strlen(TEST_DYNAMIC_GET_PAYLOAD);
 	memcpy(dynamic_payload, TEST_DYNAMIC_GET_PAYLOAD, dynamic_payload_len);

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1120,7 +1124,7 @@ ZTEST(server_function_tests, test_http2_dynamic_delete)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request_delete_dynamic,
+	ret = send(client_fd, request_delete_dynamic,
 			 sizeof(request_delete_dynamic), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -1146,7 +1150,7 @@ ZTEST(server_function_tests, test_http1_dynamic_upgrade_delete)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1176,7 +1180,7 @@ ZTEST(server_function_tests, test_http1_dynamic_delete)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1212,7 +1216,7 @@ ZTEST(server_function_tests, test_http1_connection_close)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request_1, strlen(http1_request_1), 0);
+	ret = send(client_fd, http1_request_1, strlen(http1_request_1), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1225,7 +1229,7 @@ ZTEST(server_function_tests, test_http1_connection_close)
 	/* With no connection: close, the server shall serve another request on
 	 * the same connection.
 	 */
-	ret = zsock_send(client_fd, http1_request_2, strlen(http1_request_2), 0);
+	ret = send(client_fd, http1_request_2, strlen(http1_request_2), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1238,7 +1242,7 @@ ZTEST(server_function_tests, test_http1_connection_close)
 	/* Second request included connection: close, so we should expect the
 	 * connection to be closed now.
 	 */
-	ret = zsock_recv(client_fd, buf, sizeof(buf), 0);
+	ret = recv(client_fd, buf, sizeof(buf), 0);
 	zassert_equal(ret, 0, "Connection should've been closed");
 }

@@ -1318,7 +1322,7 @@ ZTEST(server_function_tests, test_http2_post_missing_continuation)

 	memset(buf, 0, sizeof(buf));

-	ret = zsock_send(client_fd, request_post_dynamic,
+	ret = send(client_fd, request_post_dynamic,
 			 sizeof(request_post_dynamic), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -1328,7 +1332,7 @@ ZTEST(server_function_tests, test_http2_post_missing_continuation)
 	expect_http2_settings_frame(&offset, false);
 	expect_http2_settings_frame(&offset, true);

-	ret = zsock_recv(client_fd, buf, sizeof(buf), 0);
+	ret = recv(client_fd, buf, sizeof(buf), 0);
 	zassert_equal(ret, 0, "Connection should've been closed");
 }

@@ -1346,7 +1350,7 @@ ZTEST(server_function_tests, test_http2_post_trailing_headers)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request_post_dynamic,
+	ret = send(client_fd, request_post_dynamic,
 			 sizeof(request_post_dynamic), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -1401,7 +1405,7 @@ ZTEST(server_function_tests, test_http2_rst_stream)

 	memset(buf, 0, sizeof(buf));

-	ret = zsock_send(client_fd, request_rst_stream,
+	ret = send(client_fd, request_rst_stream,
 			 sizeof(request_rst_stream), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -1412,7 +1416,7 @@ ZTEST(server_function_tests, test_http2_rst_stream)
 	expect_http2_settings_frame(&offset, false);
 	expect_http2_settings_frame(&offset, true);

-	ret = zsock_recv(client_fd, buf, sizeof(buf), 0);
+	ret = recv(client_fd, buf, sizeof(buf), 0);
 	zassert_equal(ret, 0, "Connection should've been closed");
 }

@@ -1427,7 +1431,7 @@ static void test_http1_header_capture_common(const char *request)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request, strlen(request), 0);
+	ret = send(client_fd, request, strlen(request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	test_read_data(&offset, sizeof(http1_header_capture_common_response) - 1);
@@ -1540,7 +1544,7 @@ static void common_verify_http2_get_header_capture_request(const uint8_t *reques
 	dynamic_payload_len = strlen(TEST_DYNAMIC_GET_PAYLOAD);
 	memcpy(dynamic_payload, TEST_DYNAMIC_GET_PAYLOAD, dynamic_payload_len);

-	ret = zsock_send(client_fd, request, request_len, 0);
+	ret = send(client_fd, request, request_len, 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -1667,7 +1671,7 @@ ZTEST(server_function_tests, test_http2_header_concurrent)
 	int ret;
 	size_t offset = 0;

-	ret = zsock_send(client_fd, request, sizeof(request), 0);
+	ret = send(client_fd, request, sizeof(request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	/* Wait for response on both resources before checking captured headers */
@@ -1720,7 +1724,7 @@ static void test_http1_dynamic_response_headers(const char *request, const char
 	int ret;
 	size_t offset = 0;

-	ret = zsock_send(client_fd, request, strlen(request), 0);
+	ret = send(client_fd, request, strlen(request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	test_read_data(&offset, strlen(expected_response));
@@ -1753,7 +1757,7 @@ static void test_http2_dynamic_response_headers(const uint8_t *request, size_t r
 	const uint8_t expected_flags = end_stream ? HTTP2_FLAG_END_HEADERS | HTTP2_FLAG_END_STREAM
 						  : HTTP2_FLAG_END_HEADERS;

-	ret = zsock_send(client_fd, request, request_len, 0);
+	ret = send(client_fd, request, request_len, 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	expect_http2_settings_frame(offset, false);
@@ -2187,7 +2191,7 @@ ZTEST(server_function_tests, test_http1_409_method_not_allowed)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2214,7 +2218,7 @@ ZTEST(server_function_tests, test_http1_upgrade_409_method_not_allowed)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2245,7 +2249,7 @@ ZTEST(server_function_tests, test_http2_409_method_not_allowed)
 	size_t offset = 0;
 	int ret;

-	ret = zsock_send(client_fd, request_post_static,
+	ret = send(client_fd, request_post_static,
 			 sizeof(request_post_static), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -2274,7 +2278,7 @@ ZTEST(server_function_tests, test_http1_500_internal_server_error)

 	dynamic_error = true;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2304,7 +2308,7 @@ ZTEST(server_function_tests, test_http1_upgrade_500_internal_server_error)

 	dynamic_error = true;

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2341,7 +2345,7 @@ ZTEST(server_function_tests, test_http2_500_internal_server_error)

 	dynamic_error = true;

-	ret = zsock_send(client_fd, request_get_dynamic,
+	ret = send(client_fd, request_get_dynamic,
 			 sizeof(request_get_dynamic), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

@@ -2367,7 +2371,7 @@ ZTEST(server_function_tests_no_init, test_http_server_start_stop)
 	sa.sin_family = AF_INET;
 	sa.sin_port = htons(SERVER_PORT);

-	ret = zsock_inet_pton(AF_INET, SERVER_IPV4_ADDR, &sa.sin_addr.s_addr);
+	ret = inet_pton(AF_INET, SERVER_IPV4_ADDR, &sa.sin_addr.s_addr);
 	zassert_equal(1, ret, "inet_pton() failed to convert %s", SERVER_IPV4_ADDR);

 	zassert_ok(http_server_start(), "Failed to start the server");
@@ -2379,13 +2383,13 @@ ZTEST(server_function_tests_no_init, test_http_server_start_stop)
 	zassert_ok(http_server_start(), "Failed to start the server");

 	/* Server should be listening now. */
-	ret = zsock_socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
+	ret = socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
 	zassert_not_equal(ret, -1, "failed to create client socket (%d)", errno);
 	client_fd = ret;

-	zassert_ok(zsock_connect(client_fd, (struct sockaddr *)&sa, sizeof(sa)),
+	zassert_ok(connect(client_fd, (struct sockaddr *)&sa, sizeof(sa)),
 		   "failed to connect to the server (%d)", errno);
-	zassert_ok(zsock_close(client_fd), "close() failed on the client fd (%d)", errno);
+	zassert_ok(close(client_fd), "close() failed on the client fd (%d)", errno);
 	client_fd = -1;

 	/* Check if the server can be restarted again after client connected. */
@@ -2395,13 +2399,13 @@ ZTEST(server_function_tests_no_init, test_http_server_start_stop)
 	/* Let the server thread run. */
 	k_msleep(CONFIG_HTTP_SERVER_RESTART_DELAY + 10);

-	ret = zsock_socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
+	ret = socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
 	zassert_not_equal(ret, -1, "failed to create client socket (%d)", errno);
 	client_fd = ret;

-	zassert_ok(zsock_connect(client_fd, (struct sockaddr *)&sa, sizeof(sa)),
+	zassert_ok(connect(client_fd, (struct sockaddr *)&sa, sizeof(sa)),
 		   "failed to connect to the server (%d)", errno);
-	zassert_ok(zsock_close(client_fd), "close() failed on the client fd (%d)", errno);
+	zassert_ok(close(client_fd), "close() failed on the client fd (%d)", errno);
 	client_fd = -1;

 	zassert_ok(http_server_stop(), "Failed to stop the server");
@@ -2695,7 +2699,7 @@ ZTEST(server_function_tests, test_http1_static_fs)
 	ret = setup_fs("");
 	zassert_equal(ret, TC_PASS, "Failed to mount fs");

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2752,7 +2756,7 @@ ZTEST(server_function_tests, test_http1_static_fs_compression)
 		ret = setup_fs(file_ending_map[i]);
 		zassert_equal(ret, TC_PASS, "Failed to mount fs");

-		ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+		ret = send(client_fd, http1_request, strlen(http1_request), 0);
 		zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 		memset(buf, 0, sizeof(buf));
@@ -2770,7 +2774,7 @@ ZTEST(server_function_tests, test_http1_static_fs_compression)
 	ret = setup_fs(file_ending_map[HTTP_BR]);
 	zassert_equal(ret, TC_PASS, "Failed to mount fs");

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
@@ -2805,14 +2809,14 @@ static void http_server_tests_before(void *fixture)
 		return;
 	}

-	ret = zsock_socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
+	ret = socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
 	if (ret < 0) {
 		printk("Failed to create client socket (%d)\n", errno);
 		return;
 	}
 	client_fd = ret;

-	ret = zsock_setsockopt(client_fd, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = setsockopt(client_fd, SOL_SOCKET, SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	if (ret < 0) {
 		printk("Failed to set timeout (%d)\n", errno);
@@ -2822,13 +2826,13 @@ static void http_server_tests_before(void *fixture)
 	sa.sin_family = AF_INET;
 	sa.sin_port = htons(SERVER_PORT);

-	ret = zsock_inet_pton(AF_INET, SERVER_IPV4_ADDR, &sa.sin_addr.s_addr);
+	ret = inet_pton(AF_INET, SERVER_IPV4_ADDR, &sa.sin_addr.s_addr);
 	if (ret != 1) {
 		printk("inet_pton() failed to convert %s\n", SERVER_IPV4_ADDR);
 		return;
 	}

-	ret = zsock_connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
+	ret = connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
 	if (ret < 0) {
 		printk("Failed to connect (%d)\n", errno);
 	}
@@ -2839,7 +2843,7 @@ static void http_server_tests_after(void *fixture)
 	ARG_UNUSED(fixture);

 	if (client_fd >= 0) {
-		(void)zsock_close(client_fd);
+		(void)close(client_fd);
 		client_fd = -1;
 	}

diff --git a/tests/net/lib/http_server/crime/src/main.c b/tests/net/lib/http_server/crime/src/main.c
index a00118ed1c5..e3ab4d8749b 100644
--- a/tests/net/lib/http_server/crime/src/main.c
+++ b/tests/net/lib/http_server/crime/src/main.c
@@ -8,8 +8,12 @@

 #include <string.h>

+#include <arpa/inet.h>
+#include <netinet/in.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
 #include <zephyr/net/http/service.h>
-#include <zephyr/net/socket.h>
 #include <zephyr/ztest.h>

 #define BUFFER_SIZE  256
@@ -73,23 +77,23 @@ static void test_crime(void)

 	zassert_ok(http_server_start(), "Failed to start the server");

-	ret = zsock_socket(AF_INET, SOCK_STREAM, proto);
+	ret = socket(AF_INET, SOCK_STREAM, proto);
 	zassert_not_equal(ret, -1, "failed to create client socket (%d)", errno);
 	client_fd = ret;

 	sa.sin_family = AF_INET;
 	sa.sin_port = htons(SERVER_PORT);

-	ret = zsock_inet_pton(AF_INET, MY_IPV4_ADDR, &sa.sin_addr.s_addr);
+	ret = inet_pton(AF_INET, MY_IPV4_ADDR, &sa.sin_addr.s_addr);
 	zassert_not_equal(-1, ret, "inet_pton() failed (%d)", errno);
 	zassert_not_equal(0, ret, "%s is not a valid IPv4 address", MY_IPV4_ADDR);
 	zassert_equal(1, ret, "inet_pton() failed to convert %s", MY_IPV4_ADDR);

 	memset(buf, '\0', sizeof(buf));
-	ptr = (char *)zsock_inet_ntop(AF_INET, &sa.sin_addr, buf, sizeof(buf));
+	ptr = (char *)inet_ntop(AF_INET, &sa.sin_addr, buf, sizeof(buf));
 	zassert_not_equal(ptr, NULL, "inet_ntop() failed (%d)", errno);

-	ret = zsock_connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
+	ret = connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
 	zassert_not_equal(ret, -1, "failed to connect (%s/%d)", strerror(errno), errno);

 	char *http1_request = "GET / HTTP/1.1\r\n"
@@ -98,17 +102,17 @@ static void test_crime(void)
 			      "Accept-Encoding: deflate, gzip, br\r\n"
 			      "\r\n";

-	ret = zsock_send(client_fd, http1_request, strlen(http1_request), 0);
+	ret = send(client_fd, http1_request, strlen(http1_request), 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);

 	memset(buf, 0, sizeof(buf));
-	recv_len = zsock_recv(client_fd, buf, sizeof(buf), 0);
+	recv_len = recv(client_fd, buf, sizeof(buf), 0);
 	zassert_not_equal(recv_len, -1, "recv() failed (%d)", errno);

 	len = sizeof(index_html_gz);

 	while (recv_len < len) {
-		ret = zsock_recv(client_fd, buf + recv_len, sizeof(buf) - recv_len, 0);
+		ret = recv(client_fd, buf + recv_len, sizeof(buf) - recv_len, 0);
 		zassert_not_equal(ret, -1, "recv() failed (%d)", errno);

 		recv_len += ret;
@@ -125,7 +129,7 @@ static void test_crime(void)
 	zassert_equal(ret, 0,
 		      "inc_file and compressed_inc_file contents are not identical (%d)", ret);

-	ret = zsock_close(client_fd);
+	ret = close(client_fd);
 	zassert_not_equal(-1, ret, "close() failed on the client fd (%d)", errno);

 	zassert_ok(http_server_stop(), "Failed to stop the server");
diff --git a/tests/net/lib/http_server/hpack/src/main.c b/tests/net/lib/http_server/hpack/src/main.c
index c3cc732b1a7..5c79e098f21 100644
--- a/tests/net/lib/http_server/hpack/src/main.c
+++ b/tests/net/lib/http_server/hpack/src/main.c
@@ -5,7 +5,7 @@
  */

 #include <zephyr/net/http/hpack.h>
-#include <zephyr/net/net_ip.h>
+#include <netinet/in.h>
 #include <zephyr/ztest.h>

 struct huffman_codes {
diff --git a/tests/net/lib/http_server/tls/src/main.c b/tests/net/lib/http_server/tls/src/main.c
index 6d710eb350e..3cd51ef59c3 100644
--- a/tests/net/lib/http_server/tls/src/main.c
+++ b/tests/net/lib/http_server/tls/src/main.c
@@ -12,6 +12,11 @@
 #include <zephyr/net/socket.h>
 #include <zephyr/net/tls_credentials.h>
 #include <zephyr/sys/util.h>
+#include <arpa/inet.h>
+#include <netinet/in.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
 #include <zephyr/ztest.h>

 LOG_MODULE_REGISTER(net_test, CONFIG_NET_SOCKETS_LOG_LEVEL);
@@ -148,7 +153,7 @@ static void test_tls(void)

 	zassert_ok(http_server_start(), "Failed to start the server");

-	ret = zsock_socket(AF_INET, SOCK_STREAM, proto);
+	ret = socket(AF_INET, SOCK_STREAM, proto);
 	zassert_not_equal(ret, -1, "failed to create client socket (%d)", errno);
 	client_fd = ret;

@@ -163,11 +168,11 @@ static void test_tls(void)
 		sec_tag_list_size = sizeof(sec_tag_list);
 		sec_tag_list = sec_tag_list_verify_none;

-		ret = zsock_setsockopt(client_fd, SOL_TLS, TLS_SEC_TAG_LIST,
+		ret = setsockopt(client_fd, SOL_TLS, TLS_SEC_TAG_LIST,
 				       sec_tag_list, sec_tag_list_size);
 		zassert_not_equal(ret, -1, "failed to set TLS_SEC_TAG_LIST (%d)", errno);

-		ret = zsock_setsockopt(client_fd, SOL_TLS, TLS_HOSTNAME, "zephyr.local",
+		ret = setsockopt(client_fd, SOL_TLS, TLS_HOSTNAME, "zephyr.local",
 				       sizeof("zephyr.local"));
 		zassert_not_equal(ret, -1, "failed to set TLS_HOSTNAME (%d)", errno);
 	}
@@ -175,31 +180,31 @@ static void test_tls(void)
 	sa.sin_family = AF_INET;
 	sa.sin_port = htons(SERVER_PORT);

-	ret = zsock_inet_pton(AF_INET, MY_IPV4_ADDR, &sa.sin_addr.s_addr);
+	ret = inet_pton(AF_INET, MY_IPV4_ADDR, &sa.sin_addr.s_addr);
 	zassert_not_equal(-1, ret, "inet_pton() failed (%d)", errno);
 	zassert_not_equal(ret, 0, "%s is not a valid IPv4 address", MY_IPV4_ADDR);
 	zassert_equal(ret, 1, "inet_pton() failed to convert %s", MY_IPV4_ADDR);

 	memset(buf, '\0', sizeof(buf));
-	ptr = (char *)zsock_inet_ntop(AF_INET, &sa.sin_addr, buf, sizeof(buf));
+	ptr = (char *)inet_ntop(AF_INET, &sa.sin_addr, buf, sizeof(buf));
 	zassert_not_equal(ptr, NULL, "inet_ntop() failed (%d)", errno);

-	ret = zsock_connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
+	ret = connect(client_fd, (struct sockaddr *)&sa, sizeof(sa));
 	zassert_not_equal(ret, -1, "failed to connect (%d)", errno);

-	ret = zsock_send(client_fd, http1_request, sizeof(http1_request) - 1, 0);
+	ret = send(client_fd, http1_request, sizeof(http1_request) - 1, 0);
 	zassert_not_equal(ret, -1, "send() failed (%d)", errno);
 	zassert_equal(ret, sizeof(http1_request) - 1, "expected: %zu actual: %d",
 		      sizeof(http1_request) - 1, ret);

 	memset(buf, 0, sizeof(buf));
-	recv_len = zsock_recv(client_fd, buf, sizeof(buf), 0);
+	recv_len = recv(client_fd, buf, sizeof(buf), 0);
 	zassert_not_equal(recv_len, -1, "recv() failed (%d)", errno);

 	len = sizeof(index_html_gz);

 	while (recv_len < len) {
-		ret = zsock_recv(client_fd, buf + recv_len, sizeof(buf) - recv_len, 0);
+		ret = recv(client_fd, buf + recv_len, sizeof(buf) - recv_len, 0);
 		zassert_not_equal(ret, -1, "recv() failed (%d)", errno);

 		recv_len += ret;
@@ -216,7 +221,7 @@ static void test_tls(void)
 	zassert_equal(ret, 0,
 		      "inc_file and compressed_inc_file contents are not identical (%d)", ret);

-	ret = zsock_close(client_fd);
+	ret = close(client_fd);
 	zassert_not_equal(ret, -1, "close() failed on the client fd (%d)", errno);

 	zassert_ok(http_server_stop(), "Failed to stop the server");

