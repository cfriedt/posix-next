diff --git a/include/zephyr/kernel/process.h b/include/zephyr/kernel/process.h
index c0839c20608..59262ec78bb 100644
--- a/include/zephyr/kernel/process.h
+++ b/include/zephyr/kernel/process.h
@@ -269,6 +269,10 @@ struct k_process {
 	/** Cloned address-space context (system layer), or NULL */
 	void *vm;
 #endif /* CONFIG_PROCESS_VM */
+#ifdef CONFIG_ZVFS_FDTABLE
+	/** Per-process descriptor table (system layer); NULL shares the boot table */
+	void *fds;
+#endif /* CONFIG_ZVFS_FDTABLE */
 	/** @endcond */
 };
 
diff --git a/include/zephyr/sys/internal/fdtable_priv.h b/include/zephyr/sys/internal/fdtable_priv.h
index a98c4083efc..6a0fbc0dc9b 100644
--- a/include/zephyr/sys/internal/fdtable_priv.h
+++ b/include/zephyr/sys/internal/fdtable_priv.h
@@ -37,9 +37,56 @@ struct fd_slot *zvfs_fd_slot_get(int fd);
 struct fd_entry *zvfs_fd_entry_get(int fd);
 struct fd_entry *zvfs_fd_syscall_verify(int fd);
 
-int zvfs_fd_obj_refcount(void *obj);
+struct k_process;
 
-void zvfs_fd_offset_set(struct fd_entry *entry, size_t offset);
+#if defined(CONFIG_ZVFS_FDTABLE) && defined(CONFIG_SYS_PROCESS)
+/* give @a child its own descriptor table: a copy of the caller's */
+int z_zvfs_fds_clone(struct k_process *child, k_tid_t leader);
+/*
+ * Operations on a paused child's table: install the caller's @a parentfd
+ * as the child's @a childfd, dup within the child's table, and close in
+ * the child's table.
+ */
+int z_zvfs_fds_child_set(k_tid_t child, int childfd, int parentfd);
+int z_zvfs_fds_child_dup2(k_tid_t child, int oldfd, int newfd);
+int z_zvfs_fds_child_close(k_tid_t child, int fd);
+/* close every descriptor in the caller's table marked FD_CLOEXEC */
+void zvfs_fds_cloexec(void);
+#else
+static inline int z_zvfs_fds_clone(struct k_process *child, k_tid_t leader)
+{
+	ARG_UNUSED(child);
+	ARG_UNUSED(leader);
+	return 0;
+}
+
+static inline int z_zvfs_fds_child_set(k_tid_t child, int childfd, int parentfd)
+{
+	ARG_UNUSED(child);
+	ARG_UNUSED(childfd);
+	ARG_UNUSED(parentfd);
+	return 0;
+}
+
+static inline int z_zvfs_fds_child_dup2(k_tid_t child, int oldfd, int newfd)
+{
+	ARG_UNUSED(child);
+	ARG_UNUSED(oldfd);
+	ARG_UNUSED(newfd);
+	return 0;
+}
+
+static inline int z_zvfs_fds_child_close(k_tid_t child, int fd)
+{
+	ARG_UNUSED(child);
+	ARG_UNUSED(fd);
+	return 0;
+}
+
+static inline void zvfs_fds_cloexec(void)
+{
+}
+#endif
 
 /** zvfs_poll_prepare_fd() armed *pev with the fd's wait condition(s) */
 #define ZVFS_POLL_PREPARED 0
@@ -47,12 +94,17 @@ void zvfs_fd_offset_set(struct fd_entry *entry, size_t offset);
 #define ZVFS_POLL_READY    1
 
 struct k_poll_event;
+struct zvfs_flock;
 struct zvfs_pollfd;
 
 int zvfs_poll_prepare_fd(struct zvfs_pollfd *pfd, struct k_poll_event **pev,
 			 struct k_poll_event *pev_end);
 int zvfs_poll_update_fd(struct zvfs_pollfd *pfd, struct k_poll_event **pev);
 
+int zvfs_fd_obj_refcount(void *obj);
+
+void zvfs_fd_offset_set(struct fd_entry *entry, size_t offset);
+
 bool supports_pread_pwrite(uint32_t mode);
 ssize_t zvfs_rw_unlocked(int fd, void *buf, size_t sz, bool is_write, const size_t *from_offset);
 
diff --git a/kernel/include/kernel_internal.h b/kernel/include/kernel_internal.h
index 35ebe5fe359..0d180df0516 100644
--- a/kernel/include/kernel_internal.h
+++ b/kernel/include/kernel_internal.h
@@ -185,6 +185,25 @@ static inline int z_session_cell_free(struct k_session *session)
 }
 #endif /* CONFIG_SYS_PROCESS */
 
+/*
+ * Per-process descriptor-table providers (zvfs). The exit sweep runs in the
+ * dying thread's own context and may block; the drop runs under the process
+ * lock and only detaches, deferring closes and frees to the provider.
+ */
+#if defined(CONFIG_ZVFS_FDTABLE) && defined(CONFIG_PROCESS)
+void z_process_fds_exit(void);
+void z_process_fds_drop(struct k_process *proc);
+#else
+static inline void z_process_fds_exit(void)
+{
+}
+
+static inline void z_process_fds_drop(struct k_process *proc)
+{
+	ARG_UNUSED(proc);
+}
+#endif /* CONFIG_ZVFS_FDTABLE && CONFIG_PROCESS */
+
 #ifdef CONFIG_USERSPACE
 /* permission-principal index management and the reap-time sweep */
 int16_t z_process_perm_index_alloc(void);
diff --git a/kernel/process.c b/kernel/process.c
index dab3b2374f7..a05c258eb45 100644
--- a/kernel/process.c
+++ b/kernel/process.c
@@ -338,6 +338,7 @@ static void process_free_locked(struct k_process *proc)
 	if ((proc->flags & K_PROCESS_FLAG_LEADER_RELEASE) != 0U) {
 		z_process_leader_release(proc->leader);
 	}
+	z_process_fds_drop(proc);
 	proc->state = K_PROCESS_UNUSED;
 	/* the single pid-retirement point: the leader handle may be reused now */
 	proc->leader = NULL;
@@ -1028,6 +1029,9 @@ void z_impl_k_exit(int code)
 	proc->state = K_PROCESS_EXITING;
 	k_spin_unlock(&process_lock, key);
 
+	/* POSIX: descriptors close at termination, not reap; may block here */
+	z_process_fds_exit();
+
 	k_thread_abort(k_current_get());
 
 	CODE_UNREACHABLE;
diff --git a/lib/os/Kconfig b/lib/os/Kconfig
index dde7e0048da..41fe363215c 100644
--- a/lib/os/Kconfig
+++ b/lib/os/Kconfig
@@ -20,6 +20,18 @@ config ZVFS_OPEN_MAX
 	  requirements, ends up being zero, then no file descriptors will be
 	  available.
 
+config ZVFS_PROC_OPEN_MAX
+	int "Per-process open file descriptor limit"
+	depends on ZVFS && PROCESS
+	default 0
+	help
+	  Size of each process's file descriptor table, the limit POSIX's
+	  OPEN_MAX describes. The system-wide table size (ZVFS_OPEN_MAX plus
+	  subsystem contributions) independently bounds the total number of
+	  open file descriptions. 0 sizes each process's table to the
+	  system-wide size. Kernel processes share one boot descriptor table
+	  regardless of this option.
+
 config ZVFS_OPEN_IGNORE_MIN
 	bool "Ignore the minimum fd count requirement"
 	help
diff --git a/lib/os/process.c b/lib/os/process.c
index e1d84f1aef0..2f26e9b668a 100644
--- a/lib/os/process.c
+++ b/lib/os/process.c
@@ -21,6 +21,7 @@
 #include <zephyr/internal/syscall_handler.h>
 #include <zephyr/sys/math_extras.h>
 #include <zephyr/sys/elastipool.h>
+#include <zephyr/sys/internal/fdtable_priv.h>
 #include <zephyr/sys/process.h>
 #include <zephyr/sys/thread.h>
 #include <kernel_internal.h>
@@ -1088,9 +1089,18 @@ int z_sys_clone_vm_copy(const struct sys_clone_args *args, k_pid_t *child)
 
 	(void)k_mem_domain_add_thread(z_process_vm_domain(proc->vm), tid);
 
+	/* the child's descriptor table: a copy of the caller's */
+	ret = z_zvfs_fds_clone(proc, tid);
+	if (ret < 0) {
+		k_thread_abort(tid);
+		(void)z_process_record_free(proc);
+		return ret;
+	}
+
 	ret = k_process_init(proc, &tid, 1);
 	if (ret < 0) {
 		k_thread_abort(tid);
+		z_process_fds_drop(proc);
 		(void)z_process_record_free(proc);
 		return ret;
 	}
@@ -1173,6 +1183,19 @@ int z_sys_clone_process(const struct sys_clone_args *args, k_pid_t *child)
 				      K_FOREVER);
 	}
 
+	/* the child's descriptor table: a copy of the caller's */
+	ret = z_zvfs_fds_clone(proc, tid);
+	if (ret < 0) {
+		k_thread_abort(tid);
+#ifdef CONFIG_SYS_THREAD
+		if (pooled) {
+			(void)sys_thread_destroy(tid);
+		}
+#endif /* CONFIG_SYS_THREAD */
+		(void)z_process_record_free(proc);
+		return ret;
+	}
+
 	ret = k_process_init(proc, &tid, 1);
 	if (ret < 0) {
 		k_thread_abort(tid);
@@ -1181,6 +1204,7 @@ int z_sys_clone_process(const struct sys_clone_args *args, k_pid_t *child)
 			(void)sys_thread_destroy(tid);
 		}
 #endif /* CONFIG_SYS_THREAD */
+		z_process_fds_drop(proc);
 		(void)z_process_record_free(proc);
 		return ret;
 	}
diff --git a/lib/os/zvfs/zvfs_fdtable.c b/lib/os/zvfs/zvfs_fdtable.c
index a2602b6b22f..6b47f4221ce 100644
--- a/lib/os/zvfs/zvfs_fdtable.c
+++ b/lib/os/zvfs/zvfs_fdtable.c
@@ -25,6 +25,8 @@
 
 #include <zephyr/init.h>
 #include <zephyr/kernel.h>
+#include <zephyr/kernel/process.h>
+#include <zephyr/sys/elastipool.h>
 #include <zephyr/sys/fdtable.h>
 #include <zephyr/sys/internal/fdtable_priv.h>
 #include <zephyr/sys/internal/kobject_internal.h>
@@ -71,7 +73,7 @@ static struct fd_entry fdtable[ZVFS_OPEN_SIZE] = {
 #endif
 };
 
-/* the boot descriptor table, shared by every thread */
+/* the boot descriptor table, shared by every thread without its own */
 static struct fd_slot fd_boot_slots[ZVFS_OPEN_SIZE] = {
 #ifdef CONFIG_ZVFS_RESERVE_STDIO_FDS
 	{.entry = &fdtable[0]},
@@ -82,12 +84,70 @@ static struct fd_slot fd_boot_slots[ZVFS_OPEN_SIZE] = {
 
 static K_MUTEX_DEFINE(fdtable_lock);
 
+#ifdef CONFIG_PROCESS
+#if CONFIG_ZVFS_PROC_OPEN_MAX > 0
+#define ZVFS_PROC_OPEN_SIZE CONFIG_ZVFS_PROC_OPEN_MAX
+/* fd_set bitsets and the description pool are sized by the system limit */
+BUILD_ASSERT(CONFIG_ZVFS_PROC_OPEN_MAX <= ZVFS_OPEN_SIZE,
+	     "per-process descriptor limit exceeds the system-wide table size");
+#else
+#define ZVFS_PROC_OPEN_SIZE ZVFS_OPEN_SIZE
+#endif
+
+/* a process's descriptor table; kernel processes share the boot table */
+struct zvfs_proc_fds {
+	sys_dnode_t node;
+	struct fd_slot slots[ZVFS_PROC_OPEN_SIZE];
+};
+
+static struct k_spinlock fds_graveyard_lock;
+static sys_dlist_t fds_graveyard = SYS_DLIST_STATIC_INIT(&fds_graveyard);
+
+#ifdef CONFIG_SYS_PROCESS
+/* one table per dynamically created process: sized as the process records */
+#define FDS_POOL_MAX (SYS_PROCESS_MIN + CONFIG_SYS_PROCESS_MAX)
+
+__aligned(WB_UP(__alignof(struct zvfs_proc_fds))) static uint8_t
+	fds_pool_storage[ROUND_UP(sizeof(struct zvfs_proc_fds), __alignof(struct zvfs_proc_fds)) *
+			 SYS_PROCESS_MIN];
+SYS_ELASTIPOOL_DEFINE_ADVANCED(fds_pool, sizeof(struct zvfs_proc_fds),
+			       __alignof(struct zvfs_proc_fds), SYS_PROCESS_MIN, FDS_POOL_MAX,
+			       SYS_ELASTIPOOL_KERNEL_HEAP_ALLOC_FN(SYS_PROCESS_MIN, FDS_POOL_MAX),
+			       fds_pool_storage, static);
+#endif /* CONFIG_SYS_PROCESS */
+#endif /* CONFIG_PROCESS */
+
 /* the calling thread's descriptor table */
-static struct fd_slot *fd_slots(void)
+static struct fd_slot *fd_slots_n(int *n)
 {
+#ifdef CONFIG_PROCESS
+	/*
+	 * Library-side user-mode callers (fileno, FILE locking) cannot read
+	 * the thread struct or a heap-allocated table; they resolve the boot
+	 * table, as before per-process tables. Syscall paths resolve here in
+	 * kernel mode and see the caller's own table.
+	 */
+	if (!k_is_user_context()) {
+		k_tid_t self = k_current_get();
+		struct k_process *proc = (self == NULL) ? NULL : self->process;
+
+		if ((proc != NULL) && (proc->fds != NULL)) {
+			*n = ZVFS_PROC_OPEN_SIZE;
+			return ((struct zvfs_proc_fds *)proc->fds)->slots;
+		}
+	}
+#endif
+	*n = ZVFS_OPEN_SIZE;
 	return fd_boot_slots;
 }
 
+static struct fd_slot *fd_slots(void)
+{
+	int unused;
+
+	return fd_slots_n(&unused);
+}
+
 static int fd_entry_ref(struct fd_entry *entry)
 {
 	return atomic_inc(&entry->refcount) + 1;
@@ -145,7 +205,8 @@ static struct fd_entry *_find_description(void)
 
 static int _find_fd_slot_at_or_above(int minfd)
 {
-	struct fd_slot *slots = fd_slots();
+	int n;
+	struct fd_slot *slots = fd_slots_n(&n);
 	int fd;
 
 	if (minfd < 0) {
@@ -153,7 +214,7 @@ static int _find_fd_slot_at_or_above(int minfd)
 		return -1;
 	}
 
-	for (fd = minfd; fd < ZVFS_OPEN_SIZE; fd++) {
+	for (fd = minfd; fd < n; fd++) {
 		if (slots[fd].entry == NULL) {
 			return fd;
 		}
@@ -250,9 +311,14 @@ int z_impl_zvfs_dup2(int oldfd, int newfd)
 		return -1;
 	}
 
-	if (newfd < 0 || newfd >= ZVFS_OPEN_SIZE) {
-		errno = EBADF;
-		return -1;
+	{
+		int n;
+
+		(void)fd_slots_n(&n);
+		if (newfd < 0 || newfd >= n) {
+			errno = EBADF;
+			return -1;
+		}
 	}
 
 	if (zvfs_fd_entry_get(newfd) != NULL) {
@@ -300,14 +366,15 @@ static int z_vrfy_zvfs_dup2(int oldfd, int newfd)
 
 struct fd_slot *zvfs_fd_slot_get(int fd)
 {
-	struct fd_slot *slots = fd_slots();
+	int n;
+	struct fd_slot *slots = fd_slots_n(&n);
 
-	if ((fd < 0) || (fd >= ZVFS_OPEN_SIZE)) {
+	if ((fd < 0) || (fd >= n)) {
 		errno = EBADF;
 		return NULL;
 	}
 
-	fd = k_array_index_sanitize(fd, ZVFS_OPEN_SIZE);
+	fd = k_array_index_sanitize(fd, n);
 
 	if (slots[fd].entry == NULL) {
 		errno = EBADF;
@@ -349,11 +416,14 @@ static int _check_fd(int fd)
 #if defined(CONFIG_ZTEST) && defined(CONFIG_USERSPACE)
 void *zvfs_test_fd_entry_kobj(int fd)
 {
-	if ((fd < 0) || (fd >= ZVFS_OPEN_SIZE)) {
+	int n;
+	struct fd_slot *slots = fd_slots_n(&n);
+
+	if ((fd < 0) || (fd >= n)) {
 		return NULL;
 	}
 
-	return fd_slots()[k_array_index_sanitize(fd, ZVFS_OPEN_SIZE)].entry;
+	return slots[k_array_index_sanitize(fd, n)].entry;
 }
 #endif
 
@@ -532,6 +602,7 @@ FILE *zvfs_fdopen(int fd, const char *mode)
 
 int zvfs_fileno(FILE *file)
 {
+	int n;
 	struct fd_slot *slots;
 
 	if (!IS_ARRAY_ELEMENT(fdtable, file)) {
@@ -539,8 +610,8 @@ int zvfs_fileno(FILE *file)
 		return -1;
 	}
 
-	slots = fd_slots();
-	for (int fd = 0; fd < ZVFS_OPEN_SIZE; fd++) {
+	slots = fd_slots_n(&n);
+	for (int fd = 0; fd < n; fd++) {
 		if (slots[fd].entry == (struct fd_entry *)file) {
 			return fd;
 		}
@@ -550,6 +621,245 @@ int zvfs_fileno(FILE *file)
 	return -1;
 }
 
+#ifdef CONFIG_PROCESS
+#ifdef CONFIG_SYS_PROCESS
+/* mirror zvfs_close() for a descriptor no thread can resolve anymore */
+static void fds_orphan_close(struct fd_slot *slot, int fd)
+{
+	struct fd_entry *entry = slot->entry;
+
+	(void)k_mutex_lock(&entry->lock, K_FOREVER);
+	if (entry->vtable != NULL && entry->vtable->close != NULL && entry->obj != NULL &&
+	    zvfs_fd_obj_refcount(entry->obj) <= 1) {
+		if (entry->mode & ZVFS_MODE_IFSOCK) {
+			(void)entry->vtable->close2(entry->obj, fd);
+		} else {
+			(void)entry->vtable->close(entry->obj);
+		}
+	}
+	k_mutex_unlock(&entry->lock);
+
+	slot->entry = NULL;
+	slot->flags = 0;
+	(void)fd_entry_unref(entry);
+}
+
+static void fds_graveyard_drain(void)
+{
+	while (true) {
+		sys_dnode_t *node = NULL;
+		struct zvfs_proc_fds *t;
+
+		K_SPINLOCK(&fds_graveyard_lock) {
+			node = sys_dlist_get(&fds_graveyard);
+		}
+		if (node == NULL) {
+			break;
+		}
+		t = CONTAINER_OF(node, struct zvfs_proc_fds, node);
+		for (int fd = 0; fd < ZVFS_PROC_OPEN_SIZE; fd++) {
+			if (t->slots[fd].entry != NULL) {
+				fds_orphan_close(&t->slots[fd], fd);
+			}
+		}
+		K_SPINLOCK(&fds_graveyard_lock) {
+			(void)sys_elastipool_free(&fds_pool, t);
+		}
+	}
+}
+
+int z_zvfs_fds_clone(struct k_process *child, k_tid_t leader)
+{
+	int n;
+	struct fd_slot *parent;
+	struct zvfs_proc_fds *t;
+
+	ARG_UNUSED(leader);
+
+	fds_graveyard_drain();
+
+	t = NULL;
+	K_SPINLOCK(&fds_graveyard_lock) {
+		(void)sys_elastipool_alloc(&fds_pool, (void **)&t);
+	}
+	if (t == NULL) {
+		return -ENOMEM;
+	}
+	memset(t, 0, sizeof(*t));
+	sys_dnode_init(&t->node);
+
+	(void)k_mutex_lock(&fdtable_lock, K_FOREVER);
+	parent = fd_slots_n(&n);
+	for (int fd = 0; fd < MIN(n, ZVFS_PROC_OPEN_SIZE); fd++) {
+		struct fd_entry *entry = parent[fd].entry;
+
+		if (entry == NULL) {
+			continue;
+		}
+		/* the child's descriptor shares the description, flags copied */
+		(void)fd_entry_ref(entry);
+		t->slots[fd].entry = entry;
+		t->slots[fd].flags = parent[fd].flags;
+#ifdef CONFIG_USERSPACE
+		k_object_access_grant(entry, leader);
+		if ((entry->obj != NULL) && (k_object_find(entry->obj) != NULL)) {
+			k_object_access_grant(entry->obj, leader);
+		}
+#endif
+	}
+	child->fds = t;
+	k_mutex_unlock(&fdtable_lock);
+
+	return 0;
+}
+
+void zvfs_fds_cloexec(void)
+{
+	int n;
+	struct fd_slot *slots = fd_slots_n(&n);
+
+	/* close every descriptor the caller marked FD_CLOEXEC */
+	for (int fd = 0; fd < n; fd++) {
+		if ((slots[fd].entry != NULL) && ((slots[fd].flags & ZVFS_FD_CLOEXEC) != 0)) {
+			(void)zvfs_close(fd);
+		}
+	}
+}
+#endif /* CONFIG_SYS_PROCESS */
+
+void z_process_fds_exit(void)
+{
+	int n;
+	struct fd_slot *slots = fd_slots_n(&n);
+
+	/* kernel processes share the boot table; it is never swept */
+	if (slots == fd_boot_slots) {
+		return;
+	}
+
+	for (int fd = 0; fd < n; fd++) {
+		if (slots[fd].entry != NULL) {
+			(void)zvfs_close(fd);
+		}
+	}
+}
+
+void z_process_fds_drop(struct k_process *proc)
+{
+	struct zvfs_proc_fds *t = proc->fds;
+
+	if (t == NULL) {
+		return;
+	}
+	proc->fds = NULL;
+
+	/* the caller holds process_lock: closes and frees defer to the drain */
+	K_SPINLOCK(&fds_graveyard_lock) {
+		sys_dlist_append(&fds_graveyard, &t->node);
+	}
+}
+
+#ifdef CONFIG_SYS_PROCESS
+/*
+ * Child-table operations: the child is created paused, so its table has
+ * no concurrent user; the caller's table and the shared pool are covered
+ * by fdtable_lock.
+ */
+static struct zvfs_proc_fds *child_fds(k_tid_t child)
+{
+	struct k_process *proc = (child == NULL) ? NULL : child->process;
+
+	return (proc == NULL) ? NULL : proc->fds;
+}
+
+int z_zvfs_fds_child_set(k_tid_t child, int childfd, int parentfd)
+{
+	int ret = 0;
+	struct zvfs_proc_fds *t = child_fds(child);
+
+	if ((t == NULL) || (childfd < 0) || (childfd >= ZVFS_PROC_OPEN_SIZE)) {
+		return -EBADF;
+	}
+
+	(void)k_mutex_lock(&fdtable_lock, K_FOREVER);
+
+	struct fd_slot *src = zvfs_fd_slot_get(parentfd);
+
+	if (src == NULL) {
+		ret = -EBADF;
+	} else {
+		if (t->slots[childfd].entry != NULL) {
+			fds_orphan_close(&t->slots[childfd], childfd);
+		}
+		(void)fd_entry_ref(src->entry);
+		t->slots[childfd].entry = src->entry;
+		t->slots[childfd].flags = src->flags;
+#ifdef CONFIG_USERSPACE
+		k_object_access_grant(src->entry, child);
+		if ((src->entry->obj != NULL) && (k_object_find(src->entry->obj) != NULL)) {
+			k_object_access_grant(src->entry->obj, child);
+		}
+#endif
+	}
+
+	k_mutex_unlock(&fdtable_lock);
+
+	return ret;
+}
+
+int z_zvfs_fds_child_dup2(k_tid_t child, int oldfd, int newfd)
+{
+	int ret = 0;
+	struct zvfs_proc_fds *t = child_fds(child);
+
+	if ((t == NULL) || (oldfd < 0) || (oldfd >= ZVFS_PROC_OPEN_SIZE) || (newfd < 0) ||
+	    (newfd >= ZVFS_PROC_OPEN_SIZE)) {
+		return -EBADF;
+	}
+
+	(void)k_mutex_lock(&fdtable_lock, K_FOREVER);
+
+	if (t->slots[oldfd].entry == NULL) {
+		ret = -EBADF;
+	} else if (oldfd != newfd) {
+		if (t->slots[newfd].entry != NULL) {
+			fds_orphan_close(&t->slots[newfd], newfd);
+		}
+		(void)fd_entry_ref(t->slots[oldfd].entry);
+		t->slots[newfd].entry = t->slots[oldfd].entry;
+		/* dup2() semantics: the new descriptor's FD_CLOEXEC is clear */
+		t->slots[newfd].flags = 0;
+	}
+
+	k_mutex_unlock(&fdtable_lock);
+
+	return ret;
+}
+
+int z_zvfs_fds_child_close(k_tid_t child, int fd)
+{
+	int ret = 0;
+	struct zvfs_proc_fds *t = child_fds(child);
+
+	if ((t == NULL) || (fd < 0) || (fd >= ZVFS_PROC_OPEN_SIZE)) {
+		return -EBADF;
+	}
+
+	(void)k_mutex_lock(&fdtable_lock, K_FOREVER);
+
+	if (t->slots[fd].entry == NULL) {
+		ret = -EBADF;
+	} else {
+		fds_orphan_close(&t->slots[fd], fd);
+	}
+
+	k_mutex_unlock(&fdtable_lock);
+
+	return ret;
+}
+#endif /* CONFIG_SYS_PROCESS */
+#endif /* CONFIG_PROCESS */
+
 #if defined(CONFIG_POSIX_DEVICE_IO) || defined(CONFIG_ZVFS_RESERVE_STDIO_FDS)
 /*
  * fd operations for stdio/stdout/stderr
diff --git a/tests/kernel/process/src/main.c b/tests/kernel/process/src/main.c
index d2bac61dfff..a975c397fd4 100644
--- a/tests/kernel/process/src/main.c
+++ b/tests/kernel/process/src/main.c
@@ -1065,7 +1065,12 @@ static void process_init_two_threads(void)
 	zassert_equal(k_process_init(&pi_record2, list, 1), -EBUSY);
 
 	/* both listed threads are members and observe the leader handle as pid */
+	pi_seen_pid_b = NULL;
 	k_thread_start(list[1]);
+	/* b records its pid before a's k_exit() can abort it */
+	while (pi_seen_pid_b == NULL) {
+		k_msleep(1);
+	}
 	k_thread_start(list[0]);
 	zassert_ok(k_waitpid(list[0], NULL, &ws, 0, K_FOREVER));
 	zassert_true(K_WIFEXITED(ws));
