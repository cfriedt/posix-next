diff --git a/lib/os/zvfs/zvfs_fs.c b/lib/os/zvfs/zvfs_fs.c
index ed5cf6941ff..7f01fbedd6b 100644
--- a/lib/os/zvfs/zvfs_fs.c
+++ b/lib/os/zvfs/zvfs_fs.c
@@ -246,6 +246,13 @@ static ssize_t fs_write_offs_vmeth(void *obj, const void *buffer, size_t count,
 {
 	struct zvfs_fs_desc *ptr = obj;
 	ssize_t rc;
+	long saved;
+
+	saved = fs_tell(&ptr->file);
+	if (saved < 0) {
+		errno = -saved;
+		return -1;
+	}

 	rc = fs_seek(&ptr->file, (off_t)offset, FS_SEEK_SET);
 	if (rc < 0) {
@@ -253,7 +260,17 @@ static ssize_t fs_write_offs_vmeth(void *obj, const void *buffer, size_t count,
 		return -1;
 	}

-	return fs_write_vmeth(obj, buffer, count);
+	rc = fs_write_vmeth(obj, buffer, count);
+	if (rc < 0) {
+		return rc;
+	}
+
+	if (fs_seek(&ptr->file, saved, FS_SEEK_SET) < 0) {
+		errno = EIO;
+		return -1;
+	}
+
+	return rc;
 }

 static ssize_t fs_read_vmeth(void *obj, void *buffer, size_t count)
@@ -274,6 +291,13 @@ static ssize_t fs_read_offs_vmeth(void *obj, void *buffer, size_t count, size_t
 {
 	struct zvfs_fs_desc *ptr = obj;
 	ssize_t rc;
+	long saved;
+
+	saved = fs_tell(&ptr->file);
+	if (saved < 0) {
+		errno = -saved;
+		return -1;
+	}

 	rc = fs_seek(&ptr->file, (off_t)offset, FS_SEEK_SET);
 	if (rc < 0) {
@@ -281,7 +305,17 @@ static ssize_t fs_read_offs_vmeth(void *obj, void *buffer, size_t count, size_t
 		return -1;
 	}

-	return fs_read_vmeth(obj, buffer, count);
+	rc = fs_read_vmeth(obj, buffer, count);
+	if (rc < 0) {
+		return rc;
+	}
+
+	if (fs_seek(&ptr->file, saved, FS_SEEK_SET) < 0) {
+		errno = EIO;
+		return -1;
+	}
+
+	return rc;
 }

 static struct fd_op_vtable fs_fd_op_vtable = {
diff --git a/lib/os/zvfs/zvfs_rw.c b/lib/os/zvfs/zvfs_rw.c
index 2cbfc877ef6..8fe258c1683 100644
--- a/lib/os/zvfs/zvfs_rw.c
+++ b/lib/os/zvfs/zvfs_rw.c
@@ -13,6 +13,7 @@ bool supports_pread_pwrite(uint32_t mode)
 {
 	switch (mode & ZVFS_MODE_IFMT) {
 	case ZVFS_MODE_IFSHM:
+	case ZVFS_MODE_IFREG:
 		return true;
 	default:
 		return false;
