diff --git a/include/zephyr/net/net_ip.h b/include/zephyr/net/net_ip.h
--- a/include/zephyr/net/net_ip.h
+++ b/include/zephyr/net/net_ip.h
@@ -91,6 +91,53 @@
 	ZSOCK_TYPE_RAW                   /**< RAW socket type      */
 };
 
+
+/** shutdown() how values (compatible with Linux SHUT_*). */
+#define ZSOCK_SHUT_RD   0
+#define ZSOCK_SHUT_WR   1
+#define ZSOCK_SHUT_RDWR 2
+
+/** Socket-level setsockopt/getsockopt level (SOL_SOCKET). */
+#define ZSOCK_SOL_SOCKET 1
+
+#define ZSOCK_SO_DEBUG        1
+#define ZSOCK_SO_REUSEADDR    2
+#define ZSOCK_SO_TYPE         3
+#define ZSOCK_SO_ERROR        4
+#define ZSOCK_SO_DONTROUTE    5
+#define ZSOCK_SO_BROADCAST    6
+#define ZSOCK_SO_SNDBUF       7
+#define ZSOCK_SO_RCVBUF       8
+#define ZSOCK_SO_KEEPALIVE    9
+#define ZSOCK_SO_OOBINLINE    10
+#define ZSOCK_SO_PRIORITY     12
+#define ZSOCK_SO_LINGER       13
+#define ZSOCK_SO_REUSEPORT    15
+#define ZSOCK_SO_RCVLOWAT     18
+#define ZSOCK_SO_SNDLOWAT     19
+#define ZSOCK_SO_RCVTIMEO     20
+#define ZSOCK_SO_SNDTIMEO     21
+#define ZSOCK_SO_BINDTODEVICE 25
+#define ZSOCK_SO_ACCEPTCONN   30
+#define ZSOCK_SO_TIMESTAMPING 37
+#define ZSOCK_SO_PROTOCOL     38
+#define ZSOCK_SO_DOMAIN       39
+#define ZSOCK_SO_SOCKS5       60
+#define ZSOCK_SO_TXTIME       61
+
+/** Ancillary data type for TX time scheduling (same as ZSOCK_SO_TXTIME). */
+#define ZSOCK_SCM_TXTIME ZSOCK_SO_TXTIME
+
+/** Maximum pending connection queue length for listen(). */
+#define ZSOCK_SOMAXCONN 128
+
+/** recv/recvmsg flags (ZSOCK_MSG_* values are compatible with Linux). */
+#define ZSOCK_MSG_PEEK     0x02
+#define ZSOCK_MSG_CTRUNC   0x08
+#define ZSOCK_MSG_TRUNC    0x20
+#define ZSOCK_MSG_DONTWAIT 0x40
+#define ZSOCK_MSG_WAITALL  0x100
+
 /** @brief Convert 16-bit value from network to host byte order.
  *
  * @param x The network byte order value to convert.
diff --git a/include/zephyr/net/socket.h b/include/zephyr/net/socket.h
--- a/include/zephyr/net/socket.h
+++ b/include/zephyr/net/socket.h
@@ -62,41 +62,6 @@
 /** @} */
 
 /**
- * @name Options for sending and receiving data
- * @{
- */
-/** zsock_recv: Read data without removing it from socket input queue */
-#define ZSOCK_MSG_PEEK 0x02
-/** zsock_recvmsg: Control data buffer too small.
- */
-#define ZSOCK_MSG_CTRUNC 0x08
-/** zsock_recv: return the real length of the datagram, even when it was longer
- *  than the passed buffer
- */
-#define ZSOCK_MSG_TRUNC 0x20
-/** zsock_recv/zsock_send: Override operation to non-blocking */
-#define ZSOCK_MSG_DONTWAIT 0x40
-/** zsock_recv: block until the full amount of data can be returned */
-#define ZSOCK_MSG_WAITALL 0x100
-/** @} */
-
-/**
- * @name Options for shutdown() function
- * @{
- */
-/* Well-known values, e.g. from Linux man 2 shutdown:
- * "The constants SHUT_RD, SHUT_WR, SHUT_RDWR have the value 0, 1, 2,
- * respectively". Some software uses numeric values.
- */
-/** zsock_shutdown: Shut down for reading */
-#define ZSOCK_SHUT_RD 0
-/** zsock_shutdown: Shut down for writing */
-#define ZSOCK_SHUT_WR 1
-/** zsock_shutdown: Shut down for both reading and writing */
-#define ZSOCK_SHUT_RDWR 2
-/** @} */
-
-/**
  * @defgroup secure_sockets_options Socket options for TLS
  * @since 1.13
  * @version 0.8.0
@@ -873,81 +838,9 @@
 /** @} */
 
 /**
- * @name Socket level options (SOL_SOCKET)
+ * @name Socket timestamping flags (for ZSOCK_SO_TIMESTAMPING)
  * @{
  */
-/** Socket-level option */
-#define SOL_SOCKET 1
-
-/* Socket options for SOL_SOCKET level */
-
-/** Recording debugging information (ignored, for compatibility) */
-#define SO_DEBUG 1
-/** address reuse */
-#define SO_REUSEADDR 2
-/** Type of the socket */
-#define SO_TYPE 3
-/** Async error */
-#define SO_ERROR 4
-/** Bypass normal routing and send directly to host (ignored, for compatibility) */
-#define SO_DONTROUTE 5
-/** Transmission of broadcast messages is supported (ignored, for compatibility) */
-#define SO_BROADCAST 6
-
-/** Size of socket send buffer */
-#define SO_SNDBUF 7
-/** Size of socket recv buffer */
-#define SO_RCVBUF 8
-
-/** Enable sending keep-alive messages on connections */
-#define SO_KEEPALIVE 9
-/** Place out-of-band data into receive stream (ignored, for compatibility) */
-#define SO_OOBINLINE 10
-/** Socket priority */
-#define SO_PRIORITY 12
-/** Socket lingers on close (ignored, for compatibility) */
-#define SO_LINGER 13
-/** Allow multiple sockets to reuse a single port */
-#define SO_REUSEPORT 15
-
-/** Receive low watermark (ignored, for compatibility) */
-#define SO_RCVLOWAT 18
-/** Send low watermark (ignored, for compatibility) */
-#define SO_SNDLOWAT 19
-
-/**
- * Receive timeout
- * Applies to receive functions like recv(), but not to connect()
- */
-#define SO_RCVTIMEO 20
-/** Send timeout */
-#define SO_SNDTIMEO 21
-
-/** Bind a socket to an interface */
-#define SO_BINDTODEVICE	25
-
-/** Socket accepts incoming connections (ignored, for compatibility) */
-#define SO_ACCEPTCONN 30
-
-/** Timestamp TX RX or both packets. Supports multiple timestamp sources. */
-#define SO_TIMESTAMPING 37
-
-/** Protocol used with the socket */
-#define SO_PROTOCOL 38
-
-/** Domain used with SOCKET */
-#define SO_DOMAIN 39
-
-/** Enable SOCKS5 for Socket */
-#define SO_SOCKS5 60
-
-/** Socket TX time (when the data should be sent) */
-#define SO_TXTIME 61
-/** Socket TX time (same as SO_TXTIME) */
-#define SCM_TXTIME SO_TXTIME
-
-/** Timestamp generation flags */
-
 /** Request RX timestamps generated by network adapter. */
 #define SOF_TIMESTAMPING_RX_HARDWARE BIT(0)
 /**
@@ -955,9 +848,6 @@
  * This can be enabled via socket option or control messages.
  */
 #define SOF_TIMESTAMPING_TX_HARDWARE BIT(1)
-
-/** */
-
 /** @} */
 
 /**
@@ -1153,14 +1043,6 @@
 /** @} */
 
 /**
- * @name Backlog size for listen()
- * @{
- */
-/** listen: The maximum backlog queue length */
-#define SOMAXCONN 128
-/** @} */
-
-/**
  * @name Macros for checking special IPv6 addresses.
  * @{
  */
diff --git a/drivers/net/nsos_adapt.c b/drivers/net/nsos_adapt.c
--- a/drivers/net/nsos_adapt.c
+++ b/drivers/net/nsos_adapt.c
@@ -630,7 +630,7 @@
 			socklen_t optlen = sizeof(err);
 			int ret;
 
-			ret = getsockopt(fd, SOL_SOCKET, SO_ERROR, &err, &optlen);
+			ret = getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &err, &optlen);
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
 			}
@@ -645,7 +645,7 @@
 			int ret;
 			int err;
 
-			ret = getsockopt(fd, SOL_SOCKET, SO_TYPE, &type, &optlen);
+			ret = getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &type, &optlen);
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
 			}
@@ -663,7 +663,7 @@
 			int ret;
 			int err;
 
-			ret = getsockopt(fd, SOL_SOCKET, SO_PROTOCOL, &proto, &optlen);
+			ret = getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &proto, &optlen);
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
 			}
@@ -681,7 +681,7 @@
 			int ret;
 			int err;
 
-			ret = getsockopt(fd, SOL_SOCKET, SO_DOMAIN, &family, &optlen);
+			ret = getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &family, &optlen);
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
 			}
@@ -694,22 +694,22 @@
 			return 0;
 		}
 		case NSOS_MID_SO_RCVBUF:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_RCVBUF,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_SNDBUF:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_SNDBUF,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_REUSEADDR:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_REUSEADDR,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_REUSEPORT:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_REUSEPORT,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_LINGER:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_LINGER,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_LINGER,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_KEEPALIVE:
-			return nsos_adapt_getsockopt_int(fd, SOL_SOCKET, SO_KEEPALIVE,
+			return nsos_adapt_getsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE,
 							 nsos_mid_optval, nsos_mid_optlen);
 		}
 		break;
@@ -763,7 +763,7 @@
 	case NSOS_MID_SOL_SOCKET:
 		switch (nsos_mid_optname) {
 		case NSOS_MID_SO_PRIORITY:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_PRIORITY,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_PRIORITY,
 							 nsos_mid_optval, nsos_mid_optlen);
 
 		case NSOS_MID_SO_RCVTIMEO: {
@@ -774,7 +774,7 @@
 			};
 			int ret;
 
-			ret = setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO,
+			ret = setsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO,
 					 &tv, sizeof(tv));
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
@@ -790,7 +790,7 @@
 			};
 			int ret;
 
-			ret = setsockopt(fd, SOL_SOCKET, SO_SNDTIMEO,
+			ret = setsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO,
 					 &tv, sizeof(tv));
 			if (ret < 0) {
 				return -nsi_errno_to_mid(errno);
@@ -799,22 +799,22 @@
 			return 0;
 		}
 		case NSOS_MID_SO_RCVBUF:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_RCVBUF,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_SNDBUF:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_SNDBUF,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_REUSEADDR:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_REUSEADDR,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_REUSEPORT:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_REUSEPORT,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_LINGER:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_LINGER,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_LINGER,
 							 nsos_mid_optval, nsos_mid_optlen);
 		case NSOS_MID_SO_KEEPALIVE:
-			return nsos_adapt_setsockopt_int(fd, SOL_SOCKET, SO_KEEPALIVE,
+			return nsos_adapt_setsockopt_int(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE,
 							 nsos_mid_optval, nsos_mid_optlen);
 		}
 		break;
diff --git a/drivers/net/nsos_sockets.c b/drivers/net/nsos_sockets.c
--- a/drivers/net/nsos_sockets.c
+++ b/drivers/net/nsos_sockets.c
@@ -1064,9 +1064,9 @@
 	struct nsos_socket *sock = obj;
 
 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
-		case SO_ERROR: {
+		case ZSOCK_SO_ERROR: {
 			int nsos_mid_err;
 			int err;
 
@@ -1086,7 +1086,7 @@
 
 			return 0;
 		}
-		case SO_TYPE: {
+		case ZSOCK_SO_TYPE: {
 			int nsos_mid_type;
 			int err;
 
@@ -1110,7 +1110,7 @@
 
 			return 0;
 		}
-		case SO_PROTOCOL: {
+		case ZSOCK_SO_PROTOCOL: {
 			int nsos_mid_proto;
 			int err;
 
@@ -1134,7 +1134,7 @@
 
 			return 0;
 		}
-		case SO_DOMAIN: {
+		case ZSOCK_SO_DOMAIN: {
 			int nsos_mid_family;
 			int err;
 
@@ -1158,23 +1158,23 @@
 
 			return 0;
 		}
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_RCVBUF,
 						   optval, optlen);
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_SNDBUF,
 						   optval, optlen);
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEADDR,
 						   optval, optlen);
-		case SO_REUSEPORT:
+		case ZSOCK_SO_REUSEPORT:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEPORT,
 						   optval, optlen);
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_KEEPALIVE,
 						   optval, optlen);
@@ -1242,9 +1242,9 @@
 	struct nsos_socket *sock = obj;
 
 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
-		case SO_PRIORITY: {
+		case ZSOCK_SO_PRIORITY: {
 			int nsos_mid_priority;
 			int err;
 
@@ -1265,7 +1265,7 @@
 
 			return 0;
 		}
-		case SO_RCVTIMEO: {
+		case ZSOCK_SO_RCVTIMEO: {
 			const struct zsock_timeval *tv = optval;
 			struct nsos_mid_timeval nsos_mid_tv;
 			int err;
@@ -1294,7 +1294,7 @@
 
 			return 0;
 		}
-		case SO_SNDTIMEO: {
+		case ZSOCK_SO_SNDTIMEO: {
 			const struct zsock_timeval *tv = optval;
 			struct nsos_mid_timeval nsos_mid_tv;
 			int err;
@@ -1323,27 +1323,27 @@
 
 			return 0;
 		}
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_RCVBUF,
 						   optval, optlen);
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_SNDBUF,
 						   optval, optlen);
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEADDR,
 						   optval, optlen);
-		case SO_REUSEPORT:
+		case ZSOCK_SO_REUSEPORT:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEPORT,
 						   optval, optlen);
-		case SO_LINGER:
+		case ZSOCK_SO_LINGER:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_LINGER,
 						   optval, optlen);
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_KEEPALIVE,
 						   optval, optlen);
diff --git a/drivers/usb/device/usb_dc_native_posix_adapt.c b/drivers/usb/device/usb_dc_native_posix_adapt.c
--- a/drivers/usb/device/usb_dc_native_posix_adapt.c
+++ b/drivers/usb/device/usb_dc_native_posix_adapt.c
@@ -283,7 +283,7 @@
 		posix_exit(EXIT_FAILURE);
 	}
 
-	if (setsockopt(listenfd, SOL_SOCKET, SO_REUSEADDR,
+	if (setsockopt(listenfd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR,
 		       (const char *)&reuse, sizeof(reuse)) < 0) {
 		LOG_WRN("setsockopt() failed: %s", strerror(errno));
 	}
@@ -298,7 +298,7 @@
 		posix_exit(EXIT_FAILURE);
 	}
 
-	if (listen(listenfd, SOMAXCONN) < 0) {
+	if (listen(listenfd, ZSOCK_SOMAXCONN) < 0) {
 		LOG_ERR("listen() failed: %s", strerror(errno));
 		posix_exit(EXIT_FAILURE);
 	}
diff --git a/drivers/wifi/simplelink/simplelink_sockets.c b/drivers/wifi/simplelink/simplelink_sockets.c
--- a/drivers/wifi/simplelink/simplelink_sockets.c
+++ b/drivers/wifi/simplelink/simplelink_sockets.c
@@ -767,7 +767,7 @@
 			goto exit;
 		}
 	} else {
-		/* Can be SOL_SOCKET or TI specific: */
+		/* Can be ZSOCK_SOL_SOCKET or TI specific: */
 
 		/* Note: this logic should match SimpleLink SDK's socket.c: */
 		switch (optname) {
@@ -786,9 +786,9 @@
 			 * EINVAL in order to not break "off-the-shelf" BSD
 			 * code.
 			 */
-		case SO_BROADCAST:
-		case SO_REUSEADDR:
-		case SO_SNDBUF:
+		case ZSOCK_SO_BROADCAST:
+		case ZSOCK_SO_REUSEADDR:
+		case ZSOCK_SO_SNDBUF:
 			retval = slcb_SetErrno(EINVAL);
 			goto exit;
 		default:
@@ -826,7 +826,7 @@
 			goto exit;
 		}
 	} else {
-		/* Can be SOL_SOCKET or TI specific: */
+		/* Can be ZSOCK_SOL_SOCKET or TI specific: */
 
 		/* Note: this logic should match SimpleLink SDK's socket.c: */
 		switch (optname) {
@@ -842,9 +842,9 @@
 			 * errno to EINVAL in order to not break "off-the-shelf"
 			 * BSD code.
 			 */
-		case SO_BROADCAST:
-		case SO_REUSEADDR:
-		case SO_SNDBUF:
+		case ZSOCK_SO_BROADCAST:
+		case ZSOCK_SO_REUSEADDR:
+		case ZSOCK_SO_SNDBUF:
 			retval = slcb_SetErrno(EINVAL);
 			goto exit;
 		default:
diff --git a/modules/openthread/platform/dhcp6_pd.c b/modules/openthread/platform/dhcp6_pd.c
--- a/modules/openthread/platform/dhcp6_pd.c
+++ b/modules/openthread/platform/dhcp6_pd.c
@@ -126,15 +126,15 @@
 	VerifyOrExit(zsock_bind(dhcpv6_pd_client_sock, (struct zsock_sockaddr *)&addr,
 				sizeof(addr)) == 0);
 
-	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, SOL_SOCKET, SO_REUSEADDR,
+	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR,
 				      &on, sizeof(on)) == 0);
-	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, SOL_SOCKET, SO_REUSEPORT,
+	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT,
 				      &on, sizeof(on)) == 0);
 
 	VerifyOrExit(net_if_get_name(net_if_get_by_index(infra_if_index), name,
 				     CONFIG_NET_INTERFACE_NAME_LEN) > 0);
 	memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 				      sizeof(if_req)) == 0);
 	VerifyOrExit(zsock_setsockopt(dhcpv6_pd_client_sock, IPPROTO_IPV6, IPV6_MULTICAST_HOPS,
 				      &hop_limit, sizeof(hop_limit)) == 0);
diff --git a/modules/openthread/platform/mdns_socket.c b/modules/openthread/platform/mdns_socket.c
--- a/modules/openthread/platform/mdns_socket.c
+++ b/modules/openthread/platform/mdns_socket.c
@@ -120,12 +120,12 @@
 				     CONFIG_NET_INTERFACE_NAME_LEN) > 0,
 		     error = OT_ERROR_FAILED);
 	memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 				      sizeof(if_req)) == 0,
 		     error = OT_ERROR_FAILED);
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, SOL_SOCKET, SO_REUSEADDR, &on, sizeof(on)) == 0,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &on, sizeof(on)) == 0,
 		     error = OT_ERROR_FAILED);
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, SOL_SOCKET, SO_REUSEPORT, &on, sizeof(on)) == 0,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT, &on, sizeof(on)) == 0,
 		     error = OT_ERROR_FAILED);
 	VerifyOrExit(zsock_setsockopt(mdns_sock_v6, IPPROTO_IPV6, IPV6_V6ONLY, &on,
 				      sizeof(on)) == 0,
@@ -181,12 +181,12 @@
 				     CONFIG_NET_INTERFACE_NAME_LEN) > 0,
 		     error = OT_ERROR_FAILED);
 	memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 				      sizeof(if_req)) == 0,
 		     error = OT_ERROR_FAILED);
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, SOL_SOCKET, SO_REUSEADDR, &on, sizeof(on)) == 0,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &on, sizeof(on)) == 0,
 		     error = OT_ERROR_FAILED);
-	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, SOL_SOCKET, SO_REUSEPORT, &on, sizeof(on)) == 0,
+	VerifyOrExit(zsock_setsockopt(mdns_sock_v4, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT, &on, sizeof(on)) == 0,
 		     error = OT_ERROR_FAILED);
 	mreq_v4.imr_ifindex = ail_iface_idx;
 	mreq_v4.imr_multiaddr.s_addr = ZSOCK_HTONL(0xE00000FB);
@@ -337,7 +337,7 @@
 	VerifyOrExit(evt->event.revents & ZSOCK_POLLIN);
 	VerifyOrExit(openthread_border_router_allocate_message((void **)&req) == OT_ERROR_NONE);
 #if defined(CONFIG_NET_IPV4)
-	(void)zsock_getsockopt(evt->event.fd, SOL_SOCKET, SO_DOMAIN, &family, &optlen);
+	(void)zsock_getsockopt(evt->event.fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &family, &optlen);
 
 	if (family == ZSOCK_AF_INET) {
 		addrlen = sizeof(addr_v4);
diff --git a/modules/openthread/platform/trel.c b/modules/openthread/platform/trel.c
--- a/modules/openthread/platform/trel.c
+++ b/modules/openthread/platform/trel.c
@@ -153,7 +153,7 @@
 				     CONFIG_NET_INTERFACE_NAME_LEN) > 0,
 		     error = OT_ERROR_FAILED);
 	memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-	VerifyOrExit(zsock_setsockopt(trel_sock, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+	VerifyOrExit(zsock_setsockopt(trel_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 				      sizeof(if_req)) == 0,
 		      error = OT_ERROR_FAILED);
 
diff --git a/modules/openthread/platform/udp.c b/modules/openthread/platform/udp.c
--- a/modules/openthread/platform/udp.c
+++ b/modules/openthread/platform/udp.c
@@ -170,14 +170,14 @@
 
 	switch (aNetifIdentifier) {
 	case OT_NETIF_UNSPECIFIED:
-		VerifyOrExit(zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE, NULL, 0) == 0,
+		VerifyOrExit(zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, NULL, 0) == 0,
 			     error = OT_ERROR_FAILED);
 		break;
 	case OT_NETIF_THREAD_HOST:
 		VerifyOrExit(net_if_get_name(ot_iface_ptr, name, CONFIG_NET_INTERFACE_NAME_LEN) > 0,
 			     error = OT_ERROR_FAILED);
 		memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-		VerifyOrExit(zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+		VerifyOrExit(zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 					      sizeof(if_req)) == 0,
 			     error = OT_ERROR_FAILED);
 		break;
@@ -188,7 +188,7 @@
 					     CONFIG_NET_INTERFACE_NAME_LEN) > 0,
 			     error = OT_ERROR_FAILED);
 		memcpy(if_req.ifr_name, name, MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
-		VerifyOrExit(zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE, &if_req,
+		VerifyOrExit(zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &if_req,
 					      sizeof(if_req)) == 0,
 			     error = OT_ERROR_FAILED);
 		break;
diff --git a/modules/thrift/src/thrift/transport/TSSLSocket.cpp b/modules/thrift/src/thrift/transport/TSSLSocket.cpp
--- a/modules/thrift/src/thrift/transport/TSSLSocket.cpp
+++ b/modules/thrift/src/thrift/transport/TSSLSocket.cpp
@@ -161,7 +161,7 @@
 #ifdef SO_NOSIGPIPE
 	{
 		int one = 1;
-		setsockopt(socket_, SOL_SOCKET, SO_NOSIGPIPE, &one, sizeof(one));
+		setsockopt(socket_, ZSOCK_SOL_SOCKET, SO_NOSIGPIPE, &one, sizeof(one));
 	}
 #endif
 
@@ -221,7 +221,7 @@
 		int val;
 		socklen_t lon;
 		lon = sizeof(int);
-		int ret2 = getsockopt(socket_, SOL_SOCKET, SO_ERROR, cast_sockopt(&val), &lon);
+		int ret2 = getsockopt(socket_, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, cast_sockopt(&val), &lon);
 		if (ret2 == -1) {
 			int errno_copy = THRIFT_GET_SOCKET_ERROR;
 			GlobalOutput.perror("TSocket::open() getsockopt() " + getSocketInfo(),
diff --git a/samples/net/cellular_modem/src/main.c b/samples/net/cellular_modem/src/main.c
--- a/samples/net/cellular_modem/src/main.c
+++ b/samples/net/cellular_modem/src/main.c
@@ -285,7 +285,7 @@
 	{
 		const struct timeval tv = { .tv_sec = 10 };
 
-		if (zsock_setsockopt(socket_fd, SOL_SOCKET, SO_RCVTIMEO, &tv, sizeof(tv)) < 0) {
+		if (zsock_setsockopt(socket_fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &tv, sizeof(tv)) < 0) {
 			printk("Failed to set socket receive timeout (%d)\n", errno);
 			return -1;
 		}
diff --git a/scripts/coredump/coredump_gdbserver.py b/scripts/coredump/coredump_gdbserver.py
--- a/scripts/coredump/coredump_gdbserver.py
+++ b/scripts/coredump/coredump_gdbserver.py
@@ -124,7 +124,7 @@
 
         # Reuse address so we don't have to wait for socket to be
         # close before we can bind to the port again
-        gdbserver.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
+        gdbserver.setsockopt(socket.ZSOCK_SOL_SOCKET, socket.ZSOCK_SO_REUSEADDR, 1)
 
         gdbserver.bind((GDBSERVER_HOST, args.port))
         gdbserver.listen(1)
diff --git a/subsys/net/ip/net_context.c b/subsys/net/ip/net_context.c
--- a/subsys/net/ip/net_context.c
+++ b/subsys/net/ip/net_context.c
@@ -2329,8 +2329,8 @@
 	for (cmsg = CMSG_FIRSTHDR(msghdr); cmsg != NULL;
 	     cmsg = CMSG_NXTHDR(msghdr, cmsg)) {
 		if (cmsg->cmsg_len == CMSG_LEN(sizeof(uint64_t)) &&
-		    cmsg->cmsg_level == SOL_SOCKET &&
-		    cmsg->cmsg_type == SCM_TXTIME) {
+		    cmsg->cmsg_level == ZSOCK_SOL_SOCKET &&
+		    cmsg->cmsg_type == ZSOCK_SCM_TXTIME) {
 			net_pkt_set_timestamp_ns(pkt, *(net_time_t *)CMSG_DATA(cmsg));
 			break;
 		}
diff --git a/subsys/net/lib/dhcpv4/dhcpv4_server.c b/subsys/net/lib/dhcpv4/dhcpv4_server.c
--- a/subsys/net/lib/dhcpv4/dhcpv4_server.c
+++ b/subsys/net/lib/dhcpv4/dhcpv4_server.c
@@ -1618,7 +1618,7 @@
 		goto error;
 	}
 
-	ret = zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
 	if (ret < 0) {
 		ret = -errno;
diff --git a/subsys/net/lib/dns/dispatcher.c b/subsys/net/lib/dns/dispatcher.c
--- a/subsys/net/lib/dns/dispatcher.c
+++ b/subsys/net/lib/dns/dispatcher.c
@@ -133,13 +133,13 @@
 
 	k_mutex_lock(&dispatcher->lock, K_FOREVER);
 
-	(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-			       SO_DOMAIN, &family, &optlen);
+	(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+			       ZSOCK_SO_DOMAIN, &family, &optlen);
 
 	if ((pev->event.revents & ZSOCK_POLLERR) ||
 	    (pev->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		if (sock_error > 0) {
 			NET_ERR("Receiver IPv%d socket error (%d)",
 				family == ZSOCK_AF_INET ? 4 : 6, sock_error);
diff --git a/subsys/net/lib/dns/llmnr_responder.c b/subsys/net/lib/dns/llmnr_responder.c
--- a/subsys/net/lib/dns/llmnr_responder.c
+++ b/subsys/net/lib/dns/llmnr_responder.c
@@ -459,7 +459,7 @@
 	queries = ret;
 
 	optlen = sizeof(int);
-	(void)zsock_getsockopt(sock, SOL_SOCKET, SO_DOMAIN, &family, &optlen);
+	(void)zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &family, &optlen);
 
 	NET_DBG("Received %d %s from %s (id 0x%04x)", queries,
 		queries > 1 ? "queries" : "query",
@@ -519,10 +519,10 @@
 
 	if ((pev->event.revents & ZSOCK_POLLERR) ||
 	    (pev->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_DOMAIN, &family, &optlen);
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_DOMAIN, &family, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		NET_ERR("Receiver IPv%d socket error (%d)",
 			family == ZSOCK_AF_INET ? 4 : 6, sock_error);
 		ret = DNS_EAI_SYSTEM;
diff --git a/subsys/net/lib/dns/mdns_responder.c b/subsys/net/lib/dns/mdns_responder.c
--- a/subsys/net/lib/dns/mdns_responder.c
+++ b/subsys/net/lib/dns/mdns_responder.c
@@ -1364,7 +1364,7 @@
 			memcpy(if_req.ifr_name, name,
 			       MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
 
-			ret = zsock_setsockopt(v6, SOL_SOCKET, SO_BINDTODEVICE,
+			ret = zsock_setsockopt(v6, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 					       &if_req, sizeof(if_req));
 			if (ret < 0) {
 				NET_DBG("Cannot bind sock %d to interface %d (%d)",
@@ -1461,7 +1461,7 @@
 			memcpy(if_req.ifr_name, name,
 			       MIN(sizeof(name) - 1, sizeof(if_req.ifr_name) - 1));
 
-			ret = zsock_setsockopt(v4, SOL_SOCKET, SO_BINDTODEVICE,
+			ret = zsock_setsockopt(v4, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 					       &if_req, sizeof(if_req));
 			if (ret < 0) {
 				NET_DBG("Cannot bind sock %d to interface %d (%d)",
diff --git a/subsys/net/lib/dns/resolve.c b/subsys/net/lib/dns/resolve.c
--- a/subsys/net/lib/dns/resolve.c
+++ b/subsys/net/lib/dns/resolve.c
@@ -410,7 +410,7 @@
 		return ret;
 	}
 
-	ret = zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE,
+	ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 			       &ifreq, sizeof(ifreq));
 	if (ret < 0) {
 		ret = -errno;
diff --git a/subsys/net/lib/http/http_client.c b/subsys/net/lib/http/http_client.c
--- a/subsys/net/lib/http/http_client.c
+++ b/subsys/net/lib/http/http_client.c
@@ -511,7 +511,7 @@
 			int sock_err;
 			zsock_socklen_t optlen = sizeof(sock_err);
 
-			(void)zsock_getsockopt(sock, SOL_SOCKET, SO_ERROR, &sock_err, &optlen);
+			(void)zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &sock_err, &optlen);
 			ret = -sock_err;
 			goto error;
 		} else if (fds[0].revents & ZSOCK_POLLNVAL) {
diff --git a/subsys/net/lib/http/http_server_core.c b/subsys/net/lib/http/http_server_core.c
--- a/subsys/net/lib/http/http_server_core.c
+++ b/subsys/net/lib/http/http_server_core.c
@@ -207,7 +207,7 @@
 		}
 #endif /* defined(CONFIG_NET_SOCKETS_SOCKOPT_TLS) */
 
-		if (zsock_setsockopt(fd, SOL_SOCKET, SO_REUSEADDR, &(int){1},
+		if (zsock_setsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &(int){1},
 				     sizeof(int)) < 0) {
 			LOG_ERR("setsockopt: %d", errno);
 			zsock_close(fd);
@@ -604,8 +604,8 @@
 			}
 
 			if (ctx->fds[i].revents & ZSOCK_POLLERR) {
-				(void)zsock_getsockopt(ctx->fds[i].fd, SOL_SOCKET,
-						       SO_ERROR, &sock_error, &optlen);
+				(void)zsock_getsockopt(ctx->fds[i].fd, ZSOCK_SOL_SOCKET,
+						       ZSOCK_SO_ERROR, &sock_error, &optlen);
 				LOG_DBG("Error on fd %d %d", ctx->fds[i].fd, sock_error);
 
 				if (i >= ctx->listen_fds) {
diff --git a/subsys/net/lib/latmon/latmon.c b/subsys/net/lib/latmon/latmon.c
--- a/subsys/net/lib/latmon/latmon.c
+++ b/subsys/net/lib/latmon/latmon.c
@@ -381,7 +381,7 @@
 		return -1;
 	}
 
-	zsock_setsockopt(s, SOL_SOCKET, SO_REUSEADDR, &on, sizeof(on));
+	zsock_setsockopt(s, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &on, sizeof(on));
 	if (zsock_bind(s, (struct zsock_sockaddr *)&addr, sizeof(addr)) < 0) {
 		LOG_ERR("failed to bind latmon socket : %d", errno);
 		zsock_close(s);
diff --git a/subsys/net/lib/mqtt/mqtt_transport_socket_tcp.c b/subsys/net/lib/mqtt/mqtt_transport_socket_tcp.c
--- a/subsys/net/lib/mqtt/mqtt_transport_socket_tcp.c
+++ b/subsys/net/lib/mqtt/mqtt_transport_socket_tcp.c
@@ -37,8 +37,8 @@
 		strncpy(ifname.ifr_name, client->transport.if_name,
 			sizeof(ifname.ifr_name) - 1);
 
-		ret = zsock_setsockopt(client->transport.tcp.sock, SOL_SOCKET,
-				       SO_BINDTODEVICE, &ifname,
+		ret = zsock_setsockopt(client->transport.tcp.sock, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_BINDTODEVICE, &ifname,
 				       sizeof(struct ifreq));
 		if (ret < 0) {
 			NET_ERR("Failed to bind ot interface %s error (%d)",
@@ -52,7 +52,7 @@
 #if defined(CONFIG_SOCKS)
 	if (client->transport.proxy.addrlen != 0) {
 		ret = setsockopt(client->transport.tcp.sock,
-				 SOL_SOCKET, SO_SOCKS5,
+				 ZSOCK_SOL_SOCKET, ZSOCK_SO_SOCKS5,
 				 &client->transport.proxy.addr,
 				 client->transport.proxy.addrlen);
 		if (ret < 0) {
diff --git a/subsys/net/lib/mqtt/mqtt_transport_socket_tls.c b/subsys/net/lib/mqtt/mqtt_transport_socket_tls.c
--- a/subsys/net/lib/mqtt/mqtt_transport_socket_tls.c
+++ b/subsys/net/lib/mqtt/mqtt_transport_socket_tls.c
@@ -38,8 +38,8 @@
 		strncpy(ifname.ifr_name, client->transport.if_name,
 			sizeof(ifname.ifr_name) - 1);
 
-		ret = zsock_setsockopt(client->transport.tls.sock, SOL_SOCKET,
-				       SO_BINDTODEVICE, &ifname,
+		ret = zsock_setsockopt(client->transport.tls.sock, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_BINDTODEVICE, &ifname,
 				       sizeof(struct ifreq));
 		if (ret < 0) {
 			NET_ERR("Failed to bind ot interface %s error (%d)",
@@ -53,7 +53,7 @@
 #if defined(CONFIG_SOCKS)
 	if (client->transport.proxy.addrlen != 0) {
 		ret = setsockopt(client->transport.tls.sock,
-				 SOL_SOCKET, SO_SOCKS5,
+				 ZSOCK_SOL_SOCKET, ZSOCK_SO_SOCKS5,
 				 &client->transport.proxy.addr,
 				 client->transport.proxy.addrlen);
 		if (ret < 0) {
diff --git a/subsys/net/lib/mqtt_sn/mqtt_sn_transport_udp.c b/subsys/net/lib/mqtt_sn/mqtt_sn_transport_udp.c
--- a/subsys/net/lib/mqtt_sn/mqtt_sn_transport_udp.c
+++ b/subsys/net/lib/mqtt_sn/mqtt_sn_transport_udp.c
@@ -56,7 +56,7 @@
 	LOG_DBG("Socket %d", udp->sock);
 
 	optval = 1;
-	err = zsock_setsockopt(udp->sock, SOL_SOCKET, SO_REUSEADDR, &optval, sizeof(optval));
+	err = zsock_setsockopt(udp->sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &optval, sizeof(optval));
 	if (err < 0) {
 		return errno;
 	}
diff --git a/subsys/net/lib/ptp/transport.c b/subsys/net/lib/ptp/transport.c
--- a/subsys/net/lib/ptp/transport.c
+++ b/subsys/net/lib/ptp/transport.c
@@ -49,8 +49,8 @@
 		return -1;
 	}
 
-	if (zsock_setsockopt(socket, SOL_SOCKET, SO_REUSEADDR, &feature_on, sizeof(feature_on))) {
-		LOG_ERR("Failed to set SO_REUSEADDR");
+	if (zsock_setsockopt(socket, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &feature_on, sizeof(feature_on))) {
+		LOG_ERR("Failed to set ZSOCK_SO_REUSEADDR");
 		goto error;
 	}
 
@@ -61,21 +61,21 @@
 
 	cnt = net_if_get_name(iface, ifreq.ifr_name, INTERFACE_NAME_LEN);
 	if (cnt > 0 && zsock_setsockopt(socket,
-					SOL_SOCKET,
-					SO_BINDTODEVICE,
+					ZSOCK_SOL_SOCKET,
+					ZSOCK_SO_BINDTODEVICE,
 					ifreq.ifr_name,
 					sizeof(ifreq.ifr_name))) {
 		LOG_ERR("Failed to set socket binding to an interface");
 		goto error;
 	}
 
-	if (zsock_setsockopt(socket, SOL_SOCKET, SO_TIMESTAMPING, &ts_mask, sizeof(ts_mask))) {
-		LOG_ERR("Failed to set SO_TIMESTAMPING");
+	if (zsock_setsockopt(socket, ZSOCK_SOL_SOCKET, ZSOCK_SO_TIMESTAMPING, &ts_mask, sizeof(ts_mask))) {
+		LOG_ERR("Failed to set ZSOCK_SO_TIMESTAMPING");
 		goto error;
 	}
 
-	if (zsock_setsockopt(socket, SOL_SOCKET, SO_PRIORITY, &priority, sizeof(priority))) {
-		LOG_ERR("Failed to set SO_PRIORITY");
+	if (zsock_setsockopt(socket, ZSOCK_SOL_SOCKET, ZSOCK_SO_PRIORITY, &priority, sizeof(priority))) {
+		LOG_ERR("Failed to set ZSOCK_SO_PRIORITY");
 		goto error;
 	}
 
@@ -315,7 +315,7 @@
 	}
 
 	for (cmsg = CMSG_FIRSTHDR(&msghdr); cmsg != NULL; cmsg = CMSG_NXTHDR(&msghdr, cmsg)) {
-		if (cmsg->cmsg_level == SOL_SOCKET && cmsg->cmsg_type == SO_TIMESTAMPING) {
+		if (cmsg->cmsg_level == ZSOCK_SOL_SOCKET && cmsg->cmsg_type == ZSOCK_SO_TIMESTAMPING) {
 			memcpy(&msg->timestamp.host, CMSG_DATA(cmsg), sizeof(struct net_ptp_time));
 		}
 	}
diff --git a/subsys/net/lib/sockets/socket_dispatcher.c b/subsys/net/lib/sockets/socket_dispatcher.c
--- a/subsys/net/lib/sockets/socket_dispatcher.c
+++ b/subsys/net/lib/sockets/socket_dispatcher.c
@@ -316,7 +316,7 @@
 {
 	int fd;
 
-	if ((level == SOL_SOCKET) && (optname == SO_BINDTODEVICE)) {
+	if ((level == ZSOCK_SOL_SOCKET) && (optname == ZSOCK_SO_BINDTODEVICE)) {
 		struct net_if *iface;
 		const struct ifreq *ifreq = optval;
 
diff --git a/subsys/net/lib/sockets/socket_obj_core.c b/subsys/net/lib/sockets/socket_obj_core.c
--- a/subsys/net/lib/sockets/socket_obj_core.c
+++ b/subsys/net/lib/sockets/socket_obj_core.c
@@ -174,7 +174,7 @@
 		goto out;
 	}
 
-	ret = zsock_getsockopt(sock, SOL_SOCKET, SO_DOMAIN, &family, &optlen);
+	ret = zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &family, &optlen);
 	if (ret < 0) {
 		NET_ERR("Cannot get socket domain (%d)", -errno);
 		goto out;
diff --git a/subsys/net/lib/sockets/sockets_inet.c b/subsys/net/lib/sockets/sockets_inet.c
--- a/subsys/net/lib/sockets/sockets_inet.c
+++ b/subsys/net/lib/sockets/sockets_inet.c
@@ -1027,7 +1027,7 @@
 	net_context_get_option(ctx, NET_OPT_TIMESTAMPING, &timestamping, NULL);
 
 	if (timestamping) {
-		return insert_pktinfo(msg, SOL_SOCKET, SO_TIMESTAMPING,
+		return insert_pktinfo(msg, ZSOCK_SOL_SOCKET, ZSOCK_SO_TIMESTAMPING,
 				      net_pkt_timestamp(pkt), sizeof(struct net_ptp_time));
 	}
 
@@ -1809,9 +1809,9 @@
 	int ret;
 
 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
-		case SO_ERROR: {
+		case ZSOCK_SO_ERROR: {
 			if (*optlen != sizeof(int)) {
 				errno = EINVAL;
 				return -1;
@@ -1822,7 +1822,7 @@
 			return 0;
 		}
 
-		case SO_TYPE: {
+		case ZSOCK_SO_TYPE: {
 			int type = (int)net_context_get_type(ctx);
 
 			if (*optlen != sizeof(type)) {
@@ -1835,7 +1835,7 @@
 			return 0;
 		}
 
-		case SO_TXTIME:
+		case ZSOCK_SO_TXTIME:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_TXTIME)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_TXTIME,
@@ -1849,7 +1849,7 @@
 			}
 			break;
 
-		case SO_PROTOCOL: {
+		case ZSOCK_SO_PROTOCOL: {
 			int proto = (int)net_context_get_proto(ctx);
 
 			if (*optlen != sizeof(proto)) {
@@ -1862,7 +1862,7 @@
 			return 0;
 		}
 
-		case SO_DOMAIN: {
+		case ZSOCK_SO_DOMAIN: {
 			if (*optlen != sizeof(int)) {
 				errno = EINVAL;
 				return -1;
@@ -1875,7 +1875,7 @@
 
 		break;
 
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_RCVBUF)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_RCVBUF,
@@ -1889,7 +1889,7 @@
 			}
 			break;
 
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_SNDBUF)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_SNDBUF,
@@ -1903,7 +1903,7 @@
 			}
 			break;
 
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_REUSEADDR)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_REUSEADDR,
@@ -1917,7 +1917,7 @@
 			}
 			break;
 
-		case SO_REUSEPORT:
+		case ZSOCK_SO_REUSEPORT:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_REUSEPORT)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_REUSEPORT,
@@ -1931,7 +1931,7 @@
 			}
 			break;
 
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			if (IS_ENABLED(CONFIG_NET_TCP_KEEPALIVE) &&
 			    net_context_get_proto(ctx) == ZSOCK_IPPROTO_TCP) {
 				ret = net_tcp_get_option(ctx,
@@ -1947,7 +1947,7 @@
 
 			break;
 
-		case SO_TIMESTAMPING:
+		case ZSOCK_SO_TIMESTAMPING:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_TIMESTAMPING)) {
 				ret = net_context_get_option(ctx,
 							     NET_OPT_TIMESTAMPING,
@@ -2328,9 +2328,9 @@
 	int ret;
 
 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_RCVBUF)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_RCVBUF,
@@ -2345,7 +2345,7 @@
 
 			break;
 
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_SNDBUF)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_SNDBUF,
@@ -2360,7 +2360,7 @@
 
 			break;
 
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_REUSEADDR)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_REUSEADDR,
@@ -2375,7 +2375,7 @@
 
 			break;
 
-		case SO_REUSEPORT:
+		case ZSOCK_SO_REUSEPORT:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_REUSEPORT)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_REUSEPORT,
@@ -2390,7 +2390,7 @@
 
 			break;
 
-		case SO_PRIORITY:
+		case ZSOCK_SO_PRIORITY:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_PRIORITY)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_PRIORITY,
@@ -2405,7 +2405,7 @@
 
 			break;
 
-		case SO_RCVTIMEO:
+		case ZSOCK_SO_RCVTIMEO:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_RCVTIMEO)) {
 				const struct zsock_timeval *tv = optval;
 				k_timeout_t timeout;
@@ -2437,7 +2437,7 @@
 
 			break;
 
-		case SO_SNDTIMEO:
+		case ZSOCK_SO_SNDTIMEO:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_SNDTIMEO)) {
 				const struct zsock_timeval *tv = optval;
 				k_timeout_t timeout;
@@ -2468,7 +2468,7 @@
 
 			break;
 
-		case SO_TXTIME:
+		case ZSOCK_SO_TXTIME:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_TXTIME)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_TXTIME,
@@ -2483,7 +2483,7 @@
 
 			break;
 
-		case SO_SOCKS5:
+		case ZSOCK_SO_SOCKS5:
 			if (IS_ENABLED(CONFIG_SOCKS)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_SOCKS5,
@@ -2500,7 +2500,7 @@
 
 			break;
 
-		case SO_BINDTODEVICE: {
+		case ZSOCK_SO_BINDTODEVICE: {
 			struct net_if *iface;
 			const struct ifreq *ifreq = optval;
 
@@ -2557,11 +2557,11 @@
 			return 0;
 		}
 
-		case SO_LINGER:
+		case ZSOCK_SO_LINGER:
 			/* ignored. for compatibility purposes only */
 			return 0;
 
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			if (IS_ENABLED(CONFIG_NET_TCP_KEEPALIVE) &&
 			    net_context_get_proto(ctx) == ZSOCK_IPPROTO_TCP) {
 				ret = net_tcp_set_option(ctx,
@@ -2577,7 +2577,7 @@
 
 			break;
 
-		case SO_TIMESTAMPING:
+		case ZSOCK_SO_TIMESTAMPING:
 			if (IS_ENABLED(CONFIG_NET_CONTEXT_TIMESTAMPING)) {
 				ret = net_context_set_option(ctx,
 							     NET_OPT_TIMESTAMPING,
diff --git a/subsys/net/lib/sockets/sockets_tls.c b/subsys/net/lib/sockets/sockets_tls.c
--- a/subsys/net/lib/sockets/sockets_tls.c
+++ b/subsys/net/lib/sockets/sockets_tls.c
@@ -763,7 +763,7 @@
 			int optval;
 			zsock_socklen_t optlen = sizeof(optval);
 
-			if (zsock_getsockopt(fds.fd, SOL_SOCKET, SO_ERROR,
+			if (zsock_getsockopt(fds.fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR,
 					     &optval, &optlen) == 0) {
 				NET_ERR("TLS underlying socket poll error %d",
 					-optval);
@@ -3656,7 +3656,7 @@
 		return -1;
 	}
 
-	if ((level == SOL_SOCKET) && (optname == SO_PROTOCOL)) {
+	if ((level == ZSOCK_SOL_SOCKET) && (optname == ZSOCK_SO_PROTOCOL)) {
 		/* Protocol type is overridden during socket creation. Its
 		 * value is restored here to return current value.
 		 */
@@ -3669,11 +3669,11 @@
 	}
 
 	/* In case error was set on a socket at the TLS layer (for example due
-	 * to receiving TLS alert), handle SO_ERROR here, and report that error.
-	 * Otherwise, forward the SO_ERROR option request to the underlying
+	 * to receiving TLS alert), handle ZSOCK_SO_ERROR here, and report that error.
+	 * Otherwise, forward the ZSOCK_SO_ERROR option request to the underlying
 	 * TCP/UDP socket to handle.
 	 */
-	if ((level == SOL_SOCKET) && (optname == SO_ERROR) && ctx->error != 0) {
+	if ((level == ZSOCK_SOL_SOCKET) && (optname == ZSOCK_SO_ERROR) && ctx->error != 0) {
 		if (*optlen != sizeof(int)) {
 			errno = EINVAL;
 			return -1;
@@ -3784,12 +3784,12 @@
 	/* Underlying socket is used in non-blocking mode, hence implement
 	 * timeout at the TLS socket level.
 	 */
-	if ((level == SOL_SOCKET) && (optname == SO_SNDTIMEO)) {
+	if ((level == ZSOCK_SOL_SOCKET) && (optname == ZSOCK_SO_SNDTIMEO)) {
 		err = set_timeout_opt(&ctx->options.timeout_tx, optval, optlen);
 		goto out;
 	}
 
-	if ((level == SOL_SOCKET) && (optname == SO_RCVTIMEO)) {
+	if ((level == ZSOCK_SOL_SOCKET) && (optname == ZSOCK_SO_RCVTIMEO)) {
 		err = set_timeout_opt(&ctx->options.timeout_rx, optval, optlen);
 		goto out;
 	}
diff --git a/subsys/net/lib/zperf/zperf_common.c b/subsys/net/lib/zperf/zperf_common.c
--- a/subsys/net/lib/zperf/zperf_common.c
+++ b/subsys/net/lib/zperf/zperf_common.c
@@ -240,10 +240,10 @@
 			goto error;
 		}
 
-		if (zsock_setsockopt(sock, SOL_SOCKET, SO_PRIORITY,
+		if (zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_PRIORITY,
 				     &prio,
 				     sizeof(prio)) != 0) {
-			NET_WARN("Failed to set SOL_SOCKET - SO_PRIORITY socket option.");
+			NET_WARN("Failed to set ZSOCK_SOL_SOCKET - ZSOCK_SO_PRIORITY socket option.");
 			ret = -errno;
 			goto error;
 		}
diff --git a/subsys/net/lib/zperf/zperf_tcp_receiver.c b/subsys/net/lib/zperf/zperf_tcp_receiver.c
--- a/subsys/net/lib/zperf/zperf_tcp_receiver.c
+++ b/subsys/net/lib/zperf/zperf_tcp_receiver.c
@@ -136,10 +136,10 @@
 
 	if ((pev->event.revents & ZSOCK_POLLERR) ||
 	    (pev->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_DOMAIN, &family, &optlen);
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_DOMAIN, &family, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		NET_ERR("TCP receiver IPv%d socket error (%d)",
 			family == ZSOCK_AF_INET ? 4 : 6, sock_error);
 		ret = -sock_error;
@@ -161,8 +161,8 @@
 				    &addrlen);
 		if (sock < 0) {
 			ret = -errno;
-			(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-					       SO_DOMAIN, &family, &optlen);
+			(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+					       ZSOCK_SO_DOMAIN, &family, &optlen);
 			NET_ERR("TCP receiver IPv%d accept error (%d)",
 				family == ZSOCK_AF_INET ? 4 : 6, ret);
 			goto error;
@@ -191,8 +191,8 @@
 	} else {
 		ret = zsock_recv(pev->event.fd, buf, sizeof(buf), 0);
 		if (ret < 0) {
-			(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-					       SO_DOMAIN, &family, &optlen);
+			(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+					       ZSOCK_SO_DOMAIN, &family, &optlen);
 			NET_ERR("recv failed on IPv%d socket (%d)",
 				family == ZSOCK_AF_INET ? 4 : 6,
 				errno);
diff --git a/subsys/net/lib/zperf/zperf_udp_receiver.c b/subsys/net/lib/zperf/zperf_udp_receiver.c
--- a/subsys/net/lib/zperf/zperf_udp_receiver.c
+++ b/subsys/net/lib/zperf/zperf_udp_receiver.c
@@ -327,10 +327,10 @@
 
 	if ((pev->event.revents & ZSOCK_POLLERR) ||
 	    (pev->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_DOMAIN, &family, &optlen);
-		(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_DOMAIN, &family, &optlen);
+		(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		NET_ERR("UDP receiver IPv%d socket error (%d)",
 			family == ZSOCK_AF_INET ? 4 : 6, sock_error);
 		ret = -sock_error;
@@ -351,8 +351,8 @@
 
 		if (ret < 0) {
 			ret = -errno;
-			(void)zsock_getsockopt(pev->event.fd, SOL_SOCKET,
-					       SO_DOMAIN, &family, &optlen);
+			(void)zsock_getsockopt(pev->event.fd, ZSOCK_SOL_SOCKET,
+					       ZSOCK_SO_DOMAIN, &family, &optlen);
 			NET_ERR("recv failed on IPv%d socket (%d)",
 				family == ZSOCK_AF_INET ? 4 : 6, -ret);
 			goto error;
diff --git a/subsys/net/lib/zperf/zperf_udp_uploader.c b/subsys/net/lib/zperf/zperf_udp_uploader.c
--- a/subsys/net/lib/zperf/zperf_udp_uploader.c
+++ b/subsys/net/lib/zperf/zperf_udp_uploader.c
@@ -114,7 +114,7 @@
 			continue;
 		} else {
 			/* Receive statistics */
-			ret = zsock_setsockopt(sock, SOL_SOCKET, SO_RCVTIMEO, &rcvtimeo,
+			ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &rcvtimeo,
 					       sizeof(rcvtimeo));
 			if (ret < 0) {
 				NET_ERR("setsockopt error (%d)", errno);
@@ -346,9 +346,9 @@
 		strncpy(req.ifr_name, param->if_name, IFNAMSIZ);
 		req.ifr_name[IFNAMSIZ - 1] = 0;
 
-		if (zsock_setsockopt(sock, SOL_SOCKET, SO_BINDTODEVICE, &req,
+		if (zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &req,
 				     sizeof(struct ifreq)) != 0) {
-			NET_WARN("setsockopt SO_BINDTODEVICE error (%d)", -errno);
+			NET_WARN("setsockopt ZSOCK_SO_BINDTODEVICE error (%d)", -errno);
 		}
 	}
 
diff --git a/subsys/shell/backends/shell_telnet.c b/subsys/shell/backends/shell_telnet.c
--- a/subsys/shell/backends/shell_telnet.c
+++ b/subsys/shell/backends/shell_telnet.c
@@ -473,8 +473,8 @@
 
 	if ((evt->event.revents & ZSOCK_POLLERR) ||
 	    (evt->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(evt->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(evt->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		NET_ERR("Telnet socket %d error (%d)", evt->event.fd, sock_error);
 
 		if (evt->event.fd == sh_telnet->fds[SOCK_ID_CLIENT].fd) {
diff --git a/subsys/shell/backends/shell_websocket.c b/subsys/shell/backends/shell_websocket.c
--- a/subsys/shell/backends/shell_websocket.c
+++ b/subsys/shell/backends/shell_websocket.c
@@ -163,8 +163,8 @@
 
 	if ((evt->event.revents & ZSOCK_POLLERR) ||
 	    (evt->event.revents & ZSOCK_POLLNVAL)) {
-		(void)zsock_getsockopt(evt->event.fd, SOL_SOCKET,
-				       SO_ERROR, &sock_error, &optlen);
+		(void)zsock_getsockopt(evt->event.fd, ZSOCK_SOL_SOCKET,
+				       ZSOCK_SO_ERROR, &sock_error, &optlen);
 		LOG_ERR("Websocket socket %d error (%d)", evt->event.fd, sock_error);
 
 		if (evt->event.fd == ws->fds[0].fd) {
diff --git a/subsys/usb/host/usbip.c b/subsys/usb/host/usbip.c
--- a/subsys/usb/host/usbip.c
+++ b/subsys/usb/host/usbip.c
@@ -652,7 +652,7 @@
 		return;
 	}
 
-	if (zsock_setsockopt(listenfd, SOL_SOCKET, SO_REUSEADDR,
+	if (zsock_setsockopt(listenfd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR,
 			     (const char *)&reuse, sizeof(reuse)) < 0) {
 		LOG_INF("setsockopt() failed: %s", strerror(errno));
 	}
diff --git a/tests/net/ieee802154/l2/src/ieee802154_test.c b/tests/net/ieee802154/l2/src/ieee802154_test.c
--- a/tests/net/ieee802154/l2/src/ieee802154_test.c
+++ b/tests/net/ieee802154/l2/src/ieee802154_test.c
@@ -416,7 +416,7 @@
 		goto release_fd;
 	}
 
-	if (zsock_setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval, sizeof(timeo_optval))) {
+	if (zsock_setsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval, sizeof(timeo_optval))) {
 		NET_ERR("*** Failed to set reception timeout on packet socket : %d", errno);
 		goto release_fd;
 	}
@@ -1127,7 +1127,7 @@
 		goto release_fd;
 	}
 
-	if (zsock_setsockopt(fd, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval, sizeof(timeo_optval))) {
+	if (zsock_setsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval, sizeof(timeo_optval))) {
 		NET_ERR("*** Failed to set reception timeout on packet socket : %d", errno);
 		goto release_fd;
 	}
diff --git a/tests/net/lib/http_server/core/src/main.c b/tests/net/lib/http_server/core/src/main.c
--- a/tests/net/lib/http_server/core/src/main.c
+++ b/tests/net/lib/http_server/core/src/main.c
@@ -2812,7 +2812,7 @@
 	}
 	client_fd = ret;
 
-	ret = zsock_setsockopt(client_fd, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(client_fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	if (ret < 0) {
 		printk("Failed to set timeout (%d)\n", errno);
diff --git a/tests/net/lib/mqtt/v3_1_1/mqtt_client/src/main.c b/tests/net/lib/mqtt/v3_1_1/mqtt_client/src/main.c
--- a/tests/net/lib/mqtt/v3_1_1/mqtt_client/src/main.c
+++ b/tests/net/lib/mqtt/v3_1_1/mqtt_client/src/main.c
@@ -129,10 +129,10 @@
 		printk("Failed to create server socket\n");
 	}
 
-	ret = zsock_setsockopt(s_sock, SOL_SOCKET, SO_REUSEADDR, &reuseaddr,
+	ret = zsock_setsockopt(s_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &reuseaddr,
 			       sizeof(reuseaddr));
 	if (ret < 0) {
-		printk("Failed to set SO_REUSEADDR on server socket, %d\n", errno);
+		printk("Failed to set ZSOCK_SO_REUSEADDR on server socket, %d\n", errno);
 	}
 
 	ret = zsock_bind(s_sock, (struct zsock_sockaddr *)&bind_addr, sizeof(bind_addr));
diff --git a/tests/net/socket/af_inet_raw/src/main.c b/tests/net/socket/af_inet_raw/src/main.c
--- a/tests/net/socket/af_inet_raw/src/main.c
+++ b/tests/net/socket/af_inet_raw/src/main.c
@@ -143,7 +143,7 @@
 	*sock = zsock_socket(family, ZSOCK_TYPE_RAW, proto);
 	zassert_true(*sock >= 0, "Failed to create RAW socket(%d)", errno);
 
-	ret = zsock_setsockopt(*sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(*sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_ok(ret, "setsockopt failed (%d)", errno);
 }
@@ -172,7 +172,7 @@
 	ret = zsock_bind(*sock, bind_addr, addrlen);
 	zassert_ok(ret, "Binding UDP socket failed (%d)", errno);
 
-	ret = zsock_setsockopt(*sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(*sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_ok(ret, "setsockopt failed (%d)", errno);
 }
@@ -206,7 +206,7 @@
 	ret = zsock_bind(packet_sock, (struct zsock_sockaddr *)&addr, sizeof(addr));
 	zassert_ok(ret, "Binding packet socket failed (%d)", errno);
 
-	ret = zsock_setsockopt(packet_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(packet_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_ok(ret, "setsockopt failed (%d)", errno);
 }
diff --git a/tests/net/socket/af_packet/src/main.c b/tests/net/socket/af_packet/src/main.c
--- a/tests/net/socket/af_packet/src/main.c
+++ b/tests/net/socket/af_packet/src/main.c
@@ -195,7 +195,7 @@
 	*sock = zsock_socket(ZSOCK_AF_PACKET, type, proto);
 	zassert_true(*sock >= 0, "Cannot create packet socket (%d)", -errno);
 
-	ret = zsock_setsockopt(*sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(*sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_ok(ret, "setsockopt failed (%d)", errno);
 }
@@ -270,7 +270,7 @@
 	ret = zsock_bind(*sock, (struct zsock_sockaddr *) sockaddr, sizeof(*sockaddr));
 	zassert_equal(ret, 0, "Cannot bind DGRAM (UDP) socket (%d)", -errno);
 
-	ret = zsock_setsockopt(*sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(*sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_ok(ret, "setsockopt failed (%d)", errno);
 }
diff --git a/tests/net/socket/misc/src/main.c b/tests/net/socket/misc/src/main.c
--- a/tests/net/socket/misc/src/main.c
+++ b/tests/net/socket/misc/src/main.c
@@ -217,9 +217,9 @@
 	/* Bind server socket with interface 2. */
 
 	strcpy(ifreq.ifr_name, DEV2_NAME);
-	ret = zsock_setsockopt(sock_s, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_s, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", errno);
 
 	/* Bind client socket with interface 1 and send a packet. */
 
@@ -228,9 +228,9 @@
 	strcpy(ifreq.ifr_name, DEV1_NAME);
 	strcpy(send_buf, DEV1_NAME);
 
-	ret = zsock_setsockopt(sock_c, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_c, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", errno);
 
 	ret = zsock_sendto(sock_c, send_buf, strlen(send_buf) + 1, 0,
 			   peer_addr_1, peer_addrlen);
@@ -253,9 +253,9 @@
 	strcpy(ifreq.ifr_name, DEV2_NAME);
 	strcpy(send_buf, DEV2_NAME);
 
-	ret = zsock_setsockopt(sock_c, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_c, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", errno);
 
 	ret = zsock_sendto(sock_c, send_buf, strlen(send_buf) + 1, 0,
 			   peer_addr_2, peer_addrlen);
@@ -281,9 +281,9 @@
 	/* Remove the binding from the server socket. */
 
 	strcpy(ifreq.ifr_name, "");
-	ret = zsock_setsockopt(sock_s, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_s, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", errno);
 
 	/* Bind client socket with interface 1 again. */
 
@@ -291,9 +291,9 @@
 	strcpy(ifreq.ifr_name, DEV1_NAME);
 	strcpy(send_buf, DEV1_NAME);
 
-	ret = zsock_setsockopt(sock_c, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_c, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", errno);
 
 	ret = zsock_sendto(sock_c, send_buf, strlen(send_buf) + 1, 0,
 			   peer_addr_1, peer_addrlen);
@@ -936,19 +936,19 @@
 	sock_u = zsock_socket(ZSOCK_AF_INET6, ZSOCK_TYPE_DGRAM, ZSOCK_IPPROTO_UDP);
 	zassert_true(sock_u >= 0, "UDP socket open failed");
 
-	ret = zsock_getsockopt(sock_t, SOL_SOCKET, SO_DOMAIN, &domain, &optlen);
+	ret = zsock_getsockopt(sock_t, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &domain, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed, %d", -errno);
 	zassert_equal(domain, ZSOCK_AF_INET, "Mismatch domain value %d vs %d",
 		      ZSOCK_AF_INET, domain);
 
-	ret = zsock_getsockopt(sock_u, SOL_SOCKET, SO_DOMAIN, &domain, &optlen);
+	ret = zsock_getsockopt(sock_u, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &domain, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed, %d", -errno);
 	zassert_equal(domain, ZSOCK_AF_INET6, "Mismatch domain value %d vs %d",
 		      ZSOCK_AF_INET6, domain);
 
 	/* setsockopt() is not supported for this option */
 	domain = ZSOCK_AF_INET;
-	ret = zsock_setsockopt(sock_u, SOL_SOCKET, SO_DOMAIN, &domain, optlen);
+	ret = zsock_setsockopt(sock_u, ZSOCK_SOL_SOCKET, ZSOCK_SO_DOMAIN, &domain, optlen);
 	zassert_equal(ret, -1, "setsockopt succeed");
 	zassert_equal(errno, ENOPROTOOPT, "Invalid errno %d", errno);
 
diff --git a/tests/net/socket/offload_dispatcher/src/main.c b/tests/net/socket/offload_dispatcher/src/main.c
--- a/tests/net/socket/offload_dispatcher/src/main.c
+++ b/tests/net/socket/offload_dispatcher/src/main.c
@@ -606,7 +606,7 @@
 	struct timeval optval = { 0 };
 	zsock_socklen_t optlen = sizeof(optval);
 
-	ret = zsock_getsockopt(test_sock, SOL_SOCKET, SO_RCVTIMEO,
+	ret = zsock_getsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO,
 			       &optval, &optlen);
 	zassert_equal(0, ret, "getsockopt() failed");
 	zassert_true(test_socket_ctx[OFFLOAD_1].socket_called,
@@ -623,7 +623,7 @@
 	int ret;
 	struct timeval optval = { 0 };
 
-	ret = zsock_setsockopt(test_sock, SOL_SOCKET, SO_RCVTIMEO,
+	ret = zsock_setsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO,
 			       &optval, sizeof(optval));
 	zassert_equal(0, ret, "setsockopt() failed");
 	zassert_true(test_socket_ctx[OFFLOAD_1].socket_called,
@@ -700,7 +700,7 @@
 		.sin_family = ZSOCK_AF_INET
 	};
 
-	ret = zsock_setsockopt(test_sock, SOL_SOCKET, SO_BINDTODEVICE,
+	ret = zsock_setsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 			       &ifreq, sizeof(ifreq));
 	zassert_equal(0, ret, "setsockopt() failed");
 	zassert_false(test_socket_ctx[OFFLOAD_1].socket_called,
@@ -733,7 +733,7 @@
 	};
 	struct zsock_sockaddr_in addr = test_peer_addr;
 
-	ret = zsock_setsockopt(test_sock, SOL_SOCKET, SO_BINDTODEVICE,
+	ret = zsock_setsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 			       &ifreq, sizeof(ifreq));
 
 	zassert_equal(0, ret, "setsockopt() failed");
@@ -781,7 +781,7 @@
 	zassert_not_null(obj, "No obj found");
 	zassert_true(net_socket_is_tls(obj), "Socket is not a native TLS sock");
 
-	ret = zsock_setsockopt(test_sock, SOL_SOCKET, SO_BINDTODEVICE,
+	ret = zsock_setsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 			       &ifreq, sizeof(ifreq));
 	zassert_equal(0, ret, "setsockopt() failed");
 	zassert_false(test_socket_ctx[OFFLOAD_1].socket_called,
@@ -828,7 +828,7 @@
 	zassert_not_null(obj, "No obj found");
 	zassert_true(net_socket_is_tls(obj), "Socket is not a native TLS sock");
 
-	ret = zsock_setsockopt(test_sock, SOL_SOCKET, SO_BINDTODEVICE,
+	ret = zsock_setsockopt(test_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE,
 			       &ifreq, sizeof(ifreq));
 	zassert_equal(0, ret, "setsockopt() failed");
 	zassert_false(test_socket_ctx[OFFLOAD_1].socket_called,
diff --git a/tests/net/socket/reuseaddr_reuseport/src/main.c b/tests/net/socket/reuseaddr_reuseport/src/main.c
--- a/tests/net/socket/reuseaddr_reuseport/src/main.c
+++ b/tests/net/socket/reuseaddr_reuseport/src/main.c
@@ -107,7 +107,7 @@
 {
 	int ret;
 
-	ret = zsock_getsockopt(sock, SOL_SOCKET, SO_REUSEADDR, optval, optlen);
+	ret = zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, optval, optlen);
 	zassert_equal(ret, 0, "getsocketopt() failed with error %d", errno);
 }
 
@@ -115,7 +115,7 @@
 {
 	int ret;
 
-	ret = zsock_setsockopt(sock, SOL_SOCKET, SO_REUSEADDR, optval, optlen);
+	ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, optval, optlen);
 	zassert_equal(ret, 0, "setsocketopt() failed with error %d", errno);
 }
 
@@ -130,7 +130,7 @@
 {
 	int ret;
 
-	ret = zsock_getsockopt(sock, SOL_SOCKET, SO_REUSEPORT, optval, optlen);
+	ret = zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT, optval, optlen);
 	zassert_equal(ret, 0, "getsocketopt() failed with error %d", errno);
 }
 
@@ -138,7 +138,7 @@
 {
 	int ret;
 
-	ret = zsock_setsockopt(sock, SOL_SOCKET, SO_REUSEPORT, optval, optlen);
+	ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEPORT, optval, optlen);
 	zassert_equal(ret, 0, "setsocketopt() failed with error %d", errno);
 }
 
@@ -265,31 +265,31 @@
 	/* Read initial value */
 	test_getsocketopt_reuseaddr(server_sock, (void *)&value, &value_size);
 	zassert_equal(value_size, sizeof(int), "incorrect value size returned by getsocketopt()");
-	zassert_equal(value, (int) false, "SO_REUSEADDR incorrectly set (expected false)");
+	zassert_equal(value, (int) false, "ZSOCK_SO_REUSEADDR incorrectly set (expected false)");
 
 	/* Enable option */
 	value = 1;
 	test_setsocketopt_reuseaddr(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseaddr(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEADDR not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEADDR not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = 2;
 	test_setsocketopt_reuseaddr(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseaddr(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEADDR not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEADDR not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = 0x100;
 	test_setsocketopt_reuseaddr(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseaddr(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEADDR not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEADDR not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = -1;
 	test_setsocketopt_reuseaddr(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseaddr(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEADDR not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEADDR not correctly set, returned %d", value);
 
 	zsock_close(server_sock);
 }
@@ -316,7 +316,7 @@
 	/* Try to bind the second socket, should fail */
 	test_bind_fail(server_sock2, &bind_addr2, sizeof(bind_addr2));
 
-	/* Enable SO_REUSEADDR option for the second socket */
+	/* Enable ZSOCK_SO_REUSEADDR option for the second socket */
 	test_enable_reuseaddr(server_sock2);
 
 	/* Try to bind the second socket again */
@@ -399,7 +399,7 @@
 	/* Set the first socket to LISTEN state */
 	test_listen(server_sock1);
 
-	/* Enable SO_REUSEADDR option for the second socket */
+	/* Enable ZSOCK_SO_REUSEADDR option for the second socket */
 	test_enable_reuseaddr(server_sock2);
 
 	/* Try to bind the second socket, should fail */
@@ -482,7 +482,7 @@
 	/* Bind the server socket, should fail */
 	test_bind_fail(server_sock, (struct zsock_sockaddr *) &bind_addr, sizeof(bind_addr));
 
-	/* Enable SO_REUSEADDR option for the new server socket */
+	/* Enable ZSOCK_SO_REUSEADDR option for the new server socket */
 	test_enable_reuseaddr(server_sock);
 
 	/* Try to bind the new server socket again, should work now */
@@ -542,31 +542,31 @@
 	/* Read initial value */
 	test_getsocketopt_reuseport(server_sock, (void *)&value, &value_size);
 	zassert_equal(value_size, sizeof(int), "incorrect value size returned by getsocketopt()");
-	zassert_equal(value, (int) false, "SO_REUSEPORT incorrectly set (expected false)");
+	zassert_equal(value, (int) false, "ZSOCK_SO_REUSEPORT incorrectly set (expected false)");
 
 	/* Enable option */
 	value = 1;
 	test_setsocketopt_reuseport(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseport(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEPORT not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEPORT not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = 2;
 	test_setsocketopt_reuseport(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseport(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEPORT not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEPORT not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = 0x100;
 	test_setsocketopt_reuseport(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseport(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEPORT not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEPORT not correctly set, returned %d", value);
 
 	/* Enable option (with other value as linux takes any int here) */
 	value = -1;
 	test_setsocketopt_reuseport(server_sock, (void *)&value, sizeof(value));
 	test_getsocketopt_reuseport(server_sock, (void *)&value, &value_size);
-	zassert_equal(value, (int) true, "SO_REUSEPORT not correctly set, returned %d", value);
+	zassert_equal(value, (int) true, "ZSOCK_SO_REUSEPORT not correctly set, returned %d", value);
 
 	zsock_close(server_sock);
 }
@@ -587,7 +587,7 @@
 	prepare_sock_tcp(family, first_ip, LOCAL_PORT, &server_sock1, &bind_addr1);
 	prepare_sock_tcp(family, second_ip, LOCAL_PORT, &server_sock2, &bind_addr2);
 
-	/* Depending on the expected result, we enable SO_REUSEPORT for the first socket */
+	/* Depending on the expected result, we enable ZSOCK_SO_REUSEPORT for the first socket */
 	if (should_succeed) {
 		test_enable_reuseport(server_sock1);
 	}
@@ -598,7 +598,7 @@
 	/* Try to bind the second socket, should fail */
 	test_bind_fail(server_sock2, &bind_addr2, sizeof(bind_addr2));
 
-	/* Enable SO_REUSEPORT option for the second socket */
+	/* Enable ZSOCK_SO_REUSEPORT option for the second socket */
 	test_enable_reuseport(server_sock2);
 
 	/* Try to bind the second socket again */
@@ -924,7 +924,7 @@
 	prepare_sock_tcp(family, client_ip, LOCAL_PORT + 1, &client_sock1, &client_addr);
 	prepare_sock_tcp(family, client_ip, LOCAL_PORT, &client_sock2, &connect_addr);
 
-	/* Enable SO_REUSEPORT option for the two sockets */
+	/* Enable ZSOCK_SO_REUSEPORT option for the two sockets */
 	test_enable_reuseport(client_sock1);
 	test_enable_reuseport(client_sock2);
 
diff --git a/tests/net/socket/tcp/src/main.c b/tests/net/socket/tcp/src/main.c
--- a/tests/net/socket/tcp/src/main.c
+++ b/tests/net/socket/tcp/src/main.c
@@ -529,7 +529,7 @@
 	test_accept(s_sock, &new_sock, &addr, &addrlen);
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in), "wrong addrlen");
 
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1290,12 +1290,12 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4);
 	prepare_sock_tcp_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_TYPE, &optval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_STREAM, "getsockopt got invalid type");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_TYPE, &optval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_STREAM, "getsockopt got invalid type");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
@@ -1319,12 +1319,12 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4);
 	prepare_sock_tcp_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_PROTOCOL, &optval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_IPPROTO_TCP, "getsockopt got invalid protocol");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_PROTOCOL, &optval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_IPPROTO_TCP, "getsockopt got invalid protocol");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
@@ -1347,26 +1347,26 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4);
 	prepare_sock_tcp_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6);
 
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_RCVBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &optval, sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", rv);
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_RCVBUF, &retval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &retval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", rv);
 	zassert_equal(retval, optval, "getsockopt got invalid rcvbuf");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_RCVBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &optval, sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", rv);
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_RCVBUF, &retval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &retval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", rv);
 	zassert_equal(retval, optval, "getsockopt got invalid rcvbuf");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
 	optval = -1;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_RCVBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &optval, sizeof(optval));
 	zassert_equal(rv, -1, "setsockopt failed (%d)", rv);
 
 	optval = UINT16_MAX + 1;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_RCVBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &optval, sizeof(optval));
 	zassert_equal(rv, -1, "setsockopt failed (%d)", rv);
 
 	test_close(sock1);
@@ -1400,7 +1400,7 @@
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in), "wrong addrlen");
 
 	/* Lower server-side RX window size. */
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			      sizeof(buf_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1436,26 +1436,26 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4);
 	prepare_sock_tcp_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6);
 
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_SNDBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &optval, sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", rv);
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_SNDBUF, &retval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &retval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", rv);
 	zassert_equal(retval, optval, "getsockopt got invalid rcvbuf");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_SNDBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &optval, sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", rv);
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_SNDBUF, &retval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &retval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", rv);
 	zassert_equal(retval, optval, "getsockopt got invalid rcvbuf");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
 	optval = -1;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_SNDBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &optval, sizeof(optval));
 	zassert_equal(rv, -1, "setsockopt failed (%d)", rv);
 
 	optval = UINT16_MAX + 1;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_RCVBUF, &optval, sizeof(optval));
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &optval, sizeof(optval));
 	zassert_equal(rv, -1, "setsockopt failed (%d)", rv);
 
 	test_close(sock1);
@@ -1481,7 +1481,7 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, SERVER_PORT, &s_sock, &s_saddr);
 
 	/* Lower client-side TX window size. */
-	rv = zsock_setsockopt(c_sock, SOL_SOCKET, SO_SNDBUF, &buf_optval,
+	rv = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDBUF, &buf_optval,
 			      sizeof(buf_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1544,12 +1544,12 @@
 	test_accept(s_sock, &new_sock, &addr, &addrlen);
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in), "wrong addrlen");
 
-	rv = zsock_setsockopt(c_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval.tv_usec = 0;
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1611,12 +1611,12 @@
 	test_accept(s_sock, &new_sock, &addr, &addrlen);
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in6), "wrong addrlen");
 
-	rv = zsock_setsockopt(c_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval.tv_usec = 0;
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1676,12 +1676,12 @@
 	test_accept(s_sock, &new_sock, &addr, &addrlen);
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in), "wrong addrlen");
 
-	rv = zsock_setsockopt(c_sock, SOL_SOCKET, SO_SNDTIMEO, &timeo_optval,
+	rv = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO, &timeo_optval,
 			      sizeof(timeo_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			      sizeof(buf_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1691,7 +1691,7 @@
 	/* Wait for ACK (empty window). */
 	k_msleep(150);
 
-	/* Client should not be able to send now and time out after SO_SNDTIMEO */
+	/* Client should not be able to send now and time out after ZSOCK_SO_SNDTIMEO */
 	start_time = k_uptime_get_32();
 	rv = zsock_send(c_sock, tx_buf, 1, 0);
 	time_diff = k_uptime_get_32() - start_time;
@@ -1737,12 +1737,12 @@
 	test_accept(s_sock, &new_sock, &addr, &addrlen);
 	zassert_equal(addrlen, sizeof(struct zsock_sockaddr_in6), "wrong addrlen");
 
-	rv = zsock_setsockopt(c_sock, SOL_SOCKET, SO_SNDTIMEO, &timeo_optval,
+	rv = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO, &timeo_optval,
 			      sizeof(timeo_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	rv = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	rv = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			      sizeof(buf_optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -1752,7 +1752,7 @@
 	/* Wait for ACK (empty window). */
 	k_msleep(150);
 
-	/* Client should not be able to send now and time out after SO_SNDTIMEO */
+	/* Client should not be able to send now and time out after ZSOCK_SO_SNDTIMEO */
 	start_time = k_uptime_get_32();
 	rv = zsock_send(c_sock, tx_buf, 1, 0);
 	time_diff = k_uptime_get_32() - start_time;
@@ -1838,10 +1838,10 @@
 			  "Invalid data received");
 	k_work_cancel_delayable(&test_data.tx_work);
 
-	/* MSG_WAITALL + SO_RCVTIMEO - make sure recv returns the amount of data
+	/* MSG_WAITALL + ZSOCK_SO_RCVTIMEO - make sure recv returns the amount of data
 	 * received so far
 	 */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval,
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval,
 			       sizeof(timeo_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1915,10 +1915,10 @@
 			  "Invalid data received");
 	k_work_cancel_delayable(&test_data.tx_work);
 
-	/* MSG_WAITALL + SO_RCVTIMEO - make sure recv returns the amount of data
+	/* MSG_WAITALL + ZSOCK_SO_RCVTIMEO - make sure recv returns the amount of data
 	 * received so far
 	 */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval,
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval,
 			       sizeof(timeo_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -2274,10 +2274,10 @@
 	zassert_true(ret > 0, "select failed, %d", errno);
 
 	/* Get the reason for the connect */
-	ret = zsock_getsockopt(fd, SOL_SOCKET, SO_ERROR, &optval, &optlen);
+	ret = zsock_getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed, %d", errno);
 
-	/* If SO_ERROR is 0, then it means that connect succeed. Any
+	/* If ZSOCK_SO_ERROR is 0, then it means that connect succeed. Any
 	 * other value (errno) means that it failed.
 	 */
 	zassert_equal(optval, ECONNREFUSED, "unexpected connect status, %d", optval);
@@ -2339,10 +2339,10 @@
 	zassert_true(closed, "poll failed, %d", errno);
 
 	/* Get the reason for the connect */
-	ret = zsock_getsockopt(fd, SOL_SOCKET, SO_ERROR, &optval, &optlen);
+	ret = zsock_getsockopt(fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed, %d", errno);
 
-	/* If SO_ERROR is 0, then it means that connect succeed. Any
+	/* If ZSOCK_SO_ERROR is 0, then it means that connect succeed. Any
 	 * other value (errno) means that it failed.
 	 */
 	zassert_equal(optval, ECONNREFUSED, "unexpected connect status, %d", optval);
@@ -2361,18 +2361,18 @@
 	prepare_sock_tcp_v4(MY_IPV4_ADDR, ANY_PORT, &sock, &bind_addr4);
 
 	/* Keep-alive should be disabled by default. */
-	ret = zsock_getsockopt(sock, SOL_SOCKET, SO_KEEPALIVE, &optval, &optlen);
+	ret = zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, 0, "getsockopt got invalid value");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
 	/* Enable keep-alive. */
 	optval = 1;
-	ret = zsock_setsockopt(sock, SOL_SOCKET, SO_KEEPALIVE,
+	ret = zsock_setsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE,
 			       &optval, sizeof(optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
-	ret = zsock_getsockopt(sock, SOL_SOCKET, SO_KEEPALIVE, &optval, &optlen);
+	ret = zsock_getsockopt(sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, 1, "getsockopt got invalid value");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
@@ -2443,10 +2443,10 @@
 
 	/* Enable keep-alive on both ends and set timeouts/retries to minimum */
 	optval = 1;
-	ret = zsock_setsockopt(*c_sock, SOL_SOCKET, SO_KEEPALIVE,
+	ret = zsock_setsockopt(*c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE,
 			       &optval, sizeof(optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
-	ret = zsock_setsockopt(*s_sock, SOL_SOCKET, SO_KEEPALIVE,
+	ret = zsock_setsockopt(*s_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_KEEPALIVE,
 			       &optval, sizeof(optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -2493,7 +2493,7 @@
 
 	test_prepare_keepalive_socks(&c_sock, &s_sock, &new_sock);
 
-	ret = zsock_setsockopt(c_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
diff --git a/tests/net/socket/tls/src/main.c b/tests/net/socket/tls/src/main.c
--- a/tests/net/socket/tls/src/main.c
+++ b/tests/net/socket/tls/src/main.c
@@ -215,12 +215,12 @@
 	prepare_sock_tls_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4, IPPROTO_TLS_1_2);
 	prepare_sock_tls_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6, IPPROTO_TLS_1_2);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_TYPE, &optval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_STREAM, "getsockopt got invalid type");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_TYPE, &optval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_STREAM, "getsockopt got invalid type");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
@@ -240,13 +240,13 @@
 	prepare_sock_tls_v4(MY_IPV4_ADDR, ANY_PORT, &sock1, &bind_addr4, IPPROTO_TLS_1_2);
 	prepare_sock_tls_v6(MY_IPV6_ADDR, ANY_PORT, &sock2, &bind_addr6, IPPROTO_TLS_1_1);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_PROTOCOL, &optval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, IPPROTO_TLS_1_2,
 		      "getsockopt got invalid protocol");
 	zassert_equal(optlen, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_PROTOCOL, &optval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, IPPROTO_TLS_1_1,
 		      "getsockopt got invalid protocol");
@@ -441,10 +441,10 @@
 			  "Invalid data received");
 	test_work_wait(&test_data.tx_work);
 
-	/* MSG_WAITALL + SO_RCVTIMEO - make sure recv returns the amount of data
+	/* MSG_WAITALL + ZSOCK_SO_RCVTIMEO - make sure recv returns the amount of data
 	 * received so far
 	 */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval,
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval,
 			       sizeof(timeo_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -497,10 +497,10 @@
 			  "Invalid data received");
 	test_work_wait(&test_data.tx_work);
 
-	/* MSG_WAITALL + SO_RCVTIMEO - make sure recv returns the amount of data
+	/* MSG_WAITALL + ZSOCK_SO_RCVTIMEO - make sure recv returns the amount of data
 	 * received so far
 	 */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVTIMEO, &timeo_optval,
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &timeo_optval,
 			       sizeof(timeo_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1179,8 +1179,8 @@
 
 	test_prepare_tls_connection(ZSOCK_AF_INET6);
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			       sizeof(buf_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1272,8 +1272,8 @@
 
 	test_prepare_tls_connection(ZSOCK_AF_INET6);
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			       sizeof(buf_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1374,7 +1374,7 @@
 
 	test_prepare_tls_connection(ZSOCK_AF_INET6);
 
-	ret = zsock_setsockopt(c_sock, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	ret = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			       sizeof(optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1417,12 +1417,12 @@
 
 	test_prepare_tls_connection(ZSOCK_AF_INET6);
 
-	ret = zsock_setsockopt(c_sock, SOL_SOCKET, SO_SNDTIMEO, &timeo_optval,
+	ret = zsock_setsockopt(c_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO, &timeo_optval,
 			       sizeof(timeo_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			       sizeof(buf_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1432,7 +1432,7 @@
 	/* Wait for ACK (empty window). */
 	k_msleep(150);
 
-	/* Client should not be able to send now and time out after SO_SNDTIMEO */
+	/* Client should not be able to send now and time out after ZSOCK_SO_SNDTIMEO */
 	start_time = k_uptime_get_32();
 	ret = zsock_send(c_sock, TEST_STR_SMALL, sizeof(TEST_STR_SMALL) - 1, 0);
 	time_diff = k_uptime_get_32() - start_time;
@@ -1630,8 +1630,8 @@
 	zassert_equal(ret, 1, "poll() should've report event");
 	zassert_equal(fds[0].revents, ZSOCK_POLLOUT, "No POLLOUT event");
 
-	/* Simulate window full scenario with SO_RCVBUF option. */
-	ret = zsock_setsockopt(new_sock, SOL_SOCKET, SO_RCVBUF, &buf_optval,
+	/* Simulate window full scenario with ZSOCK_SO_RCVBUF option. */
+	ret = zsock_setsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVBUF, &buf_optval,
 			       sizeof(buf_optval));
 	zassert_equal(ret, 0, "setsockopt failed (%d)", errno);
 
@@ -1765,7 +1765,7 @@
 	zassert_equal(ret, 1, "poll() should've report event");
 	zassert_true(fds[0].revents & ZSOCK_POLLERR, "No POLLERR event");
 
-	ret = zsock_getsockopt(new_sock, SOL_SOCKET, SO_ERROR, &optval, &optlen);
+	ret = zsock_getsockopt(new_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ECONNABORTED, "getsockopt got invalid error %d",
 		      optval);
@@ -1802,7 +1802,7 @@
 	zassert_equal(ret, 1, "poll() should've report event");
 	zassert_true(fds[0].revents & ZSOCK_POLLERR, "No POLLERR event");
 
-	ret = zsock_getsockopt(s_sock, SOL_SOCKET, SO_ERROR, &optval, &optlen);
+	ret = zsock_getsockopt(s_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_ERROR, &optval, &optlen);
 	zassert_equal(ret, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ECONNABORTED, "getsockopt got invalid error %d",
 		      optval);
diff --git a/tests/net/socket/tls_ext/src/main.c b/tests/net/socket/tls_ext/src/main.c
--- a/tests/net/socket/tls_ext/src/main.c
+++ b/tests/net/socket/tls_ext/src/main.c
@@ -232,8 +232,8 @@
 	zassert_not_equal(r, -1, "failed to create server socket (%d)", errno);
 	server_fd = r;
 
-	r = setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &yes, sizeof(yes));
-	zassert_not_equal(r, -1, "failed to set SO_REUSEADDR (%d)", errno);
+	r = setsockopt(server_fd, ZSOCK_SOL_SOCKET, ZSOCK_SO_REUSEADDR, &yes, sizeof(yes));
+	zassert_not_equal(r, -1, "failed to set ZSOCK_SO_REUSEADDR (%d)", errno);
 
 	switch (peer_verify) {
 	case TLS_PEER_VERIFY_NONE:
diff --git a/tests/net/socket/udp/src/main.c b/tests/net/socket/udp/src/main.c
--- a/tests/net/socket/udp/src/main.c
+++ b/tests/net/socket/udp/src/main.c
@@ -342,12 +342,12 @@
 	zassert_equal(rv, 0, "bind failed");
 
 	optval = 2;
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_PRIORITY, &optval,
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_PRIORITY, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval = 6;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_PRIORITY, &optval,
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_PRIORITY, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed");
 
@@ -462,7 +462,7 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(int));
-	cmsg->cmsg_level = SOL_SOCKET;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
 	cmsg->cmsg_type = 1122;
 	*(int *)CMSG_DATA(cmsg) = 42;
 
@@ -566,7 +566,7 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(int));
-	cmsg->cmsg_level = SOL_SOCKET;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
 	cmsg->cmsg_type = 1122;
 	*(int *)CMSG_DATA(cmsg) = 42;
 
@@ -627,7 +627,7 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(int));
-	cmsg->cmsg_level = SOL_SOCKET;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
 	cmsg->cmsg_type = 1122;
 	*(int *)CMSG_DATA(cmsg) = 42;
 
@@ -687,7 +687,7 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(int));
-	cmsg->cmsg_level = SOL_SOCKET;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
 	cmsg->cmsg_type = 1122;
 	*(int *)CMSG_DATA(cmsg) = 42;
 
@@ -716,12 +716,12 @@
 	prepare_sock_udp_v4(MY_IPV4_ADDR, 55555, &sock1, &bind_addr4);
 	prepare_sock_udp_v6(MY_IPV6_ADDR, 55555, &sock2, &bind_addr6);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_TYPE, &optval, &optsize);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optsize);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_DGRAM, "getsockopt got invalid type");
 	zassert_equal(optsize, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_TYPE, &optval, &optsize);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_TYPE, &optval, &optsize);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_TYPE_DGRAM, "getsockopt got invalid type");
 	zassert_equal(optsize, sizeof(optval), "getsockopt got invalid size");
@@ -750,24 +750,24 @@
 	zassert_equal(rv, 0, "bind failed");
 
 	optval = true;
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_TXTIME, &optval,
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_TXTIME, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval = false;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_TXTIME, &optval,
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_TXTIME, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed");
 
 	optlen = sizeof(optval);
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_TXTIME, &optval, &optlen);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_TXTIME, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optlen, sizeof(optval), "invalid optlen %d vs %d",
 		      optlen, sizeof(optval));
 	zassert_equal(optval, true, "getsockopt txtime");
 
 	optlen = sizeof(optval);
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_TXTIME, &optval, &optlen);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_TXTIME, &optval, &optlen);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optlen, sizeof(optval), "invalid optlen %d vs %d",
 		      optlen, sizeof(optval));
@@ -803,12 +803,12 @@
 	rv = zsock_bind(sock2, (struct zsock_sockaddr *)&bind_addr6, sizeof(bind_addr6));
 	zassert_equal(rv, 0, "bind failed");
 
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval.tv_usec = 400000;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_RCVTIMEO, &optval,
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
@@ -860,12 +860,12 @@
 	rv = zsock_bind(sock2, (struct zsock_sockaddr *)&bind_addr6, sizeof(bind_addr6));
 	zassert_equal(rv, 0, "bind failed");
 
-	rv = zsock_setsockopt(sock1, SOL_SOCKET, SO_SNDTIMEO, &optval,
+	rv = zsock_setsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed (%d)", errno);
 
 	optval.tv_usec = 0;
-	rv = zsock_setsockopt(sock2, SOL_SOCKET, SO_SNDTIMEO, &optval,
+	rv = zsock_setsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_SNDTIMEO, &optval,
 			      sizeof(optval));
 	zassert_equal(rv, 0, "setsockopt failed");
 
@@ -886,12 +886,12 @@
 	prepare_sock_udp_v4(MY_IPV4_ADDR, 55555, &sock1, &bind_addr4);
 	prepare_sock_udp_v6(MY_IPV6_ADDR, 55555, &sock2, &bind_addr6);
 
-	rv = zsock_getsockopt(sock1, SOL_SOCKET, SO_PROTOCOL, &optval, &optsize);
+	rv = zsock_getsockopt(sock1, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optsize);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_IPPROTO_UDP, "getsockopt got invalid protocol");
 	zassert_equal(optsize, sizeof(optval), "getsockopt got invalid size");
 
-	rv = zsock_getsockopt(sock2, SOL_SOCKET, SO_PROTOCOL, &optval, &optsize);
+	rv = zsock_getsockopt(sock2, ZSOCK_SOL_SOCKET, ZSOCK_SO_PROTOCOL, &optval, &optsize);
 	zassert_equal(rv, 0, "getsockopt failed (%d)", errno);
 	zassert_equal(optval, ZSOCK_IPPROTO_UDP, "getsockopt got invalid protocol");
 	zassert_equal(optsize, sizeof(optval), "getsockopt got invalid size");
@@ -1090,12 +1090,12 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(txtime));
-	cmsg->cmsg_level = SOL_SOCKET;
-	cmsg->cmsg_type = SCM_TXTIME;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
+	cmsg->cmsg_type = ZSOCK_SCM_TXTIME;
 	*(net_time_t *)CMSG_DATA(cmsg) = txtime;
 
 	optval = true;
-	rv = zsock_setsockopt(client_sock, SOL_SOCKET, SO_TXTIME, &optval,
+	rv = zsock_setsockopt(client_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_TXTIME, &optval,
 			      sizeof(optval));
 
 	test_started = true;
@@ -1442,7 +1442,7 @@
 
 	cmsg = CMSG_FIRSTHDR(&msg);
 	cmsg->cmsg_len = CMSG_LEN(sizeof(int));
-	cmsg->cmsg_level = SOL_SOCKET;
+	cmsg->cmsg_level = ZSOCK_SOL_SOCKET;
 	cmsg->cmsg_type = 1122;
 	*(int *)CMSG_DATA(cmsg) = 42;
 
@@ -2005,14 +2005,14 @@
 	zassert_true(ret > 0, "cannot get interface name (%d)", ret);
 
 	strncpy(ifreq.ifr_name, ifname, sizeof(ifreq.ifr_name));
-	ret = zsock_setsockopt(sock_c, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(sock_c, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", -errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", -errno);
 
 	ret = zsock_connect(sock_c, addr_s, addrlen_s);
 	zassert_equal(ret, 0, "connect failed");
 
-	ret = zsock_setsockopt(sock_s, SOL_SOCKET, SO_RCVTIMEO,
+	ret = zsock_setsockopt(sock_s, ZSOCK_SOL_SOCKET, ZSOCK_SO_RCVTIMEO,
 			       &timeo_optval, sizeof(timeo_optval));
 	zassert_equal(ret, 0, "Cannot set receive timeout (%d)", -errno);
 
diff --git a/tests/net/vlan/src/main.c b/tests/net/vlan/src/main.c
--- a/tests/net/vlan/src/main.c
+++ b/tests/net/vlan/src/main.c
@@ -1033,9 +1033,9 @@
 	zassert_true(ret > 0, "cannot get interface name (%d/%s)", ret, strerror(-ret));
 
 	strncpy(ifreq.ifr_name, ifname, sizeof(ifreq.ifr_name));
-	ret = zsock_setsockopt(client_sock, SOL_SOCKET, SO_BINDTODEVICE, &ifreq,
+	ret = zsock_setsockopt(client_sock, ZSOCK_SOL_SOCKET, ZSOCK_SO_BINDTODEVICE, &ifreq,
 			       sizeof(ifreq));
-	zassert_equal(ret, 0, "SO_BINDTODEVICE failed, %d", -errno);
+	zassert_equal(ret, 0, "ZSOCK_SO_BINDTODEVICE failed, %d", -errno);
 
 	ret = add_neighbor(iface, &peer_vlan_addr);
 	zassert_true(ret, "Cannot add neighbor");
