diff --git a/drivers/can/can_native_linux_adapt.c b/drivers/can/can_native_linux_adapt.c
index 2c50c6640c7..92fcda647ae 100644
--- a/drivers/can/can_native_linux_adapt.c
+++ b/drivers/can/can_native_linux_adapt.c
@@ -47,7 +47,7 @@ int linux_socketcan_iface_open(const char *if_name)
 	struct ifreq ifr;
 	int fd, opt, ret = -EINVAL;

-	fd = socket(PF_CAN, SOCK_RAW | SOCK_CLOEXEC, CAN_RAW);
+	fd = socket(PF_CAN, ZSOCK_SOCK_RAW | SOCK_CLOEXEC, CAN_RAW);
 	if (fd < 0) {
 		return -errno;
 	}
@@ -66,7 +66,7 @@ int linux_socketcan_iface_open(const char *if_name)
 	addr.can_ifindex = ifr.ifr_ifindex;
 	addr.can_family = PF_CAN;

-	ret = bind(fd, (struct sockaddr *)&addr, sizeof(addr));
+	ret = bind(fd, (struct zsock_sockaddr *)&addr, sizeof(addr));
 	if (ret < 0) {
 		close(fd);
 		return -errno;
@@ -116,16 +116,16 @@ int linux_socketcan_poll_data(int fd)
 int linux_socketcan_read_data(int fd, void *buf, size_t buf_len, bool *msg_confirm)
 {
 	struct canfd_frame *frame = (struct canfd_frame *)buf;
-	struct msghdr msg = {0};
+	struct zsock_msghdr msg = {0};

-	struct iovec iov = {
+	struct zsock_iovec iov = {
 		.iov_base = buf,
 		.iov_len = buf_len,
 	};
 	msg.msg_iov = &iov;
 	msg.msg_iovlen = 1;

-	int ret = (int)recvmsg(fd, &msg, MSG_WAITALL);
+	int ret = (int)recvmsg(fd, &msg, ZSOCK_MSG_WAITALL);

 	if (msg_confirm != NULL) {
 		*msg_confirm = (msg.msg_flags & MSG_CONFIRM) != 0;
diff --git a/drivers/ethernet/dsa/dsa_nxp_imx_netc.c b/drivers/ethernet/dsa/dsa_nxp_imx_netc.c
index 8ad2541cd90..1b6d82959ae 100644
--- a/drivers/ethernet/dsa/dsa_nxp_imx_netc.c
+++ b/drivers/ethernet/dsa/dsa_nxp_imx_netc.c
@@ -138,7 +138,7 @@ static int dsa_netc_switch_setup(const struct dsa_switch_context *dsa_switch_ctx
 	 * Trap gPTP frames to cpu port to perform gPTP protocol.
 	 */
 	netc_tb_ipf_config_t ipf_entry_cfg = {
-		.keye.etherType = htons(NET_ETH_PTYPE_PTP),
+		.keye.etherType = zsock_htons(NET_ETH_PTYPE_PTP),
 		.keye.etherTypeMask = 0xffff,
 		.keye.srcPort = 0,
 		.keye.srcPortMask = 0x0,
diff --git a/drivers/ethernet/eth_adin2111.c b/drivers/ethernet/eth_adin2111.c
index 687ef6e5553..5ad8ef6666d 100644
--- a/drivers/ethernet/eth_adin2111.c
+++ b/drivers/ethernet/eth_adin2111.c
@@ -317,7 +317,7 @@ int eth_adin2111_oa_data_read(const struct device *dev, const uint16_t port_idx)

 		if (ftr & ADIN2111_OA_DATA_FTR_EV) {
 			pkt = net_pkt_rx_alloc_with_buffer(iface, CONFIG_ETH_ADIN2111_BUFFER_SIZE,
-							   AF_UNSPEC, 0,
+							   ZSOCK_AF_UNSPEC, 0,
 							   K_MSEC(CONFIG_ETH_ADIN2111_TIMEOUT));
 			if (!pkt) {
 				LOG_ERR("OA RX: cannot allcate packet space, skipping.");
@@ -439,7 +439,7 @@ static int eth_adin2111_reg_read_generic(const struct device *dev,
 #endif /* CONFIG_ETH_ADIN2111_SPI_CFG0 */

 	/* spi header */
-	*(uint16_t *)buf = htons((ADIN2111_READ_TXN_CTRL | reg));
+	*(uint16_t *)buf = zsock_htons((ADIN2111_READ_TXN_CTRL | reg));
 #if CONFIG_ETH_ADIN2111_SPI_CFG0
 	buf[2] = crc8_ccitt(0, buf, ADIN2111_SPI_HEADER_SIZE);
 	/* TA */
@@ -471,7 +471,7 @@ static int eth_adin2111_reg_read_generic(const struct device *dev,
 	}
 #endif /* CONFIG_ETH_ADIN2111_SPI_CFG0 */

-	*val = ntohl((*(uint32_t *)(&buf[header_len])));
+	*val = zsock_ntohl((*(uint32_t *)(&buf[header_len])));

 	return ret;
 }
@@ -490,14 +490,14 @@ static int eth_adin2111_reg_write_generic(const struct device *dev,
 #endif /* CONFIG_ETH_ADIN2111_SPI_CFG0 */

 	/* spi header */
-	*(uint16_t *)buf = htons((ADIN2111_WRITE_TXN_CTRL | reg));
+	*(uint16_t *)buf = zsock_htons((ADIN2111_WRITE_TXN_CTRL | reg));
 	#if CONFIG_ETH_ADIN2111_SPI_CFG0
 	buf[2] = crc8_ccitt(0, buf, header_size);
 	++header_size;
 #endif /* CONFIG_ETH_ADIN2111_SPI_CFG0 */

 	/* reg */
-	*(uint32_t *)(buf + header_size) = htonl(val);
+	*(uint32_t *)(buf + header_size) = zsock_htonl(val);
 #if CONFIG_ETH_ADIN2111_SPI_CFG0
 	buf[header_size + data_size] = crc8_ccitt(0, &buf[header_size], data_size);
 	++data_size;
@@ -576,7 +576,7 @@ static int adin2111_read_fifo(const struct device *dev, const uint16_t port_idx)
 	fsize -= ADIN2111_FRAME_HEADER_SIZE;

 	/* spi header */
-	*(uint16_t *)cmd_buf = htons((ADIN2111_READ_TXN_CTRL | rx_reg));
+	*(uint16_t *)cmd_buf = zsock_htons((ADIN2111_READ_TXN_CTRL | rx_reg));
 #if CONFIG_ETH_ADIN2111_SPI_CFG0
 	cmd_buf[2] = crc8_ccitt(0, cmd_buf, ADIN2111_SPI_HEADER_SIZE);
 	/* TA */
@@ -608,7 +608,7 @@ static int adin2111_read_fifo(const struct device *dev, const uint16_t port_idx)
 	/* remove CRC32 and pass to the stack */
 	fsize_real = fsize - sizeof(uint32_t);

-	pkt = net_pkt_rx_alloc_with_buffer(iface, fsize_real, AF_UNSPEC, 0,
+	pkt = net_pkt_rx_alloc_with_buffer(iface, fsize_real, ZSOCK_AF_UNSPEC, 0,
 					   K_MSEC(CONFIG_ETH_ADIN2111_TIMEOUT));
 	if (!pkt) {
 		eth_stats_update_errors_rx(iface);
@@ -838,7 +838,7 @@ static int adin2111_port_send(const struct device *dev, struct net_pkt *pkt)
 			eth_adin2111_lock(adin, K_FOREVER);
 		}

-		ret = eth_adin2111_send_oa_frame(cfg->adin, pkt, htons(cfg->port_idx));
+		ret = eth_adin2111_send_oa_frame(cfg->adin, pkt, zsock_htons(cfg->port_idx));

 		goto end_check;
 	}
@@ -887,14 +887,14 @@ static int adin2111_port_send(const struct device *dev, struct net_pkt *pkt)
 	memset(ctx->buf, 0, burst_size + ADIN2111_WRITE_HEADER_SIZE);

 	/* spi header */
-	*(uint16_t *)ctx->buf = htons(ADIN2111_TXN_CTRL_TX_REG);
+	*(uint16_t *)ctx->buf = zsock_htons(ADIN2111_TXN_CTRL_TX_REG);
 #if CONFIG_ETH_ADIN2111_SPI_CFG0
 	ctx->buf[2] = crc8_ccitt(0, ctx->buf, header_size);
 	++header_size;
 #endif /* CONFIG_ETH_ADIN2111_SPI_CFG0 */

 	/* frame header */
-	*(uint16_t *)(ctx->buf + header_size) = htons(cfg->port_idx);
+	*(uint16_t *)(ctx->buf + header_size) = zsock_htons(cfg->port_idx);

 	/* read pkt into tx buffer */
 	ret = net_pkt_read(pkt,
diff --git a/drivers/ethernet/eth_cyclonev.c b/drivers/ethernet/eth_cyclonev.c
index f5f0e80ff99..dff8c652505 100644
--- a/drivers/ethernet/eth_cyclonev.c
+++ b/drivers/ethernet/eth_cyclonev.c
@@ -680,7 +680,7 @@ static void eth_cyclonev_receive(struct eth_cyclonev_priv *p)
 		p->rx_current_desc_number = last_desc_index;

 		/* Allocate packet with buffer */
-		pkt = net_pkt_rx_alloc_with_buffer(p->iface, frame_length, AF_UNSPEC, 0, K_NO_WAIT);
+		pkt = net_pkt_rx_alloc_with_buffer(p->iface, frame_length, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_ERR("net_pkt_rx_alloc_with_buffer() failed");
 			eth_stats_update_errors_rx(p->iface);
diff --git a/drivers/ethernet/eth_e1000.c b/drivers/ethernet/eth_e1000.c
index 62fad9772f9..bb922be32bd 100644
--- a/drivers/ethernet/eth_e1000.c
+++ b/drivers/ethernet/eth_e1000.c
@@ -157,7 +157,7 @@ static struct net_pkt *e1000_rx(struct e1000_dev *dev)

 	hexdump(buf, len, "%zd byte(s)", len);

-	pkt = net_pkt_rx_alloc_with_buffer(dev->iface, len, AF_UNSPEC, 0,
+	pkt = net_pkt_rx_alloc_with_buffer(dev->iface, len, ZSOCK_AF_UNSPEC, 0,
 					   K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("Out of buffers");
diff --git a/drivers/ethernet/eth_enc28j60.c b/drivers/ethernet/eth_enc28j60.c
index 0c4cbbf4648..61874c16f1b 100644
--- a/drivers/ethernet/eth_enc28j60.c
+++ b/drivers/ethernet/eth_enc28j60.c
@@ -559,7 +559,7 @@ static void enc28j60_read_packet(const struct device *dev, uint16_t frm_len)

 	/* Get the frame from the buffer */
 	pkt = net_pkt_rx_alloc_with_buffer(get_iface(context), frm_len,
-					   AF_UNSPEC, 0, K_MSEC(config->timeout));
+					   ZSOCK_AF_UNSPEC, 0, K_MSEC(config->timeout));
 	if (!pkt) {
 		LOG_ERR("%s: Could not allocate rx buffer", dev->name);
 		eth_stats_update_errors_rx(get_iface(context));
diff --git a/drivers/ethernet/eth_enc424j600.c b/drivers/ethernet/eth_enc424j600.c
index c5584a3b365..4de9e449296 100644
--- a/drivers/ethernet/eth_enc424j600.c
+++ b/drivers/ethernet/eth_enc424j600.c
@@ -387,7 +387,7 @@ static int enc424j600_rx(const struct device *dev)

 	/* Get the frame from the buffer */
 	pkt = net_pkt_rx_alloc_with_buffer(context->iface, frm_len,
-					   AF_UNSPEC, 0,
+					   ZSOCK_AF_UNSPEC, 0,
 					   K_MSEC(config->timeout));
 	if (!pkt) {
 		LOG_ERR("Could not allocate rx buffer");
diff --git a/drivers/ethernet/eth_esp32.c b/drivers/ethernet/eth_esp32.c
index eb51cf35f41..7e5e4bebeb7 100644
--- a/drivers/ethernet/eth_esp32.c
+++ b/drivers/ethernet/eth_esp32.c
@@ -111,7 +111,7 @@ static struct net_pkt *eth_esp32_rx(
 	}

 	struct net_pkt *pkt = net_pkt_rx_alloc_with_buffer(
-		dev_data->iface, receive_len, AF_UNSPEC, 0, K_MSEC(100));
+		dev_data->iface, receive_len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (pkt == NULL) {
 		eth_stats_update_errors_rx(dev_data->iface);
 		LOG_ERR("Could not allocate rx buffer");
diff --git a/drivers/ethernet/eth_gecko.c b/drivers/ethernet/eth_gecko.c
index 4f2c70715e8..72b2450fe65 100644
--- a/drivers/ethernet/eth_gecko.c
+++ b/drivers/ethernet/eth_gecko.c
@@ -193,7 +193,7 @@ static struct net_pkt *frame_get(const struct device *dev)
 	if (eofIdx != UINT32_MAX) {
 		/* Allocate room for full frame */
 		rx_frame = net_pkt_rx_alloc_with_buffer(dev_data->iface,
-					total_len, AF_UNSPEC, 0, K_NO_WAIT);
+					total_len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!rx_frame) {
 			LOG_ERR("Failed to obtain RX buffer");
 			ETH_RX_DISABLE(eth);
diff --git a/drivers/ethernet/eth_ivshmem.c b/drivers/ethernet/eth_ivshmem.c
index 742d8e6089f..d2959057508 100644
--- a/drivers/ethernet/eth_ivshmem.c
+++ b/drivers/ethernet/eth_ivshmem.c
@@ -139,7 +139,7 @@ static struct net_pkt *eth_ivshmem_rx(const struct device *dev)
 	}

 	struct net_pkt *pkt = net_pkt_rx_alloc_with_buffer(
-		dev_data->iface, rx_len, AF_UNSPEC, 0, K_MSEC(100));
+		dev_data->iface, rx_len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (pkt == NULL) {
 		LOG_ERR("Failed to allocate rx buffer");
 		eth_stats_update_errors_rx(dev_data->iface);
diff --git a/drivers/ethernet/eth_lan9250.c b/drivers/ethernet/eth_lan9250.c
index f16e2eaac94..bf601cf5c71 100644
--- a/drivers/ethernet/eth_lan9250.c
+++ b/drivers/ethernet/eth_lan9250.c
@@ -486,7 +486,7 @@ static int lan9250_rx(const struct device *dev)
 	}

 	/* Get the frame from the buffer */
-	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, pkt_len, AF_UNSPEC, 0,
+	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, pkt_len, ZSOCK_AF_UNSPEC, 0,
 					   K_MSEC(config->timeout));
 	if (!pkt) {
 		LOG_ERR("%s: Could not allocate rx buffer", dev->name);
diff --git a/drivers/ethernet/eth_litex_liteeth.c b/drivers/ethernet/eth_litex_liteeth.c
index cbb5d52587c..cd3e8ab6a7d 100644
--- a/drivers/ethernet/eth_litex_liteeth.c
+++ b/drivers/ethernet/eth_litex_liteeth.c
@@ -136,7 +136,7 @@ static void eth_rx(const struct device *port)
 	rxslot = litex_read8(config->rx_slot_addr);

 	/* obtain rx buffer */
-	pkt = net_pkt_rx_alloc_with_buffer(context->iface, len, AF_UNSPEC, 0,
+	pkt = net_pkt_rx_alloc_with_buffer(context->iface, len, ZSOCK_AF_UNSPEC, 0,
 					   K_NO_WAIT);
 	if (pkt == NULL) {
 		LOG_ERR("Failed to obtain RX buffer of length %u", len);
diff --git a/drivers/ethernet/eth_native_tap.c b/drivers/ethernet/eth_native_tap.c
index 32b7f1aa32b..650308568f5 100644
--- a/drivers/ethernet/eth_native_tap.c
+++ b/drivers/ethernet/eth_native_tap.c
@@ -113,7 +113,7 @@ static struct gptp_hdr *check_gptp_msg(struct net_if *iface,
 	struct net_eth_hdr *hdr;

 	hdr = (struct net_eth_hdr *)msg_start;
-	if (ntohs(hdr->type) != NET_ETH_PTYPE_PTP) {
+	if (zsock_ntohs(hdr->type) != NET_ETH_PTYPE_PTP) {
 		return NULL;
 	}

@@ -215,7 +215,7 @@ static struct net_pkt *prepare_pkt(struct eth_context *ctx,
 	struct net_pkt *pkt;

 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, count,
-					   AF_UNSPEC, 0, NET_BUF_TIMEOUT);
+					   ZSOCK_AF_UNSPEC, 0, NET_BUF_TIMEOUT);
 	if (!pkt) {
 		*status = -ENOMEM;
 		return NULL;
@@ -313,7 +313,7 @@ static void eth_iface_init(struct net_if *iface)
 		mac_addr_cmd_opt ? mac_addr_cmd_opt : CONFIG_ETH_NATIVE_TAP_MAC_ADDR;
 #endif
 #ifdef CONFIG_NET_IPV4
-	struct in_addr addr, netmask;
+	struct zsock_in_addr addr, netmask;
 #endif

 	ctx->iface = iface;
@@ -376,11 +376,11 @@ static void eth_iface_init(struct net_if *iface)

 #ifdef CONFIG_NET_IPV4
 	if (ipv4_addr_cmd_opt != NULL) {
-		if (net_addr_pton(AF_INET, ipv4_addr_cmd_opt, &addr) == 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, ipv4_addr_cmd_opt, &addr) == 0) {
 			net_if_ipv4_addr_add(iface, &addr, NET_ADDR_MANUAL, 0);

 			if (ipv4_nm_cmd_opt != NULL) {
-				if (net_addr_pton(AF_INET, ipv4_nm_cmd_opt, &netmask) == 0) {
+				if (net_addr_pton(ZSOCK_AF_INET, ipv4_nm_cmd_opt, &netmask) == 0) {
 					net_if_ipv4_set_netmask_by_addr(iface, &addr, &netmask);
 				} else {
 					NET_ERR("Invalid netmask: %s", ipv4_nm_cmd_opt);
@@ -392,7 +392,7 @@ static void eth_iface_init(struct net_if *iface)
 	}

 	if (ipv4_gw_cmd_opt != NULL) {
-		if (net_addr_pton(AF_INET, ipv4_gw_cmd_opt, &addr) == 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, ipv4_gw_cmd_opt, &addr) == 0) {
 			net_if_ipv4_set_gw(iface, &addr);
 		} else {
 			NET_ERR("Invalid gateway: %s", ipv4_gw_cmd_opt);
diff --git a/drivers/ethernet/eth_numaker.c b/drivers/ethernet/eth_numaker.c
index 8e6aa6bf2ee..4b4920a4d0c 100644
--- a/drivers/ethernet/eth_numaker.c
+++ b/drivers/ethernet/eth_numaker.c
@@ -391,7 +391,7 @@ static void m_numaker_gmacdev_packet_rx(const struct device *dev)
 		/* Allocate a memory buffer chain from buffer pool
 		 * Using root iface. It will be updated in net_recv_data()
 		 */
-		pkt = net_pkt_rx_alloc_with_buffer(data->iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+		pkt = net_pkt_rx_alloc_with_buffer(data->iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_ERR("pkt alloc frame-len=%d failed", len);
 			goto next;
diff --git a/drivers/ethernet/eth_nxp_enet.c b/drivers/ethernet/eth_nxp_enet.c
index 40b2d69f4b2..55b2bc8631c 100644
--- a/drivers/ethernet/eth_nxp_enet.c
+++ b/drivers/ethernet/eth_nxp_enet.c
@@ -144,9 +144,9 @@ static bool eth_get_ptp_data(struct net_if *iface, struct net_pkt *pkt)
 	bool pkt_is_ptp;

 	if (net_eth_is_vlan_enabled(eth_ctx, iface)) {
-		pkt_is_ptp = ntohs(hdr_vlan->type) == NET_ETH_PTYPE_PTP;
+		pkt_is_ptp = zsock_ntohs(hdr_vlan->type) == NET_ETH_PTYPE_PTP;
 	} else {
-		pkt_is_ptp = ntohs(NET_ETH_HDR(pkt)->type) == NET_ETH_PTYPE_PTP;
+		pkt_is_ptp = zsock_ntohs(NET_ETH_HDR(pkt)->type) == NET_ETH_PTYPE_PTP;
 	}

 	if (pkt_is_ptp) {
@@ -376,7 +376,7 @@ static int eth_nxp_enet_rx(const struct device *dev)

 	/* Using root iface. It will be updated in net_recv_data() */
 	pkt = net_pkt_rx_alloc_with_buffer(data->iface, frame_length,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		goto flush;
 	}
diff --git a/drivers/ethernet/eth_nxp_s32_gmac.c b/drivers/ethernet/eth_nxp_s32_gmac.c
index fc64e226311..e468e3a9192 100644
--- a/drivers/ethernet/eth_nxp_s32_gmac.c
+++ b/drivers/ethernet/eth_nxp_s32_gmac.c
@@ -414,7 +414,7 @@ static struct net_pkt *eth_nxp_s32_get_pkt(const struct device *dev,

 	/* Using root iface, it will be updated in net_recv_data() */
 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, rx_info->PktLen,
-					   AF_UNSPEC, 0, ETH_NXP_S32_BUF_TIMEOUT);
+					   ZSOCK_AF_UNSPEC, 0, ETH_NXP_S32_BUF_TIMEOUT);
 	if (!pkt) {
 		LOG_ERR("Failed to allocate rx buffer of length %u", rx_info->PktLen);
 		goto exit;
diff --git a/drivers/ethernet/eth_nxp_s32_netc.c b/drivers/ethernet/eth_nxp_s32_netc.c
index 9a5ddd745d3..6606d308edb 100644
--- a/drivers/ethernet/eth_nxp_s32_netc.c
+++ b/drivers/ethernet/eth_nxp_s32_netc.c
@@ -180,7 +180,7 @@ static struct net_pkt *nxp_s32_eth_get_pkt(const struct device *dev,

 	/* Use root iface, it will be updated later in net_recv_data() */
 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, buf->length,
-					   AF_UNSPEC, 0, NETC_TIMEOUT);
+					   ZSOCK_AF_UNSPEC, 0, NETC_TIMEOUT);
 	if (!pkt) {
 		goto exit;
 	}
diff --git a/drivers/ethernet/eth_renesas_ra.c b/drivers/ethernet/eth_renesas_ra.c
index a0fd19e9d81..b70b087db07 100644
--- a/drivers/ethernet/eth_renesas_ra.c
+++ b/drivers/ethernet/eth_renesas_ra.c
@@ -339,7 +339,7 @@ static struct net_pkt *renesas_ra_eth_rx(const struct device *dev)
 		goto out;
 	}

-	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, len, AF_UNSPEC, 0, K_MSEC(100));
+	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (!pkt) {
 		LOG_ERR("Failed to obtain RX buffer");
 		goto out;
diff --git a/drivers/ethernet/eth_sam_gmac.c b/drivers/ethernet/eth_sam_gmac.c
index c0893566b97..b758692ea86 100644
--- a/drivers/ethernet/eth_sam_gmac.c
+++ b/drivers/ethernet/eth_sam_gmac.c
@@ -564,7 +564,7 @@ static struct gptp_hdr *check_gptp_msg(struct net_if *iface,
 	struct net_eth_hdr *hdr;

 	hdr = (struct net_eth_hdr *)msg_start;
-	if (ntohs(hdr->type) != NET_ETH_PTYPE_PTP) {
+	if (zsock_ntohs(hdr->type) != NET_ETH_PTYPE_PTP) {
 		return NULL;
 	}

diff --git a/drivers/ethernet/eth_sensry_sy1xx_mac.c b/drivers/ethernet/eth_sensry_sy1xx_mac.c
index d254bb13734..de8346f64f2 100644
--- a/drivers/ethernet/eth_sensry_sy1xx_mac.c
+++ b/drivers/ethernet/eth_sensry_sy1xx_mac.c
@@ -503,7 +503,7 @@ static int sy1xx_mac_receive_data(const struct device *dev, uint8_t *rx, uint16_
 	struct net_pkt *rx_pkt;
 	int ret;

-	rx_pkt = net_pkt_alloc_with_buffer(data->iface, len, AF_UNSPEC, 0, K_FOREVER);
+	rx_pkt = net_pkt_alloc_with_buffer(data->iface, len, ZSOCK_AF_UNSPEC, 0, K_FOREVER);
 	if (rx_pkt == NULL) {
 		LOG_ERR("rx packet allocation failed");
 		return -EINVAL;
diff --git a/drivers/ethernet/eth_smsc911x.c b/drivers/ethernet/eth_smsc911x.c
index 39e93951bba..6a916c6b48f 100644
--- a/drivers/ethernet/eth_smsc911x.c
+++ b/drivers/ethernet/eth_smsc911x.c
@@ -567,7 +567,7 @@ static struct net_pkt *smsc_recv_pkt(const struct device *dev,
 	rem_size -= 4U;

 	pkt = net_pkt_rx_alloc_with_buffer(context->iface, rem_size,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("Failed to obtain RX buffer");
 		smsc_discard_pkt();
diff --git a/drivers/ethernet/eth_smsc91x.c b/drivers/ethernet/eth_smsc91x.c
index eef7dc85a6b..0da32d119dc 100644
--- a/drivers/ethernet/eth_smsc91x.c
+++ b/drivers/ethernet/eth_smsc91x.c
@@ -437,7 +437,7 @@ static void smsc_recv_pkt(struct eth_context *data)
 				rx_buffer[len - 1] = smsc_read_1(sc, DATA0);
 			}

-			pkt = net_pkt_rx_alloc_with_buffer(data->iface, len, AF_UNSPEC, 0,
+			pkt = net_pkt_rx_alloc_with_buffer(data->iface, len, ZSOCK_AF_UNSPEC, 0,
 							   K_NO_WAIT);
 			if (!pkt) {
 				LOG_ERR("Failed to obtain RX buffer");
diff --git a/drivers/ethernet/eth_stellaris.c b/drivers/ethernet/eth_stellaris.c
index dc7503e67af..06975e72a5b 100644
--- a/drivers/ethernet/eth_stellaris.c
+++ b/drivers/ethernet/eth_stellaris.c
@@ -146,7 +146,7 @@ static struct net_pkt *eth_stellaris_rx_pkt(const struct device *dev,
 	frame_len = reg_val & 0x0000ffff;

 	pkt = net_pkt_rx_alloc_with_buffer(iface, frame_len,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		return NULL;
 	}
diff --git a/drivers/ethernet/eth_stm32_hal_ptp.c b/drivers/ethernet/eth_stm32_hal_ptp.c
index 9d668221765..e7d43aa5b98 100644
--- a/drivers/ethernet/eth_stm32_hal_ptp.c
+++ b/drivers/ethernet/eth_stm32_hal_ptp.c
@@ -29,7 +29,7 @@ LOG_MODULE_REGISTER(eth_stm32_hal_ptp, CONFIG_ETHERNET_LOG_LEVEL);

 bool eth_stm32_is_ptp_pkt(struct net_if *iface, struct net_pkt *pkt)
 {
-	if (ntohs(NET_ETH_HDR(pkt)->type) != NET_ETH_PTYPE_PTP) {
+	if (zsock_ntohs(NET_ETH_HDR(pkt)->type) != NET_ETH_PTYPE_PTP) {
 		return false;
 	}

diff --git a/drivers/ethernet/eth_stm32_hal_v1.c b/drivers/ethernet/eth_stm32_hal_v1.c
index 3fa8dfd0a65..45a2f658863 100644
--- a/drivers/ethernet/eth_stm32_hal_v1.c
+++ b/drivers/ethernet/eth_stm32_hal_v1.c
@@ -135,7 +135,7 @@ struct net_pkt *eth_stm32_rx(const struct device *dev)
 	dma_buffer = (uint8_t *)heth->RxFrameInfos.buffer;

 	pkt = net_pkt_rx_alloc_with_buffer(dev_data->iface,
-					   total_len, AF_UNSPEC, 0, K_MSEC(100));
+					   total_len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (!pkt) {
 		LOG_ERR("Failed to obtain RX buffer");
 		goto release_desc;
diff --git a/drivers/ethernet/eth_stm32_hal_v2.c b/drivers/ethernet/eth_stm32_hal_v2.c
index 4303ead523d..db13341f1dd 100644
--- a/drivers/ethernet/eth_stm32_hal_v2.c
+++ b/drivers/ethernet/eth_stm32_hal_v2.c
@@ -424,7 +424,7 @@ struct net_pkt *eth_stm32_rx(const struct device *dev)
 #endif /* CONFIG_PTP_CLOCK_STM32_HAL */

 	pkt = net_pkt_rx_alloc_with_buffer(dev_data->iface,
-					   total_len, AF_UNSPEC, 0, K_MSEC(100));
+					   total_len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (!pkt) {
 		LOG_ERR("Failed to obtain RX buffer");
 		goto release_desc;
diff --git a/drivers/ethernet/eth_virtio_net.c b/drivers/ethernet/eth_virtio_net.c
index 19de2d914e5..72888ccf830 100644
--- a/drivers/ethernet/eth_virtio_net.c
+++ b/drivers/ethernet/eth_virtio_net.c
@@ -163,7 +163,7 @@ void virtnet_rx_cb(void *priv, uint32_t len)

 	len -= sizeof(struct _virtio_net_hdr);
 	struct net_pkt *pkt =
-		net_pkt_rx_alloc_with_buffer(data->iface, len, AF_UNSPEC, 0, K_FOREVER);
+		net_pkt_rx_alloc_with_buffer(data->iface, len, ZSOCK_AF_UNSPEC, 0, K_FOREVER);

 	if (pkt == NULL) {
 		LOG_ERR("received packet, but could not pass it to the operating system");
diff --git a/drivers/ethernet/eth_w5500.c b/drivers/ethernet/eth_w5500.c
index 65d2385db49..e8ea05f101c 100644
--- a/drivers/ethernet/eth_w5500.c
+++ b/drivers/ethernet/eth_w5500.c
@@ -232,7 +232,7 @@ static void w5500_rx(const struct device *dev)
 	rx_len = sys_get_be16(header) - 2;

 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, rx_len,
-			AF_UNSPEC, 0, K_MSEC(config->timeout));
+			ZSOCK_AF_UNSPEC, 0, K_MSEC(config->timeout));
 	if (!pkt) {
 		eth_stats_update_errors_rx(ctx->iface);
 		return;
diff --git a/drivers/ethernet/eth_xilinx_axienet.c b/drivers/ethernet/eth_xilinx_axienet.c
index 63b4c55d583..2b505c73d1f 100644
--- a/drivers/ethernet/eth_xilinx_axienet.c
+++ b/drivers/ethernet/eth_xilinx_axienet.c
@@ -150,7 +150,7 @@ static void xilinx_axienet_rx_callback(const struct device *dma, void *user_data
 	data->rx_completed_buffer_index = next_descriptor;

 	packet_size = dma_xilinx_axi_dma_last_received_frame_length(dma);
-	pkt = net_pkt_rx_alloc_with_buffer(data->interface, packet_size, AF_UNSPEC, 0, K_NO_WAIT);
+	pkt = net_pkt_rx_alloc_with_buffer(data->interface, packet_size, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);

 	if (!pkt) {
 		LOG_ERR("Could not allocate a packet!");
diff --git a/drivers/ethernet/eth_xlnx_gem.c b/drivers/ethernet/eth_xlnx_gem.c
index 3b15aaf6b1c..c12a5799ebe 100644
--- a/drivers/ethernet/eth_xlnx_gem.c
+++ b/drivers/ethernet/eth_xlnx_gem.c
@@ -1534,7 +1534,7 @@ static void eth_xlnx_gem_handle_rx_pending(const struct device *dev)
 		 * now that the total frame length is known.
 		 */
 		pkt = net_pkt_rx_alloc_with_buffer(dev_data->iface, rx_data_length,
-						   AF_UNSPEC, 0, K_NO_WAIT);
+						   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (pkt == NULL) {
 			LOG_ERR("RX packet buffer alloc failed: %u bytes",
 				rx_data_length);
diff --git a/drivers/ethernet/eth_xmc4xxx.c b/drivers/ethernet/eth_xmc4xxx.c
index 31ca10db9a2..3666524cdc4 100644
--- a/drivers/ethernet/eth_xmc4xxx.c
+++ b/drivers/ethernet/eth_xmc4xxx.c
@@ -312,7 +312,7 @@ static int eth_xmc4xxx_send(const struct device *dev, struct net_pkt *pkt)
 #if defined(CONFIG_NET_GPTP)
 			struct net_eth_hdr *hdr = NET_ETH_HDR(pkt);

-			if (ntohs(hdr->type) == NET_ETH_PTYPE_PTP) {
+			if (zsock_ntohs(hdr->type) == NET_ETH_PTYPE_PTP) {
 				dma_desc->status |= ETH_MAC_DMA_TDES0_TTSE;
 			}
 #endif
diff --git a/drivers/ethernet/intel/eth_intel_igc.c b/drivers/ethernet/intel/eth_intel_igc.c
index 6a2ee666e15..13a2897c9d5 100644
--- a/drivers/ethernet/intel/eth_intel_igc.c
+++ b/drivers/ethernet/intel/eth_intel_igc.c
@@ -650,20 +650,20 @@ static int eth_intel_igc_tx_data(const struct device *dev, struct net_pkt *pkt)
 /**
  * @brief Identify the address family of received packets as per header type.
  */
-static sa_family_t eth_intel_igc_get_sa_family(uint8_t *rx_buf)
+static zsock_sa_family_t eth_intel_igc_get_sa_family(uint8_t *rx_buf)
 {
 	struct net_eth_hdr *eth_hdr = (struct net_eth_hdr *)rx_buf;

-	switch (ntohs(eth_hdr->type)) {
+	switch (zsock_ntohs(eth_hdr->type)) {
 	case NET_ETH_PTYPE_IP:
-		return AF_INET;
+		return ZSOCK_AF_INET;
 	case NET_ETH_PTYPE_IPV6:
-		return AF_INET6;
+		return ZSOCK_AF_INET6;
 	default:
 		break;
 	}

-	return AF_UNSPEC;
+	return ZSOCK_AF_UNSPEC;
 }

 /**
diff --git a/drivers/ethernet/nxp_imx_netc/eth_nxp_imx_netc.c b/drivers/ethernet/nxp_imx_netc/eth_nxp_imx_netc.c
index 2562cd42747..b65aab932f8 100644
--- a/drivers/ethernet/nxp_imx_netc/eth_nxp_imx_netc.c
+++ b/drivers/ethernet/nxp_imx_netc/eth_nxp_imx_netc.c
@@ -118,7 +118,7 @@ static int netc_eth_rx(const struct device *dev)
 	}
 #endif
 	/* Copy to pkt */
-	pkt = net_pkt_rx_alloc_with_buffer(iface_dst, length, AF_UNSPEC, 0, NETC_TIMEOUT);
+	pkt = net_pkt_rx_alloc_with_buffer(iface_dst, length, ZSOCK_AF_UNSPEC, 0, NETC_TIMEOUT);
 	if (pkt == NULL) {
 		eth_stats_update_errors_rx(iface_dst);
 		ret = -ENOBUFS;
@@ -409,7 +409,7 @@ int netc_eth_tx(const struct device *dev, struct net_pkt *pkt)
 	k_mutex_lock(&data->tx_mutex, K_FOREVER);

 #ifdef CONFIG_PTP_CLOCK_NXP_NETC
-	pkt_is_gptp = ntohs(NET_ETH_HDR(pkt)->type) == NET_ETH_PTYPE_PTP;
+	pkt_is_gptp = zsock_ntohs(NET_ETH_HDR(pkt)->type) == NET_ETH_PTYPE_PTP;
 	if ((pkt_is_gptp || net_pkt_is_tx_timestamping(pkt)) &&
 	    (netc_eth_get_ptp_clock(dev) != NULL)) {
 		opt.flags |= kEP_TX_OPT_REQ_TS;
diff --git a/drivers/ieee802154/ieee802154_b91.c b/drivers/ieee802154/ieee802154_b91.c
index a5d03db3cec..0becccb5c44 100644
--- a/drivers/ieee802154/ieee802154_b91.c
+++ b/drivers/ieee802154/ieee802154_b91.c
@@ -212,7 +212,7 @@ static void b91_handle_ack(void)

 	/* allocate ack packet */
 	ack_pkt = net_pkt_rx_alloc_with_buffer(data.iface, B91_ACK_FRAME_LEN,
-					       AF_UNSPEC, 0, K_NO_WAIT);
+					       ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!ack_pkt) {
 		LOG_ERR("No free packet available.");
 		return;
@@ -308,7 +308,7 @@ static void b91_rf_rx_isr(void)
 		}

 		/* get packet pointer from NET stack */
-		pkt = net_pkt_rx_alloc_with_buffer(data.iface, length, AF_UNSPEC, 0, K_NO_WAIT);
+		pkt = net_pkt_rx_alloc_with_buffer(data.iface, length, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_ERR("No pkt available");
 			goto exit;
diff --git a/drivers/ieee802154/ieee802154_cc1200.c b/drivers/ieee802154/ieee802154_cc1200.c
index b273c1f0ace..369ea529697 100644
--- a/drivers/ieee802154/ieee802154_cc1200.c
+++ b/drivers/ieee802154/ieee802154_cc1200.c
@@ -471,7 +471,7 @@ static void cc1200_rx(void *p1, void *p2, void *p3)
 		}

 		pkt = net_pkt_rx_alloc_with_buffer(cc1200->iface, pkt_len,
-						   AF_UNSPEC, 0, K_NO_WAIT);
+						   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_ERR("No free pkt available");
 			goto flush;
diff --git a/drivers/ieee802154/ieee802154_cc13xx_cc26xx.c b/drivers/ieee802154/ieee802154_cc13xx_cc26xx.c
index d5ab993593a..52e95bacf70 100644
--- a/drivers/ieee802154/ieee802154_cc13xx_cc26xx.c
+++ b/drivers/ieee802154/ieee802154_cc13xx_cc26xx.c
@@ -424,7 +424,7 @@ static void ieee802154_cc13xx_cc26xx_rx_done(
 				len, seq, rssi, lqi);

 			pkt = net_pkt_rx_alloc_with_buffer(
-				drv_data->iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+				drv_data->iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 			if (!pkt) {
 				LOG_WRN("Cannot allocate packet");
 				continue;
diff --git a/drivers/ieee802154/ieee802154_cc13xx_cc26xx_subg.c b/drivers/ieee802154/ieee802154_cc13xx_cc26xx_subg.c
index 3a9f215ce0e..914f3cdde1a 100644
--- a/drivers/ieee802154/ieee802154_cc13xx_cc26xx_subg.c
+++ b/drivers/ieee802154/ieee802154_cc13xx_cc26xx_subg.c
@@ -353,7 +353,7 @@ static void drv_rx_done(struct ieee802154_cc13xx_cc26xx_subg_data *drv_data)
 				len, rssi, status);

 			pkt = net_pkt_rx_alloc_with_buffer(
-				drv_data->iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+				drv_data->iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 			if (!pkt) {
 				LOG_WRN("Cannot allocate packet");
 				continue;
diff --git a/drivers/ieee802154/ieee802154_cc2520.c b/drivers/ieee802154/ieee802154_cc2520.c
index e81de86c099..66d7198faec 100644
--- a/drivers/ieee802154/ieee802154_cc2520.c
+++ b/drivers/ieee802154/ieee802154_cc2520.c
@@ -618,7 +618,7 @@ static void cc2520_rx(void *p1, void *p2, void *p3)
 		}

 		pkt = net_pkt_rx_alloc_with_buffer(cc2520->iface, pkt_len,
-						   AF_UNSPEC, 0, K_NO_WAIT);
+						   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_ERR("No pkt available");
 			goto flush;
diff --git a/drivers/ieee802154/ieee802154_dw1000.c b/drivers/ieee802154/ieee802154_dw1000.c
index 1f5d01c498a..c11b3205ea4 100644
--- a/drivers/ieee802154/ieee802154_dw1000.c
+++ b/drivers/ieee802154/ieee802154_dw1000.c
@@ -411,7 +411,7 @@ static inline void dwt_irq_handle_rx(const struct device *dev, uint32_t sys_stat
 	}

 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, pkt_len,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No buf available");
 		goto rx_out_enable_rx;
diff --git a/drivers/ieee802154/ieee802154_esp32.c b/drivers/ieee802154/ieee802154_esp32.c
index dff3a6ec485..8cf3d0b5ead 100644
--- a/drivers/ieee802154/ieee802154_esp32.c
+++ b/drivers/ieee802154/ieee802154_esp32.c
@@ -78,7 +78,7 @@ void esp_ieee802154_receive_done(uint8_t *frame, esp_ieee802154_frame_info_t *fr

 	LOG_HEXDUMP_DBG(payload, len, "RX buffer:");

-	pkt = net_pkt_rx_alloc_with_buffer(esp32_data.iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+	pkt = net_pkt_rx_alloc_with_buffer(esp32_data.iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No pkt available");
 		goto exit;
@@ -233,7 +233,7 @@ static int handle_ack(struct ieee802154_esp32_data *data)
 		ack_len = data->ack_frame[0] - IEEE802154_FCS_LENGTH;
 	}

-	ack_pkt = net_pkt_rx_alloc_with_buffer(data->iface, ack_len, AF_UNSPEC, 0, K_NO_WAIT);
+	ack_pkt = net_pkt_rx_alloc_with_buffer(data->iface, ack_len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!ack_pkt) {
 		LOG_ERR("No free packet available.");
 		err = -ENOMEM;
diff --git a/drivers/ieee802154/ieee802154_kw41z.c b/drivers/ieee802154/ieee802154_kw41z.c
index 244d350b215..7d09b33d3d7 100644
--- a/drivers/ieee802154/ieee802154_kw41z.c
+++ b/drivers/ieee802154/ieee802154_kw41z.c
@@ -513,7 +513,7 @@ static inline void kw41z_rx(struct kw41z_context *kw41z, uint8_t len)
 #endif

 	pkt = net_pkt_rx_alloc_with_buffer(kw41z->iface, pkt_len,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No buf available");
 		goto out;
@@ -581,7 +581,7 @@ static void handle_ack(struct kw41z_context *kw41z, uint8_t seq_number)
 	uint8_t ack_psdu[ACK_FRAME_LEN];

 	ack_pkt = net_pkt_rx_alloc_with_buffer(kw41z->iface, ACK_FRAME_LEN,
-					       AF_UNSPEC, 0, K_NO_WAIT);
+					       ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!ack_pkt) {
 		LOG_ERR("No free packet available.");
 		return;
diff --git a/drivers/ieee802154/ieee802154_mcr20a.c b/drivers/ieee802154/ieee802154_mcr20a.c
index 2c82ca7566c..a3e4143e341 100644
--- a/drivers/ieee802154/ieee802154_mcr20a.c
+++ b/drivers/ieee802154/ieee802154_mcr20a.c
@@ -567,7 +567,7 @@ static inline void mcr20a_rx(const struct device *dev, uint8_t len)
 	pkt_len = len - MCR20A_FCS_LENGTH;

 	pkt = net_pkt_rx_alloc_with_buffer(mcr20a->iface, pkt_len,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No buf available");
 		goto out;
diff --git a/drivers/ieee802154/ieee802154_mcxw.c b/drivers/ieee802154/ieee802154_mcxw.c
index 6bc71cecdd8..8878f1a5cd3 100644
--- a/drivers/ieee802154/ieee802154_mcxw.c
+++ b/drivers/ieee802154/ieee802154_mcxw.c
@@ -313,7 +313,7 @@ static int handle_ack(struct mcxw_context *mcxw_radio)
 	int err = 0;

 	len = mcxw_radio->rx_ack_frame.length;
-	pkt = net_pkt_rx_alloc_with_buffer(mcxw_radio->iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+	pkt = net_pkt_rx_alloc_with_buffer(mcxw_radio->iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No free packet available.");
 		err = -ENOMEM;
@@ -519,7 +519,7 @@ void mcxw_rx_thread(void *arg1, void *arg2, void *arg3)
 			continue;
 		}

-		pkt = net_pkt_rx_alloc_with_buffer(mcxw_radio->iface, rx_frame.length, AF_UNSPEC, 0,
+		pkt = net_pkt_rx_alloc_with_buffer(mcxw_radio->iface, rx_frame.length, ZSOCK_AF_UNSPEC, 0,
 						   K_FOREVER);

 		if (net_pkt_write(pkt, rx_frame.psdu, rx_frame.length)) {
diff --git a/drivers/ieee802154/ieee802154_nrf5.c b/drivers/ieee802154/ieee802154_nrf5.c
index 60f45edb9c7..eaa07e74fc5 100644
--- a/drivers/ieee802154/ieee802154_nrf5.c
+++ b/drivers/ieee802154/ieee802154_nrf5.c
@@ -196,7 +196,7 @@ static void nrf5_rx_thread(void *arg1, void *arg2, void *arg3)
 		 * thus stops acknowledging consecutive frames).
 		 */
 		pkt = net_pkt_rx_alloc_with_buffer(nrf5_radio->iface, pkt_len,
-						   AF_UNSPEC, 0, K_FOREVER);
+						   ZSOCK_AF_UNSPEC, 0, K_FOREVER);

 		if (net_pkt_write(pkt, rx_frame->psdu + 1, pkt_len)) {
 			goto drop;
@@ -433,7 +433,7 @@ static int handle_ack(struct nrf5_802154_data *nrf5_radio)
 	}

 	ack_pkt = net_pkt_rx_alloc_with_buffer(nrf5_radio->iface, ack_len,
-					       AF_UNSPEC, 0, K_NO_WAIT);
+					       ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!ack_pkt) {
 		LOG_ERR("No free packet available.");
 		err = -ENOMEM;
diff --git a/drivers/ieee802154/ieee802154_rf2xx.c b/drivers/ieee802154/ieee802154_rf2xx.c
index 79c24125471..773047fa8e1 100644
--- a/drivers/ieee802154/ieee802154_rf2xx.c
+++ b/drivers/ieee802154/ieee802154_rf2xx.c
@@ -213,7 +213,7 @@ static void rf2xx_trx_rx(const struct device *dev)
 	}

 	pkt = net_pkt_rx_alloc_with_buffer(ctx->iface, pkt_len,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("No RX buffer available");
 		return;
diff --git a/drivers/ieee802154/ieee802154_stm32wba.c b/drivers/ieee802154/ieee802154_stm32wba.c
index 5f1714a213c..a54c04bc4c3 100644
--- a/drivers/ieee802154/ieee802154_stm32wba.c
+++ b/drivers/ieee802154/ieee802154_stm32wba.c
@@ -124,7 +124,7 @@ static void stm32wba_802154_rx_thread(void *arg1, void *arg2, void *arg3)
 		 * thus stops acknowledging consecutive frames).
 		 */
 		pkt = net_pkt_rx_alloc_with_buffer(stm32wba_radio->iface, pkt_len,
-						   AF_UNSPEC, 0, K_FOREVER);
+						   ZSOCK_AF_UNSPEC, 0, K_FOREVER);

 		if (net_pkt_write(pkt, rx_frame->psdu, pkt_len) != 0) {
 			LOG_ERR("Failed to write packet data");
@@ -466,7 +466,7 @@ static int handle_ack(struct stm32wba_802154_data_t *stm32wba_radio)
 	}

 	ack_pkt = net_pkt_rx_alloc_with_buffer(stm32wba_radio->iface, ack_len,
-					       AF_UNSPEC, 0, K_NO_WAIT);
+					       ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (ack_pkt == NULL) {
 		LOG_ERR("No free packet available.");
 		return -ENOMEM;
diff --git a/drivers/ieee802154/ieee802154_uart_pipe.c b/drivers/ieee802154/ieee802154_uart_pipe.c
index 3e006adacd1..90c86eb2b61 100644
--- a/drivers/ieee802154/ieee802154_uart_pipe.c
+++ b/drivers/ieee802154/ieee802154_uart_pipe.c
@@ -129,7 +129,7 @@ static uint8_t *upipe_rx(uint8_t *buf, size_t *off)

 	if (upipe->rx_len == upipe->rx_off) {
 		pkt = net_pkt_rx_alloc_with_buffer(upipe->iface, upipe->rx_len,
-						   AF_UNSPEC, 0, K_NO_WAIT);
+						   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!pkt) {
 			LOG_DBG("No pkt available");
 			goto flush;
diff --git a/drivers/modem/hl7800.c b/drivers/modem/hl7800.c
index 8d6dca55ab7..85294f3835b 100644
--- a/drivers/modem/hl7800.c
+++ b/drivers/modem/hl7800.c
@@ -377,11 +377,11 @@ static const char OK_STRING[] = "OK";

 struct hl7800_socket {
 	struct net_context *context;
-	sa_family_t family;
+	zsock_sa_family_t family;
 	enum net_sock_type type;
 	enum net_ip_protocol ip_proto;
-	struct sockaddr src;
-	struct sockaddr dst;
+	struct zsock_sockaddr src;
+	struct zsock_sockaddr dst;

 	bool created;
 	bool reconfig;
@@ -418,9 +418,9 @@ struct hl7800_config {
 struct hl7800_iface_ctx {
 	struct net_if *iface;
 	uint8_t mac_addr[6];
-	struct in_addr ipv4Addr, subnet, gateway, dns_v4;
+	struct zsock_in_addr ipv4Addr, subnet, gateway, dns_v4;
 #ifdef CONFIG_NET_IPV6
-	struct in6_addr ipv6Addr, dns_v6;
+	struct zsock_in6_addr ipv6Addr, dns_v6;
 	char dns_v6_string[HL7800_IPV6_ADDR_LEN];
 #endif
 	bool restarting;
@@ -843,23 +843,23 @@ static void socket_put(struct hl7800_socket *sock)
 	sock->error = 0;
 	sock->rx_size = 0;
 	sock->state = SOCK_IDLE;
-	(void)memset(&sock->src, 0, sizeof(struct sockaddr));
-	(void)memset(&sock->dst, 0, sizeof(struct sockaddr));
+	(void)memset(&sock->src, 0, sizeof(struct zsock_sockaddr));
+	(void)memset(&sock->dst, 0, sizeof(struct zsock_sockaddr));
 }

-char *hl7800_sprint_ip_addr(const struct sockaddr *addr)
+char *hl7800_sprint_ip_addr(const struct zsock_sockaddr *addr)
 {
 	static char buf[NET_IPV6_ADDR_LEN];

 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
-		return net_addr_ntop(AF_INET6, &net_sin6(addr)->sin6_addr, buf,
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		return net_addr_ntop(ZSOCK_AF_INET6, &net_sin6(addr)->sin6_addr, buf,
 				     sizeof(buf));
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-		if (addr->sa_family == AF_INET) {
-		return net_addr_ntop(AF_INET, &net_sin(addr)->sin_addr, buf,
+		if (addr->sa_family == ZSOCK_AF_INET) {
+		return net_addr_ntop(ZSOCK_AF_INET, &net_sin(addr)->sin_addr, buf,
 				     sizeof(buf));
 	} else
 #endif
@@ -1460,7 +1460,7 @@ static int send_data(struct hl7800_socket *sock, struct net_pkt *pkt)
 	send_len = net_buf_frags_len(frag);
 	/* start sending data */
 	k_sem_reset(&sock->sock_send_sem);
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		snprintk(buf, sizeof(buf), "AT+KTCPSND=%d,%zu", sock->socket_id,
 			 send_len);
 	} else {
@@ -1471,7 +1471,7 @@ static int send_data(struct hl7800_socket *sock, struct net_pkt *pkt)
 		}
 		snprintk(buf, sizeof(buf), "AT+KUDPSND=%d,\"%s\",%u,%zu",
 			 sock->socket_id, dst_addr,
-			 ntohs(net_sin(&sock->dst)->sin_port), send_len);
+			 zsock_ntohs(net_sin(&sock->dst)->sin_port), send_len);
 	}
 	send_at_cmd(sock, buf, K_NO_WAIT, 0, false);

@@ -1510,7 +1510,7 @@ static int send_data(struct hl7800_socket *sock, struct net_pkt *pkt)
 		ret = -ETIMEDOUT;
 	}
 done:
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		if (sock->error == 0) {
 			sock->state = SOCK_CONNECTED;
 		}
@@ -1612,15 +1612,15 @@ static int pkt_setup_ip_data(struct net_pkt *pkt, struct hl7800_socket *sock)
 #endif

 #if defined(CONFIG_NET_IPV6)
-	if (net_pkt_family(pkt) == AF_INET6) {
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
 		if (net_ipv6_create(
 			    pkt,
-			    &((struct sockaddr_in6 *)&sock->dst)->sin6_addr,
-			    &((struct sockaddr_in6 *)&sock->src)->sin6_addr)) {
+			    &((struct zsock_sockaddr_in6 *)&sock->dst)->sin6_addr,
+			    &((struct zsock_sockaddr_in6 *)&sock->src)->sin6_addr)) {
 			return -1;
 		}
-		net_pkt_set_remote_address(pkt, &sock->dst, sizeof(struct sockaddr_in6));
-		pkt->remote.sa_family = AF_INET6;
+		net_pkt_set_remote_address(pkt, &sock->dst, sizeof(struct zsock_sockaddr_in6));
+		pkt->remote.sa_family = ZSOCK_AF_INET6;
 		src_port = net_sin6(&sock->src)->sin6_port;
 		dst_port = net_sin6(&sock->dst)->sin6_port;

@@ -1628,14 +1628,14 @@ static int pkt_setup_ip_data(struct net_pkt *pkt, struct hl7800_socket *sock)
 	}
 #endif
 #if defined(CONFIG_NET_IPV4)
-	if (net_pkt_family(pkt) == AF_INET) {
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET) {
 		if (net_ipv4_create(
-			    pkt, &((struct sockaddr_in *)&sock->dst)->sin_addr,
-			    &((struct sockaddr_in *)&sock->src)->sin_addr)) {
+			    pkt, &((struct zsock_sockaddr_in *)&sock->dst)->sin_addr,
+			    &((struct zsock_sockaddr_in *)&sock->src)->sin_addr)) {
 			return -1;
 		}
-		net_pkt_set_remote_address(pkt, &sock->dst, sizeof(struct sockaddr_in));
-		pkt->remote.sa_family = AF_INET;
+		net_pkt_set_remote_address(pkt, &sock->dst, sizeof(struct zsock_sockaddr_in));
+		pkt->remote.sa_family = ZSOCK_AF_INET;
 		src_port = net_sin(&sock->src)->sin_port;
 		dst_port = net_sin(&sock->dst)->sin_port;

@@ -1644,7 +1644,7 @@ static int pkt_setup_ip_data(struct net_pkt *pkt, struct hl7800_socket *sock)
 #endif

 #if defined(CONFIG_NET_UDP)
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		if (net_udp_create(pkt, dst_port, src_port)) {
 			return -1;
 		}
@@ -1653,7 +1653,7 @@ static int pkt_setup_ip_data(struct net_pkt *pkt, struct hl7800_socket *sock)
 	}
 #endif
 #if defined(CONFIG_NET_TCP)
-	if (sock->ip_proto == IPPROTO_TCP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_TCP) {
 		NET_PKT_DATA_ACCESS_DEFINE(tcp_access, struct net_tcp_hdr);

 		tcp = (struct net_tcp_hdr *)net_pkt_get_data(pkt, &tcp_access);
@@ -1922,7 +1922,7 @@ static void dns_work_cb(struct k_work *work)
 #if defined(CONFIG_DNS_RESOLVER) && !defined(CONFIG_DNS_SERVER_IP_ADDRESSES)
 	int ret;
 	struct dns_resolve_context *dnsCtx;
-	struct sockaddr temp_addr;
+	struct zsock_sockaddr temp_addr;
 	bool valid_address = false;
 	bool retry = false;
 	static const char *const dns_servers_str[] = {
@@ -2014,7 +2014,7 @@ char *mdm_hl7800_get_imsi(void)
  * a01.a02.a03.a04.a05.a06.a07.a08.a09.a10.a11.a12.a13.a14.a15.a16 to
  * an IPv6 address.
  */
-static int hl7800_net_addr6_pton(const char *src, struct in6_addr *dst)
+static int hl7800_net_addr6_pton(const char *src, struct zsock_in6_addr *dst)
 {
 	int num_sections = 8;
 	int i, len;
@@ -2042,7 +2042,7 @@ static int hl7800_net_addr6_pton(const char *src, struct in6_addr *dst)
 			return -EINVAL;
 		}
 		ipv6_section = (ipv6_section << 8) | (uint16_t)strtol(src, NULL, 10);
-		UNALIGNED_PUT(htons(ipv6_section), &dst->s6_addr16[i]);
+		UNALIGNED_PUT(zsock_htons(ipv6_section), &dst->s6_addr16[i]);

 		src = strchr(src, '.');
 		if (src) {
@@ -2068,8 +2068,8 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 	size_t out_len;
 	char value[MDM_IP_INFO_RESP_SIZE];
 	char *search_start, *addr_start, *sm_start;
-	struct in_addr new_ipv4_addr;
-	struct in6_addr new_ipv6_addr;
+	struct zsock_in_addr new_ipv4_addr;
+	struct zsock_in6_addr new_ipv6_addr;
 	bool is_ipv4;
 	int addr_len;
 	char temp_addr_str[HL7800_IPV6_ADDR_LEN];
@@ -2127,7 +2127,7 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 	temp_addr_str[addr_len] = 0;
 	LOG_DBG("IP addr: %s", temp_addr_str);
 	if (is_ipv4) {
-		ret = net_addr_pton(AF_INET, temp_addr_str, &new_ipv4_addr);
+		ret = net_addr_pton(ZSOCK_AF_INET, temp_addr_str, &new_ipv4_addr);
 	} else {
 		ret = hl7800_net_addr6_pton(temp_addr_str, &new_ipv6_addr);
 	}
@@ -2143,7 +2143,7 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 		addr_len = delims[3] - sm_start;
 		strncpy(temp_addr_str, sm_start, addr_len);
 		temp_addr_str[addr_len] = 0;
-		ret = net_addr_pton(AF_INET, temp_addr_str, &iface_ctx.subnet);
+		ret = net_addr_pton(ZSOCK_AF_INET, temp_addr_str, &iface_ctx.subnet);
 		if (ret < 0) {
 			LOG_ERR("Invalid subnet");
 			return true;
@@ -2154,7 +2154,7 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 		addr_len = delims[4] - addr_start;
 		strncpy(temp_addr_str, addr_start, addr_len);
 		temp_addr_str[addr_len] = 0;
-		ret = net_addr_pton(AF_INET, temp_addr_str, &iface_ctx.gateway);
+		ret = net_addr_pton(ZSOCK_AF_INET, temp_addr_str, &iface_ctx.gateway);
 		if (ret < 0) {
 			LOG_ERR("Invalid gateway");
 			return true;
@@ -2173,7 +2173,7 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 		}
 		strncpy(iface_ctx.dns_v4_string, addr_start, addr_len);
 		iface_ctx.dns_v4_string[addr_len] = 0;
-		ret = net_addr_pton(AF_INET, iface_ctx.dns_v4_string, &iface_ctx.dns_v4);
+		ret = net_addr_pton(ZSOCK_AF_INET, iface_ctx.dns_v4_string, &iface_ctx.dns_v4);
 		LOG_DBG("IPv4 DNS addr: %s", iface_ctx.dns_v4_string);
 	}
 #ifdef CONFIG_NET_IPV6
@@ -2186,7 +2186,7 @@ static bool on_cmd_atcmdinfo_ipaddr(struct net_buf **buf, uint16_t len)
 		strncpy(iface_ctx.dns_v6_string, addr_start, addr_len);

 		ret = hl7800_net_addr6_pton(iface_ctx.dns_v6_string, &iface_ctx.dns_v6);
-		net_addr_ntop(AF_INET6, &iface_ctx.dns_v6, iface_ctx.dns_v6_string,
+		net_addr_ntop(ZSOCK_AF_INET6, &iface_ctx.dns_v6, iface_ctx.dns_v6_string,
 			      sizeof(iface_ctx.dns_v6_string));
 		LOG_DBG("IPv6 DNS addr: %s", iface_ctx.dns_v6_string);
 	}
@@ -3346,7 +3346,7 @@ static void notify_all_tcp_sockets_closed(void)

 	for (i = 0; i < MDM_MAX_SOCKETS; i++) {
 		sock = &iface_ctx.sockets[i];
-		if ((sock->context != NULL) && (sock->type == SOCK_STREAM)) {
+		if ((sock->context != NULL) && (sock->type == ZSOCK_SOCK_STREAM)) {
 			LOG_DBG("Sock %d closed", sock->socket_id);
 			/* signal RX callback with null packet */
 			if (sock->recv_cb) {
@@ -3936,7 +3936,7 @@ static void sock_notif_cb_work(struct k_work *work)
 		k_work_reschedule_for_queue(&hl7800_workq, &sock->notif_work,
 					    MDM_SOCK_NOTIF_DELAY);
 	} else {
-		if (sock->type == SOCK_STREAM) {
+		if (sock->type == ZSOCK_SOCK_STREAM) {
 			LOG_DBG("Sock %d trigger NULL packet", sock->socket_id);
 			k_work_submit_to_queue(&hl7800_workq, &sock->recv_cb_work);
 		}
@@ -4020,9 +4020,9 @@ static int delete_socket(struct hl7800_socket *sock, enum net_sock_type type, ui
 {
 	char cmd[sizeof("AT+KUDPCLOSE=###")];

-	if (type == SOCK_STREAM) {
+	if (type == ZSOCK_SOCK_STREAM) {
 		snprintk(cmd, sizeof(cmd), "AT+KTCPDEL=%d", id);
-	} else if (type == SOCK_DGRAM) {
+	} else if (type == ZSOCK_SOCK_DGRAM) {
 		snprintk(cmd, sizeof(cmd), "AT+KUDPCLOSE=%d", id);
 	}

@@ -4058,9 +4058,9 @@ static bool on_cmd_sockcreate(enum net_sock_type type, struct net_buf **buf, uin
 	out_len = net_buf_linearize(value, sizeof(value), *buf, 0, len);
 	value[out_len] = 0;
 	iface_ctx.last_socket_id = strtol(value, NULL, 10);
-	if (type == SOCK_STREAM) {
+	if (type == ZSOCK_SOCK_STREAM) {
 		LOG_DBG("+KTCPCFG: %d", iface_ctx.last_socket_id);
-	} else if (type == SOCK_DGRAM) {
+	} else if (type == ZSOCK_SOCK_DGRAM) {
 		LOG_DBG("+KUDPCFG: %d", iface_ctx.last_socket_id);
 	}

@@ -4097,13 +4097,13 @@ done:
 /* Handler: +KTCPCFG: <session_id> */
 static bool on_cmd_sock_tcp_create(struct net_buf **buf, uint16_t len)
 {
-	return on_cmd_sockcreate(SOCK_STREAM, buf, len);
+	return on_cmd_sockcreate(ZSOCK_SOCK_STREAM, buf, len);
 }

 /* Handler: +KUDPCFG: <session_id> */
 static bool on_cmd_sock_udp_create(struct net_buf **buf, uint16_t len)
 {
-	return on_cmd_sockcreate(SOCK_DGRAM, buf, len);
+	return on_cmd_sockcreate(ZSOCK_SOCK_DGRAM, buf, len);
 }

 static void sockreadrecv_cb_work(struct k_work *work)
@@ -4270,7 +4270,7 @@ rx_err:
 	net_pkt_unref(sock->recv_pkt);
 	sock->recv_pkt = NULL;
 done:
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		if (sock->error == 0) {
 			sock->state = SOCK_CONNECTED;
 		}
@@ -4317,7 +4317,7 @@ static int start_socket_rx(struct hl7800_socket *sock, uint16_t rx_size)

 	LOG_DBG("Start socket RX ID:%d size:%d", sock->socket_id, rx_size);
 	sock->state = SOCK_RX;
-	if (sock->type == SOCK_DGRAM) {
+	if (sock->type == ZSOCK_SOCK_DGRAM) {
 #if defined(CONFIG_NET_IPV4)
 		if (rx_size > (net_if_get_mtu(iface_ctx.iface) - NET_IPV4UDPH_LEN)) {
 			sock->rx_size =
@@ -5859,12 +5859,12 @@ static int configure_TCP_socket(struct hl7800_socket *sock)
 	restore_on_boot = true;
 #endif

-	if (sock->dst.sa_family == AF_INET6) {
+	if (sock->dst.sa_family == ZSOCK_AF_INET6) {
 		af = MDM_HL7800_SOCKET_AF_IPV6;
-		dst_port = ntohs(net_sin6(&sock->dst)->sin6_port);
-	} else if (sock->dst.sa_family == AF_INET) {
+		dst_port = zsock_ntohs(net_sin6(&sock->dst)->sin6_port);
+	} else if (sock->dst.sa_family == ZSOCK_AF_INET) {
 		af = MDM_HL7800_SOCKET_AF_IPV4;
-		dst_port = ntohs(net_sin(&sock->dst)->sin_port);
+		dst_port = zsock_ntohs(net_sin(&sock->dst)->sin_port);
 	} else {
 		return -EINVAL;
 	}
@@ -5897,9 +5897,9 @@ static int configure_UDP_socket(struct hl7800_socket *sock)

 	sock->socket_id = MDM_CREATE_SOCKET_ID;

-	if (sock->family == AF_INET) {
+	if (sock->family == ZSOCK_AF_INET) {
 		af = MDM_HL7800_SOCKET_AF_IPV4;
-	} else if (sock->family == AF_INET6) {
+	} else if (sock->family == ZSOCK_AF_INET6) {
 		af = MDM_HL7800_SOCKET_AF_IPV6;
 	} else {
 		return -EINVAL;
@@ -5964,7 +5964,7 @@ done:
 	return ret;
 }

-static int offload_get(sa_family_t family, enum net_sock_type type,
+static int offload_get(zsock_sa_family_t family, enum net_sock_type type,
 		       enum net_ip_protocol ip_proto,
 		       struct net_context **context)
 {
@@ -5994,7 +5994,7 @@ static int offload_get(sa_family_t family, enum net_sock_type type,
 	 * TCP socket needs to be created later once the
 	 * connection IP address is known.
 	 */
-	if (type == SOCK_DGRAM) {
+	if (type == ZSOCK_SOCK_DGRAM) {
 		wakeup_hl7800();

 		/* reconfig IP connection if necessary */
@@ -6016,7 +6016,7 @@ done:
 }

 static int offload_bind(struct net_context *context,
-			const struct sockaddr *addr, socklen_t addr_len)
+			const struct zsock_sockaddr *addr, zsock_socklen_t addr_len)
 {
 	struct hl7800_socket *sock = NULL;

@@ -6033,14 +6033,14 @@ static int offload_bind(struct net_context *context,
 	/* save bind address information */
 	sock->src.sa_family = addr->sa_family;
 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
+	if (addr->sa_family == ZSOCK_AF_INET6) {
 		net_ipaddr_copy(&net_sin6(&sock->src)->sin6_addr,
 				&net_sin6(addr)->sin6_addr);
 		net_sin6(&sock->src)->sin6_port = net_sin6(addr)->sin6_port;
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-		if (addr->sa_family == AF_INET) {
+		if (addr->sa_family == ZSOCK_AF_INET) {
 		net_ipaddr_copy(&net_sin(&sock->src)->sin_addr,
 				&net_sin(addr)->sin_addr);
 		net_sin(&sock->src)->sin_port = net_sin(addr)->sin_port;
@@ -6060,7 +6060,7 @@ static int offload_listen(struct net_context *context, int backlog)
 }

 static int offload_connect(struct net_context *context,
-			   const struct sockaddr *addr, socklen_t addr_len,
+			   const struct zsock_sockaddr *addr, zsock_socklen_t addr_len,
 			   net_context_connect_cb_t cb, int32_t timeout,
 			   void *user_data)
 {
@@ -6087,7 +6087,7 @@ static int offload_connect(struct net_context *context,
 	sock->dst.sa_family = addr->sa_family;

 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
+	if (addr->sa_family == ZSOCK_AF_INET6) {
 		net_ipaddr_copy(&net_sin6(&sock->dst)->sin6_addr,
 				&net_sin6(addr)->sin6_addr);
 		dst_port = net_sin6(addr)->sin6_port;
@@ -6095,7 +6095,7 @@ static int offload_connect(struct net_context *context,
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-		if (addr->sa_family == AF_INET) {
+		if (addr->sa_family == ZSOCK_AF_INET) {
 		net_ipaddr_copy(&net_sin(&sock->dst)->sin_addr,
 				&net_sin(addr)->sin_addr);
 		dst_port = net_sin(addr)->sin_port;
@@ -6107,13 +6107,13 @@ static int offload_connect(struct net_context *context,
 	}

 	if (dst_port < 0) {
-		LOG_ERR("Invalid port: %d", ntohs(dst_port));
+		LOG_ERR("Invalid port: %d", zsock_ntohs(dst_port));
 		return -EINVAL;
 	}

 	hl7800_lock();

-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		wakeup_hl7800();

 		reconfigure_IP_connection();
@@ -6152,8 +6152,8 @@ static int offload_accept(struct net_context *context, net_tcp_accept_cb_t cb,
 	return -ENOTSUP;
 }

-static int offload_sendto(struct net_pkt *pkt, const struct sockaddr *dst_addr,
-			  socklen_t addr_len, net_context_send_cb_t cb,
+static int offload_sendto(struct net_pkt *pkt, const struct zsock_sockaddr *dst_addr,
+			  zsock_socklen_t addr_len, net_context_send_cb_t cb,
 			  int32_t timeout, void *user_data)
 {
 	struct net_context *context = net_pkt_context(pkt);
@@ -6171,14 +6171,14 @@ static int offload_sendto(struct net_pkt *pkt, const struct sockaddr *dst_addr,
 	}

 #if defined(CONFIG_NET_IPV6)
-	if (dst_addr->sa_family == AF_INET6) {
+	if (dst_addr->sa_family == ZSOCK_AF_INET6) {
 		net_ipaddr_copy(&net_sin6(&sock->dst)->sin6_addr,
 				&net_sin6(dst_addr)->sin6_addr);
 		net_sin6(&sock->dst)->sin6_port = net_sin6(dst_addr)->sin6_port;
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-		if (dst_addr->sa_family == AF_INET) {
+		if (dst_addr->sa_family == ZSOCK_AF_INET) {
 		net_ipaddr_copy(&net_sin(&sock->dst)->sin_addr,
 				&net_sin(dst_addr)->sin_addr);
 		net_sin(&sock->dst)->sin_port = net_sin(dst_addr)->sin_port;
@@ -6215,17 +6215,17 @@ static int offload_send(struct net_pkt *pkt, net_context_send_cb_t cb,
 			int32_t timeout, void *user_data)
 {
 	struct net_context *context = net_pkt_context(pkt);
-	socklen_t addr_len;
+	zsock_socklen_t addr_len;

 	addr_len = 0;
 #if defined(CONFIG_NET_IPV6)
-	if (net_pkt_family(pkt) == AF_INET6) {
-		addr_len = sizeof(struct sockaddr_in6);
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
+		addr_len = sizeof(struct zsock_sockaddr_in6);
 	} else
 #endif /* CONFIG_NET_IPV6 */
 #if defined(CONFIG_NET_IPV4)
-		if (net_pkt_family(pkt) == AF_INET) {
-		addr_len = sizeof(struct sockaddr_in);
+		if (net_pkt_family(pkt) == ZSOCK_AF_INET) {
+		addr_len = sizeof(struct zsock_sockaddr_in);
 	} else
 #endif /* CONFIG_NET_IPV4 */
 	{
@@ -6278,7 +6278,7 @@ static int offload_put(struct net_context *context)
 	hl7800_lock();

 	/* close connection */
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		snprintk(cmd, sizeof(cmd), "AT+KTCPCLOSE=%d",
 			 sock->socket_id);
 	} else {
@@ -6288,11 +6288,11 @@ static int offload_put(struct net_context *context)

 	wakeup_hl7800();

-	if ((sock->type == SOCK_DGRAM) || (sock->error != -ENOTCONN)) {
+	if ((sock->type == ZSOCK_SOCK_DGRAM) || (sock->error != -ENOTCONN)) {
 		send_at_cmd(sock, cmd, MDM_CMD_SEND_TIMEOUT, 0, false);
 	}

-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		/* delete session */
 		delete_socket(sock, sock->type, sock->socket_id);
 	}
@@ -6301,7 +6301,7 @@ static int offload_put(struct net_context *context)

 	socket_put(sock);
 	net_context_unref(context);
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		/* TCP contexts are referenced twice,
 		 * once for the app and once for the stack.
 		 * Since TCP stack is not used for offload,
diff --git a/drivers/modem/hl78xx/hl78xx.h b/drivers/modem/hl78xx/hl78xx.h
index d48f5b2d887..d483cf7ebd2 100644
--- a/drivers/modem/hl78xx/hl78xx.h
+++ b/drivers/modem/hl78xx/hl78xx.h
@@ -349,7 +349,7 @@ struct hl78xx_config {
 struct socket_read_data {
 	char *recv_buf;
 	size_t recv_buf_len;
-	struct sockaddr *recv_addr;
+	struct zsock_sockaddr *recv_addr;
 	uint16_t recv_read_len;
 };

diff --git a/drivers/modem/hl78xx/hl78xx_sockets.c b/drivers/modem/hl78xx/hl78xx_sockets.c
index 9840ea906a3..7efc996c0b5 100644
--- a/drivers/modem/hl78xx/hl78xx_sockets.c
+++ b/drivers/modem/hl78xx/hl78xx_sockets.c
@@ -61,11 +61,11 @@ RING_BUF_DECLARE(mdm_recv_pool, CONFIG_MODEM_HL78XX_UART_BUFFER_SIZES);
 struct hl78xx_dns_info {
 #ifdef CONFIG_NET_IPV4
 	char v4_string[NET_IPV4_ADDR_LEN];
-	struct in_addr v4;
+	struct zsock_in_addr v4;
 #endif
 #ifdef CONFIG_NET_IPV6
 	char v6_string[NET_IPV6_ADDR_LEN];
-	struct in6_addr v6;
+	struct zsock_in6_addr v6;
 #endif
 	bool ready;
 };
@@ -73,19 +73,19 @@ struct hl78xx_dns_info {
 /* IPv4 information is optional and only present when IPv4 is enabled */
 #ifdef CONFIG_NET_IPV4
 struct hl78xx_ipv4_info {
-	struct in_addr addr;
-	struct in_addr subnet;
-	struct in_addr gateway;
-	struct in_addr new_addr;
+	struct zsock_in_addr addr;
+	struct zsock_in_addr subnet;
+	struct zsock_in_addr gateway;
+	struct zsock_in_addr new_addr;
 };
 #endif
 /* IPv6 information is optional and only present when IPv6 is enabled */
 #ifdef CONFIG_NET_IPV6
 struct hl78xx_ipv6_info {
-	struct in6_addr addr;
-	struct in6_addr subnet;
-	struct in6_addr gateway;
-	struct in6_addr new_addr;
+	struct zsock_in6_addr addr;
+	struct zsock_in6_addr subnet;
+	struct zsock_in6_addr gateway;
+	struct zsock_in6_addr new_addr;
 };
 #endif
 /* TLS information is optional and only present when TLS is enabled */
@@ -237,15 +237,15 @@ static void modem_pipe_callback(struct modem_pipe *pipe, enum modem_pipe_event e
 /* Socket I/O helpers */
 static int on_cmd_sockread_common(int socket_id, uint16_t socket_data_length, uint16_t len,
 				  void *user_data);
-static ssize_t offload_recvfrom(void *obj, void *buf, size_t len, int flags, struct sockaddr *from,
-				socklen_t *fromlen);
-static int prepare_send_cmd(const struct modem_socket *sock, const struct sockaddr *dst_addr,
+static ssize_t offload_recvfrom(void *obj, void *buf, size_t len, int flags, struct zsock_sockaddr *from,
+				zsock_socklen_t *fromlen);
+static int prepare_send_cmd(const struct modem_socket *sock, const struct zsock_sockaddr *dst_addr,
 			    size_t buf_len, char *cmd_buf, size_t cmd_buf_size);
 static int send_data_buffer(struct hl78xx_socket_data *socket_data, const char *buf,
 			    const size_t buf_len, int *sock_written);

 /* Socket lifecycle */
-static int create_socket(struct modem_socket *sock, const struct sockaddr *addr,
+static int create_socket(struct modem_socket *sock, const struct zsock_sockaddr *addr,
 			 struct hl78xx_socket_data *data);
 static int socket_close(struct hl78xx_socket_data *socket_data, struct modem_socket *sock);
 static int socket_delete(struct hl78xx_socket_data *socket_data, struct modem_socket *sock);
@@ -256,7 +256,7 @@ static void socket_notify_data(int socket_id, int new_total, void *user_data);
  */
 #if defined(CONFIG_NET_SOCKETS_SOCKOPT_TLS) && defined(CONFIG_MODEM_HL78XX_SOCKETS_SOCKOPT_TLS)
 static int map_credentials(struct hl78xx_socket_data *socket_data, const void *optval,
-			   socklen_t optlen);
+			   zsock_socklen_t optlen);
 static int hl78xx_configure_chipper_suit(struct hl78xx_socket_data *socket_data);
 #endif /* CONFIG_NET_SOCKETS_SOCKOPT_TLS */

@@ -678,7 +678,7 @@ void hl78xx_on_cgdcontrdp(struct modem_chat *chat, char **argv, uint16_t argc, v
  */
 static bool parse_ip(bool is_ipv4, const char *ip_str, void *out_addr)
 {
-	int ret = net_addr_pton(is_ipv4 ? AF_INET : AF_INET6, ip_str, out_addr);
+	int ret = net_addr_pton(is_ipv4 ? ZSOCK_AF_INET : ZSOCK_AF_INET6, ip_str, out_addr);

 	LOG_DBG("Parsing %s address: %s -> %s", is_ipv4 ? "IPv4" : "IPv6", ip_str,
 		(ret < 0) ? "FAIL" : "OK");
@@ -728,7 +728,7 @@ static bool update_dns(struct hl78xx_socket_data *socket_data, bool is_ipv4, con
 			return false;
 		}

-		net_addr_ntop(AF_INET6, &socket_data->dns.v6, socket_data->dns.v6_string,
+		net_addr_ntop(ZSOCK_AF_INET6, &socket_data->dns.v6, socket_data->dns.v6_string,
 			      sizeof(socket_data->dns.v6_string));
 		LOG_DBG("Parsed IPv6 DNS: %s", socket_data->dns.v6_string);
 	}
@@ -829,12 +829,12 @@ static int validate_socket(const struct modem_socket *sock, struct hl78xx_socket
 		return -1;
 	}

-	bool not_connected = (!sock->is_connected && sock->type != SOCK_DGRAM);
+	bool not_connected = (!sock->is_connected && sock->type != ZSOCK_SOCK_DGRAM);
 	bool tcp_disconnected =
-		(sock->type == SOCK_STREAM &&
+		(sock->type == ZSOCK_SOCK_STREAM &&
 		 !socket_data->tcp_conn_status[HL78XX_TCP_STATUS_ID(sock->id)].is_connected);
 	bool udp_not_created =
-		(sock->type == SOCK_DGRAM &&
+		(sock->type == ZSOCK_SOCK_DGRAM &&
 		 !socket_data->udp_conn_status[HL78XX_UDP_STATUS_ID(sock->id)].is_created);

 	if (not_connected || tcp_disconnected || udp_not_created) {
@@ -1195,7 +1195,7 @@ void dns_work_cb(const struct device *dev, bool hard_reset)
 	struct hl78xx_socket_data *socket_data =
 		(struct hl78xx_socket_data *)data->offload_dev->data;
 	struct dns_resolve_context *dnsCtx;
-	struct sockaddr temp_addr;
+	struct zsock_sockaddr temp_addr;
 	bool valid_address = false;
 	bool retry = false;
 	const char *const dns_servers_str[DNS_SERVERS_COUNT] = {
@@ -1338,17 +1338,17 @@ int modem_handle_data_capture(size_t target_len, struct hl78xx_data *data)
 				      target_len, data);
 }

-static int extract_ip_family_and_port(const struct sockaddr *addr, int *af, uint16_t *port)
+static int extract_ip_family_and_port(const struct zsock_sockaddr *addr, int *af, uint16_t *port)
 {
 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
-		*port = ntohs(net_sin6(addr)->sin6_port);
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		*port = zsock_ntohs(net_sin6(addr)->sin6_port);
 		*af = MDM_HL78XX_SOCKET_AF_IPV6;
 	} else {
 #endif /* CONFIG_NET_IPV6 */
 #if defined(CONFIG_NET_IPV4)
-		if (addr->sa_family == AF_INET) {
-			*port = ntohs(net_sin(addr)->sin_port);
+		if (addr->sa_family == ZSOCK_AF_INET) {
+			*port = zsock_ntohs(net_sin(addr)->sin_port);
 			*af = MDM_HL78XX_SOCKET_AF_IPV4;
 		} else {
 #endif /* CONFIG_NET_IPV4 */
@@ -1364,7 +1364,7 @@ static int extract_ip_family_and_port(const struct sockaddr *addr, int *af, uint
 }

 static int format_ip_and_setup_tls(struct hl78xx_socket_data *socket_data,
-				   const struct sockaddr *addr, char *ip_str, size_t ip_str_len,
+				   const struct zsock_sockaddr *addr, char *ip_str, size_t ip_str_len,
 				   struct modem_socket *sock)
 {
 	int ret = modem_context_sprint_ip_addr(addr, ip_str, ip_str_len);
@@ -1374,7 +1374,7 @@ static int format_ip_and_setup_tls(struct hl78xx_socket_data *socket_data,
 		errno = ENOMEM;
 		return -1;
 	}
-	if (sock->ip_proto == IPPROTO_TCP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_TCP) {
 		/* Determine actual length of the formatted IP string (it may be
 		 * shorter than the provided buffer size). Copy at most
 		 * MDM_MAX_HOSTNAME_LEN - 1 bytes and ensure NUL-termination to
@@ -1417,7 +1417,7 @@ static int send_tcp_or_tls_config(struct modem_socket *sock, uint16_t dst_port,
 	return 0;
 }

-static int send_udp_config(const struct sockaddr *addr, struct hl78xx_socket_data *socket_data,
+static int send_udp_config(const struct zsock_sockaddr *addr, struct hl78xx_socket_data *socket_data,
 			   struct modem_socket *sock)
 {
 	int ret = 0;
@@ -1443,7 +1443,7 @@ error:
 	return -1;
 }

-static int create_socket(struct modem_socket *sock, const struct sockaddr *addr,
+static int create_socket(struct modem_socket *sock, const struct zsock_sockaddr *addr,
 			 struct hl78xx_socket_data *data)
 {
 	LOG_DBG("entry fd=%d id=%d", sock->sock_fd, sock->id);
@@ -1461,7 +1461,7 @@ static int create_socket(struct modem_socket *sock, const struct sockaddr *addr,
 	if (format_ip_and_setup_tls(data, addr, ip_str, sizeof(ip_str), sock) < 0) {
 		return -1;
 	}
-	is_udp = (sock->ip_proto == IPPROTO_UDP);
+	is_udp = (sock->ip_proto == ZSOCK_IPPROTO_UDP);
 	if (is_udp) {
 		ret = send_udp_config(addr, data, sock);
 		LOG_DBG("send_udp_config returned %d", ret);
@@ -1469,7 +1469,7 @@ static int create_socket(struct modem_socket *sock, const struct sockaddr *addr,
 	}
 	mode = (sock->ip_proto == IPPROTO_TLS_1_2) ? 3 : 0;
 	/* only TCP and TLS are supported */
-	if (sock->ip_proto != IPPROTO_TCP && sock->ip_proto != IPPROTO_TLS_1_2) {
+	if (sock->ip_proto != ZSOCK_IPPROTO_TCP && sock->ip_proto != ZSOCK_IPPROTO_TLS_1_2) {
 		LOG_ERR("Unsupported protocol: %d", sock->ip_proto);
 		errno = EPROTONOSUPPORT;
 		return -1;
@@ -1487,7 +1487,7 @@ static int socket_close(struct hl78xx_socket_data *socket_data, struct modem_soc
 	char buf[sizeof("AT+KTCPCLOSE=##\r")];
 	int ret = 0;

-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		snprintk(buf, sizeof(buf), "AT+KUDPCLOSE=%d", sock->id);
 	} else {
 		snprintk(buf, sizeof(buf), "AT+KTCPCLOSE=%d", sock->id);
@@ -1506,7 +1506,7 @@ static int socket_delete(struct hl78xx_socket_data *socket_data, struct modem_so
 	char buf[sizeof("AT+KTCPDEL=##\r")];
 	int ret = 0;

-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		/**
 		 * snprintk(buf, sizeof(buf), "AT+KUDPDEL=%d", sock->id);
 		 * No need to delete udp config here according to ref guide. The at UDPCLOSE
@@ -1579,7 +1579,7 @@ static int offload_close(void *obj)
 	return 0;
 }

-static int offload_bind(void *obj, const struct sockaddr *addr, socklen_t addrlen)
+static int offload_bind(void *obj, const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	struct hl78xx_socket_data *socket_data = hl78xx_socket_data_from_sock(sock);
@@ -1606,7 +1606,7 @@ static int offload_bind(void *obj, const struct sockaddr *addr, socklen_t addrle
 	return 0;
 }

-static int offload_connect(void *obj, const struct sockaddr *addr, socklen_t addrlen)
+static int offload_connect(void *obj, const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	struct hl78xx_socket_data *socket_data = hl78xx_socket_data_from_sock(sock);
@@ -1637,7 +1637,7 @@ static int offload_connect(void *obj, const struct sockaddr *addr, socklen_t add
 	}
 	memcpy(&sock->dst, addr, sizeof(*addr));
 	/* skip socket connect if UDP */
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		errno = 0;
 		return 0;
 	}
@@ -1709,7 +1709,7 @@ static void prepare_read_command(struct hl78xx_socket_data *socket_data, char *s
 				 size_t bufsize, struct modem_socket *sock, size_t read_size)
 {
 	snprintk(sendbuf, bufsize, "AT+K%sRCV=%d,%zd%s",
-		 sock->ip_proto == IPPROTO_UDP ? "UDP" : "TCP", sock->id, read_size,
+		 sock->ip_proto == ZSOCK_IPPROTO_UDP ? "UDP" : "TCP", sock->id, read_size,
 		 socket_data->mdata_global->chat.delimiter);
 }

@@ -1754,7 +1754,7 @@ static int hl78xx_perform_receive_transaction(struct hl78xx_socket_data *socket_

 static void setup_socket_data(struct hl78xx_socket_data *socket_data, struct modem_socket *sock,
 			      struct socket_read_data *sock_data, void *buf, size_t len,
-			      struct sockaddr *from, uint16_t read_size)
+			      struct zsock_sockaddr *from, uint16_t read_size)
 {
 	memset(sock_data, 0, sizeof(*sock_data));
 	sock_data->recv_buf = buf;
@@ -1777,20 +1777,20 @@ static void check_tcp_state_if_needed(struct hl78xx_socket_data *socket_data,
 {
 	const char *check_ktcp_stat = "AT+KTCPSTAT";
 	/* Only check for TCP sockets */
-	if (sock->type != SOCK_STREAM) {
+	if (sock->type != ZSOCK_SOCK_STREAM) {
 		return;
 	}
 	if (atomic_test_and_clear_bit(&socket_data->mdata_global->state_leftover,
 				      MODEM_SOCKET_DATA_LEFTOVER_STATE_BIT) &&
-	    sock && sock->ip_proto == IPPROTO_TCP) {
+	    sock && sock->ip_proto == ZSOCK_IPPROTO_TCP) {
 		modem_dynamic_cmd_send(socket_data->mdata_global, NULL, check_ktcp_stat,
 				       strlen(check_ktcp_stat), hl78xx_get_ktcp_state_match(), 1,
 				       true);
 	}
 }

-static ssize_t offload_recvfrom(void *obj, void *buf, size_t len, int flags, struct sockaddr *from,
-				socklen_t *fromlen)
+static ssize_t offload_recvfrom(void *obj, void *buf, size_t len, int flags, struct zsock_sockaddr *from,
+				zsock_socklen_t *fromlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	struct hl78xx_socket_data *socket_data = hl78xx_socket_data_from_sock(sock);
@@ -1892,12 +1892,12 @@ int check_if_any_socket_connected(const struct device *dev)
  * Helpers used by sendto/recv paths, preparing commands and transmitting
  * data over the modem pipe.
  */
-static int prepare_send_cmd(const struct modem_socket *sock, const struct sockaddr *dst_addr,
+static int prepare_send_cmd(const struct modem_socket *sock, const struct zsock_sockaddr *dst_addr,
 			    size_t buf_len, char *cmd_buf, size_t cmd_buf_size)
 {
 	int ret = 0;

-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		char ip_str[NET_IPV6_ADDR_LEN];
 		uint16_t dst_port = 0;

@@ -1952,7 +1952,7 @@ static int send_data_buffer(struct hl78xx_socket_data *socket_data, const char *
 	return 0;
 }

-static int validate_and_prepare(struct modem_socket *sock, const struct sockaddr **dst_addr,
+static int validate_and_prepare(struct modem_socket *sock, const struct zsock_sockaddr **dst_addr,
 				size_t *buf_len, char *cmd_buf, size_t cmd_buf_len)
 {
 	/* Validate args and prepare send command */
@@ -1960,15 +1960,15 @@ static int validate_and_prepare(struct modem_socket *sock, const struct sockaddr
 		errno = EINVAL;
 		return -1;
 	}
-	if (sock->type != SOCK_DGRAM && !sock->is_connected) {
+	if (sock->type != ZSOCK_SOCK_DGRAM && !sock->is_connected) {
 		errno = ENOTCONN;
 		return -1;
 	}
-	if (!*dst_addr && sock->ip_proto == IPPROTO_UDP) {
+	if (!*dst_addr && sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		*dst_addr = &sock->dst;
 	}
 	if (*buf_len > MDM_MAX_DATA_LENGTH) {
-		if (sock->type == SOCK_DGRAM) {
+		if (sock->type == ZSOCK_SOCK_DGRAM) {
 			errno = EMSGSIZE;
 			return -1;
 		}
@@ -1999,7 +1999,7 @@ static int transmit_regular_data(struct hl78xx_socket_data *socket_data, const c

 /* send binary data via the +KUDPSND/+KTCPSND commands */
 static ssize_t send_socket_data(void *obj, struct hl78xx_socket_data *socket_data,
-				const struct sockaddr *dst_addr, const char *buf, size_t buf_len,
+				const struct zsock_sockaddr *dst_addr, const char *buf, size_t buf_len,
 				k_timeout_t timeout)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
@@ -2045,7 +2045,7 @@ cleanup:
 /* ===== TLS implementation (conditional) ================================
  * TLS credential upload and chipper settings helper implementations.
  */
-static int handle_tls_sockopts(void *obj, int optname, const void *optval, socklen_t optlen)
+static int handle_tls_sockopts(void *obj, int optname, const void *optval, zsock_socklen_t optlen)
 {
 	int ret;
 	struct modem_socket *sock = (struct modem_socket *)obj;
@@ -2092,7 +2092,7 @@ static int handle_tls_sockopts(void *obj, int optname, const void *optval, sockl
 }

 static int offload_setsockopt(void *obj, int level, int optname, const void *optval,
-			      socklen_t optlen)
+			      zsock_socklen_t optlen)
 {
 	int ret = 0;

@@ -2113,7 +2113,7 @@ static int offload_setsockopt(void *obj, int level, int optname, const void *opt
 #endif /* CONFIG_MODEM_HL78XX_SOCKETS_SOCKOPT_TLS */

 static ssize_t offload_sendto(void *obj, const void *buf, size_t len, int flags,
-			      const struct sockaddr *to, socklen_t tolen)
+			      const struct zsock_sockaddr *to, zsock_socklen_t tolen)
 {
 	int ret = 0;
 	struct modem_socket *sock = (struct modem_socket *)obj;
@@ -2137,7 +2137,7 @@ static ssize_t offload_sendto(void *obj, const void *buf, size_t len, int flags,
 	 * destination address is provided or the socket has a stored dst. The
 	 * helper validate_and_prepare will supply sock->dst for UDP when needed.
 	 */
-	if (sock->type != SOCK_DGRAM && !sock->is_connected) {
+	if (sock->type != ZSOCK_SOCK_DGRAM && !sock->is_connected) {
 		errno = ENOTCONN;
 		return -1;
 	}
@@ -2180,7 +2180,7 @@ static int offload_ioctl(void *obj, unsigned int request, va_list args)
 						pev_end);

 		if (ret == -1 && errno == ENOTSUP && (pfd->events & ZSOCK_POLLOUT) &&
-		    sock->ip_proto == IPPROTO_UDP) {
+		    sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 			/* Not Implemented */
 			/*
 			 *	You can implement this later when needed
@@ -2226,11 +2226,11 @@ static ssize_t offload_write(void *obj, const void *buffer, size_t count)
 	return offload_sendto(obj, buffer, count, 0, NULL, 0);
 }

-static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
+static ssize_t offload_sendmsg(void *obj, const struct zsock_msghdr *msg, int flags)
 {
 	ssize_t sent = 0;
-	struct iovec bkp_iovec = {0};
-	struct msghdr crafted_msg = {.msg_name = msg->msg_name, .msg_namelen = msg->msg_namelen};
+	struct zsock_iovec bkp_iovec = {0};
+	struct zsock_msghdr crafted_msg = {.msg_name = msg->msg_name, .msg_namelen = msg->msg_namelen};
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	struct hl78xx_socket_data *socket_data = hl78xx_socket_data_from_sock(sock);
 	size_t full_len = 0;
@@ -2453,7 +2453,7 @@ cleanup:
 }

 static int map_credentials(struct hl78xx_socket_data *socket_data, const void *optval,
-			   socklen_t optlen)
+			   zsock_socklen_t optlen)
 {
 	const sec_tag_t *sec_tags = (const sec_tag_t *)optval;
 	int ret = 0;
@@ -2560,22 +2560,22 @@ static bool offload_is_supported(int family, int type, int proto)
 	bool fam_ok = false;

 #ifdef CONFIG_NET_IPV4
-	if (family == AF_INET) {
+	if (family == ZSOCK_AF_INET) {
 		fam_ok = true;
 	}
 #endif
 #ifdef CONFIG_NET_IPV6
-	if (family == AF_INET6) {
+	if (family == ZSOCK_AF_INET6) {
 		fam_ok = true;
 	}
 #endif
 	if (!fam_ok) {
 		return false;
 	}
-	if (!(type == SOCK_DGRAM || type == SOCK_STREAM)) {
+	if (!(type == ZSOCK_SOCK_DGRAM || type == ZSOCK_SOCK_STREAM)) {
 		return false;
 	}
-	if (proto == IPPROTO_TCP || proto == IPPROTO_UDP) {
+	if (proto == ZSOCK_IPPROTO_TCP || proto == ZSOCK_IPPROTO_UDP) {
 		return true;
 	}
 #if defined(CONFIG_MODEM_HL78XX_SOCKETS_SOCKOPT_TLS)
@@ -2594,7 +2594,7 @@ static bool offload_is_supported(int family, int type, int proto)
 		inst, "hl78xx_dev", hl78xx_socket_init, NULL, &hl78xx_socket_data_##inst, NULL,    \
 		CONFIG_MODEM_HL78XX_OFFLOAD_INIT_PRIORITY, &api_funcs, MDM_MAX_DATA_LENGTH);       \
                                                                                                    \
-	NET_SOCKET_OFFLOAD_REGISTER(inst, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, AF_UNSPEC,          \
+	NET_SOCKET_OFFLOAD_REGISTER(inst, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, ZSOCK_AF_UNSPEC,          \
 				    offload_is_supported, offload_socket);

 #define MODEM_OFFLOAD_DEVICE_SWIR_HL78XX(inst) MODEM_HL78XX_DEFINE_OFFLOAD_INSTANCE(inst)
diff --git a/drivers/modem/modem_context.c b/drivers/modem/modem_context.c
index 01cd1ced6a5..f6ffaa6888a 100644
--- a/drivers/modem/modem_context.c
+++ b/drivers/modem/modem_context.c
@@ -20,27 +20,27 @@ LOG_MODULE_REGISTER(modem_context, CONFIG_MODEM_LOG_LEVEL);

 static struct modem_context *contexts[CONFIG_MODEM_CONTEXT_MAX_NUM];

-int modem_context_sprint_ip_addr(const struct sockaddr *addr, char *buf, size_t buf_size)
+int modem_context_sprint_ip_addr(const struct zsock_sockaddr *addr, char *buf, size_t buf_size)
 {
 	static const char unknown_str[] = "unk";

-	if (addr->sa_family == AF_INET6) {
+	if (addr->sa_family == ZSOCK_AF_INET6) {
 		if (buf_size < NET_IPV6_ADDR_LEN) {
 			return -ENOMEM;
 		}

-		if (net_addr_ntop(AF_INET6, &net_sin6(addr)->sin6_addr,
+		if (net_addr_ntop(ZSOCK_AF_INET6, &net_sin6(addr)->sin6_addr,
 					buf, buf_size) == NULL) {
 			return -ENOMEM;
 		}
 		return 0;
 	}

-	if (addr->sa_family == AF_INET) {
+	if (addr->sa_family == ZSOCK_AF_INET) {
 		if (buf_size < NET_IPV4_ADDR_LEN) {
 			return -ENOMEM;
 		}
-		if (net_addr_ntop(AF_INET, &net_sin(addr)->sin_addr,
+		if (net_addr_ntop(ZSOCK_AF_INET, &net_sin(addr)->sin_addr,
 					buf, buf_size) == NULL) {
 			return -ENOMEM;
 		}
@@ -56,17 +56,17 @@ int modem_context_sprint_ip_addr(const struct sockaddr *addr, char *buf, size_t
 	return 0;
 }

-int modem_context_get_addr_port(const struct sockaddr *addr, uint16_t *port)
+int modem_context_get_addr_port(const struct zsock_sockaddr *addr, uint16_t *port)
 {
 	if (!addr || !port) {
 		return -EINVAL;
 	}

-	if (addr->sa_family == AF_INET6) {
-		*port = ntohs(net_sin6(addr)->sin6_port);
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		*port = zsock_ntohs(net_sin6(addr)->sin6_port);
 		return 0;
-	} else if (addr->sa_family == AF_INET) {
-		*port = ntohs(net_sin(addr)->sin_port);
+	} else if (addr->sa_family == ZSOCK_AF_INET) {
+		*port = zsock_ntohs(net_sin(addr)->sin_port);
 		return 0;
 	}

diff --git a/drivers/modem/modem_context.h b/drivers/modem/modem_context.h
index 8c75e24aeb1..1a02dc6d25b 100644
--- a/drivers/modem/modem_context.h
+++ b/drivers/modem/modem_context.h
@@ -81,7 +81,7 @@ struct modem_context {
  *
  * @retval 0 if ok, < 0 if error.
  */
-int modem_context_sprint_ip_addr(const struct sockaddr *addr, char *buf, size_t buf_size);
+int modem_context_sprint_ip_addr(const struct zsock_sockaddr *addr, char *buf, size_t buf_size);

 /**
  * @brief  Get port from IP address
@@ -91,7 +91,7 @@ int modem_context_sprint_ip_addr(const struct sockaddr *addr, char *buf, size_t
  *
  * @retval 0 if ok, < 0 if error.
  */
-int modem_context_get_addr_port(const struct sockaddr *addr, uint16_t *port);
+int modem_context_get_addr_port(const struct zsock_sockaddr *addr, uint16_t *port);

 /**
  * @brief  Gets modem context by id.
diff --git a/drivers/modem/modem_socket.c b/drivers/modem/modem_socket.c
index 81c5a5cd31d..ab81b86bf4e 100644
--- a/drivers/modem/modem_socket.c
+++ b/drivers/modem/modem_socket.c
@@ -241,8 +241,8 @@ void modem_socket_put(struct modem_socket_config *cfg, int sock_fd)
 	sock->sock_fd = -1;
 	sock->is_waiting = false;
 	sock->is_connected = false;
-	(void)memset(&sock->src, 0, sizeof(struct sockaddr));
-	(void)memset(&sock->dst, 0, sizeof(struct sockaddr));
+	(void)memset(&sock->src, 0, sizeof(struct zsock_sockaddr));
+	(void)memset(&sock->dst, 0, sizeof(struct zsock_sockaddr));
 	memset(&sock->packet_sizes, 0, sizeof(sock->packet_sizes));
 	sock->packet_count = 0;
 	k_sem_reset(&sock->sem_data_ready);
@@ -279,7 +279,7 @@ int modem_socket_poll(struct modem_socket_config *cfg, struct zsock_pollfd *fds,
 		sock = modem_socket_from_fd(cfg, fds[i].fd);
 		if (sock) {
 			/*
-			 * Handle user check for POLLOUT events:
+			 * Handle user check for ZSOCK_POLLOUT events:
 			 * we consider the socket to always be writable.
 			 */
 			if (fds[i].events & ZSOCK_POLLOUT) {
diff --git a/drivers/modem/modem_socket.h b/drivers/modem/modem_socket.h
index 1c333f818df..99662ef26aa 100644
--- a/drivers/modem/modem_socket.h
+++ b/drivers/modem/modem_socket.h
@@ -24,11 +24,11 @@ extern "C" {
 #endif

 __net_socket struct modem_socket {
-	sa_family_t family;
+	zsock_sa_family_t family;
 	enum net_sock_type type;
 	int ip_proto;
-	struct sockaddr src;
-	struct sockaddr dst;
+	struct zsock_sockaddr src;
+	struct zsock_sockaddr dst;

 	/** The number identifying the socket handle inside the modem */
 	int id;
diff --git a/drivers/modem/quectel-bg9x.c b/drivers/modem/quectel-bg9x.c
index f6f7bcd8d06..421a563aa7b 100644
--- a/drivers/modem/quectel-bg9x.c
+++ b/drivers/modem/quectel-bg9x.c
@@ -438,7 +438,7 @@ MODEM_CMD_DEFINE(on_cmd_unsol_rdy)
  * Desc: This function will send "binary" data over the socket object.
  */
 static ssize_t send_socket_data(struct modem_socket *sock,
-				const struct sockaddr *dst_addr,
+				const struct zsock_sockaddr *dst_addr,
 				struct modem_cmd *handler_cmds,
 				size_t handler_cmds_len,
 				const char *buf, size_t buf_len,
@@ -518,8 +518,8 @@ exit:
  * Desc: This function will send data on the socket object.
  */
 static ssize_t offload_sendto(void *obj, const void *buf, size_t len,
-			      int flags, const struct sockaddr *to,
-			      socklen_t tolen)
+			      int flags, const struct zsock_sockaddr *to,
+			      zsock_socklen_t tolen)
 {
 	int ret;
 	struct modem_socket *sock = (struct modem_socket *) obj;
@@ -549,7 +549,7 @@ static ssize_t offload_sendto(void *obj, const void *buf, size_t len,
 	}

 	/* UDP is not supported. */
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		errno = ENOTSUP;
 		return -1;
 	}
@@ -575,8 +575,8 @@ static ssize_t offload_sendto(void *obj, const void *buf, size_t len,
  * Desc: This function will receive data on the socket object.
  */
 static ssize_t offload_recvfrom(void *obj, void *buf, size_t len,
-				int flags, struct sockaddr *from,
-				socklen_t *fromlen)
+				int flags, struct zsock_sockaddr *from,
+				zsock_socklen_t *fromlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	char   sendbuf[sizeof("AT+QIRD=##,####")] = {0};
@@ -684,8 +684,8 @@ static int offload_ioctl(void *obj, unsigned int request, va_list args)
 /* Func: offload_connect
  * Desc: This function will connect with a provided TCP.
  */
-static int offload_connect(void *obj, const struct sockaddr *addr,
-						   socklen_t addrlen)
+static int offload_connect(void *obj, const struct zsock_sockaddr *addr,
+						   zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock     = (struct modem_socket *) obj;
 	uint16_t	    dst_port  = 0;
@@ -713,14 +713,14 @@ static int offload_connect(void *obj, const struct sockaddr *addr,
 	}

 	/* Find the correct destination port. */
-	if (addr->sa_family == AF_INET6) {
-		dst_port = ntohs(net_sin6(addr)->sin6_port);
-	} else if (addr->sa_family == AF_INET) {
-		dst_port = ntohs(net_sin(addr)->sin_port);
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		dst_port = zsock_ntohs(net_sin6(addr)->sin6_port);
+	} else if (addr->sa_family == ZSOCK_AF_INET) {
+		dst_port = zsock_ntohs(net_sin(addr)->sin_port);
 	}

 	/* UDP is not supported. */
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		errno = ENOTSUP;
 		return -1;
 	}
@@ -811,7 +811,7 @@ static int offload_close(void *obj)
 /* Func: offload_sendmsg
  * Desc: This function sends messages to the modem.
  */
-static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
+static ssize_t offload_sendmsg(void *obj, const struct zsock_msghdr *msg, int flags)
 {
 	ssize_t sent = 0;
 	int rc;
@@ -1129,16 +1129,16 @@ static struct offloaded_if_api api_funcs = {

 static bool offload_is_supported(int family, int type, int proto)
 {
-	if (family != AF_INET &&
-	    family != AF_INET6) {
+	if (family != ZSOCK_AF_INET &&
+	    family != ZSOCK_AF_INET6) {
 		return false;
 	}

-	if (type != SOCK_STREAM) {
+	if (type != ZSOCK_SOCK_STREAM) {
 		return false;
 	}

-	if (proto != IPPROTO_TCP) {
+	if (proto != ZSOCK_IPPROTO_TCP) {
 		return false;
 	}

@@ -1287,4 +1287,4 @@ NET_DEVICE_DT_INST_OFFLOAD_DEFINE(0, modem_init, NULL,

 /* Register NET sockets. */
 NET_SOCKET_OFFLOAD_REGISTER(quectel_bg9x, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY,
-			    AF_UNSPEC, offload_is_supported, offload_socket);
+			    ZSOCK_AF_UNSPEC, offload_is_supported, offload_socket);
diff --git a/drivers/modem/quectel-bg9x.h b/drivers/modem/quectel-bg9x.h
index bda84b2e8eb..ba538152284 100644
--- a/drivers/modem/quectel-bg9x.h
+++ b/drivers/modem/quectel-bg9x.h
@@ -123,7 +123,7 @@ struct modem_data {
 struct socket_read_data {
 	char		 *recv_buf;
 	size_t		 recv_buf_len;
-	struct sockaddr	 *recv_addr;
+	struct zsock_sockaddr	 *recv_addr;
 	uint16_t	 recv_read_len;
 };

diff --git a/drivers/modem/simcom/sim7080/sim7080.c b/drivers/modem/simcom/sim7080/sim7080.c
index d3cb6987dc2..f72baaabc11 100644
--- a/drivers/modem/simcom/sim7080/sim7080.c
+++ b/drivers/modem/simcom/sim7080/sim7080.c
@@ -102,18 +102,18 @@ static struct offloaded_if_api api_funcs = {

 static bool offload_is_supported(int family, int type, int proto)
 {
-	if (family != AF_INET &&
-	    family != AF_INET6) {
+	if (family != ZSOCK_AF_INET &&
+	    family != ZSOCK_AF_INET6) {
 		return false;
 	}

-	if (type != SOCK_DGRAM &&
-	    type != SOCK_STREAM) {
+	if (type != ZSOCK_SOCK_DGRAM &&
+	    type != ZSOCK_SOCK_STREAM) {
 		return false;
 	}

-	if (proto != IPPROTO_TCP &&
-	    proto != IPPROTO_UDP) {
+	if (proto != ZSOCK_IPPROTO_TCP &&
+	    proto != ZSOCK_IPPROTO_UDP) {
 		return false;
 	}

@@ -881,4 +881,4 @@ NET_DEVICE_DT_INST_OFFLOAD_DEFINE(0, modem_init, NULL, &mdata, NULL,
 				  MDM_MAX_DATA_LENGTH);

 NET_SOCKET_OFFLOAD_REGISTER(simcom_sim7080, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY,
-			    AF_UNSPEC, offload_is_supported, sim7080_offload_socket);
+			    ZSOCK_AF_UNSPEC, offload_is_supported, sim7080_offload_socket);
diff --git a/drivers/modem/simcom/sim7080/sim7080.h b/drivers/modem/simcom/sim7080/sim7080.h
index 57ca6c6e8a0..6879cb042a3 100644
--- a/drivers/modem/simcom/sim7080/sim7080.h
+++ b/drivers/modem/simcom/sim7080/sim7080.h
@@ -177,7 +177,7 @@ struct sim7080_data {
 struct socket_read_data {
 	char *recv_buf;
 	size_t recv_buf_len;
-	struct sockaddr *recv_addr;
+	struct zsock_sockaddr *recv_addr;
 	uint16_t recv_read_len;
 };

diff --git a/drivers/modem/simcom/sim7080/sim7080_dns.c b/drivers/modem/simcom/sim7080/sim7080_dns.c
index 105de0ce3b2..050388b7188 100644
--- a/drivers/modem/simcom/sim7080/sim7080_dns.c
+++ b/drivers/modem/simcom/sim7080/sim7080_dns.c
@@ -14,7 +14,7 @@ LOG_MODULE_REGISTER(modem_simcom_sim7080_dns, CONFIG_MODEM_LOG_LEVEL);
 #include "sim7080.h"

 static struct zsock_addrinfo dns_result;
-static struct sockaddr dns_result_addr;
+static struct zsock_sockaddr dns_result_addr;
 static char dns_result_canonname[DNS_MAX_NAME_SIZE + 1];

 /*
@@ -53,7 +53,7 @@ MODEM_CMD_DEFINE(on_cmd_cdnsgip)

 	*ipv4 = '\0';
 	net_addr_pton(dns_result.ai_family, ips,
-			  &((struct sockaddr_in *)&dns_result_addr)->sin_addr);
+			  &((struct zsock_sockaddr_in *)&dns_result_addr)->sin_addr);
 	ret = 0;

 exit:
@@ -83,8 +83,8 @@ static int offload_getaddrinfo(const char *node, const char *service,
 	(void)memset(&dns_result_addr, 0, sizeof(dns_result_addr));

 	/* Currently only support IPv4. */
-	dns_result.ai_family = AF_INET;
-	dns_result_addr.sa_family = AF_INET;
+	dns_result.ai_family = ZSOCK_AF_INET;
+	dns_result_addr.sa_family = ZSOCK_AF_INET;
 	dns_result.ai_addr = &dns_result_addr;
 	dns_result.ai_addrlen = sizeof(dns_result_addr);
 	dns_result.ai_canonname = dns_result_canonname;
@@ -98,20 +98,20 @@ static int offload_getaddrinfo(const char *node, const char *service,
 	}

 	if (port > 0U) {
-		if (dns_result.ai_family == AF_INET) {
-			net_sin(&dns_result_addr)->sin_port = htons(port);
+		if (dns_result.ai_family == ZSOCK_AF_INET) {
+			net_sin(&dns_result_addr)->sin_port = zsock_htons(port);
 		}
 	}

 	/* Check if node is an IP address */
 	if (net_addr_pton(dns_result.ai_family, node,
-			  &((struct sockaddr_in *)&dns_result_addr)->sin_addr) == 0) {
+			  &((struct zsock_sockaddr_in *)&dns_result_addr)->sin_addr) == 0) {
 		*res = &dns_result;
 		return 0;
 	}

 	/* user flagged node as numeric host, but we failed net_addr_pton */
-	if (hints && hints->ai_flags & AI_NUMERICHOST) {
+	if (hints && hints->ai_flags & ZSOCK_AI_NUMERICHOST) {
 		return DNS_EAI_NONAME;
 	}

diff --git a/drivers/modem/simcom/sim7080/sim7080_sock.c b/drivers/modem/simcom/sim7080/sim7080_sock.c
index 66b52d9a141..2fefa572bad 100644
--- a/drivers/modem/simcom/sim7080/sim7080_sock.c
+++ b/drivers/modem/simcom/sim7080/sim7080_sock.c
@@ -31,7 +31,7 @@ MODEM_CMD_DEFINE(on_cmd_caopen)
 /*
  * Connects an modem socket. Protocol can either be TCP or UDP.
  */
-static int offload_connect(void *obj, const struct sockaddr *addr, socklen_t addrlen)
+static int offload_connect(void *obj, const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	uint16_t dst_port = 0;
@@ -60,14 +60,14 @@ static int offload_connect(void *obj, const struct sockaddr *addr, socklen_t add
 	}

 	/* get the destination port */
-	if (addr->sa_family == AF_INET6) {
-		dst_port = ntohs(net_sin6(addr)->sin6_port);
-	} else if (addr->sa_family == AF_INET) {
-		dst_port = ntohs(net_sin(addr)->sin_port);
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		dst_port = zsock_ntohs(net_sin6(addr)->sin6_port);
+	} else if (addr->sa_family == ZSOCK_AF_INET) {
+		dst_port = zsock_ntohs(net_sin(addr)->sin_port);
 	}

 	/* Get protocol */
-	protocol = (sock->type == SOCK_STREAM) ? "TCP" : "UDP";
+	protocol = (sock->type == ZSOCK_SOCK_STREAM) ? "TCP" : "UDP";

 	ret = modem_context_sprint_ip_addr(addr, ip_str, sizeof(ip_str));
 	if (ret != 0) {
@@ -124,7 +124,7 @@ MODEM_CMD_DEFINE(on_cmd_casend)
  * then send a OK or ERROR.
  */
 static ssize_t offload_sendto(void *obj, const void *buf, size_t len, int flags,
-				  const struct sockaddr *dest_addr, socklen_t addrlen)
+				  const struct zsock_sockaddr *dest_addr, zsock_socklen_t addrlen)
 {
 	int ret;
 	struct modem_socket *sock = (struct modem_socket *)obj;
@@ -304,7 +304,7 @@ MODEM_CMD_DEFINE(on_cmd_carecv)
  * Read data from a given socket.
  */
 static ssize_t offload_recvfrom(void *obj, void *buf, size_t max_len, int flags,
-				struct sockaddr *src_addr, socklen_t *addrlen)
+				struct zsock_sockaddr *src_addr, zsock_socklen_t *addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	char sendbuf[sizeof("AT+CARECV=##,####")];
@@ -377,7 +377,7 @@ exit:
 /*
  * Sends messages to the modem.
  */
-static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
+static ssize_t offload_sendmsg(void *obj, const struct zsock_msghdr *msg, int flags)
 {
 	struct modem_socket *sock = obj;
 	ssize_t sent = 0;
@@ -391,7 +391,7 @@ static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
 		return -EINVAL;
 	}

-	if (sock->type == SOCK_DGRAM) {
+	if (sock->type == ZSOCK_SOCK_DGRAM) {
 		/*
 		 * Current implementation only handles single contiguous fragment at a time, so
 		 * prevent sending multiple datagrams.
diff --git a/drivers/modem/ublox-sara-r4.c b/drivers/modem/ublox-sara-r4.c
index b8dc9b76be5..4331db19558 100644
--- a/drivers/modem/ublox-sara-r4.c
+++ b/drivers/modem/ublox-sara-r4.c
@@ -104,7 +104,7 @@ static struct k_work_q modem_workq;
 struct socket_read_data {
 	char *recv_buf;
 	size_t recv_buf_len;
-	struct sockaddr *recv_addr;
+	struct zsock_sockaddr *recv_addr;
 	uint16_t recv_read_len;
 };

@@ -165,7 +165,7 @@ static struct modem_context mctx;

 #if defined(CONFIG_DNS_RESOLVER)
 static struct zsock_addrinfo result;
-static struct sockaddr result_addr;
+static struct zsock_sockaddr result_addr;
 static char result_canonname[DNS_MAX_NAME_SIZE + 1];
 #endif

@@ -287,7 +287,7 @@ MODEM_CMD_DEFINE(on_cmd_sockwrite);

 /* send binary data via the +USO[ST/WR] commands */
 static ssize_t send_socket_data(void *obj,
-				const struct msghdr *msg,
+				const struct zsock_msghdr *msg,
 				k_timeout_t timeout)
 {
 	int ret;
@@ -300,7 +300,7 @@ static ssize_t send_socket_data(void *obj,
 		MODEM_CMD("+USOST: ", on_cmd_sockwrite, 2U, ","),
 		MODEM_CMD("+USOWR: ", on_cmd_sockwrite, 2U, ","),
 	};
-	struct sockaddr *dst_addr = msg->msg_name;
+	struct zsock_sockaddr *dst_addr = msg->msg_name;
 	size_t buf_len = 0;

 	if (!sock) {
@@ -315,12 +315,12 @@ static ssize_t send_socket_data(void *obj,
 		buf_len += msg->msg_iov[i].iov_len;
 	}

-	if (!sock->is_connected && sock->ip_proto != IPPROTO_UDP) {
+	if (!sock->is_connected && sock->ip_proto != ZSOCK_IPPROTO_UDP) {
 		errno = ENOTCONN;
 		return -1;
 	}

-	if (!dst_addr && sock->ip_proto == IPPROTO_UDP) {
+	if (!dst_addr && sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		dst_addr = &sock->dst;
 	}

@@ -329,7 +329,7 @@ static ssize_t send_socket_data(void *obj,
 	 * the socket in one command
 	 */
 	if (buf_len > MDM_MAX_DATA_LENGTH) {
-		if (sock->type == SOCK_DGRAM) {
+		if (sock->type == ZSOCK_SOCK_DGRAM) {
 			errno = EMSGSIZE;
 			return -1;
 		}
@@ -340,7 +340,7 @@ static ssize_t send_socket_data(void *obj,
 	/* The number of bytes written will be reported by the modem */
 	mdata.sock_written = 0;

-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		char ip_str[NET_IPV6_ADDR_LEN];

 		ret = modem_context_sprint_ip_addr(dst_addr, ip_str, sizeof(ip_str));
@@ -398,7 +398,7 @@ static ssize_t send_socket_data(void *obj,
 	 * if using AT+USOST. This if condition is matched with
 	 * the command selection above.
 	 */
-	if (sock->ip_proto != IPPROTO_UDP) {
+	if (sock->ip_proto != ZSOCK_IPPROTO_UDP) {
 		k_sleep(MDM_PROMPT_CMD_DELAY);
 	}

@@ -868,10 +868,10 @@ MODEM_CMD_DEFINE(on_cmd_dns)
 	argv[0][strlen(argv[0]) - 1] = '\0';

 	/* FIXME: Hard-code DNS on SARA-R4 to return IPv4 */
-	result_addr.sa_family = AF_INET;
+	result_addr.sa_family = ZSOCK_AF_INET;
 	/* skip beginning quote when parsing */
 	(void)net_addr_pton(result.ai_family, &argv[0][1],
-			    &((struct sockaddr_in *)&result_addr)->sin_addr);
+			    &((struct zsock_sockaddr_in *)&result_addr)->sin_addr);
 	return 0;
 }
 #endif
@@ -1376,7 +1376,7 @@ error:
  * generic socket creation function
  * which can be called in bind() or connect()
  */
-static int create_socket(struct modem_socket *sock, const struct sockaddr *addr)
+static int create_socket(struct modem_socket *sock, const struct zsock_sockaddr *addr)
 {
 	int ret;
 	static const struct modem_cmd cmd =
@@ -1385,14 +1385,14 @@ static int create_socket(struct modem_socket *sock, const struct sockaddr *addr)
 	uint16_t local_port = 0U, proto = 6U;

 	if (addr) {
-		if (addr->sa_family == AF_INET6) {
-			local_port = ntohs(net_sin6(addr)->sin6_port);
-		} else if (addr->sa_family == AF_INET) {
-			local_port = ntohs(net_sin(addr)->sin_port);
+		if (addr->sa_family == ZSOCK_AF_INET6) {
+			local_port = zsock_ntohs(net_sin6(addr)->sin6_port);
+		} else if (addr->sa_family == ZSOCK_AF_INET) {
+			local_port = zsock_ntohs(net_sin(addr)->sin_port);
 		}
 	}

-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		proto = 17U;
 	}

@@ -1492,7 +1492,7 @@ static int offload_close(void *obj)
 		return 0;
 	}

-	if (sock->is_connected || sock->ip_proto == IPPROTO_UDP) {
+	if (sock->is_connected || sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		snprintk(buf, sizeof(buf), "AT+USOCL=%d", sock->id);

 		ret = modem_cmd_send(&mctx.iface, &mctx.cmd_handler,
@@ -1507,8 +1507,8 @@ static int offload_close(void *obj)
 	return 0;
 }

-static int offload_bind(void *obj, const struct sockaddr *addr,
-			socklen_t addrlen)
+static int offload_bind(void *obj, const struct zsock_sockaddr *addr,
+			zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;

@@ -1525,8 +1525,8 @@ static int offload_bind(void *obj, const struct sockaddr *addr,
 	return 0;
 }

-static int offload_connect(void *obj, const struct sockaddr *addr,
-			   socklen_t addrlen)
+static int offload_connect(void *obj, const struct zsock_sockaddr *addr,
+			   zsock_socklen_t addrlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	int ret;
@@ -1556,17 +1556,17 @@ static int offload_connect(void *obj, const struct sockaddr *addr,
 	}

 	memcpy(&sock->dst, addr, sizeof(*addr));
-	if (addr->sa_family == AF_INET6) {
-		dst_port = ntohs(net_sin6(addr)->sin6_port);
-	} else if (addr->sa_family == AF_INET) {
-		dst_port = ntohs(net_sin(addr)->sin_port);
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		dst_port = zsock_ntohs(net_sin6(addr)->sin6_port);
+	} else if (addr->sa_family == ZSOCK_AF_INET) {
+		dst_port = zsock_ntohs(net_sin(addr)->sin_port);
 	} else {
 		errno = EAFNOSUPPORT;
 		return -1;
 	}

 	/* skip socket connect if UDP */
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		errno = 0;
 		return 0;
 	}
@@ -1595,8 +1595,8 @@ static int offload_connect(void *obj, const struct sockaddr *addr,
 }

 static ssize_t offload_recvfrom(void *obj, void *buf, size_t len,
-				int flags, struct sockaddr *from,
-				socklen_t *fromlen)
+				int flags, struct zsock_sockaddr *from,
+				zsock_socklen_t *fromlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;
 	int ret, next_packet_size;
@@ -1625,7 +1625,7 @@ static ssize_t offload_recvfrom(void *obj, void *buf, size_t len,
 			return -1;
 		}

-		if (!sock->is_connected && sock->ip_proto != IPPROTO_UDP) {
+		if (!sock->is_connected && sock->ip_proto != ZSOCK_IPPROTO_UDP) {
 			errno = 0;
 			return 0;
 		}
@@ -1644,7 +1644,7 @@ static ssize_t offload_recvfrom(void *obj, void *buf, size_t len,
 	}

 	snprintk(sendbuf, sizeof(sendbuf), "AT+USO%s=%d,%zd",
-		 sock->ip_proto == IPPROTO_UDP ? "RF" : "RD", sock->id,
+		 sock->ip_proto == ZSOCK_IPPROTO_UDP ? "RF" : "RD", sock->id,
 		 len < next_packet_size ? len : next_packet_size);

 	/* socket read settings */
@@ -1680,16 +1680,16 @@ exit:
 }

 static ssize_t offload_sendto(void *obj, const void *buf, size_t len,
-			      int flags, const struct sockaddr *to,
-			      socklen_t tolen)
+			      int flags, const struct zsock_sockaddr *to,
+			      zsock_socklen_t tolen)
 {
-	struct iovec msg_iov = {
+	struct zsock_iovec msg_iov = {
 		.iov_base = (void *)buf,
 		.iov_len = len,
 	};
-	struct msghdr msg = {
+	struct zsock_msghdr msg = {
 		.msg_iovlen = 1,
-		.msg_name = (struct sockaddr *)to,
+		.msg_name = (struct zsock_sockaddr *)to,
 		.msg_namelen = tolen,
 		.msg_iov = &msg_iov,
 	};
@@ -1747,12 +1747,12 @@ static ssize_t offload_write(void *obj, const void *buffer, size_t count)
 	return offload_sendto(obj, buffer, count, 0, NULL, 0);
 }

-static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
+static ssize_t offload_sendmsg(void *obj, const struct zsock_msghdr *msg, int flags)
 {
 	ssize_t sent = 0;
 	int bkp_iovec_idx;
-	struct iovec bkp_iovec = {0};
-	struct msghdr crafted_msg = {
+	struct zsock_iovec bkp_iovec = {0};
+	struct zsock_msghdr crafted_msg = {
 		.msg_name = msg->msg_name,
 		.msg_namelen = msg->msg_namelen,
 	};
@@ -1829,7 +1829,7 @@ static ssize_t offload_sendmsg(void *obj, const struct msghdr *msg, int flags)
 }

 #if defined(CONFIG_NET_SOCKETS_SOCKOPT_TLS)
-static int map_credentials(struct modem_socket *sock, const void *optval, socklen_t optlen)
+static int map_credentials(struct modem_socket *sock, const void *optval, zsock_socklen_t optlen)
 {
 	sec_tag_t *sec_tags = (sec_tag_t *)optval;
 	int ret = 0;
@@ -1875,14 +1875,14 @@ static int map_credentials(struct modem_socket *sock, const void *optval, sockle
 	return 0;
 }
 #else
-static int map_credentials(struct modem_socket *sock, const void *optval, socklen_t optlen)
+static int map_credentials(struct modem_socket *sock, const void *optval, zsock_socklen_t optlen)
 {
 	return -EINVAL;
 }
 #endif

 static int offload_setsockopt(void *obj, int level, int optname,
-			      const void *optval, socklen_t optlen)
+			      const void *optval, zsock_socklen_t optlen)
 {
 	struct modem_socket *sock = (struct modem_socket *)obj;

@@ -1934,18 +1934,18 @@ static const struct socket_op_vtable offload_socket_fd_op_vtable = {

 static bool offload_is_supported(int family, int type, int proto)
 {
-	if (family != AF_INET &&
-	    family != AF_INET6) {
+	if (family != ZSOCK_AF_INET &&
+	    family != ZSOCK_AF_INET6) {
 		return false;
 	}

-	if (type != SOCK_DGRAM &&
-	    type != SOCK_STREAM) {
+	if (type != ZSOCK_SOCK_DGRAM &&
+	    type != ZSOCK_SOCK_STREAM) {
 		return false;
 	}

-	if (proto != IPPROTO_TCP &&
-	    proto != IPPROTO_UDP &&
+	if (proto != ZSOCK_IPPROTO_TCP &&
+	    proto != ZSOCK_IPPROTO_UDP &&
 	    proto != IPPROTO_TLS_1_2) {
 		return false;
 	}
@@ -1954,7 +1954,7 @@ static bool offload_is_supported(int family, int type, int proto)
 }

 NET_SOCKET_OFFLOAD_REGISTER(ublox_sara_r4, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY,
-			    AF_UNSPEC, offload_is_supported, offload_socket);
+			    ZSOCK_AF_UNSPEC, offload_is_supported, offload_socket);

 #if defined(CONFIG_DNS_RESOLVER)
 /* TODO: This is a bare-bones implementation of DNS handling
@@ -1976,8 +1976,8 @@ static int offload_getaddrinfo(const char *node, const char *service,
 	(void)memset(&result, 0, sizeof(result));
 	(void)memset(&result_addr, 0, sizeof(result_addr));
 	/* FIXME: Hard-code DNS to return only IPv4 */
-	result.ai_family = AF_INET;
-	result_addr.sa_family = AF_INET;
+	result.ai_family = ZSOCK_AF_INET;
+	result_addr.sa_family = ZSOCK_AF_INET;
 	result.ai_addr = &result_addr;
 	result.ai_addrlen = sizeof(result_addr);
 	result.ai_canonname = result_canonname;
@@ -1992,21 +1992,21 @@ static int offload_getaddrinfo(const char *node, const char *service,

 	if (port > 0U) {
 		/* FIXME: DNS is hard-coded to return only IPv4 */
-		if (result.ai_family == AF_INET) {
-			net_sin(&result_addr)->sin_port = htons(port);
+		if (result.ai_family == ZSOCK_AF_INET) {
+			net_sin(&result_addr)->sin_port = zsock_htons(port);
 		}
 	}

 	/* check to see if node is an IP address */
 	if (net_addr_pton(result.ai_family, node,
-			  &((struct sockaddr_in *)&result_addr)->sin_addr)
+			  &((struct zsock_sockaddr_in *)&result_addr)->sin_addr)
 	    == 0) {
 		*res = &result;
 		return 0;
 	}

 	/* user flagged node as numeric host, but we failed net_addr_pton */
-	if (hints && hints->ai_flags & AI_NUMERICHOST) {
+	if (hints && hints->ai_flags & ZSOCK_AI_NUMERICHOST) {
 		return DNS_EAI_NONAME;
 	}

@@ -2039,7 +2039,7 @@ static const struct socket_dns_offload offload_dns_ops = {
 };
 #endif

-static int net_offload_dummy_get(sa_family_t family,
+static int net_offload_dummy_get(zsock_sa_family_t family,
 				 enum net_sock_type type,
 				 enum net_ip_protocol ip_proto,
 				 struct net_context **context)
diff --git a/drivers/modem/wncm14a2a.c b/drivers/modem/wncm14a2a.c
index 12b9b4b341a..e69b9b1a90f 100644
--- a/drivers/modem/wncm14a2a.c
+++ b/drivers/modem/wncm14a2a.c
@@ -105,11 +105,11 @@ static struct k_work_q wncm14a2a_workq;

 struct wncm14a2a_socket {
 	struct net_context *context;
-	sa_family_t family;
+	zsock_sa_family_t family;
 	enum net_sock_type type;
 	enum net_ip_protocol ip_proto;
-	struct sockaddr src;
-	struct sockaddr dst;
+	struct zsock_sockaddr src;
+	struct zsock_sockaddr dst;

 	int socket_id;

@@ -277,23 +277,23 @@ static void socket_put(struct wncm14a2a_socket *sock)

 	sock->context = NULL;
 	sock->socket_id = 0;
-	(void)memset(&sock->src, 0, sizeof(struct sockaddr));
-	(void)memset(&sock->dst, 0, sizeof(struct sockaddr));
+	(void)memset(&sock->src, 0, sizeof(struct zsock_sockaddr));
+	(void)memset(&sock->dst, 0, sizeof(struct zsock_sockaddr));
 }

-char *wncm14a2a_sprint_ip_addr(const struct sockaddr *addr)
+char *wncm14a2a_sprint_ip_addr(const struct zsock_sockaddr *addr)
 {
 	static char buf[NET_IPV6_ADDR_LEN];

 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
-		return net_addr_ntop(AF_INET6, &net_sin6(addr)->sin6_addr,
+	if (addr->sa_family == ZSOCK_AF_INET6) {
+		return net_addr_ntop(ZSOCK_AF_INET6, &net_sin6(addr)->sin6_addr,
 				     buf, sizeof(buf));
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-	if (addr->sa_family == AF_INET) {
-		return net_addr_ntop(AF_INET, &net_sin(addr)->sin_addr,
+	if (addr->sa_family == ZSOCK_AF_INET) {
+		return net_addr_ntop(ZSOCK_AF_INET, &net_sin(addr)->sin_addr,
 				     buf, sizeof(buf));
 	} else
 #endif
@@ -434,29 +434,29 @@ static int pkt_setup_ip_data(struct net_pkt *pkt,
 	uint16_t src_port = 0U, dst_port = 0U;

 #if defined(CONFIG_NET_IPV6)
-	if (net_pkt_family(pkt) == AF_INET6) {
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
 		if (net_ipv6_create(
 			    pkt,
-			    &((struct sockaddr_in6 *)&sock->dst)->sin6_addr,
-			    &((struct sockaddr_in6 *)&sock->src)->sin6_addr)) {
+			    &((struct zsock_sockaddr_in6 *)&sock->dst)->sin6_addr,
+			    &((struct zsock_sockaddr_in6 *)&sock->src)->sin6_addr)) {
 			return -1;
 		}
-		src_port = ntohs(net_sin6(&sock->src)->sin6_port);
-		dst_port = ntohs(net_sin6(&sock->dst)->sin6_port);
+		src_port = zsock_ntohs(net_sin6(&sock->src)->sin6_port);
+		dst_port = zsock_ntohs(net_sin6(&sock->dst)->sin6_port);

 		hdr_len = sizeof(struct net_ipv6_hdr);
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-	if (net_pkt_family(pkt) == AF_INET) {
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET) {
 		if (net_ipv4_create(
 			    pkt,
-			    &((struct sockaddr_in *)&sock->dst)->sin_addr,
-			    &((struct sockaddr_in *)&sock->src)->sin_addr)) {
+			    &((struct zsock_sockaddr_in *)&sock->dst)->sin_addr,
+			    &((struct zsock_sockaddr_in *)&sock->src)->sin_addr)) {
 			return -1;
 		}
-		src_port = ntohs(net_sin(&sock->src)->sin_port);
-		dst_port = ntohs(net_sin(&sock->dst)->sin_port);
+		src_port = zsock_ntohs(net_sin(&sock->src)->sin_port);
+		dst_port = zsock_ntohs(net_sin(&sock->dst)->sin_port);

 		hdr_len = sizeof(struct net_ipv4_hdr);
 	} else
@@ -466,7 +466,7 @@ static int pkt_setup_ip_data(struct net_pkt *pkt,
 	}

 #if defined(CONFIG_NET_UDP)
-	if (sock->ip_proto == IPPROTO_UDP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_UDP) {
 		if (net_udp_create(pkt, dst_port, src_port)) {
 			return -1;
 		}
@@ -475,7 +475,7 @@ static int pkt_setup_ip_data(struct net_pkt *pkt,
 	} else
 #endif
 #if defined(CONFIG_NET_TCP)
-	if (sock->ip_proto == IPPROTO_TCP) {
+	if (sock->ip_proto == ZSOCK_IPPROTO_TCP) {
 		NET_PKT_DATA_ACCESS_DEFINE(tcp_access, struct net_tcp_hdr);
 		struct net_tcp_hdr *tcp;

@@ -1448,7 +1448,7 @@ error:

 /*** OFFLOAD FUNCTIONS ***/

-static int offload_get(sa_family_t family,
+static int offload_get(zsock_sa_family_t family,
 		       enum net_sock_type type,
 		       enum net_ip_protocol ip_proto,
 		       struct net_context **context)
@@ -1471,7 +1471,7 @@ static int offload_get(sa_family_t family,
 	sock->socket_id = MDM_MAX_SOCKETS + 1; /* socket # needs assigning */

 	snprintk(buf, sizeof(buf), "AT@SOCKCREAT=%d,%d", type,
-		 family == AF_INET ? 0 : 1);
+		 family == ZSOCK_AF_INET ? 0 : 1);
 	ret = send_at_cmd(NULL, buf, MDM_CMD_TIMEOUT);
 	if (ret < 0) {
 		LOG_ERR("AT@SOCKCREAT ret:%d", ret);
@@ -1482,8 +1482,8 @@ static int offload_get(sa_family_t family,
 }

 static int offload_bind(struct net_context *context,
-			const struct sockaddr *addr,
-			socklen_t addrlen)
+			const struct zsock_sockaddr *addr,
+			zsock_socklen_t addrlen)
 {
 	struct wncm14a2a_socket *sock = NULL;

@@ -1500,14 +1500,14 @@ static int offload_bind(struct net_context *context,
 	/* save bind address information */
 	sock->src.sa_family = addr->sa_family;
 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
+	if (addr->sa_family == ZSOCK_AF_INET6) {
 		net_ipaddr_copy(&net_sin6(&sock->src)->sin6_addr,
 				&net_sin6(addr)->sin6_addr);
 		net_sin6(&sock->src)->sin6_port = net_sin6(addr)->sin6_port;
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-	if (addr->sa_family == AF_INET) {
+	if (addr->sa_family == ZSOCK_AF_INET) {
 		net_ipaddr_copy(&net_sin(&sock->src)->sin_addr,
 				&net_sin(addr)->sin_addr);
 		net_sin(&sock->src)->sin_port = net_sin(addr)->sin_port;
@@ -1527,8 +1527,8 @@ static int offload_listen(struct net_context *context, int backlog)
 }

 static int offload_connect(struct net_context *context,
-			   const struct sockaddr *addr,
-			   socklen_t addrlen,
+			   const struct zsock_sockaddr *addr,
+			   zsock_socklen_t addrlen,
 			   net_context_connect_cb_t cb,
 			   int32_t timeout,
 			   void *user_data)
@@ -1561,18 +1561,18 @@ static int offload_connect(struct net_context *context,
 	sock->dst.sa_family = addr->sa_family;

 #if defined(CONFIG_NET_IPV6)
-	if (addr->sa_family == AF_INET6) {
+	if (addr->sa_family == ZSOCK_AF_INET6) {
 		net_ipaddr_copy(&net_sin6(&sock->dst)->sin6_addr,
 				&net_sin6(addr)->sin6_addr);
-		dst_port = ntohs(net_sin6(addr)->sin6_port);
+		dst_port = zsock_ntohs(net_sin6(addr)->sin6_port);
 		net_sin6(&sock->dst)->sin6_port = dst_port;
 	} else
 #endif
 #if defined(CONFIG_NET_IPV4)
-	if (addr->sa_family == AF_INET) {
+	if (addr->sa_family == ZSOCK_AF_INET) {
 		net_ipaddr_copy(&net_sin(&sock->dst)->sin_addr,
 				&net_sin(addr)->sin_addr);
-		dst_port = ntohs(net_sin(addr)->sin_port);
+		dst_port = zsock_ntohs(net_sin(addr)->sin_port);
 		net_sin(&sock->dst)->sin_port = dst_port;
 	} else
 #endif
@@ -1618,8 +1618,8 @@ static int offload_accept(struct net_context *context,
 }

 static int offload_sendto(struct net_pkt *pkt,
-			  const struct sockaddr *dst_addr,
-			  socklen_t addrlen,
+			  const struct zsock_sockaddr *dst_addr,
+			  zsock_socklen_t addrlen,
 			  net_context_send_cb_t cb,
 			  int32_t timeout,
 			  void *user_data)
@@ -1658,16 +1658,16 @@ static int offload_send(struct net_pkt *pkt,
 			void *user_data)
 {
 	struct net_context *context = net_pkt_context(pkt);
-	socklen_t addrlen;
+	zsock_socklen_t addrlen;

 #if defined(CONFIG_NET_IPV6)
-	if (net_pkt_family(pkt) == AF_INET6) {
-		addrlen = sizeof(struct sockaddr_in6);
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
+		addrlen = sizeof(struct zsock_sockaddr_in6);
 	} else
 #endif /* CONFIG_NET_IPV6 */
 #if defined(CONFIG_NET_IPV4)
-	if (net_pkt_family(pkt) == AF_INET) {
-		addrlen = sizeof(struct sockaddr_in);
+	if (net_pkt_family(pkt) == ZSOCK_AF_INET) {
+		addrlen = sizeof(struct zsock_sockaddr_in);
 	} else
 #endif /* CONFIG_NET_IPV4 */
 	{
@@ -1729,7 +1729,7 @@ static int offload_put(struct net_context *context)

 	socket_put(sock);
 	net_context_unref(context);
-	if (sock->type == SOCK_STREAM) {
+	if (sock->type == ZSOCK_SOCK_STREAM) {
 		/* TCP contexts are referenced twice,
 		 *  once for the app and once for the stack.
 		 *  Since TCP stack is not used for offload,
diff --git a/drivers/net/canbus.c b/drivers/net/canbus.c
index 41cbb586852..a3c894f238e 100644
--- a/drivers/net/canbus.c
+++ b/drivers/net/canbus.c
@@ -57,7 +57,7 @@ static void net_canbus_recv(const struct device *dev, struct can_frame *frame, v
 }

 static int net_canbus_setsockopt(const struct device *dev, void *obj, int level,
-				 int optname, const void *optval, socklen_t optlen)
+				 int optname, const void *optval, zsock_socklen_t optlen)
 {
 	const struct net_canbus_config *cfg = dev->config;
 	struct net_canbus_context *context = dev->data;
diff --git a/drivers/net/loopback.c b/drivers/net/loopback.c
index 465cd1e1f88..5c452876e6d 100644
--- a/drivers/net/loopback.c
+++ b/drivers/net/loopback.c
@@ -51,8 +51,8 @@ static void loopback_init(struct net_if *iface)
 			     NET_LINK_DUMMY);

 	if (IS_ENABLED(CONFIG_NET_IPV4)) {
-		struct in_addr ipv4_loopback = INADDR_LOOPBACK_INIT;
-		struct in_addr netmask = { { { 255, 0, 0, 0 } } };
+		struct zsock_in_addr ipv4_loopback = INADDR_LOOPBACK_INIT;
+		struct zsock_in_addr netmask = { { { 255, 0, 0, 0 } } };

 		ifaddr = net_if_ipv4_addr_add(iface, &ipv4_loopback,
 					      NET_ADDR_AUTOCONF, 0);
@@ -64,7 +64,7 @@ static void loopback_init(struct net_if *iface)
 	}

 	if (IS_ENABLED(CONFIG_NET_IPV6)) {
-		struct in6_addr ipv6_loopback = IN6ADDR_LOOPBACK_INIT;
+		struct zsock_in6_addr ipv6_loopback = ZSOCK_IN6ADDR_LOOPBACK_INIT;

 		ifaddr = net_if_ipv6_addr_add(iface, &ipv6_loopback,
 					      NET_ADDR_AUTOCONF, 0);
@@ -147,7 +147,7 @@ static int loopback_send(const struct device *dev, struct net_pkt *pkt)
 	 * the test to control this remotely.
 	 */
 	if (!COND_CODE_1(CONFIG_NET_TEST, (loopback_dont_swap_addresses), (false))) {
-		if (net_pkt_family(pkt) == AF_INET6) {
+		if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
 			net_ipv6_addr_copy_raw(NET_IPV6_HDR(cloned)->src,
 					       NET_IPV6_HDR(pkt)->dst);
 			net_ipv6_addr_copy_raw(NET_IPV6_HDR(cloned)->dst,
diff --git a/drivers/net/nsos_sockets.c b/drivers/net/nsos_sockets.c
index b5b90d16594..c68ebf893e4 100644
--- a/drivers/net/nsos_sockets.c
+++ b/drivers/net/nsos_sockets.c
@@ -68,16 +68,16 @@ NET_IF_DECLARE(nsos_socket, 0);
 static int socket_family_to_nsos_mid(int family, int *family_mid)
 {
 	switch (family) {
-	case AF_UNSPEC:
+	case ZSOCK_AF_UNSPEC:
 		*family_mid = NSOS_MID_AF_UNSPEC;
 		break;
-	case AF_INET:
+	case ZSOCK_AF_INET:
 		*family_mid = NSOS_MID_AF_INET;
 		break;
-	case AF_INET6:
+	case ZSOCK_AF_INET6:
 		*family_mid = NSOS_MID_AF_INET6;
 		break;
-	case AF_UNIX:
+	case ZSOCK_AF_UNIX:
 		*family_mid = NSOS_MID_AF_UNIX;
 		break;
 	case AF_PACKET:
@@ -93,10 +93,10 @@ static int socket_family_to_nsos_mid(int family, int *family_mid)
 static int socket_proto_to_nsos_mid(int proto, int *proto_mid)
 {
 	switch (proto) {
-	case IPPROTO_IP:
+	case ZSOCK_IPPROTO_IP:
 		*proto_mid = NSOS_MID_IPPROTO_IP;
 		break;
-	case IPPROTO_ICMP:
+	case ZSOCK_IPPROTO_ICMP:
 		*proto_mid = NSOS_MID_IPPROTO_ICMP;
 		break;
 	case IPPROTO_IGMP:
@@ -105,19 +105,19 @@ static int socket_proto_to_nsos_mid(int proto, int *proto_mid)
 	case IPPROTO_IPIP:
 		*proto_mid = NSOS_MID_IPPROTO_IPIP;
 		break;
-	case IPPROTO_TCP:
+	case ZSOCK_IPPROTO_TCP:
 		*proto_mid = NSOS_MID_IPPROTO_TCP;
 		break;
-	case IPPROTO_UDP:
+	case ZSOCK_IPPROTO_UDP:
 		*proto_mid = NSOS_MID_IPPROTO_UDP;
 		break;
-	case IPPROTO_IPV6:
+	case ZSOCK_IPPROTO_IPV6:
 		*proto_mid = NSOS_MID_IPPROTO_IPV6;
 		break;
-	case IPPROTO_RAW:
+	case ZSOCK_IPPROTO_RAW:
 		*proto_mid = NSOS_MID_IPPROTO_RAW;
 		break;
-	case htons(IPPROTO_ETH_P_ALL):
+	case zsock_htons(ZSOCK_IPPROTO_ETH_P_ALL):
 		*proto_mid = NSOS_MID_IPPROTO_ETH_P_ALL;
 		break;
 	default:
@@ -130,13 +130,13 @@ static int socket_proto_to_nsos_mid(int proto, int *proto_mid)
 static int socket_type_to_nsos_mid(int type, int *type_mid)
 {
 	switch (type) {
-	case SOCK_STREAM:
+	case ZSOCK_SOCK_STREAM:
 		*type_mid = NSOS_MID_SOCK_STREAM;
 		break;
-	case SOCK_DGRAM:
+	case ZSOCK_SOCK_DGRAM:
 		*type_mid = NSOS_MID_SOCK_DGRAM;
 		break;
-	case SOCK_RAW:
+	case ZSOCK_SOCK_RAW:
 		*type_mid = NSOS_MID_SOCK_RAW;
 		break;
 	default:
@@ -421,7 +421,7 @@ static int nsos_ioctl(void *obj, unsigned int request, va_list args)
 	return -EINVAL;
 }

-static int sockaddr_to_nsos_mid(const struct sockaddr *addr, socklen_t addrlen,
+static int sockaddr_to_nsos_mid(const struct zsock_sockaddr *addr, zsock_socklen_t addrlen,
 				struct nsos_mid_sockaddr **addr_mid, size_t *addrlen_mid)
 {
 	if (!addr || !addrlen) {
@@ -432,9 +432,9 @@ static int sockaddr_to_nsos_mid(const struct sockaddr *addr, socklen_t addrlen,
 	}

 	switch (addr->sa_family) {
-	case AF_INET: {
-		const struct sockaddr_in *addr_in =
-			(const struct sockaddr_in *)addr;
+	case ZSOCK_AF_INET: {
+		const struct zsock_sockaddr_in *addr_in =
+			(const struct zsock_sockaddr_in *)addr;
 		struct nsos_mid_sockaddr_in *addr_in_mid =
 			(struct nsos_mid_sockaddr_in *)*addr_mid;

@@ -450,9 +450,9 @@ static int sockaddr_to_nsos_mid(const struct sockaddr *addr, socklen_t addrlen,

 		return 0;
 	}
-	case AF_INET6: {
-		const struct sockaddr_in6 *addr_in =
-			(const struct sockaddr_in6 *)addr;
+	case ZSOCK_AF_INET6: {
+		const struct zsock_sockaddr_in6 *addr_in =
+			(const struct zsock_sockaddr_in6 *)addr;
 		struct nsos_mid_sockaddr_in6 *addr_in_mid =
 			(struct nsos_mid_sockaddr_in6 *)*addr_mid;

@@ -470,9 +470,9 @@ static int sockaddr_to_nsos_mid(const struct sockaddr *addr, socklen_t addrlen,

 		return 0;
 	}
-	case AF_UNIX: {
-		const struct sockaddr_un *addr_un =
-			(const struct sockaddr_un *)addr;
+	case ZSOCK_AF_UNIX: {
+		const struct zsock_sockaddr_un *addr_un =
+			(const struct zsock_sockaddr_un *)addr;
 		struct nsos_mid_sockaddr_un *addr_un_mid =
 			(struct nsos_mid_sockaddr_un *)*addr_mid;

@@ -516,7 +516,7 @@ static int sockaddr_to_nsos_mid(const struct sockaddr *addr, socklen_t addrlen,
 	return -NSI_ERRNO_MID_EINVAL;
 }

-static int sockaddr_from_nsos_mid(struct sockaddr *addr, socklen_t *addrlen,
+static int sockaddr_from_nsos_mid(struct zsock_sockaddr *addr, zsock_socklen_t *addrlen,
 				  const struct nsos_mid_sockaddr *addr_mid, size_t addrlen_mid)
 {
 	if (!addr || !addrlen) {
@@ -527,9 +527,9 @@ static int sockaddr_from_nsos_mid(struct sockaddr *addr, socklen_t *addrlen,
 	case NSOS_MID_AF_INET: {
 		const struct nsos_mid_sockaddr_in *addr_in_mid =
 			(const struct nsos_mid_sockaddr_in *)addr_mid;
-		struct sockaddr_in addr_in;
+		struct zsock_sockaddr_in addr_in;

-		addr_in.sin_family = AF_INET;
+		addr_in.sin_family = ZSOCK_AF_INET;
 		addr_in.sin_port = addr_in_mid->sin_port;
 		addr_in.sin_addr.s_addr = addr_in_mid->sin_addr;

@@ -541,9 +541,9 @@ static int sockaddr_from_nsos_mid(struct sockaddr *addr, socklen_t *addrlen,
 	case NSOS_MID_AF_INET6: {
 		const struct nsos_mid_sockaddr_in6 *addr_in_mid =
 			(const struct nsos_mid_sockaddr_in6 *)addr_mid;
-		struct sockaddr_in6 addr_in;
+		struct zsock_sockaddr_in6 addr_in;

-		addr_in.sin6_family = AF_INET6;
+		addr_in.sin6_family = ZSOCK_AF_INET6;
 		addr_in.sin6_port = addr_in_mid->sin6_port;
 		memcpy(addr_in.sin6_addr.s6_addr, addr_in_mid->sin6_addr,
 		       sizeof(addr_in.sin6_addr.s6_addr));
@@ -629,7 +629,7 @@ static int nsos_poll_if_blocking(struct nsos_socket *sock, int events,
 	return 0;
 }

-static int nsos_bind(void *obj, const struct sockaddr *addr, socklen_t addrlen)
+static int nsos_bind(void *obj, const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct nsos_socket *sock = obj;
 	struct nsos_mid_sockaddr_storage addr_storage_mid;
@@ -694,7 +694,7 @@ clear_nonblock:
 	return ret;
 }

-static int nsos_connect(void *obj, const struct sockaddr *addr, socklen_t addrlen)
+static int nsos_connect(void *obj, const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct net_if *iface = NET_IF_GET(nsos_socket, 0);
 	struct nsos_socket *sock = obj;
@@ -741,7 +741,7 @@ static int nsos_listen(void *obj, int backlog)
 	return ret;
 }

-static int nsos_accept(void *obj, struct sockaddr *addr, socklen_t *addrlen)
+static int nsos_accept(void *obj, struct zsock_sockaddr *addr, zsock_socklen_t *addrlen)
 {
 	struct net_if *iface = NET_IF_GET(nsos_socket, 0);
 	struct nsos_socket *accept_sock = obj;
@@ -804,7 +804,7 @@ return_ret:
 }

 static ssize_t nsos_sendto(void *obj, const void *buf, size_t len, int flags,
-			   const struct sockaddr *addr, socklen_t addrlen)
+			   const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct net_if *iface = NET_IF_GET(nsos_socket, 0);
 	struct nsos_socket *sock = obj;
@@ -844,7 +844,7 @@ return_ret:
 	return ret;
 }

-static ssize_t nsos_sendmsg(void *obj, const struct msghdr *msg, int flags)
+static ssize_t nsos_sendmsg(void *obj, const struct zsock_msghdr *msg, int flags)
 {
 	struct net_if *iface = NET_IF_GET(nsos_socket, 0);
 	struct nsos_socket *sock = obj;
@@ -908,7 +908,7 @@ return_ret:
 }

 static ssize_t nsos_recvfrom(void *obj, void *buf, size_t len, int flags,
-			     struct sockaddr *addr, socklen_t *addrlen)
+			     struct zsock_sockaddr *addr, zsock_socklen_t *addrlen)
 {
 	struct net_if *iface = NET_IF_GET(nsos_socket, 0);
 	struct nsos_socket *sock = obj;
@@ -948,7 +948,7 @@ return_ret:
 	return ret;
 }

-static ssize_t nsos_recvmsg(void *obj, struct msghdr *msg, int flags)
+static ssize_t nsos_recvmsg(void *obj, struct zsock_msghdr *msg, int flags)
 {
 	errno = ENOTSUP;
 	return -1;
@@ -958,13 +958,13 @@ static int socket_type_from_nsos_mid(int type_mid, int *type)
 {
 	switch (type_mid) {
 	case NSOS_MID_SOCK_STREAM:
-		*type = SOCK_STREAM;
+		*type = ZSOCK_SOCK_STREAM;
 		break;
 	case NSOS_MID_SOCK_DGRAM:
-		*type = SOCK_DGRAM;
+		*type = ZSOCK_SOCK_DGRAM;
 		break;
 	case NSOS_MID_SOCK_RAW:
-		*type = SOCK_RAW;
+		*type = ZSOCK_SOCK_RAW;
 		break;
 	default:
 		return -NSI_ERRNO_MID_ESOCKTNOSUPPORT;
@@ -977,10 +977,10 @@ static int socket_proto_from_nsos_mid(int proto_mid, int *proto)
 {
 	switch (proto_mid) {
 	case NSOS_MID_IPPROTO_IP:
-		*proto = IPPROTO_IP;
+		*proto = ZSOCK_IPPROTO_IP;
 		break;
 	case NSOS_MID_IPPROTO_ICMP:
-		*proto = IPPROTO_ICMP;
+		*proto = ZSOCK_IPPROTO_ICMP;
 		break;
 	case NSOS_MID_IPPROTO_IGMP:
 		*proto = IPPROTO_IGMP;
@@ -989,19 +989,19 @@ static int socket_proto_from_nsos_mid(int proto_mid, int *proto)
 		*proto = IPPROTO_IPIP;
 		break;
 	case NSOS_MID_IPPROTO_TCP:
-		*proto = IPPROTO_TCP;
+		*proto = ZSOCK_IPPROTO_TCP;
 		break;
 	case NSOS_MID_IPPROTO_UDP:
-		*proto = IPPROTO_UDP;
+		*proto = ZSOCK_IPPROTO_UDP;
 		break;
 	case NSOS_MID_IPPROTO_IPV6:
-		*proto = IPPROTO_IPV6;
+		*proto = ZSOCK_IPPROTO_IPV6;
 		break;
 	case NSOS_MID_IPPROTO_RAW:
-		*proto = IPPROTO_RAW;
+		*proto = ZSOCK_IPPROTO_RAW;
 		break;
 	case NSOS_MID_IPPROTO_ETH_P_ALL:
-		*proto = htons(IPPROTO_ETH_P_ALL);
+		*proto = zsock_htons(ZSOCK_IPPROTO_ETH_P_ALL);
 		break;
 	default:
 		return -NSI_ERRNO_MID_EPROTONOSUPPORT;
@@ -1014,16 +1014,16 @@ static int socket_family_from_nsos_mid(int family_mid, int *family)
 {
 	switch (family_mid) {
 	case NSOS_MID_AF_UNSPEC:
-		*family = AF_UNSPEC;
+		*family = ZSOCK_AF_UNSPEC;
 		break;
 	case NSOS_MID_AF_INET:
-		*family = AF_INET;
+		*family = ZSOCK_AF_INET;
 		break;
 	case NSOS_MID_AF_INET6:
-		*family = AF_INET6;
+		*family = ZSOCK_AF_INET6;
 		break;
 	case NSOS_MID_AF_UNIX:
-		*family = AF_UNIX;
+		*family = ZSOCK_AF_UNIX;
 		break;
 	case NSOS_MID_AF_PACKET:
 		*family = AF_PACKET;
@@ -1036,7 +1036,7 @@ static int socket_family_from_nsos_mid(int family_mid, int *family)
 }

 static int nsos_getsockopt_int(struct nsos_socket *sock, int nsos_mid_level, int nsos_mid_optname,
-			       void *optval, socklen_t *optlen)
+			       void *optval, zsock_socklen_t *optlen)
 {
 	size_t nsos_mid_optlen = sizeof(int);
 	int err;
@@ -1059,14 +1059,14 @@ static int nsos_getsockopt_int(struct nsos_socket *sock, int nsos_mid_level, int
 }

 static int nsos_getsockopt(void *obj, int level, int optname,
-			   void *optval, socklen_t *optlen)
+			   void *optval, zsock_socklen_t *optlen)
 {
 	struct nsos_socket *sock = obj;

 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
-		case SO_ERROR: {
+		case ZSOCK_SO_ERROR: {
 			int nsos_mid_err;
 			int err;

@@ -1086,7 +1086,7 @@ static int nsos_getsockopt(void *obj, int level, int optname,

 			return 0;
 		}
-		case SO_TYPE: {
+		case ZSOCK_SO_TYPE: {
 			int nsos_mid_type;
 			int err;

@@ -1158,15 +1158,15 @@ static int nsos_getsockopt(void *obj, int level, int optname,

 			return 0;
 		}
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_RCVBUF,
 						   optval, optlen);
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_SNDBUF,
 						   optval, optlen);
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEADDR,
 						   optval, optlen);
@@ -1174,16 +1174,16 @@ static int nsos_getsockopt(void *obj, int level, int optname,
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEPORT,
 						   optval, optlen);
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_KEEPALIVE,
 						   optval, optlen);
 		}
 		break;

-	case IPPROTO_TCP:
+	case ZSOCK_IPPROTO_TCP:
 		switch (optname) {
-		case TCP_NODELAY:
+		case ZSOCK_TCP_NODELAY:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_IPPROTO_TCP, NSOS_MID_TCP_NODELAY,
 						   optval, optlen);
@@ -1202,9 +1202,9 @@ static int nsos_getsockopt(void *obj, int level, int optname,
 		}
 		break;

-	case IPPROTO_IPV6:
+	case ZSOCK_IPPROTO_IPV6:
 		switch (optname) {
-		case IPV6_V6ONLY:
+		case ZSOCK_IPV6_V6ONLY:
 			return nsos_getsockopt_int(sock,
 						   NSOS_MID_IPPROTO_IPV6, NSOS_MID_IPV6_V6ONLY,
 						   optval, optlen);
@@ -1217,7 +1217,7 @@ static int nsos_getsockopt(void *obj, int level, int optname,
 }

 static int nsos_setsockopt_int(struct nsos_socket *sock, int nsos_mid_level, int nsos_mid_optname,
-			       const void *optval, socklen_t optlen)
+			       const void *optval, zsock_socklen_t optlen)
 {
 	int err;

@@ -1237,12 +1237,12 @@ static int nsos_setsockopt_int(struct nsos_socket *sock, int nsos_mid_level, int
 }

 static int nsos_setsockopt(void *obj, int level, int optname,
-			   const void *optval, socklen_t optlen)
+			   const void *optval, zsock_socklen_t optlen)
 {
 	struct nsos_socket *sock = obj;

 	switch (level) {
-	case SOL_SOCKET:
+	case ZSOCK_SOL_SOCKET:
 		switch (optname) {
 		case SO_PRIORITY: {
 			int nsos_mid_priority;
@@ -1265,7 +1265,7 @@ static int nsos_setsockopt(void *obj, int level, int optname,

 			return 0;
 		}
-		case SO_RCVTIMEO: {
+		case ZSOCK_SO_RCVTIMEO: {
 			const struct zsock_timeval *tv = optval;
 			struct nsos_mid_timeval nsos_mid_tv;
 			int err;
@@ -1294,7 +1294,7 @@ static int nsos_setsockopt(void *obj, int level, int optname,

 			return 0;
 		}
-		case SO_SNDTIMEO: {
+		case ZSOCK_SO_SNDTIMEO: {
 			const struct zsock_timeval *tv = optval;
 			struct nsos_mid_timeval nsos_mid_tv;
 			int err;
@@ -1323,15 +1323,15 @@ static int nsos_setsockopt(void *obj, int level, int optname,

 			return 0;
 		}
-		case SO_RCVBUF:
+		case ZSOCK_SO_RCVBUF:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_RCVBUF,
 						   optval, optlen);
-		case SO_SNDBUF:
+		case ZSOCK_SO_SNDBUF:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_SNDBUF,
 						   optval, optlen);
-		case SO_REUSEADDR:
+		case ZSOCK_SO_REUSEADDR:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEADDR,
 						   optval, optlen);
@@ -1339,20 +1339,20 @@ static int nsos_setsockopt(void *obj, int level, int optname,
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_REUSEPORT,
 						   optval, optlen);
-		case SO_LINGER:
+		case ZSOCK_SO_LINGER:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_LINGER,
 						   optval, optlen);
-		case SO_KEEPALIVE:
+		case ZSOCK_SO_KEEPALIVE:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_SOL_SOCKET, NSOS_MID_SO_KEEPALIVE,
 						   optval, optlen);
 		}
 		break;

-	case IPPROTO_TCP:
+	case ZSOCK_IPPROTO_TCP:
 		switch (optname) {
-		case TCP_NODELAY:
+		case ZSOCK_TCP_NODELAY:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_IPPROTO_TCP, NSOS_MID_TCP_NODELAY,
 						   optval, optlen);
@@ -1371,9 +1371,9 @@ static int nsos_setsockopt(void *obj, int level, int optname,
 		}
 		break;

-	case IPPROTO_IPV6:
+	case ZSOCK_IPPROTO_IPV6:
 		switch (optname) {
-		case IPV6_V6ONLY:
+		case ZSOCK_IPV6_V6ONLY:
 			return nsos_setsockopt_int(sock,
 						   NSOS_MID_IPPROTO_IPV6, NSOS_MID_IPV6_V6ONLY,
 						   optval, optlen);
@@ -1427,12 +1427,12 @@ static bool nsos_is_supported(int family, int type, int proto)
 	return true;
 }

-NET_SOCKET_OFFLOAD_REGISTER(nsos, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, AF_UNSPEC,
+NET_SOCKET_OFFLOAD_REGISTER(nsos, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, ZSOCK_AF_UNSPEC,
 			    nsos_is_supported, nsos_socket_create);

 struct zsock_addrinfo_wrap {
 	struct zsock_addrinfo addrinfo;
-	struct sockaddr_storage addr_storage;
+	struct zsock_sockaddr_storage addr_storage;
 	struct nsos_mid_addrinfo *addrinfo_mid;
 };

@@ -1483,7 +1483,7 @@ static int addrinfo_from_nsos_mid(struct nsos_mid_addrinfo *nsos_res,
 		wrap->addrinfo.ai_protocol = res_p->ai_protocol;

 		wrap->addrinfo.ai_addr =
-			(struct sockaddr *)&wrap->addr_storage;
+			(struct zsock_sockaddr *)&wrap->addr_storage;
 		wrap->addrinfo.ai_addrlen = sizeof(wrap->addr_storage);

 		sockaddr_from_nsos_mid(wrap->addrinfo.ai_addr, &wrap->addrinfo.ai_addrlen,
diff --git a/drivers/net/ppp.c b/drivers/net/ppp.c
index 98eff54a88e..35fbadd59ca 100644
--- a/drivers/net/ppp.c
+++ b/drivers/net/ppp.c
@@ -302,7 +302,7 @@ static int ppp_save_byte(struct ppp_driver_context *ppp, uint8_t byte)
 		ppp->pkt = net_pkt_rx_alloc_with_buffer(
 			ppp->iface,
 			CONFIG_NET_BUF_DATA_SIZE,
-			AF_UNSPEC, 0, K_NO_WAIT);
+			ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 		if (!ppp->pkt) {
 			LOG_ERR("[%p] cannot allocate pkt", ppp);
 			return -ENOMEM;
@@ -326,7 +326,7 @@ static int ppp_save_byte(struct ppp_driver_context *ppp, uint8_t byte)
 	if (ppp->available == 1) {
 		ret = net_pkt_alloc_buffer(ppp->pkt,
 					   CONFIG_NET_BUF_DATA_SIZE + ppp->available,
-					   AF_UNSPEC, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, K_NO_WAIT);
 		if (ret < 0) {
 			LOG_ERR("[%p] cannot allocate new data buffer", ppp);
 			goto out_of_mem;
@@ -827,10 +827,10 @@ static int ppp_send(const struct device *dev, struct net_pkt *pkt)
 	 * value here.
 	 */
 	if (!net_pkt_is_ppp(pkt)) {
-		if (net_pkt_family(pkt) == AF_INET) {
-			protocol = htons(PPP_IP);
-		} else if (net_pkt_family(pkt) == AF_INET6) {
-			protocol = htons(PPP_IPV6);
+		if (net_pkt_family(pkt) == ZSOCK_AF_INET) {
+			protocol = zsock_htons(PPP_IP);
+		} else if (net_pkt_family(pkt) == ZSOCK_AF_INET6) {
+			protocol = zsock_htons(PPP_IPV6);
 		}  else {
 			return -EPROTONOSUPPORT;
 		}
@@ -847,12 +847,12 @@ static int ppp_send(const struct device *dev, struct net_pkt *pkt)
 				  sizeof(sync_addr_ctrl), send_off);

 	if (protocol > 0) {
-		escaped = htons(ppp_escape_byte(protocol, &offset));
+		escaped = zsock_htons(ppp_escape_byte(protocol, &offset));
 		send_off = ppp_send_bytes(ppp, (uint8_t *)&escaped + offset,
 					  offset ? 1 : 2,
 					  send_off);

-		escaped = htons(ppp_escape_byte(protocol >> 8, &offset));
+		escaped = zsock_htons(ppp_escape_byte(protocol >> 8, &offset));
 		send_off = ppp_send_bytes(ppp, (uint8_t *)&escaped + offset,
 					  offset ? 1 : 2,
 					  send_off);
@@ -869,7 +869,7 @@ static int ppp_send(const struct device *dev, struct net_pkt *pkt)
 	while (buf) {
 		for (i = 0; i < buf->len; i++) {
 			/* Escape illegal bytes */
-			escaped = htons(ppp_escape_byte(buf->data[i], &offset));
+			escaped = zsock_htons(ppp_escape_byte(buf->data[i], &offset));
 			send_off = ppp_send_bytes(ppp,
 						  (uint8_t *)&escaped + offset,
 						  offset ? 1 : 2,
@@ -879,12 +879,12 @@ static int ppp_send(const struct device *dev, struct net_pkt *pkt)
 		buf = buf->frags;
 	}

-	escaped = htons(ppp_escape_byte(fcs, &offset));
+	escaped = zsock_htons(ppp_escape_byte(fcs, &offset));
 	send_off = ppp_send_bytes(ppp, (uint8_t *)&escaped + offset,
 				  offset ? 1 : 2,
 				  send_off);

-	escaped = htons(ppp_escape_byte(fcs >> 8, &offset));
+	escaped = zsock_htons(ppp_escape_byte(fcs >> 8, &offset));
 	send_off = ppp_send_bytes(ppp, (uint8_t *)&escaped + offset,
 				  offset ? 1 : 2,
 				  send_off);
diff --git a/drivers/net/slip.c b/drivers/net/slip.c
index 9f46142a325..6dcb2fe769c 100644
--- a/drivers/net/slip.c
+++ b/drivers/net/slip.c
@@ -167,11 +167,11 @@ static void process_msg(struct slip_context *slip)
 	{
 		struct net_eth_hdr *hdr = NET_ETH_HDR(pkt);

-		if (ntohs(hdr->type) == NET_ETH_PTYPE_VLAN) {
+		if (zsock_ntohs(hdr->type) == NET_ETH_PTYPE_VLAN) {
 			struct net_eth_vlan_hdr *hdr_vlan =
 				(struct net_eth_vlan_hdr *)NET_ETH_HDR(pkt);

-			net_pkt_set_vlan_tci(pkt, ntohs(hdr_vlan->vlan.tci));
+			net_pkt_set_vlan_tci(pkt, zsock_ntohs(hdr_vlan->vlan.tci));
 			vlan_tag = net_pkt_vlan_tag(pkt);
 		}
 	}
diff --git a/drivers/wifi/esp32/src/esp_wifi_drv.c b/drivers/wifi/esp32/src/esp_wifi_drv.c
index 23fd30a4884..1fdc3db8902 100644
--- a/drivers/wifi/esp32/src/esp_wifi_drv.c
+++ b/drivers/wifi/esp32/src/esp_wifi_drv.c
@@ -138,7 +138,7 @@ static esp_err_t eth_esp32_rx(void *buffer, uint16_t len, void *eb)
 		return -EIO;
 	}

-	pkt = net_pkt_rx_alloc_with_buffer(esp32_wifi_iface, len, AF_UNSPEC, 0, K_MSEC(100));
+	pkt = net_pkt_rx_alloc_with_buffer(esp32_wifi_iface, len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (!pkt) {
 		LOG_ERR("Failed to allocate net buffer");
 		esp_wifi_internal_free_rx_buffer(eb);
@@ -185,7 +185,7 @@ static esp_err_t wifi_esp32_ap_iface_rx(void *buffer, uint16_t len, void *eb)
 		return -EIO;
 	}

-	pkt = net_pkt_rx_alloc_with_buffer(esp32_wifi_iface_ap, len, AF_UNSPEC, 0, K_MSEC(100));
+	pkt = net_pkt_rx_alloc_with_buffer(esp32_wifi_iface_ap, len, ZSOCK_AF_UNSPEC, 0, K_MSEC(100));
 	if (!pkt) {
 		esp_wifi_internal_free_rx_buffer(eb);
 		LOG_ERR("Failed to get net buffer");
diff --git a/drivers/wifi/esp_at/esp.c b/drivers/wifi/esp_at/esp.c
index f35b046a1ec..f0873f761ad 100644
--- a/drivers/wifi/esp_at/esp.c
+++ b/drivers/wifi/esp_at/esp.c
@@ -516,8 +516,8 @@ static void esp_dns_work(struct k_work *work)
 #if defined(ESP_MAX_DNS)
 	struct esp_data *data = CONTAINER_OF(work, struct esp_data, dns_work);
 	struct dns_resolve_context *dnsctx;
-	struct sockaddr_in *addrs = data->dns_addresses;
-	const struct sockaddr *dns_servers[ESP_MAX_DNS + 1] = {};
+	struct zsock_sockaddr_in *addrs = data->dns_addresses;
+	const struct zsock_sockaddr *dns_servers[ESP_MAX_DNS + 1] = {};
 	int interfaces[ESP_MAX_DNS];
 	size_t i;
 	int err, ifindex;
@@ -528,7 +528,7 @@ static void esp_dns_work(struct k_work *work)
 		if (!addrs[i].sin_addr.s_addr) {
 			break;
 		}
-		dns_servers[i] = (struct sockaddr *) &addrs[i];
+		dns_servers[i] = (struct zsock_sockaddr *) &addrs[i];
 		interfaces[i] = ifindex;
 	}

@@ -550,7 +550,7 @@ MODEM_CMD_DEFINE(on_cmd_cipdns)
 #if defined(ESP_MAX_DNS)
 	struct esp_data *dev = CONTAINER_OF(data, struct esp_data,
 					    cmd_handler_data);
-	struct sockaddr_in *addrs = dev->dns_addresses;
+	struct zsock_sockaddr_in *addrs = dev->dns_addresses;
 	char **servers = (char **)argv + 1;
 	size_t num_servers = argc - 1;
 	size_t valid_servers = 0;
@@ -566,7 +566,7 @@ MODEM_CMD_DEFINE(on_cmd_cipdns)
 		servers[i] = str_unquote(servers[i]);
 		LOG_DBG("DNS[%zu]: %s", i, servers[i]);

-		err = net_addr_pton(AF_INET, servers[i], &addrs[i].sin_addr);
+		err = net_addr_pton(ZSOCK_AF_INET, servers[i], &addrs[i].sin_addr);
 		if (err) {
 			LOG_ERR("Invalid DNS address: %s",
 				servers[i]);
@@ -574,8 +574,8 @@ MODEM_CMD_DEFINE(on_cmd_cipdns)
 			break;
 		}

-		addrs[i].sin_family = AF_INET;
-		addrs[i].sin_port = htons(53);
+		addrs[i].sin_family = ZSOCK_AF_INET;
+		addrs[i].sin_port = zsock_htons(53);

 		valid_servers++;
 	}
@@ -663,11 +663,11 @@ MODEM_CMD_DEFINE(on_cmd_cipsta)
 	ip = str_unquote(argv[1]);

 	if (!strcmp(argv[0], "ip")) {
-		net_addr_pton(AF_INET, ip, &dev->ip);
+		net_addr_pton(ZSOCK_AF_INET, ip, &dev->ip);
 	} else if (!strcmp(argv[0], "gateway")) {
-		net_addr_pton(AF_INET, ip, &dev->gw);
+		net_addr_pton(ZSOCK_AF_INET, ip, &dev->gw);
 	} else if (!strcmp(argv[0], "netmask")) {
-		net_addr_pton(AF_INET, ip, &dev->nm);
+		net_addr_pton(ZSOCK_AF_INET, ip, &dev->nm);
 	} else {
 		LOG_WRN("Unknown IP type %s", argv[0]);
 	}
@@ -854,8 +854,8 @@ static int cmd_ipd_parse_hdr(struct esp_data *dev,

 	if (!ESP_PROTO_PASSIVE(esp_socket_ip_proto(*sock)) &&
 	    IS_ENABLED(CONFIG_WIFI_ESP_AT_CIPDINFO_USE)) {
-		struct sockaddr_in *recv_addr =
-			(struct sockaddr_in *) &(*sock)->context->remote;
+		struct zsock_sockaddr_in *recv_addr =
+			(struct zsock_sockaddr_in *) &(*sock)->context->remote;
 		char *remote_ip;
 		long port;

@@ -883,15 +883,15 @@ static int cmd_ipd_parse_hdr(struct esp_data *dev,
 			goto socket_unref;
 		}

-		err = net_addr_pton(AF_INET, remote_ip, &recv_addr->sin_addr);
+		err = net_addr_pton(ZSOCK_AF_INET, remote_ip, &recv_addr->sin_addr);
 		if (err) {
 			LOG_ERR("Invalid IP address");
 			err = -EBADMSG;
 			goto socket_unref;
 		}

-		recv_addr->sin_family = AF_INET;
-		recv_addr->sin_port = htons(port);
+		recv_addr->sin_family = ZSOCK_AF_INET;
+		recv_addr->sin_port = zsock_htons(port);
 	}

 	*data_offset = (str - ipd_buf);
diff --git a/drivers/wifi/esp_at/esp.h b/drivers/wifi/esp_at/esp.h
index 352d5f66829..8f781f92bde 100644
--- a/drivers/wifi/esp_at/esp.h
+++ b/drivers/wifi/esp_at/esp.h
@@ -50,10 +50,10 @@ extern "C" {
  */
 #if defined(CONFIG_WIFI_ESP_AT_PASSIVE_MODE)
 #if defined(CONFIG_WIFI_ESP_AT_VERSION_1_7)
-#define ESP_PROTO_PASSIVE(proto) (proto == IPPROTO_TCP)
+#define ESP_PROTO_PASSIVE(proto) (proto == ZSOCK_IPPROTO_TCP)
 #else
 #define ESP_PROTO_PASSIVE(proto) \
-	(proto == IPPROTO_TCP || proto == IPPROTO_UDP)
+	(proto == ZSOCK_IPPROTO_TCP || proto == ZSOCK_IPPROTO_UDP)
 #endif /* CONFIG_WIFI_ESP_AT_VERSION_1_7 */
 #else
 #define ESP_PROTO_PASSIVE(proto) 0
@@ -175,8 +175,8 @@ struct esp_socket {
 	atomic_t flags;

 	/* socket info */
-	struct sockaddr src;
-	struct sockaddr dst;
+	struct zsock_sockaddr src;
+	struct zsock_sockaddr dst;

 	/* sem */
 	union {
@@ -224,12 +224,12 @@ struct esp_data {
 	enum wifi_conn_status conn_status;

 	/* addresses  */
-	struct in_addr ip;
-	struct in_addr gw;
-	struct in_addr nm;
+	struct zsock_in_addr ip;
+	struct zsock_in_addr gw;
+	struct zsock_in_addr nm;
 	uint8_t mac_addr[6];
 #if defined(ESP_MAX_DNS)
-	struct sockaddr_in dns_addresses[ESP_MAX_DNS];
+	struct zsock_sockaddr_in dns_addresses[ESP_MAX_DNS];
 #endif

 	/* modem context */
diff --git a/drivers/wifi/esp_at/esp_offload.c b/drivers/wifi/esp_at/esp_offload.c
index 9894a6634c9..675453f45b0 100644
--- a/drivers/wifi/esp_at/esp_offload.c
+++ b/drivers/wifi/esp_at/esp_offload.c
@@ -37,8 +37,8 @@ static int _sock_connect(struct esp_data *dev, struct esp_socket *sock)
 				2 * NET_IPV4_ADDR_LEN)];
 	char dst_addr_str[NET_IPV4_ADDR_LEN];
 	char src_addr_str[NET_IPV4_ADDR_LEN];
-	struct sockaddr src;
-	struct sockaddr dst;
+	struct zsock_sockaddr src;
+	struct zsock_sockaddr dst;
 	int ret;
 	int mode;

@@ -51,7 +51,7 @@ static int _sock_connect(struct esp_data *dev, struct esp_socket *sock)
 	dst = sock->dst;
 	k_mutex_unlock(&sock->lock);

-	if (dst.sa_family == AF_INET) {
+	if (dst.sa_family == ZSOCK_AF_INET) {
 		net_addr_ntop(dst.sa_family,
 			      &net_sin(&dst)->sin_addr,
 			      dst_addr_str, sizeof(dst_addr_str));
@@ -59,13 +59,13 @@ static int _sock_connect(struct esp_data *dev, struct esp_socket *sock)
 		strcpy(dst_addr_str, "0.0.0.0");
 	}

-	if (esp_socket_ip_proto(sock) == IPPROTO_TCP) {
+	if (esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_TCP) {
 		snprintk(connect_msg, sizeof(connect_msg),
 			 "AT+CIPSTART=%d,\"TCP\",\"%s\",%d,7200",
 			 sock->link_id, dst_addr_str,
-			 ntohs(net_sin(&dst)->sin_port));
+			 zsock_ntohs(net_sin(&dst)->sin_port));
 	} else {
-		if (src.sa_family == AF_INET && net_sin(&src)->sin_port != 0) {
+		if (src.sa_family == ZSOCK_AF_INET && net_sin(&src)->sin_port != 0) {
 			net_addr_ntop(src.sa_family,
 				      &net_sin(&src)->sin_addr,
 				      src_addr_str, sizeof(src_addr_str));
@@ -87,7 +87,7 @@ static int _sock_connect(struct esp_data *dev, struct esp_socket *sock)
 			 * to 2.
 			 */
 			if ((net_sin(&dst)->sin_addr.s_addr == 0) &&
-			    (ntohs(net_sin(&dst)->sin_port) == 0)) {
+			    (zsock_ntohs(net_sin(&dst)->sin_port) == 0)) {
 				mode = 2;
 				/* Port 0 is reserved and a valid port needs to be provided when
 				 * connecting.
@@ -100,24 +100,24 @@ static int _sock_connect(struct esp_data *dev, struct esp_socket *sock)
 			snprintk(connect_msg, sizeof(connect_msg),
 				 "AT+CIPSTART=%d,\"UDP\",\"%s\",%d,%d,%d,\"%s\"",
 				 sock->link_id, dst_addr_str,
-				 ntohs(net_sin(&dst)->sin_port), ntohs(net_sin(&src)->sin_port),
+				 zsock_ntohs(net_sin(&dst)->sin_port), zsock_ntohs(net_sin(&src)->sin_port),
 				 mode, src_addr_str);
 		} else {
 			snprintk(connect_msg, sizeof(connect_msg),
 				 "AT+CIPSTART=%d,\"UDP\",\"%s\",%d",
 				 sock->link_id, dst_addr_str,
-				 ntohs(net_sin(&dst)->sin_port));
+				 zsock_ntohs(net_sin(&dst)->sin_port));
 		}
 	}

 	LOG_DBG("link %d, ip_proto %s, addr %s", sock->link_id,
-		esp_socket_ip_proto(sock) == IPPROTO_TCP ? "TCP" : "UDP",
+		esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_TCP ? "TCP" : "UDP",
 		dst_addr_str);

 	ret = esp_cmd_send(dev, NULL, 0, connect_msg, ESP_CMD_TIMEOUT);
 	if (ret == 0) {
 		esp_socket_flags_set(sock, ESP_SOCK_CONNECTED);
-		if (esp_socket_type(sock) == SOCK_STREAM) {
+		if (esp_socket_type(sock) == ZSOCK_SOCK_STREAM) {
 			net_context_set_state(sock->context,
 					      NET_CONTEXT_CONNECTED);
 		}
@@ -149,8 +149,8 @@ void esp_connect_work(struct k_work *work)
 	k_mutex_unlock(&sock->lock);
 }

-static int esp_bind(struct net_context *context, const struct sockaddr *addr,
-		    socklen_t addrlen)
+static int esp_bind(struct net_context *context, const struct zsock_sockaddr *addr,
+		    zsock_socklen_t addrlen)
 {
 	struct esp_socket *sock;
 	struct esp_data *dev;
@@ -158,11 +158,11 @@ static int esp_bind(struct net_context *context, const struct sockaddr *addr,
 	sock = (struct esp_socket *)context->offload_context;
 	dev = esp_socket_to_dev(sock);

-	if (esp_socket_ip_proto(sock) == IPPROTO_TCP) {
+	if (esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_TCP) {
 		return 0;
 	}

-	if (IS_ENABLED(CONFIG_NET_IPV4) && addr->sa_family == AF_INET) {
+	if (IS_ENABLED(CONFIG_NET_IPV4) && addr->sa_family == ZSOCK_AF_INET) {
 		LOG_DBG("link %d", sock->link_id);

 		if (esp_socket_connected(sock)) {
@@ -180,8 +180,8 @@ static int esp_bind(struct net_context *context, const struct sockaddr *addr,
 }

 static int esp_connect(struct net_context *context,
-		       const struct sockaddr *addr,
-		       socklen_t addrlen,
+		       const struct zsock_sockaddr *addr,
+		       zsock_socklen_t addrlen,
 		       net_context_connect_cb_t cb,
 		       int32_t timeout,
 		       void *user_data)
@@ -195,7 +195,7 @@ static int esp_connect(struct net_context *context,

 	LOG_DBG("link %d, timeout %d", sock->link_id, timeout);

-	if (!IS_ENABLED(CONFIG_NET_IPV4) || addr->sa_family != AF_INET) {
+	if (!IS_ENABLED(CONFIG_NET_IPV4) || addr->sa_family != ZSOCK_AF_INET) {
 		return -EAFNOSUPPORT;
 	}

@@ -275,7 +275,7 @@ static int _sock_send(struct esp_socket *sock, struct net_pkt *pkt)
 		MODEM_CMD("SEND OK", on_cmd_send_ok, 0U, ""),
 		MODEM_CMD("SEND FAIL", on_cmd_send_fail, 0U, ""),
 	};
-	struct sockaddr dst;
+	struct zsock_sockaddr dst;

 	if (!esp_flags_are_set(dev, EDF_STA_CONNECTED | EDF_AP_ENABLED)) {
 		return -ENETUNREACH;
@@ -285,7 +285,7 @@ static int _sock_send(struct esp_socket *sock, struct net_pkt *pkt)

 	LOG_DBG("link %d, len %d", sock->link_id, pkt_len);

-	if (esp_socket_ip_proto(sock) == IPPROTO_TCP) {
+	if (esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_TCP) {
 		snprintk(cmd_buf, sizeof(cmd_buf),
 			 "AT+CIPSEND=%d,%d", sock->link_id, pkt_len);
 	} else {
@@ -299,7 +299,7 @@ static int _sock_send(struct esp_socket *sock, struct net_pkt *pkt)
 		snprintk(cmd_buf, sizeof(cmd_buf),
 			 "AT+CIPSEND=%d,%d,\"%s\",%d",
 			 sock->link_id, pkt_len, addr_str,
-			 ntohs(net_sin(&dst)->sin_port));
+			 zsock_ntohs(net_sin(&dst)->sin_port));
 	}

 	k_sem_take(&dev->cmd_handler_data.sem_tx_lock, K_FOREVER);
@@ -333,7 +333,7 @@ static int _sock_send(struct esp_socket *sock, struct net_pkt *pkt)
 	}

 	/* Wait for 'SEND OK' or 'SEND FAIL' */
-	if (esp_socket_ip_proto(sock) == IPPROTO_TCP) {
+	if (esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_TCP) {
 		ret = k_sem_take(&dev->sem_response, TCP_SEND_TIMEOUT);
 	} else {
 		ret = k_sem_take(&dev->sem_response, ESP_CMD_TIMEOUT);
@@ -392,7 +392,7 @@ static int esp_socket_send_one_pkt(struct esp_socket *sock)
 		 * more to this socket, as there will be a hole in the data
 		 * stream, which application layer is not expecting.
 		 */
-		if (esp_socket_type(sock) == SOCK_STREAM) {
+		if (esp_socket_type(sock) == ZSOCK_SOCK_STREAM) {
 			if (!esp_socket_flags_test_and_set(sock,
 						ESP_SOCK_CLOSE_PENDING)) {
 				esp_socket_work_submit(sock, &sock->close_work);
@@ -420,8 +420,8 @@ void esp_send_work(struct k_work *work)
 }

 static int esp_sendto(struct net_pkt *pkt,
-		      const struct sockaddr *dst_addr,
-		      socklen_t addrlen,
+		      const struct zsock_sockaddr *dst_addr,
+		      zsock_socklen_t addrlen,
 		      net_context_send_cb_t cb,
 		      int32_t timeout,
 		      void *user_data)
@@ -441,7 +441,7 @@ static int esp_sendto(struct net_pkt *pkt,
 		return -ENETUNREACH;
 	}

-	if (esp_socket_type(sock) == SOCK_STREAM) {
+	if (esp_socket_type(sock) == ZSOCK_SOCK_STREAM) {
 		atomic_val_t flags = esp_socket_flags(sock);

 		if (!(flags & ESP_SOCK_CONNECTED) ||
@@ -463,7 +463,7 @@ static int esp_sendto(struct net_pkt *pkt,
 			if (ret < 0) {
 				return ret;
 			}
-		} else if (esp_socket_type(sock) == SOCK_DGRAM) {
+		} else if (esp_socket_type(sock) == ZSOCK_SOCK_DGRAM) {
 			memcpy(&sock->dst, dst_addr, addrlen);
 		}
 	}
@@ -565,7 +565,7 @@ MODEM_CMD_DIRECT_DEFINE(on_cmd_ciprecvdata)
 	}

 #if defined(CONFIG_WIFI_ESP_AT_CIPDINFO_USE) && !defined(CONFIG_WIFI_ESP_AT_VERSION_1_7)
-	char raw_remote_ip[INET_ADDRSTRLEN + 3] = {0};
+	char raw_remote_ip[ZSOCK_INET_ADDRSTRLEN + 3] = {0};
 	int port = 0;

 	err = cmd_ciprecvdata_parse(sock, data->rx_buf, len, &data_offset,
@@ -583,17 +583,17 @@ MODEM_CMD_DIRECT_DEFINE(on_cmd_ciprecvdata)
 	}

 #if defined(CONFIG_WIFI_ESP_AT_CIPDINFO_USE) && !defined(CONFIG_WIFI_ESP_AT_VERSION_1_7)
-	struct sockaddr_in *recv_addr =
-			(struct sockaddr_in *) &sock->context->remote;
+	struct zsock_sockaddr_in *recv_addr =
+			(struct zsock_sockaddr_in *) &sock->context->remote;

-	recv_addr->sin_port = htons(port);
-	recv_addr->sin_family = AF_INET;
+	recv_addr->sin_port = zsock_htons(port);
+	recv_addr->sin_family = ZSOCK_AF_INET;

 	/* IP addr comes within quotation marks, which is disliked by
 	 * conv function. So we remove them by subtraction 2 from
 	 * raw_remote_ip length and index from &raw_remote_ip[1].
 	 */
-	char remote_ip_addr[INET_ADDRSTRLEN];
+	char remote_ip_addr[ZSOCK_INET_ADDRSTRLEN];
 	size_t remote_ip_str_len;

 	remote_ip_str_len = MIN(sizeof(remote_ip_addr) - 1,
@@ -601,7 +601,7 @@ MODEM_CMD_DIRECT_DEFINE(on_cmd_ciprecvdata)
 	strncpy(remote_ip_addr, &raw_remote_ip[1], remote_ip_str_len);
 	remote_ip_addr[remote_ip_str_len] = '\0';

-	if (net_addr_pton(AF_INET, remote_ip_addr, &recv_addr->sin_addr) < 0) {
+	if (net_addr_pton(ZSOCK_AF_INET, remote_ip_addr, &recv_addr->sin_addr) < 0) {
 		LOG_ERR("Invalid src addr %s", remote_ip_addr);
 		err = -EIO;
 		goto socket_unref;
@@ -686,8 +686,8 @@ static int esp_recv(struct net_context *context,
 	 * traffic can be received.
 	 */
 	if (!esp_socket_connected(sock) &&
-	    esp_socket_ip_proto(sock) == IPPROTO_UDP &&
-	    sock->src.sa_family == AF_INET &&
+	    esp_socket_ip_proto(sock) == ZSOCK_IPPROTO_UDP &&
+	    sock->src.sa_family == ZSOCK_AF_INET &&
 	    net_sin(&sock->src)->sin_port != 0) {
 		_sock_connect(dev, sock);
 	}
@@ -747,7 +747,7 @@ static int esp_put(struct net_context *context)
 	return 0;
 }

-static int esp_get(sa_family_t family,
+static int esp_get(zsock_sa_family_t family,
 		   enum net_sock_type type,
 		   enum net_ip_protocol ip_proto,
 		   struct net_context **context)
@@ -757,7 +757,7 @@ static int esp_get(sa_family_t family,

 	LOG_DBG("");

-	if (family != AF_INET) {
+	if (family != ZSOCK_AF_INET) {
 		return -EAFNOSUPPORT;
 	}

diff --git a/drivers/wifi/esp_at/esp_socket.c b/drivers/wifi/esp_at/esp_socket.c
index 05f620ab223..58a89b8e88b 100644
--- a/drivers/wifi/esp_at/esp_socket.c
+++ b/drivers/wifi/esp_at/esp_socket.c
@@ -114,7 +114,7 @@ static struct net_pkt *esp_socket_prepare_pkt(struct esp_socket *sock,
 	struct net_pkt *pkt;
 	size_t to_copy;

-	pkt = net_pkt_rx_alloc_with_buffer(data->net_iface, len, AF_UNSPEC,
+	pkt = net_pkt_rx_alloc_with_buffer(data->net_iface, len, ZSOCK_AF_UNSPEC,
 					   0, RX_NET_PKT_ALLOC_TIMEOUT);
 	if (!pkt) {
 		return NULL;
@@ -180,7 +180,7 @@ void esp_socket_rx(struct esp_socket *sock, struct net_buf *buf,
 	pkt = esp_socket_prepare_pkt(sock, buf, offset, len);
 	if (!pkt) {
 		LOG_ERR("Failed to get net_pkt: len %zu", len);
-		if (esp_socket_type(sock) == SOCK_STREAM) {
+		if (esp_socket_type(sock) == ZSOCK_SOCK_STREAM) {
 			if (!esp_socket_flags_test_and_set(sock,
 						ESP_SOCK_CLOSE_PENDING)) {
 				esp_socket_work_submit(sock, &sock->close_work);
diff --git a/drivers/wifi/esp_hosted/esp_hosted_wifi.c b/drivers/wifi/esp_hosted/esp_hosted_wifi.c
index 52edfbfdbcb..09a4f5d4a1d 100644
--- a/drivers/wifi/esp_hosted/esp_hosted_wifi.c
+++ b/drivers/wifi/esp_hosted/esp_hosted_wifi.c
@@ -430,7 +430,7 @@ static int esp_hosted_recv(struct net_if *iface, void *buf, size_t len)
 		return 0;
 	}

-	pkt = net_pkt_rx_alloc_with_buffer(iface, len, AF_UNSPEC, 0, K_MSEC(1000));
+	pkt = net_pkt_rx_alloc_with_buffer(iface, len, ZSOCK_AF_UNSPEC, 0, K_MSEC(1000));
 	if (pkt == NULL) {
 		LOG_ERR("Failed to allocate net buffer");
 		return -ENOMEM;
diff --git a/drivers/wifi/eswifi/eswifi.h b/drivers/wifi/eswifi/eswifi.h
index 54bf00f0f78..1d5f2836995 100644
--- a/drivers/wifi/eswifi/eswifi.h
+++ b/drivers/wifi/eswifi/eswifi.h
@@ -141,7 +141,7 @@ int __eswifi_off_start_client(struct eswifi_dev *eswifi,
 int __eswifi_listen(struct eswifi_dev *eswifi, struct eswifi_off_socket *socket, int backlog);
 int __eswifi_accept(struct eswifi_dev *eswifi, struct eswifi_off_socket *socket);
 int __eswifi_bind(struct eswifi_dev *eswifi, struct eswifi_off_socket *socket,
-		  const struct sockaddr *addr, socklen_t addrlen);
+		  const struct zsock_sockaddr *addr, zsock_socklen_t addrlen);
 #if defined(CONFIG_NET_SOCKETS_OFFLOAD)
 int eswifi_socket_offload_init(struct eswifi_dev *leswifi);
 #endif
diff --git a/drivers/wifi/eswifi/eswifi_core.c b/drivers/wifi/eswifi/eswifi_core.c
index 976037c19fe..a073664fe9d 100644
--- a/drivers/wifi/eswifi/eswifi_core.c
+++ b/drivers/wifi/eswifi/eswifi_core.c
@@ -257,7 +257,7 @@ static void eswifi_scan(struct eswifi_dev *eswifi)
 static int eswifi_connect(struct eswifi_dev *eswifi)
 {
 	char connect[] = "C0\r";
-	struct in_addr addr;
+	struct zsock_in_addr addr;
 	char *rsp;
 	int err;

diff --git a/drivers/wifi/eswifi/eswifi_offload.c b/drivers/wifi/eswifi/eswifi_offload.c
index c16d1f75f82..f8c055db4eb 100644
--- a/drivers/wifi/eswifi/eswifi_offload.c
+++ b/drivers/wifi/eswifi/eswifi_offload.c
@@ -21,8 +21,8 @@ LOG_MODULE_DECLARE(LOG_MODULE_NAME);


 static int eswifi_off_bind(struct net_context *context,
-			   const struct sockaddr *addr,
-			   socklen_t addrlen)
+			   const struct zsock_sockaddr *addr,
+			   zsock_socklen_t addrlen)
 {
 	struct eswifi_off_socket *socket = context->offload_context;
 	struct eswifi_dev *eswifi = eswifi_by_iface_idx(context->iface);
@@ -97,8 +97,8 @@ static void eswifi_off_connect_work(struct k_work *work)
 }

 static int eswifi_off_connect(struct net_context *context,
-			      const struct sockaddr *addr,
-			      socklen_t addrlen,
+			      const struct zsock_sockaddr *addr,
+			      zsock_socklen_t addrlen,
 			      net_context_connect_cb_t cb,
 			      int32_t timeout,
 			      void *user_data)
@@ -109,8 +109,8 @@ static int eswifi_off_connect(struct net_context *context,

 	LOG_DBG("timeout=%d", timeout);

-	if (addr->sa_family != AF_INET) {
-		LOG_ERR("Only AF_INET is supported!");
+	if (addr->sa_family != ZSOCK_AF_INET) {
+		LOG_ERR("Only ZSOCK_AF_INET is supported!");
 		return -EPFNOSUPPORT;
 	}

@@ -294,8 +294,8 @@ static int eswifi_off_send(struct net_pkt *pkt,
 }

 static int eswifi_off_sendto(struct net_pkt *pkt,
-			     const struct sockaddr *dst_addr,
-			     socklen_t addrlen,
+			     const struct zsock_sockaddr *dst_addr,
+			     zsock_socklen_t addrlen,
 			     net_context_send_cb_t cb,
 			     int32_t timeout,
 			     void *user_data)
@@ -404,7 +404,7 @@ done:
 	return ret;
 }

-static int eswifi_off_get(sa_family_t family,
+static int eswifi_off_get(zsock_sa_family_t family,
 			  enum net_sock_type type,
 			  enum net_ip_protocol ip_proto,
 			  struct net_context **context)
@@ -446,7 +446,7 @@ void eswifi_offload_async_msg(struct eswifi_dev *eswifi, char *msg, size_t len)

 	if (!strncmp(msg, msg_tcp_accept, sizeof(msg_tcp_accept) - 1)) {
 		struct eswifi_off_socket *socket = NULL;
-		struct in_addr *sin_addr;
+		struct zsock_in_addr *sin_addr;
 		uint8_t ip[4];
 		uint16_t port = 0;
 		char *str;
@@ -484,19 +484,19 @@ void eswifi_offload_async_msg(struct eswifi_dev *eswifi, char *msg, size_t len)
 			return;
 		}

-		struct sockaddr_in *peer = net_sin(&socket->peer_addr);
+		struct zsock_sockaddr_in *peer = net_sin(&socket->peer_addr);

 		sin_addr = &peer->sin_addr;
 		memcpy(&sin_addr->s4_addr, ip, 4);
-		peer->sin_port = htons(port);
-		peer->sin_family = AF_INET;
+		peer->sin_port = zsock_htons(port);
+		peer->sin_family = ZSOCK_AF_INET;
 		socket->state = ESWIFI_SOCKET_STATE_CONNECTED;
 		socket->usage++;

 		/* Save information about remote. */
 		socket->context->flags |= NET_CONTEXT_REMOTE_ADDR_SET;
 		memcpy(&socket->context->remote, &socket->peer_addr,
-		       sizeof(struct sockaddr));
+		       sizeof(struct zsock_sockaddr));

 		LOG_DBG("%u.%u.%u.%u connected to port %u",
 			ip[0], ip[1], ip[2], ip[3], port);
@@ -504,7 +504,7 @@ void eswifi_offload_async_msg(struct eswifi_dev *eswifi, char *msg, size_t len)
 		if (socket->accept_cb) {
 			socket->accept_cb(socket->context,
 					  &socket->peer_addr,
-					  sizeof(struct sockaddr_in), 0,
+					  sizeof(struct zsock_sockaddr_in), 0,
 					  socket->accept_data);
 		}

diff --git a/drivers/wifi/eswifi/eswifi_offload.h b/drivers/wifi/eswifi/eswifi_offload.h
index ebb405d99a7..d54ea806fd9 100644
--- a/drivers/wifi/eswifi/eswifi_offload.h
+++ b/drivers/wifi/eswifi/eswifi_offload.h
@@ -43,7 +43,7 @@ struct eswifi_off_socket {
 	struct k_work connect_work;
 	struct k_work send_work;
 	struct k_work_delayable read_work;
-	struct sockaddr peer_addr;
+	struct zsock_sockaddr peer_addr;
 	struct k_sem read_sem;
 	struct k_sem accept_sem;
 	uint16_t port;
diff --git a/drivers/wifi/eswifi/eswifi_socket.c b/drivers/wifi/eswifi/eswifi_socket.c
index f4c546f0ff0..6b11b0b0416 100644
--- a/drivers/wifi/eswifi/eswifi_socket.c
+++ b/drivers/wifi/eswifi/eswifi_socket.c
@@ -22,9 +22,9 @@ int eswifi_socket_type_from_zephyr(int proto, enum eswifi_transport_type *type)
 	if (IS_ENABLED(CONFIG_NET_SOCKETS_SOCKOPT_TLS) &&
 	    proto >= IPPROTO_TLS_1_0 && proto <= IPPROTO_TLS_1_2) {
 		*type = ESWIFI_TRANSPORT_TCP_SSL;
-	} else if (proto == IPPROTO_TCP) {
+	} else if (proto == ZSOCK_IPPROTO_TCP) {
 		*type = ESWIFI_TRANSPORT_TCP;
-	} else if (proto == IPPROTO_UDP) {
+	} else if (proto == ZSOCK_IPPROTO_UDP) {
 		*type = ESWIFI_TRANSPORT_UDP;
 	} else {
 		return -EPFNOSUPPORT;
@@ -75,12 +75,12 @@ static int __read_data(struct eswifi_dev *eswifi, size_t len, char **data)
 }

 int __eswifi_bind(struct eswifi_dev *eswifi, struct eswifi_off_socket *socket,
-		      const struct sockaddr *addr, socklen_t addrlen)
+		      const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	int err;

-	if (addr->sa_family != AF_INET) {
-		LOG_ERR("Only AF_INET is supported!");
+	if (addr->sa_family != ZSOCK_AF_INET) {
+		LOG_ERR("Only ZSOCK_AF_INET is supported!");
 		return -EPFNOSUPPORT;
 	}

@@ -135,7 +135,7 @@ static void eswifi_off_read_work(struct k_work *work)

 	/* Verify if we can allocate a rx packet before reading data to prevent leaks */
 	pkt = net_pkt_rx_alloc_with_buffer(eswifi->iface, 1460,
-					   AF_UNSPEC, 0, K_NO_WAIT);
+					   ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("Cannot allocate rx packet");
 		goto done;
@@ -195,8 +195,8 @@ done:
 int __eswifi_off_start_client(struct eswifi_dev *eswifi,
 			      struct eswifi_off_socket *socket)
 {
-	struct sockaddr *addr = &socket->peer_addr;
-	struct in_addr *sin_addr = &net_sin(addr)->sin_addr;
+	struct zsock_sockaddr *addr = &socket->peer_addr;
+	struct zsock_in_addr *sin_addr = &net_sin(addr)->sin_addr;
 	int err;

 	LOG_DBG("");
@@ -324,8 +324,8 @@ int __eswifi_socket_new(struct eswifi_dev *eswifi, int family, int type,

 	LOG_DBG("");

-	if (family != AF_INET) {
-		LOG_ERR("Only AF_INET is supported!");
+	if (family != ZSOCK_AF_INET) {
+		LOG_ERR("Only ZSOCK_AF_INET is supported!");
 		return -EPFNOSUPPORT;
 	}

diff --git a/drivers/wifi/eswifi/eswifi_socket_offload.c b/drivers/wifi/eswifi/eswifi_socket_offload.c
index 0e3b6134062..d19421cedfc 100644
--- a/drivers/wifi/eswifi/eswifi_socket_offload.c
+++ b/drivers/wifi/eswifi/eswifi_socket_offload.c
@@ -52,8 +52,8 @@ static void __process_received(struct net_context *context,
 	k_fifo_put(&socket->fifo, pkt);
 }

-static int eswifi_socket_connect(void *obj, const struct sockaddr *addr,
-				 socklen_t addrlen)
+static int eswifi_socket_connect(void *obj, const struct zsock_sockaddr *addr,
+				 zsock_socklen_t addrlen)
 {
 	intptr_t sock = OBJ_TO_SD(obj);
 	struct eswifi_off_socket *socket;
@@ -64,8 +64,8 @@ static int eswifi_socket_connect(void *obj, const struct sockaddr *addr,
 		return -EINVAL;
 	}

-	if (addr->sa_family != AF_INET) {
-		LOG_ERR("Only AF_INET is supported!");
+	if (addr->sa_family != ZSOCK_AF_INET) {
+		LOG_ERR("Only ZSOCK_AF_INET is supported!");
 		return -EPFNOSUPPORT;
 	}

@@ -106,16 +106,16 @@ static int eswifi_socket_listen(void *obj, int backlog)
 	return ret;
 }

-static void __eswifi_socket_accept_cb(struct net_context *context, struct sockaddr *addr,
-				      socklen_t len, int val, void *data)
+static void __eswifi_socket_accept_cb(struct net_context *context, struct zsock_sockaddr *addr,
+				      zsock_socklen_t len, int val, void *data)
 {
-	struct sockaddr *addr_target = data;
+	struct zsock_sockaddr *addr_target = data;

 	memcpy(addr_target, addr, len);
 }

-static int __eswifi_socket_accept(void *obj, struct sockaddr *addr,
-				  socklen_t *addrlen)
+static int __eswifi_socket_accept(void *obj, struct zsock_sockaddr *addr,
+				  zsock_socklen_t *addrlen)
 {
 	intptr_t sock = OBJ_TO_SD(obj);
 	struct eswifi_off_socket *socket;
@@ -135,15 +135,15 @@ static int __eswifi_socket_accept(void *obj, struct sockaddr *addr,
 	k_sem_reset(&socket->accept_sem);
 	eswifi_unlock(eswifi);

-	*addrlen = sizeof(struct sockaddr_in);
+	*addrlen = sizeof(struct zsock_sockaddr_in);

 	k_sem_take(&socket->accept_sem, K_FOREVER);

 	return 0;
 }

-static int eswifi_socket_accept(void *obj, struct sockaddr *addr,
-				socklen_t *addrlen)
+static int eswifi_socket_accept(void *obj, struct zsock_sockaddr *addr,
+				zsock_socklen_t *addrlen)
 {
 	int fd = zvfs_reserve_fd();
 	intptr_t sock;
@@ -166,7 +166,7 @@ static int eswifi_socket_accept(void *obj, struct sockaddr *addr,
 }

 #if defined(CONFIG_NET_SOCKETS_SOCKOPT_TLS)
-static int map_credentials(int sd, const void *optval, socklen_t optlen)
+static int map_credentials(int sd, const void *optval, zsock_socklen_t optlen)
 {
 	sec_tag_t *sec_tags = (sec_tag_t *)optval;
 	int ret = 0;
@@ -231,14 +231,14 @@ static int map_credentials(int sd, const void *optval, socklen_t optlen)
 	return 0;
 }
 #else
-static int map_credentials(int sd, const void *optval, socklen_t optlen)
+static int map_credentials(int sd, const void *optval, zsock_socklen_t optlen)
 {
 	return 0;
 }
 #endif

 static int eswifi_socket_setsockopt(void *obj, int level, int optname,
-				    const void *optval, socklen_t optlen)
+				    const void *optval, zsock_socklen_t optlen)
 {
 	intptr_t sd = OBJ_TO_SD(obj);
 	int ret;
@@ -306,8 +306,8 @@ static ssize_t eswifi_socket_send(void *obj, const void *buf, size_t len,
 }

 static ssize_t eswifi_socket_sendto(void *obj, const void *buf, size_t len,
-				    int flags, const struct sockaddr *to,
-				    socklen_t tolen)
+				    int flags, const struct zsock_sockaddr *to,
+				    zsock_socklen_t tolen)
 {
 	if (to != NULL) {
 		errno = EOPNOTSUPP;
@@ -387,8 +387,8 @@ done:
 }

 static ssize_t eswifi_socket_recvfrom(void *obj, void *buf, size_t len,
-				      int flags, struct sockaddr *from,
-				      socklen_t *fromlen)
+				      int flags, struct zsock_sockaddr *from,
+				      zsock_socklen_t *fromlen)
 {
 	if (fromlen != NULL) {
 		errno = EOPNOTSUPP;
@@ -538,8 +538,8 @@ done:
 	return 1;
 }

-static int eswifi_socket_bind(void *obj, const struct sockaddr *addr,
-			      socklen_t addrlen)
+static int eswifi_socket_bind(void *obj, const struct zsock_sockaddr *addr,
+			      zsock_socklen_t addrlen)
 {
 	intptr_t sock = OBJ_TO_SD(obj);
 	struct eswifi_off_socket *socket;
@@ -563,12 +563,12 @@ static bool eswifi_socket_is_supported(int family, int type, int proto)
 	enum eswifi_transport_type eswifi_socket_type;
 	int err;

-	if (family != AF_INET) {
+	if (family != ZSOCK_AF_INET) {
 		return false;
 	}

-	if (type != SOCK_DGRAM &&
-	    type != SOCK_STREAM) {
+	if (type != ZSOCK_SOCK_DGRAM &&
+	    type != ZSOCK_SOCK_STREAM) {
 		return false;
 	}

@@ -657,7 +657,7 @@ static const struct socket_op_vtable eswifi_socket_fd_op_vtable = {
 };

 #ifdef CONFIG_NET_SOCKETS_OFFLOAD
-NET_SOCKET_OFFLOAD_REGISTER(eswifi, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, AF_UNSPEC,
+NET_SOCKET_OFFLOAD_REGISTER(eswifi, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, ZSOCK_AF_UNSPEC,
 			    eswifi_socket_is_supported, eswifi_socket_create);
 #endif

@@ -665,7 +665,7 @@ static int eswifi_off_getaddrinfo(const char *node, const char *service,
 				  const struct zsock_addrinfo *hints,
 				  struct zsock_addrinfo **res)
 {
-	struct sockaddr_in *ai_addr;
+	struct zsock_sockaddr_in *ai_addr;
 	struct zsock_addrinfo *ai;
 	unsigned long port = 0;
 	char *rsp;
@@ -686,7 +686,7 @@ static int eswifi_off_getaddrinfo(const char *node, const char *service,
 		return DNS_EAI_NONAME;
 	}

-	if (hints && hints->ai_family != AF_INET) {
+	if (hints && hints->ai_family != ZSOCK_AF_INET) {
 		return DNS_EAI_FAIL;
 	}

@@ -716,14 +716,14 @@ static int eswifi_off_getaddrinfo(const char *node, const char *service,
 		goto done_unlock;
 	}

-	ai->ai_family = AF_INET;
-	ai->ai_socktype = hints ? hints->ai_socktype : SOCK_STREAM;
-	ai->ai_protocol = ai->ai_socktype == SOCK_STREAM ? IPPROTO_TCP : IPPROTO_UDP;
+	ai->ai_family = ZSOCK_AF_INET;
+	ai->ai_socktype = hints ? hints->ai_socktype : ZSOCK_SOCK_STREAM;
+	ai->ai_protocol = ai->ai_socktype == ZSOCK_SOCK_STREAM ? ZSOCK_IPPROTO_TCP : ZSOCK_IPPROTO_UDP;

 	ai_addr->sin_family = ai->ai_family;
-	ai_addr->sin_port = htons(port);
+	ai_addr->sin_port = zsock_htons(port);

-	if (!net_ipaddr_parse(rsp, strlen(rsp), (struct sockaddr *)ai_addr)) {
+	if (!net_ipaddr_parse(rsp, strlen(rsp), (struct zsock_sockaddr *)ai_addr)) {
 		free(ai_addr);
 		free(*res);
 		err = DNS_EAI_FAIL;
@@ -731,7 +731,7 @@ static int eswifi_off_getaddrinfo(const char *node, const char *service,
 	}

 	ai->ai_addrlen = sizeof(*ai_addr);
-	ai->ai_addr = (struct sockaddr *)ai_addr;
+	ai->ai_addr = (struct zsock_sockaddr *)ai_addr;
 	err = 0;

 done_unlock:
diff --git a/drivers/wifi/infineon/airoc_wifi.c b/drivers/wifi/infineon/airoc_wifi.c
index 7604b82b66e..a816d6a7f70 100644
--- a/drivers/wifi/infineon/airoc_wifi.c
+++ b/drivers/wifi/infineon/airoc_wifi.c
@@ -390,7 +390,7 @@ static void airoc_wifi_network_process_ethernet_data(whd_interface_t interface,

 	if ((airoc_wifi_iface != NULL) && net_if_flag_is_set(airoc_wifi_iface, NET_IF_UP)) {

-		pkt = net_pkt_rx_alloc_with_buffer(airoc_wifi_iface, len, AF_UNSPEC, 0, K_NO_WAIT);
+		pkt = net_pkt_rx_alloc_with_buffer(airoc_wifi_iface, len, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);

 		if (pkt != NULL) {
 			if (net_pkt_write(pkt, data, len) < 0) {
diff --git a/drivers/wifi/nrf_wifi/src/net_if.c b/drivers/wifi/nrf_wifi/src/net_if.c
index 0662c6c80d0..d6ad01b75b3 100644
--- a/drivers/wifi/nrf_wifi/src/net_if.c
+++ b/drivers/wifi/nrf_wifi/src/net_if.c
@@ -319,7 +319,7 @@ static bool is_eapol(struct net_pkt *pkt)
 	uint16_t ethertype;

 	hdr = NET_ETH_HDR(pkt);
-	ethertype = ntohs(hdr->type);
+	ethertype = zsock_ntohs(hdr->type);

 	return ethertype == NET_ETH_PTYPE_EAPOL;
 }
@@ -515,10 +515,10 @@ static void ip_maddr_event_handler(struct net_if *iface,
 	}

 	switch (addr->family) {
-	case AF_INET:
+	case ZSOCK_AF_INET:
 		net_eth_ipv4_mcast_to_mac_addr(&addr->in_addr, &mac_addr);
 		break;
-	case AF_INET6:
+	case ZSOCK_AF_INET6:
 		net_eth_ipv6_mcast_to_mac_addr(&addr->in6_addr, &mac_addr);
 		break;
 	default:
diff --git a/drivers/wifi/nxp/nxp_wifi_drv.c b/drivers/wifi/nxp/nxp_wifi_drv.c
index eaac669a53a..cb07905abd3 100644
--- a/drivers/wifi/nxp/nxp_wifi_drv.c
+++ b/drivers/wifi/nxp/nxp_wifi_drv.c
@@ -107,9 +107,9 @@ int nxp_wifi_wlan_event_callback(enum wlan_event_reason reason, void *data)
 	static int auth_fail;
 #ifdef CONFIG_NXP_WIFI_SOFTAP_SUPPORT
 	wlan_uap_client_disassoc_t *disassoc_resp = data;
-	struct in_addr dhcps_addr4;
-	struct in_addr base_addr;
-	struct in_addr netmask_addr;
+	struct zsock_in_addr dhcps_addr4;
+	struct zsock_in_addr base_addr;
+	struct zsock_in_addr netmask_addr;
 	struct wifi_ap_sta_info ap_sta_info = { 0 };
 #endif

@@ -260,21 +260,21 @@ int nxp_wifi_wlan_event_callback(enum wlan_event_reason reason, void *data)

 		LOG_DBG("Soft AP \"%s\" started successfully", uap_ssid);
 #endif
-		if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &dhcps_addr4) < 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &dhcps_addr4) < 0) {
 			LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS");
 			return 0;
 		}
 		net_if_ipv4_addr_add(g_uap.netif, &dhcps_addr4, NET_ADDR_MANUAL, 0);
 		net_if_ipv4_set_gw(g_uap.netif, &dhcps_addr4);

-		if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_MASK, &netmask_addr) < 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_MASK, &netmask_addr) < 0) {
 			LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_MASK");
 			return 0;
 		}
 		net_if_ipv4_set_netmask_by_addr(g_uap.netif, &dhcps_addr4, &netmask_addr);
 		net_if_dormant_off(g_uap.netif);

-		if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_BASE, &base_addr) < 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_BASE, &base_addr) < 0) {
 			LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_BASE");
 			return 0;
 		}
@@ -353,7 +353,7 @@ int nxp_wifi_wlan_event_callback(enum wlan_event_reason reason, void *data)
 		net_if_dormant_on(g_uap.netif);
 		LOG_DBG("WLAN: UAP Stopped");

-		if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &dhcps_addr4) < 0) {
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &dhcps_addr4) < 0) {
 			LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS");
 		} else {
 			net_if_ipv4_addr_rm(g_uap.netif, &dhcps_addr4);
@@ -674,13 +674,13 @@ static int nxp_wifi_start_ap(const struct device *dev, struct wifi_connect_req_p
 		return -EAGAIN;
 	}

-	if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &ap_addr4->address) < 0) {
+	if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS, &ap_addr4->address) < 0) {
 		LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_ADDRESS");
 		return -ENOENT;
 	}
 	ap_addr4->gw = ap_addr4->address;

-	if (net_addr_pton(AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_MASK, &ap_addr4->netmask) < 0) {
+	if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NXP_WIFI_SOFTAP_IP_MASK, &ap_addr4->netmask) < 0) {
 		LOG_ERR("Invalid CONFIG_NXP_WIFI_SOFTAP_IP_MASK");
 		return -ENOENT;
 	}
diff --git a/drivers/wifi/simplelink/simplelink.c b/drivers/wifi/simplelink/simplelink.c
index c4af70ce003..03a99fa83b1 100644
--- a/drivers/wifi/simplelink/simplelink.c
+++ b/drivers/wifi/simplelink/simplelink.c
@@ -192,7 +192,7 @@ static int simplelink_mgmt_disconnect(const struct device *dev)
 	return ret ? -EIO : ret;
 }

-static int simplelink_dummy_get(sa_family_t family,
+static int simplelink_dummy_get(zsock_sa_family_t family,
 				enum net_sock_type type,
 				enum net_ip_protocol ip_proto,
 				struct net_context **context)
diff --git a/drivers/wifi/simplelink/simplelink_sockets.c b/drivers/wifi/simplelink/simplelink_sockets.c
index ab7bcba7032..ccbd447ab2a 100644
--- a/drivers/wifi/simplelink/simplelink_sockets.c
+++ b/drivers/wifi/simplelink/simplelink_sockets.c
@@ -33,8 +33,8 @@ LOG_MODULE_DECLARE(LOG_MODULE_NAME);
 #define SD_TO_OBJ(sd) ((void *)(sd + 1))
 #define OBJ_TO_SD(obj) (((int)obj) - 1)

-static int simplelink_socket_accept(void *obj, struct sockaddr *addr,
-			     socklen_t *addrlen);
+static int simplelink_socket_accept(void *obj, struct zsock_sockaddr *addr,
+			     zsock_socklen_t *addrlen);

 /*
  * Convert SL error codes into BSD errno values
@@ -189,10 +189,10 @@ static int getErrno(_i32 error)
 static int simplelink_socket_family_from_posix(int family, int *family_sl)
 {
 	switch (family) {
-	case AF_INET:
+	case ZSOCK_AF_INET:
 		*family_sl = SL_AF_INET;
 		break;
-	case AF_INET6:
+	case ZSOCK_AF_INET6:
 		*family_sl = SL_AF_INET6;
 		break;
 	default:
@@ -205,13 +205,13 @@ static int simplelink_socket_family_from_posix(int family, int *family_sl)
 static int simplelink_socket_type_from_posix(int type, int *type_sl)
 {
 	switch (type) {
-	case SOCK_STREAM:
+	case ZSOCK_SOCK_STREAM:
 		*type_sl = SL_SOCK_STREAM;
 		break;
-	case SOCK_DGRAM:
+	case ZSOCK_SOCK_DGRAM:
 		*type_sl = SL_SOCK_DGRAM;
 		break;
-	case SOCK_RAW:
+	case ZSOCK_SOCK_RAW:
 		*type_sl = SL_SOCK_RAW;
 		break;
 	default:
@@ -230,10 +230,10 @@ static int simplelink_socket_proto_from_zephyr(int proto, int *proto_sl)
 		return -EPROTONOSUPPORT;
 	} else {
 		switch (proto) {
-		case IPPROTO_TCP:
+		case ZSOCK_IPPROTO_TCP:
 			*proto_sl = SL_IPPROTO_TCP;
 			break;
-		case IPPROTO_UDP:
+		case ZSOCK_IPPROTO_UDP:
 			*proto_sl = SL_IPPROTO_UDP;
 			break;
 		default:
@@ -318,17 +318,17 @@ static int simplelink_close(void *obj)
 	return retval;
 }

-static SlSockAddr_t *translate_z_to_sl_addrlen(socklen_t addrlen,
+static SlSockAddr_t *translate_z_to_sl_addrlen(zsock_socklen_t addrlen,
 					       SlSockAddrIn_t *sl_addr_in,
 					       SlSockAddrIn6_t *sl_addr_in6,
 					       SlSocklen_t *sl_addrlen)
 {
 	SlSockAddr_t *sl_addr = NULL;

-	if (addrlen == sizeof(struct sockaddr_in)) {
+	if (addrlen == sizeof(struct zsock_sockaddr_in)) {
 		*sl_addrlen = sizeof(SlSockAddrIn_t);
 		sl_addr = (SlSockAddr_t *)sl_addr_in;
-	} else if (addrlen == sizeof(struct sockaddr_in6)) {
+	} else if (addrlen == sizeof(struct zsock_sockaddr_in6)) {
 		*sl_addrlen = sizeof(SlSockAddrIn6_t);
 		sl_addr = (SlSockAddr_t *)sl_addr_in6;
 	}
@@ -336,16 +336,16 @@ static SlSockAddr_t *translate_z_to_sl_addrlen(socklen_t addrlen,
 	return sl_addr;
 }

-static SlSockAddr_t *translate_z_to_sl_addrs(const struct sockaddr *addr,
-					     socklen_t addrlen,
+static SlSockAddr_t *translate_z_to_sl_addrs(const struct zsock_sockaddr *addr,
+					     zsock_socklen_t addrlen,
 					     SlSockAddrIn_t *sl_addr_in,
 					     SlSockAddrIn6_t *sl_addr_in6,
 					     SlSocklen_t *sl_addrlen)
 {
 	SlSockAddr_t *sl_addr = NULL;

-	if (addrlen == sizeof(struct sockaddr_in)) {
-		struct sockaddr_in *z_sockaddr_in = (struct sockaddr_in *)addr;
+	if (addrlen == sizeof(struct zsock_sockaddr_in)) {
+		struct zsock_sockaddr_in *z_sockaddr_in = (struct zsock_sockaddr_in *)addr;

 		*sl_addrlen = sizeof(SlSockAddrIn_t);
 		sl_addr_in->sin_family = SL_AF_INET;
@@ -354,9 +354,9 @@ static SlSockAddr_t *translate_z_to_sl_addrs(const struct sockaddr *addr,
 			z_sockaddr_in->sin_addr.s_addr;

 		sl_addr = (SlSockAddr_t *)sl_addr_in;
-	} else if (addrlen == sizeof(struct sockaddr_in6)) {
-		struct sockaddr_in6 *z_sockaddr_in6 =
-			(struct sockaddr_in6 *)addr;
+	} else if (addrlen == sizeof(struct zsock_sockaddr_in6)) {
+		struct zsock_sockaddr_in6 *z_sockaddr_in6 =
+			(struct zsock_sockaddr_in6 *)addr;

 		*sl_addrlen = sizeof(SlSockAddrIn6_t);
 		sl_addr_in6->sin6_family = SL_AF_INET6;
@@ -373,48 +373,48 @@ static SlSockAddr_t *translate_z_to_sl_addrs(const struct sockaddr *addr,

 static void translate_sl_to_z_addr(SlSockAddr_t *sl_addr,
 				   SlSocklen_t sl_addrlen,
-				   struct sockaddr *addr,
-				   socklen_t *addrlen)
+				   struct zsock_sockaddr *addr,
+				   zsock_socklen_t *addrlen)
 {
 	SlSockAddrIn_t *sl_addr_in;
 	SlSockAddrIn6_t *sl_addr_in6;

 	if (sl_addr->sa_family == SL_AF_INET) {
 		if (sl_addrlen == (SlSocklen_t)sizeof(SlSockAddrIn_t)) {
-			struct sockaddr_in *z_sockaddr_in =
-				(struct sockaddr_in *)addr;
+			struct zsock_sockaddr_in *z_sockaddr_in =
+				(struct zsock_sockaddr_in *)addr;

 			sl_addr_in = (SlSockAddrIn_t *)sl_addr;
-			z_sockaddr_in->sin_family = AF_INET;
+			z_sockaddr_in->sin_family = ZSOCK_AF_INET;
 			z_sockaddr_in->sin_port = sl_addr_in->sin_port;
 			z_sockaddr_in->sin_addr.s_addr =
 				sl_addr_in->sin_addr.s_addr;
-			*addrlen = sizeof(struct sockaddr_in);
+			*addrlen = sizeof(struct zsock_sockaddr_in);
 		} else {
 			*addrlen = sl_addrlen;
 		}
 	} else if (sl_addr->sa_family == SL_AF_INET6) {
 		if (sl_addrlen == sizeof(SlSockAddrIn6_t)) {
-			struct sockaddr_in6 *z_sockaddr_in6 =
-				(struct sockaddr_in6 *)addr;
+			struct zsock_sockaddr_in6 *z_sockaddr_in6 =
+				(struct zsock_sockaddr_in6 *)addr;
 			sl_addr_in6 = (SlSockAddrIn6_t *)sl_addr;

-			z_sockaddr_in6->sin6_family = AF_INET6;
+			z_sockaddr_in6->sin6_family = ZSOCK_AF_INET6;
 			z_sockaddr_in6->sin6_port = sl_addr_in6->sin6_port;
 			z_sockaddr_in6->sin6_scope_id =
 				(uint8_t)sl_addr_in6->sin6_scope_id;
 			memcpy(z_sockaddr_in6->sin6_addr.s6_addr,
 			       sl_addr_in6->sin6_addr._S6_un._S6_u32,
 			       sizeof(z_sockaddr_in6->sin6_addr.s6_addr));
-			*addrlen = sizeof(struct sockaddr_in6);
+			*addrlen = sizeof(struct zsock_sockaddr_in6);
 		} else {
 			*addrlen = sl_addrlen;
 		}
 	}
 }

-static int simplelink_accept(void *obj, struct sockaddr *addr,
-			     socklen_t *addrlen)
+static int simplelink_accept(void *obj, struct zsock_sockaddr *addr,
+			     zsock_socklen_t *addrlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	int retval;
@@ -452,8 +452,8 @@ exit:
 	return retval;
 }

-static int simplelink_bind(void *obj, const struct sockaddr *addr,
-			   socklen_t addrlen)
+static int simplelink_bind(void *obj, const struct zsock_sockaddr *addr,
+			   zsock_socklen_t addrlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	int retval;
@@ -500,8 +500,8 @@ static int simplelink_listen(void *obj, int backlog)
 	return retval;
 }

-static int simplelink_connect(void *obj, const struct sockaddr *addr,
-			      socklen_t addrlen)
+static int simplelink_connect(void *obj, const struct zsock_sockaddr *addr,
+			      zsock_socklen_t addrlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	int retval;
@@ -647,7 +647,7 @@ exit:
  * map to SimpleLink values, then set stored filenames
  * via SimpleLink's sl_SetSockOpt()
  */
-static int map_credentials(int sd, const void *optval, socklen_t optlen)
+static int map_credentials(int sd, const void *optval, zsock_socklen_t optlen)
 {
 	sec_tag_t *sec_tags = (sec_tag_t *)optval;
 	int retval = 0;
@@ -703,7 +703,7 @@ exit:
 	return retval;
 }
 #else
-static int map_credentials(int sd, const void *optval, socklen_t optlen)
+static int map_credentials(int sd, const void *optval, zsock_socklen_t optlen)
 {
 	return 0;
 }
@@ -713,7 +713,7 @@ static int map_credentials(int sd, const void *optval, socklen_t optlen)
 #define _SEC_DOMAIN_VERIF SL_SO_SECURE_DOMAIN_NAME_VERIFICATION

 static int simplelink_setsockopt(void *obj, int level, int optname,
-				 const void *optval, socklen_t optlen)
+				 const void *optval, zsock_socklen_t optlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	int retval;
@@ -767,13 +767,13 @@ static int simplelink_setsockopt(void *obj, int level, int optname,
 			goto exit;
 		}
 	} else {
-		/* Can be SOL_SOCKET or TI specific: */
+		/* Can be ZSOCK_SOL_SOCKET or TI specific: */

 		/* Note: this logic should match SimpleLink SDK's socket.c: */
 		switch (optname) {
-		case TCP_NODELAY:
+		case ZSOCK_TCP_NODELAY:
 			if (optval) {
-				/* if user wishes to have TCP_NODELAY = FALSE,
+				/* if user wishes to have ZSOCK_TCP_NODELAY = FALSE,
 				 * we return EINVAL and fail in the cases below.
 				 */
 				if (*(uint32_t *)optval) {
@@ -786,9 +786,9 @@ static int simplelink_setsockopt(void *obj, int level, int optname,
 			 * EINVAL in order to not break "off-the-shelf" BSD
 			 * code.
 			 */
-		case SO_BROADCAST:
-		case SO_REUSEADDR:
-		case SO_SNDBUF:
+		case ZSOCK_SO_BROADCAST:
+		case ZSOCK_SO_REUSEADDR:
+		case ZSOCK_SO_SNDBUF:
 			retval = slcb_SetErrno(EINVAL);
 			goto exit;
 		default:
@@ -807,7 +807,7 @@ exit:
 }

 static int simplelink_getsockopt(void *obj, int level, int optname,
-				 void *optval, socklen_t *optlen)
+				 void *optval, zsock_socklen_t *optlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	int retval;
@@ -826,12 +826,12 @@ static int simplelink_getsockopt(void *obj, int level, int optname,
 			goto exit;
 		}
 	} else {
-		/* Can be SOL_SOCKET or TI specific: */
+		/* Can be ZSOCK_SOL_SOCKET or TI specific: */

 		/* Note: this logic should match SimpleLink SDK's socket.c: */
 		switch (optname) {
-			/* TCP_NODELAY always set by the NWP, so return True */
-		case TCP_NODELAY:
+			/* ZSOCK_TCP_NODELAY always set by the NWP, so return True */
+		case ZSOCK_TCP_NODELAY:
 			if (optval) {
 				(*(_u32 *)optval) = TRUE;
 				retval = 0;
@@ -842,9 +842,9 @@ static int simplelink_getsockopt(void *obj, int level, int optname,
 			 * errno to EINVAL in order to not break "off-the-shelf"
 			 * BSD code.
 			 */
-		case SO_BROADCAST:
-		case SO_REUSEADDR:
-		case SO_SNDBUF:
+		case ZSOCK_SO_BROADCAST:
+		case ZSOCK_SO_REUSEADDR:
+		case ZSOCK_SO_SNDBUF:
 			retval = slcb_SetErrno(EINVAL);
 			goto exit;
 		default:
@@ -864,7 +864,7 @@ exit:

 /* SimpleLink does not support flags in recv.
  * However, to enable more Zephyr apps to use this socket_offload, rather than
- * failing with ENOTSUP, we can closely emulate the MSG_DONTWAIT feature using
+ * failing with ENOTSUP, we can closely emulate the ZSOCK_MSG_DONTWAIT feature using
  * SimpleLink socket options.
  */
 static int handle_recv_flags(int sd, int flags, bool set, int *nb_enabled)
@@ -903,7 +903,7 @@ static int handle_recv_flags(int sd, int flags, bool set, int *nb_enabled)
 }

 static ssize_t simplelink_recvfrom(void *obj, void *buf, size_t len, int flags,
-				   struct sockaddr *from, socklen_t *fromlen)
+				   struct zsock_sockaddr *from, zsock_socklen_t *fromlen)
 {
 	int sd = OBJ_TO_SD(obj);
 	ssize_t retval;
@@ -949,8 +949,8 @@ static ssize_t simplelink_recvfrom(void *obj, void *buf, size_t len, int flags,
 }

 static ssize_t simplelink_sendto(void *obj, const void *buf, size_t len,
-				 int flags, const struct sockaddr *to,
-				 socklen_t tolen)
+				 int flags, const struct zsock_sockaddr *to,
+				 zsock_socklen_t tolen)
 {
 	int sd = OBJ_TO_SD(obj);
 	ssize_t retval;
@@ -983,7 +983,7 @@ exit:
 	return retval;
 }

-static ssize_t simplelink_sendmsg(void *obj, const struct msghdr *msg,
+static ssize_t simplelink_sendmsg(void *obj, const struct zsock_msghdr *msg,
 				  int flags)
 {
 	errno = -ENOTSUP;
@@ -995,7 +995,7 @@ static int set_addr_info(const struct SlNetUtil_addrInfo_t *sl_ai,
 			 struct zsock_addrinfo **res)
 {
 	struct zsock_addrinfo *ai;
-	struct sockaddr *ai_addr;
+	struct zsock_sockaddr *ai_addr;
 	int retval = 0;

 	ai = calloc(1, sizeof(struct zsock_addrinfo));
@@ -1004,7 +1004,7 @@ static int set_addr_info(const struct SlNetUtil_addrInfo_t *sl_ai,
 		goto exit;
 	} else {
 		/* Now, alloc the embedded sockaddr struct: */
-		ai_addr = calloc(1, sizeof(struct sockaddr));
+		ai_addr = calloc(1, sizeof(struct zsock_sockaddr));
 		if (!ai_addr) {
 			retval = DNS_EAI_MEMORY;
 			free(ai);
@@ -1013,21 +1013,21 @@ static int set_addr_info(const struct SlNetUtil_addrInfo_t *sl_ai,
 	}

 	/* Now, fill in the fields of res (addrinfo struct): */
-	ai->ai_family = (sl_ai->ai_family == SL_AF_INET6 ? AF_INET6 : AF_INET);
+	ai->ai_family = (sl_ai->ai_family == SL_AF_INET6 ? ZSOCK_AF_INET6 : ZSOCK_AF_INET);
 	ai->ai_socktype = (sl_ai->ai_socktype == SLNETSOCK_SOCK_DGRAM ?
-		SOCK_DGRAM : SOCK_STREAM);
+		ZSOCK_SOCK_DGRAM : ZSOCK_SOCK_STREAM);
 	ai->ai_protocol = (sl_ai->ai_protocol == SLNETSOCK_PROTO_UDP ?
-		IPPROTO_UDP : IPPROTO_TCP);
+		ZSOCK_IPPROTO_UDP : ZSOCK_IPPROTO_TCP);

 	/* Fill sockaddr struct fields based on family: */
-	if (ai->ai_family == AF_INET) {
+	if (ai->ai_family == ZSOCK_AF_INET) {
 		SlNetSock_AddrIn_t *sl_addr =
 			(SlNetSock_AddrIn_t *)sl_ai->ai_addr;

 		net_sin(ai_addr)->sin_family = ai->ai_family;
 		net_sin(ai_addr)->sin_addr.s_addr = sl_addr->sin_addr.s_addr;
 		net_sin(ai_addr)->sin_port = sl_addr->sin_port;
-		ai->ai_addrlen = sizeof(struct sockaddr_in);
+		ai->ai_addrlen = sizeof(struct zsock_sockaddr_in);
 	} else {
 		SlNetSock_AddrIn6_t *sl_addr =
 			(SlNetSock_AddrIn6_t *)sl_ai->ai_addr;
@@ -1042,7 +1042,7 @@ static int set_addr_info(const struct SlNetUtil_addrInfo_t *sl_ai,
 		net_sin6(ai_addr)->sin6_addr.s6_addr32[3] =
 			sl_addr->sin6_addr._S6_un._S6_u32[3];
 		net_sin6(ai_addr)->sin6_port = sl_addr->sin6_port;
-		ai->ai_addrlen = sizeof(struct sockaddr_in6);
+		ai->ai_addrlen = sizeof(struct zsock_sockaddr_in6);
 	}
 	ai->ai_addr = ai_addr;
 	ai->ai_next = *res;
@@ -1071,31 +1071,31 @@ static int simplelink_getaddrinfo(const char *node, const char *service,

 	if (hints) {
 		/*
-		 * SlNetUtil only supports AI_NUMERICHOST and AI_PASSIVE, so
+		 * SlNetUtil only supports ZSOCK_AI_NUMERICHOST and ZSOCK_AI_PASSIVE, so
 		 * the rest are ignored.
 		 */
-		sl_hints.ai_flags |= ((hints->ai_flags & AI_PASSIVE) ?
+		sl_hints.ai_flags |= ((hints->ai_flags & ZSOCK_AI_PASSIVE) ?
 			SLNETUTIL_AI_PASSIVE : 0);
-		sl_hints.ai_flags |= ((hints->ai_flags & AI_NUMERICHOST) ?
+		sl_hints.ai_flags |= ((hints->ai_flags & ZSOCK_AI_NUMERICHOST) ?
 			SLNETUTIL_AI_NUMERICHOST : 0);
-		if (hints->ai_family == AF_UNSPEC) {
+		if (hints->ai_family == ZSOCK_AF_UNSPEC) {
 			sl_hints.ai_family = SLNETSOCK_AF_UNSPEC;
 		} else {
-			sl_hints.ai_family = (hints->ai_family == AF_INET6 ?
+			sl_hints.ai_family = (hints->ai_family == ZSOCK_AF_INET6 ?
 				SLNETSOCK_AF_INET6 : SLNETSOCK_AF_INET);
 		}
 		if (hints->ai_socktype == 0) {
 			sl_hints.ai_socktype = 0;
 		} else {
 			sl_hints.ai_socktype =
-				(hints->ai_socktype == SOCK_DGRAM ?
+				(hints->ai_socktype == ZSOCK_SOCK_DGRAM ?
 				SLNETSOCK_SOCK_DGRAM : SLNETSOCK_SOCK_STREAM);
 		}
 		if (hints->ai_protocol == 0) {
 			sl_hints.ai_protocol = 0;
 		} else {
 			sl_hints.ai_protocol =
-				(hints->ai_protocol == IPPROTO_UDP ?
+				(hints->ai_protocol == ZSOCK_IPPROTO_UDP ?
 				SLNETSOCK_PROTO_UDP : SLNETSOCK_PROTO_TCP);
 		}

@@ -1280,8 +1280,8 @@ int simplelink_socket_create(int family, int type, int proto)
 	return fd;
 }

-static int simplelink_socket_accept(void *obj, struct sockaddr *addr,
-			     socklen_t *addrlen)
+static int simplelink_socket_accept(void *obj, struct zsock_sockaddr *addr,
+			     zsock_socklen_t *addrlen)
 {
 	int fd = zvfs_reserve_fd();
 	int sock;
@@ -1304,7 +1304,7 @@ static int simplelink_socket_accept(void *obj, struct sockaddr *addr,
 }

 #ifdef CONFIG_NET_SOCKETS_OFFLOAD
-NET_SOCKET_OFFLOAD_REGISTER(simplelink, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, AF_UNSPEC,
+NET_SOCKET_OFFLOAD_REGISTER(simplelink, CONFIG_NET_SOCKETS_OFFLOAD_PRIORITY, ZSOCK_AF_UNSPEC,
 			    simplelink_is_supported, simplelink_socket_create);
 #endif

diff --git a/drivers/wifi/simplelink/simplelink_support.c b/drivers/wifi/simplelink/simplelink_support.c
index dab0739a08b..162d48f7794 100644
--- a/drivers/wifi/simplelink/simplelink_support.c
+++ b/drivers/wifi/simplelink/simplelink_support.c
@@ -97,7 +97,7 @@ static int32_t configure_simplelink(void)
 	uint8_t power;

 #if defined(CONFIG_NET_IPV4) && defined(CONFIG_NET_CONFIG_MY_IPV4_ADDR)
-	struct in_addr addr4;
+	struct zsock_in_addr addr4;
 	SlNetCfgIpV4Args_t ipV4;

 	memset(&ipV4, 0, sizeof(ipV4));
@@ -141,7 +141,7 @@ static int32_t configure_simplelink(void)
 	ASSERT_ON_ERROR(retval, WLAN_ERROR);

 #if defined(CONFIG_NET_IPV4) && defined(CONFIG_NET_CONFIG_MY_IPV4_ADDR)
-	if (net_addr_pton(AF_INET, CONFIG_NET_CONFIG_MY_IPV4_ADDR, &addr4)
+	if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NET_CONFIG_MY_IPV4_ADDR, &addr4)
 			< 0) {
 		LOG_ERR("Invalid CONFIG_NET_CONFIG_MY_IPV4_ADDR");
 		return -1;
@@ -153,7 +153,7 @@ static int32_t configure_simplelink(void)

 #if defined(CONFIG_NET_CONFIG_MY_IPV4_GW)
 	if (strcmp(CONFIG_NET_CONFIG_MY_IPV4_GW, "") != 0) {
-		if (net_addr_pton(AF_INET, CONFIG_NET_CONFIG_MY_IPV4_GW,
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NET_CONFIG_MY_IPV4_GW,
 				  &addr4) < 0) {
 			LOG_ERR("Invalid CONFIG_NET_CONFIG_MY_IPV4_GW");
 			return -1;
@@ -167,7 +167,7 @@ static int32_t configure_simplelink(void)

 #if defined(CONFIG_NET_CONFIG_MY_IPV4_NETMASK)
 	if (strcmp(CONFIG_NET_CONFIG_MY_IPV4_NETMASK, "") != 0) {
-		if (net_addr_pton(AF_INET, CONFIG_NET_CONFIG_MY_IPV4_NETMASK,
+		if (net_addr_pton(ZSOCK_AF_INET, CONFIG_NET_CONFIG_MY_IPV4_NETMASK,
 				  &addr4) < 0) {
 			LOG_ERR("Invalid CONFIG_NET_CONFIG_MY_IPV4_NETMASK");
 			return -1;
diff --git a/drivers/wifi/siwx91x/siwx91x_wifi.c b/drivers/wifi/siwx91x/siwx91x_wifi.c
index b6d9c4bbc48..b7e321ac1d0 100644
--- a/drivers/wifi/siwx91x/siwx91x_wifi.c
+++ b/drivers/wifi/siwx91x/siwx91x_wifi.c
@@ -268,7 +268,7 @@ sl_status_t sl_si91x_host_process_data_frame(sl_wifi_interface_t interface,
 	struct net_pkt *pkt;
 	int ret;

-	pkt = net_pkt_rx_alloc_with_buffer(iface, buffer->length, AF_UNSPEC, 0, K_NO_WAIT);
+	pkt = net_pkt_rx_alloc_with_buffer(iface, buffer->length, ZSOCK_AF_UNSPEC, 0, K_NO_WAIT);
 	if (!pkt) {
 		LOG_ERR("net_pkt_rx_alloc_with_buffer() failed");
 		return SL_STATUS_FAIL;
diff --git a/drivers/wifi/siwx91x/siwx91x_wifi_socket.c b/drivers/wifi/siwx91x/siwx91x_wifi_socket.c
index b84a436051f..b727bf463c3 100644
--- a/drivers/wifi/siwx91x/siwx91x_wifi_socket.c
+++ b/drivers/wifi/siwx91x/siwx91x_wifi_socket.c
@@ -30,29 +30,29 @@ enum offloaded_net_if_types siwx91x_get_type(void)
 	return L2_OFFLOADED_NET_IF_TYPE_WIFI;
 }

-/* SiWx91x does not use the standard struct sockaddr (despite it uses the same
+/* SiWx91x does not use the standard struct zsock_sockaddr (despite it uses the same
  * name):
  *   - uses Little Endian for port number while Posix uses big endian
  *   - IPv6 addresses are bytes swapped
  * Note: this function allows to have in == out.
  */
-static void siwx91x_sockaddr_swap_bytes(struct sockaddr *out,
-					const struct sockaddr *in, socklen_t in_len)
+static void siwx91x_sockaddr_swap_bytes(struct zsock_sockaddr *out,
+					const struct zsock_sockaddr *in, zsock_socklen_t in_len)
 {
-	const struct sockaddr_in6 *in6 = (const struct sockaddr_in6 *)in;
-	struct sockaddr_in6 *out6 = (struct sockaddr_in6 *)out;
+	const struct zsock_sockaddr_in6 *in6 = (const struct zsock_sockaddr_in6 *)in;
+	struct zsock_sockaddr_in6 *out6 = (struct zsock_sockaddr_in6 *)out;

 	/* In Zephyr, size of sockaddr == size of sockaddr_storage
 	 * (while in Posix sockaddr is smaller than sockaddr_storage).
 	 */
 	memcpy(out, in, in_len);
-	if (in->sa_family == AF_INET6) {
+	if (in->sa_family == ZSOCK_AF_INET6) {
 		ARRAY_FOR_EACH(in6->sin6_addr.s6_addr32, i) {
-			out6->sin6_addr.s6_addr32[i] = ntohl(in6->sin6_addr.s6_addr32[i]);
+			out6->sin6_addr.s6_addr32[i] = zsock_ntohl(in6->sin6_addr.s6_addr32[i]);
 		}
-		out6->sin6_port = ntohs(in6->sin6_port);
-	} else if (in->sa_family == AF_INET) {
-		out6->sin6_port = ntohs(in6->sin6_port);
+		out6->sin6_port = zsock_ntohs(in6->sin6_port);
+	} else if (in->sa_family == ZSOCK_AF_INET) {
+		out6->sin6_port = zsock_ntohs(in6->sin6_port);
 	}
 }

@@ -62,7 +62,7 @@ void siwx91x_on_join_ipv4(struct siwx91x_dev *sidev)
 		.mode = SL_IP_MANAGEMENT_DHCP,
 		.type = SL_IPV4,
 	};
-	struct in_addr addr4 = { };
+	struct zsock_in_addr addr4 = { };
 	int ret;

 	if (!IS_ENABLED(CONFIG_NET_IPV4)) {
@@ -85,7 +85,7 @@ void siwx91x_on_join_ipv6(struct siwx91x_dev *sidev)
 		.mode = SL_IP_MANAGEMENT_DHCP,
 		.type = SL_IPV6,
 	};
-	struct in6_addr addr6 = { };
+	struct zsock_in6_addr addr6 = { };
 	int ret;

 	if (!IS_ENABLED(CONFIG_NET_IPV6)) {
@@ -98,7 +98,7 @@ void siwx91x_on_join_ipv6(struct siwx91x_dev *sidev)
 		return;
 	}
 	ARRAY_FOR_EACH(addr6.s6_addr32, i) {
-		addr6.s6_addr32[i] = ntohl(ip_config6.ip.v6.global_address.value[i]);
+		addr6.s6_addr32[i] = zsock_ntohl(ip_config6.ip.v6.global_address.value[i]);
 	}
 	/* SiWx91x already take care of DAD and sending ND is not
 	 * supported anyway.
@@ -166,7 +166,7 @@ static void siwx91x_sock_on_recv(sl_si91x_fdset_t *read_fd, sl_si91x_fdset_t *wr
 			siwx91x_sock_on_recv);
 }

-static int siwx91x_sock_get(sa_family_t family, enum net_sock_type type,
+static int siwx91x_sock_get(zsock_sa_family_t family, enum net_sock_type type,
 			    enum net_ip_protocol ip_proto, struct net_context **context)
 {
 	struct siwx91x_dev *sidev = net_if_get_first_wifi()->if_dev->dev->data;
@@ -199,18 +199,18 @@ static int siwx91x_sock_put(struct net_context *context)
 }

 static int siwx91x_sock_bind(struct net_context *context,
-			     const struct sockaddr *addr, socklen_t addrlen)
+			     const struct zsock_sockaddr *addr, zsock_socklen_t addrlen)
 {
 	struct siwx91x_dev *sidev = net_context_get_iface(context)->if_dev->dev->data;
 	int sockfd = (int)context->offload_context;
-	struct sockaddr addr_le;
+	struct zsock_sockaddr addr_le;
 	int ret;

 	/* Zephyr tends to call bind() even if the TCP socket is a client. 917
 	 * return an error in this case.
 	 */
-	if (net_context_get_proto(context) == IPPROTO_TCP &&
-	    !((struct sockaddr_in *)addr)->sin_port) {
+	if (net_context_get_proto(context) == ZSOCK_IPPROTO_TCP &&
+	    !((struct zsock_sockaddr_in *)addr)->sin_port) {
 		return 0;
 	}
 	siwx91x_sockaddr_swap_bytes(&addr_le, addr, addrlen);
@@ -219,7 +219,7 @@ static int siwx91x_sock_bind(struct net_context *context,
 		return -errno;
 	}
 	/* WiseConnect refuses to run select on TCP listening sockets */
-	if (net_context_get_proto(context) == IPPROTO_UDP) {
+	if (net_context_get_proto(context) == ZSOCK_IPPROTO_UDP) {
 		SL_SI91X_FD_SET(sockfd, &sidev->fds_watch);
 		sl_si91x_select(SLI_NUMBER_OF_SOCKETS, &sidev->fds_watch, NULL, NULL, NULL,
 				siwx91x_sock_on_recv);
@@ -228,12 +228,12 @@ static int siwx91x_sock_bind(struct net_context *context,
 }

 static int siwx91x_sock_connect(struct net_context *context,
-				const struct sockaddr *addr, socklen_t addrlen,
+				const struct zsock_sockaddr *addr, zsock_socklen_t addrlen,
 				net_context_connect_cb_t cb, int32_t timeout, void *user_data)
 {
 	struct siwx91x_dev *sidev = net_context_get_iface(context)->if_dev->dev->data;
 	int sockfd = (int)context->offload_context;
-	struct sockaddr addr_le;
+	struct zsock_sockaddr addr_le;
 	int ret;

 	/* sl_si91x_connect() always return immediately, so we ignore timeout */
@@ -271,7 +271,7 @@ static int siwx91x_sock_accept(struct net_context *context,
 	struct siwx91x_dev *sidev = net_context_get_iface(context)->if_dev->dev->data;
 	int sockfd = (int)context->offload_context;
 	struct net_context *newcontext;
-	struct sockaddr addr_le;
+	struct zsock_sockaddr addr_le;
 	int ret;

 	/* TODO: support timeout != K_FOREVER */
@@ -311,12 +311,12 @@ static int siwx91x_sock_accept(struct net_context *context,
 }

 static int siwx91x_sock_sendto(struct net_pkt *pkt,
-			       const struct sockaddr *addr, socklen_t addrlen,
+			       const struct zsock_sockaddr *addr, zsock_socklen_t addrlen,
 			       net_context_send_cb_t cb, int32_t timeout, void *user_data)
 {
 	struct net_context *context = pkt->context;
 	int sockfd = (int)context->offload_context;
-	struct sockaddr addr_le;
+	struct zsock_sockaddr addr_le;
 	struct net_buf *buf;
 	int ret;

diff --git a/drivers/wifi/winc1500/wifi_winc1500.c b/drivers/wifi/winc1500/wifi_winc1500.c
index b0788a04c56..c1e0379a5d9 100644
--- a/drivers/wifi/winc1500/wifi_winc1500.c
+++ b/drivers/wifi/winc1500/wifi_winc1500.c
@@ -43,17 +43,17 @@ typedef void (*tpfAppResolveCb) (uint8 *pu8DomainName, uint32 u32ServerIP);
 NMI_API void registerSocketCallback(tpfAppSocketCb socket_cb,
 				    tpfAppResolveCb resolve_cb);
 NMI_API SOCKET winc1500_socket(uint16 u16Domain, uint8 u8Type, uint8 u8Flags);
-NMI_API sint8 winc1500_socket_bind(SOCKET sock, struct sockaddr *pstrAddr, uint8 u8AddrLen);
+NMI_API sint8 winc1500_socket_bind(SOCKET sock, struct zsock_sockaddr *pstrAddr, uint8 u8AddrLen);
 NMI_API sint8 winc1500_socket_listen(SOCKET sock, uint8 backlog);
-NMI_API sint8 winc1500_socket_accept(SOCKET sock, struct sockaddr *addr, uint8 *addrlen);
-NMI_API sint8 winc1500_socket_connect(SOCKET sock, struct sockaddr *pstrAddr, uint8 u8AddrLen);
+NMI_API sint8 winc1500_socket_accept(SOCKET sock, struct zsock_sockaddr *addr, uint8 *addrlen);
+NMI_API sint8 winc1500_socket_connect(SOCKET sock, struct zsock_sockaddr *pstrAddr, uint8 u8AddrLen);
 NMI_API sint16 winc1500_socket_recv(SOCKET sock, void *pvRecvBuf,
 		    uint16 u16BufLen, uint32 u32Timeoutmsec);
 NMI_API sint16 winc1500_socket_send(SOCKET sock, void *pvSendBuffer,
 		    uint16 u16SendLength, uint16 u16Flags);
 NMI_API sint16 winc1500_socket_sendto(SOCKET sock, void *pvSendBuffer,
 		      uint16 u16SendLength, uint16 flags,
-		      struct sockaddr *pstrDestAddr, uint8 u8AddrLen);
+		      struct zsock_sockaddr *pstrDestAddr, uint8 u8AddrLen);
 NMI_API sint8 winc1500_close(SOCKET sock);

 enum socket_errors {
@@ -93,7 +93,7 @@ typedef struct {

 typedef struct {
 	SOCKET			sock;
-	struct sockaddr_in	strAddr;
+	struct zsock_sockaddr_in	strAddr;
 } tstrSocketAcceptMsg;

 typedef struct {
@@ -105,7 +105,7 @@ typedef struct {
 	uint8			*pu8Buffer;
 	sint16			s16BufferSize;
 	uint16			u16RemainingSize;
-	struct sockaddr_in	strRemoteAddr;
+	struct zsock_sockaddr_in	strRemoteAddr;
 } tstrSocketRecvMsg;

 #include <driver/include/m2m_wifi.h>
@@ -289,7 +289,7 @@ static char *socket_message_to_string(uint8_t message)
 /**
  * This function is called when the socket is to be opened.
  */
-static int winc1500_get(sa_family_t family,
+static int winc1500_get(zsock_sa_family_t family,
 			enum net_sock_type type,
 			enum net_ip_protocol ip_proto,
 			struct net_context **context)
@@ -297,13 +297,13 @@ static int winc1500_get(sa_family_t family,
 	struct socket_data *sd;
 	SOCKET sock;

-	if (family != AF_INET) {
-		LOG_ERR("Only AF_INET is supported!");
+	if (family != ZSOCK_AF_INET) {
+		LOG_ERR("Only ZSOCK_AF_INET is supported!");
 		return -1;
 	}

-	/* winc1500 atmel uses AF_INET 2 instead of zephyrs AF_INET 1
-	 * we have checked if family is AF_INET so we can hardcode this
+	/* winc1500 atmel uses ZSOCK_AF_INET 2 instead of zephyrs ZSOCK_AF_INET 1
+	 * we have checked if family is ZSOCK_AF_INET so we can hardcode this
 	 * for now.
 	 */
 	sock = winc1500_socket(2, type, 0);
@@ -326,8 +326,8 @@ static int winc1500_get(sa_family_t family,
  * This function is called when user wants to bind to local IP address.
  */
 static int winc1500_bind(struct net_context *context,
-			 const struct sockaddr *addr,
-			 socklen_t addrlen)
+			 const struct zsock_sockaddr *addr,
+			 zsock_socklen_t addrlen)
 {
 	SOCKET socket = (intptr_t)context->offload_context;
 	int ret;
@@ -337,7 +337,7 @@ static int winc1500_bind(struct net_context *context,
 		return 0;
 	}

-	ret = winc1500_socket_bind((intptr_t)context->offload_context, (struct sockaddr *)addr,
+	ret = winc1500_socket_bind((intptr_t)context->offload_context, (struct zsock_sockaddr *)addr,
 		   addrlen);
 	if (ret) {
 		LOG_ERR("bind error %d %s!",
@@ -383,8 +383,8 @@ static int winc1500_listen(struct net_context *context, int backlog)
  * to a peer host.
  */
 static int winc1500_connect(struct net_context *context,
-			    const struct sockaddr *addr,
-			    socklen_t addrlen,
+			    const struct zsock_sockaddr *addr,
+			    zsock_socklen_t addrlen,
 			    net_context_connect_cb_t cb,
 			    int32_t timeout,
 			    void *user_data)
@@ -396,7 +396,7 @@ static int winc1500_connect(struct net_context *context,
 	w1500_data.socket_data[socket].connect_user_data = user_data;
 	w1500_data.socket_data[socket].ret_code = 0;

-	ret = winc1500_socket_connect(socket, (struct sockaddr *)addr, addrlen);
+	ret = winc1500_socket_connect(socket, (struct zsock_sockaddr *)addr, addrlen);
 	if (ret) {
 		LOG_ERR("connect error %d %s!",
 			ret, socket_error_string(ret));
@@ -485,8 +485,8 @@ out:
  * This function is called when user wants to send data to peer host.
  */
 static int winc1500_sendto(struct net_pkt *pkt,
-			   const struct sockaddr *dst_addr,
-			   socklen_t addrlen,
+			   const struct zsock_sockaddr *dst_addr,
+			   zsock_socklen_t addrlen,
 			   net_context_send_cb_t cb,
 			   int32_t timeout,
 			   void *user_data)
@@ -509,7 +509,7 @@ static int winc1500_sendto(struct net_pkt *pkt,
 	net_buf_add(buf, net_pkt_get_len(pkt));

 	ret = winc1500_socket_sendto(socket, buf->data, buf->len, 0,
-		     (struct sockaddr *)dst_addr, addrlen);
+		     (struct zsock_sockaddr *)dst_addr, addrlen);
 	if (ret) {
 		LOG_ERR("sendto error %d %s!", ret, socket_error_string(ret));
 		goto out;
@@ -592,7 +592,7 @@ static int winc1500_put(struct net_context *context)
 	struct socket_data *sd = &w1500_data.socket_data[sock];
 	int ret;

-	memset(&(context->remote), 0, sizeof(struct sockaddr_in));
+	memset(&(context->remote), 0, sizeof(struct zsock_sockaddr_in));
 	context->flags &= ~NET_CONTEXT_REMOTE_ADDR_SET;
 	ret = winc1500_close(sock);

@@ -654,7 +654,7 @@ static void handle_wifi_con_state_changed(void *pvMsg)
 static void handle_wifi_dhcp_conf(void *pvMsg)
 {
 	uint8_t *pu8IPAddress = (uint8_t *)pvMsg;
-	struct in_addr addr;
+	struct zsock_in_addr addr;
 	uint8_t i;

 	/* Connected and got IP address*/
@@ -873,7 +873,7 @@ static void handle_socket_msg_accept(struct socket_data *sd, void *pvMsg)
 		accept_msg->strAddr.sin_addr.s4_addr[1],
 		accept_msg->strAddr.sin_addr.s4_addr[2],
 		accept_msg->strAddr.sin_addr.s4_addr[3],
-		ntohs(accept_msg->strAddr.sin_port),
+		zsock_ntohs(accept_msg->strAddr.sin_port),
 		accept_msg->sock);

 	if (accept_msg->sock < 0) {
@@ -891,8 +891,8 @@ static void handle_socket_msg_accept(struct socket_data *sd, void *pvMsg)

 		memcpy(a_sd, sd, sizeof(struct socket_data));

-		ret = net_context_get(AF_INET, SOCK_STREAM,
-				      IPPROTO_TCP, &a_sd->context);
+		ret = net_context_get(ZSOCK_AF_INET, ZSOCK_SOCK_STREAM,
+				      ZSOCK_IPPROTO_TCP, &a_sd->context);
 		if (ret < 0) {
 			LOG_ERR("Cannot get new net context for ACCEPT");
 			return;
@@ -910,7 +910,7 @@ static void handle_socket_msg_accept(struct socket_data *sd, void *pvMsg)
 		net_context_set_state(a_sd->context, NET_CONTEXT_CONNECTED);

 		/** Setup remote */
-		a_sd->context->remote.sa_family = AF_INET;
+		a_sd->context->remote.sa_family = ZSOCK_AF_INET;
 		net_sin(&a_sd->context->remote)->sin_port =
 			accept_msg->strAddr.sin_port;
 		net_sin(&a_sd->context->remote)->sin_addr.s_addr =
@@ -918,8 +918,8 @@ static void handle_socket_msg_accept(struct socket_data *sd, void *pvMsg)
 		a_sd->context->flags |= NET_CONTEXT_REMOTE_ADDR_SET;

 		sd->accept_cb(a_sd->context,
-			      (struct sockaddr *)&accept_msg->strAddr,
-			      sizeof(struct sockaddr_in),
+			      (struct zsock_sockaddr *)&accept_msg->strAddr,
+			      sizeof(struct zsock_sockaddr_in),
 			      (accept_msg->sock > 0) ?
 			      0 : accept_msg->sock,
 			      sd->accept_user_data);
