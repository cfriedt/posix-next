diff --git a/arch/x86/Kconfig b/arch/x86/Kconfig
index 268d2dbaa45..41aea79674b 100644
--- a/arch/x86/Kconfig
+++ b/arch/x86/Kconfig
@@ -19,6 +19,7 @@ config CPU_ATOM
 	select ARCH_HAS_STACK_PROTECTION if X86_MMU
 	select ARCH_HAS_USERSPACE if X86_MMU
 	select ARCH_HAS_PROCESS_VM if X86_MMU && !X86_COMMON_PAGE_TABLE
+	select ARCH_HAS_FORK_RESUME if X86_MMU && !X86_COMMON_PAGE_TABLE
 	select X86_CPU_HAS_MMX
 	select X86_CPU_HAS_SSE
 	select X86_CPU_HAS_SSE2
@@ -33,6 +34,7 @@ config CPU_APOLLO_LAKE
 	select ARCH_HAS_STACK_PROTECTION if X86_MMU
 	select ARCH_HAS_USERSPACE if X86_MMU
 	select ARCH_HAS_PROCESS_VM if X86_MMU && !X86_COMMON_PAGE_TABLE
+	select ARCH_HAS_FORK_RESUME if X86_MMU && !X86_COMMON_PAGE_TABLE
 	select X86_MMU
 	select X86_CPU_HAS_MMX
 	select X86_CPU_HAS_SSE
@@ -51,6 +53,7 @@ config CPU_LAKEMONT_VALUE
 	select ARCH_HAS_STACK_PROTECTION if X86_MMU
 	select ARCH_HAS_USERSPACE if X86_MMU
 	select ARCH_HAS_PROCESS_VM if X86_MMU && !X86_COMMON_PAGE_TABLE
+	select ARCH_HAS_FORK_RESUME if X86_MMU && !X86_COMMON_PAGE_TABLE
 	select CPU_HAS_DCACHE
 	help
 	  This option signifies the use of a CPU from the Lakemont Value family.
@@ -61,6 +64,7 @@ config CPU_LAKEMONT_PERF
 	select ARCH_HAS_STACK_PROTECTION if X86_MMU
 	select ARCH_HAS_USERSPACE if X86_MMU
 	select ARCH_HAS_PROCESS_VM if X86_MMU && !X86_COMMON_PAGE_TABLE
+	select ARCH_HAS_FORK_RESUME if X86_MMU && !X86_COMMON_PAGE_TABLE
 	select X86_CPU_HAS_MMX
 	select X86_CPU_HAS_SSE
 	select X86_CPU_HAS_SSE2
diff --git a/arch/x86/core/ia32/thread.c b/arch/x86/core/ia32/thread.c
index 43b3bcc757b..a3be1afcacd 100644
--- a/arch/x86/core/ia32/thread.c
+++ b/arch/x86/core/ia32/thread.c
@@ -140,3 +140,43 @@ void arch_new_thread(struct k_thread *thread, k_thread_stack_t *stack,
 #endif /* CONFIG_X86_SSE */
 #endif /* CONFIG_EAGER_FPU_SHARING */
 }
+
+#ifdef CONFIG_PROCESS_VM
+/*
+ * Stage a copy of the parent's syscall state at the top of the child's
+ * privilege stack, in the exact layout the syscall exit path unwinds:
+ * [eax][edx][ecx][ebx][edi][ebp][ssf][EIP][CS][EFLAGS][ESP][SS]. The
+ * parent's ssf points at its iret frame (EIP slot); the argument slots
+ * holding its caller-context registers sit directly below the ssf slot.
+ */
+void *arch_fork_frame_init(struct k_thread *child, void *parent_ssf)
+{
+	uint32_t *p = parent_ssf;         /* &parent frame's EIP slot */
+	uint32_t *dst = (uint32_t *)((char *)child->arch.psp - (12 * sizeof(uint32_t)));
+
+	dst[0] = p[-7];                   /* eax (discarded by the exit path) */
+	dst[1] = p[-6];                   /* edx */
+	dst[2] = p[-5];                   /* ecx */
+	dst[3] = p[-4];                   /* ebx */
+	dst[4] = p[-3];                   /* edi */
+	dst[5] = p[-2];                   /* ebp */
+	dst[6] = 0;                       /* ssf slot (discarded) */
+	dst[7] = p[0];                    /* EIP */
+	dst[8] = p[1];                    /* CS */
+	dst[9] = p[2];                    /* EFLAGS */
+	dst[10] = p[3];                   /* ESP */
+	dst[11] = p[4];                   /* SS */
+
+	child->arch.fork_esi = _current->arch.fork_esi;
+
+	return dst;
+}
+
+FUNC_NORETURN void z_x86_fork_resume(void *frame);
+
+FUNC_NORETURN void arch_fork_resume(void *frame)
+{
+	z_x86_fork_resume(frame);
+	CODE_UNREACHABLE;
+}
+#endif /* CONFIG_PROCESS_VM */
diff --git a/arch/x86/core/ia32/userspace.S b/arch/x86/core/ia32/userspace.S
index 820c921e38e..afdf246a4b4 100644
--- a/arch/x86/core/ia32/userspace.S
+++ b/arch/x86/core/ia32/userspace.S
@@ -13,6 +13,9 @@
 
 /* Exports */
 GTEXT(z_x86_syscall_entry_stub)
+#ifdef CONFIG_PROCESS_VM
+GTEXT(z_x86_fork_resume)
+#endif
 GTEXT(z_x86_userspace_enter)
 GTEXT(arch_user_string_nlen)
 GTEXT(z_x86_user_string_nlen_fault_start)
@@ -217,6 +220,16 @@ _id_ok:
 	xor	%edi, %edi
 	mov	_k_syscall_table(%edi, %esi, 4), %ebx
 
+#ifdef CONFIG_PROCESS_VM
+	/* The pushed argument slots already bank the caller's registers; ESI
+	 * (the syscall id register) is the one live value fork needs that has
+	 * no slot, so stash it per-thread for the child's resume context.
+	 */
+	movl	$_kernel, %eax
+	movl	_kernel_offset_to_current(%eax), %eax
+	movl	%esi, _thread_offset_to_fork_esi(%eax)
+#endif /* CONFIG_PROCESS_VM */
+
 	/* Run the handler, which is some entry in _k_syscall_table */
 	call	*%ebx
 
@@ -291,7 +304,12 @@ _skip_z_sig_handle:
 
 	/* EAX now contains return value. Pop or xor everything else to prevent
 	 * information leak from kernel mode.
+	 *
+	 * Entered by z_x86_fork_resume() with ESP at a staged frame and EAX
+	 * holding the child's return value.
 	 */
+.global z_x86_syscall_exit
+z_x86_syscall_exit:
 	pop	%edx		/* old arg1 value, discard it */
 	pop	%edx
 	pop	%ecx
@@ -312,6 +330,32 @@ _bad_syscall:
 	jmp	_id_ok
 
 
+#ifdef CONFIG_PROCESS_VM
+/*
+ * FUNC_NORETURN void z_x86_fork_resume(void *frame)
+ *
+ * Enter user mode resuming a fork-copied syscall frame: the child returns
+ * from the fork syscall its parent made, with EAX = 0. The frame was staged
+ * at the top of this thread's privilege stack by arch_fork_frame_init(), in
+ * the exact layout the syscall exit path unwinds (argument slots, ssf slot,
+ * then the iret frame). EBP is preserved in-register by the exit path, and
+ * ESI holds the syscall id per the calling convention, so both are loaded
+ * here from the staged state.
+ */
+SECTION_FUNC(TEXT, z_x86_fork_resume)
+	cli
+	movl	4(%esp), %esp
+
+	/* user EBP from its argument slot; ESI from the per-thread stash */
+	movl	20(%esp), %ebp
+	movl	$_kernel, %eax
+	movl	_kernel_offset_to_current(%eax), %eax
+	movl	_thread_offset_to_fork_esi(%eax), %esi
+
+	xor	%eax, %eax	/* the child's fork returns 0 */
+	jmp	z_x86_syscall_exit
+#endif /* CONFIG_PROCESS_VM */
+
 /*
  * size_t arch_user_string_nlen(const char *s, size_t maxsize, int *err_arg)
  */
diff --git a/arch/x86/core/intel64/signal.c b/arch/x86/core/intel64/signal.c
index db52d22db4d..c526e525b79 100644
--- a/arch/x86/core/intel64/signal.c
+++ b/arch/x86/core/intel64/signal.c
@@ -13,7 +13,7 @@ void arch_sig_pre(struct k_thread *thread, void *arg)
 	struct arch_sig_ctx *const backup = &thread->base.sig.user;
 
 	/* Backup the original RAX, RFLAGS, RSP, and RIP */
-	backup->rax = sf->rdi;
+	backup->rax = sf->rax;
 	backup->rflags = sf->rflags;
 	backup->rsp = sf->rsp;
 	backup->rip = sf->rip;
@@ -35,7 +35,7 @@ void arch_sig_post(struct k_thread *thread, void *arg)
 	struct arch_sig_ctx *const backup = &thread->base.sig.user;
 
 	/* Restore the original RAX, RFLAGS, RIP */
-	sf->rdi = backup->rax;
+	sf->rax = backup->rax;
 	sf->rflags = backup->rflags;
 	sf->rsp = backup->rsp;
 	sf->rip = backup->rip;
diff --git a/arch/x86/core/intel64/thread.c b/arch/x86/core/intel64/thread.c
index 2b42bcb4c49..f04f073f1be 100644
--- a/arch/x86/core/intel64/thread.c
+++ b/arch/x86/core/intel64/thread.c
@@ -90,3 +90,27 @@ int arch_coprocessors_disable(struct k_thread *thread)
 
 	return -ENOTSUP;
 }
+
+#ifdef CONFIG_PROCESS_VM
+/*
+ * Stage a copy of the parent's syscall frame at the top of the child's
+ * privilege stack - exactly where a live syscall would have built it, so
+ * fxsave alignment and the exit path's unwind match by construction.
+ */
+void *arch_fork_frame_init(struct k_thread *child, void *parent_ssf)
+{
+	struct x86_ssf *dst = (struct x86_ssf *)((char *)child->arch.psp - sizeof(struct x86_ssf));
+
+	(void)memcpy(dst, parent_ssf, sizeof(*dst));
+
+	return dst;
+}
+
+FUNC_NORETURN void z_x86_fork_resume(void *frame);
+
+FUNC_NORETURN void arch_fork_resume(void *frame)
+{
+	z_x86_fork_resume(frame);
+	CODE_UNREACHABLE;
+}
+#endif /* CONFIG_PROCESS_VM */
diff --git a/arch/x86/core/intel64/userspace.S b/arch/x86/core/intel64/userspace.S
index 146fd06385c..f62d34bc538 100644
--- a/arch/x86/core/intel64/userspace.S
+++ b/arch/x86/core/intel64/userspace.S
@@ -115,6 +115,19 @@ z_x86_syscall_entry_stub:
 	movq	$0, %gs:__x86_tss64_t_usp_OFFSET
 #endif
 
+	/* Callee-saved registers: the C convention preserves them through the
+	 * kernel so the exit path never restores them, but fork() needs the
+	 * caller's complete register state to seed the child's resume.
+	 */
+	pushq	$0	/* pad: keeps the fxsave area 16-byte aligned */
+	pushq	%rbx
+	pushq	%rbp
+	pushq	%r12
+	pushq	%r13
+	pushq	%r14
+	pushq	%r15
+	pushq	$0	/* rax: syscall return stash (signal delivery) */
+
 	sti			/* re-enable interrupts */
 
 	/* call_id is in RAX. bounds-check it, must be less than
@@ -172,7 +185,7 @@ _id_ok:
 	je	_do_sig_restore
 
 	/* Store RAX (syscall return value) into privileged stack */
-	movq	%rax, (__struct_x86_ssf_rdi_OFFSET + 8)(%rsp)
+	movq	%rax, (__struct_x86_ssf_rax_OFFSET + 8)(%rsp)
 
 	/* Pass privileged stack to z_sig_handle() */
 	movq	%rsp, %rdi
@@ -210,7 +223,7 @@ _do_sig_restore:
 
 _do_sig_return:
 	/* Restore RAX (syscall return value) from privileged stack */
-	movq	(__struct_x86_ssf_rdi_OFFSET + 8)(%rsp), %rax
+	movq	(__struct_x86_ssf_rax_OFFSET + 8)(%rsp), %rax
 
 _skip_z_sig_handle:
 #endif
@@ -228,6 +241,12 @@ _skip_z_sig_handle:
 	 * values to successfully 'sysretq'.
 	 */
 	addq	$8, %rsp	/* Discard ssf */
+
+	/* Entered by z_x86_fork_resume() with RSP at a copied frame and RAX
+	 * holding the child's return value.
+	 */
+.global z_x86_syscall_exit
+z_x86_syscall_exit:
 	popq	%rcx	/* Restore return address for 'sysretq' */
 	popq	%r11	/* Restore RFLAGS for 'sysretq' */
 	popq	%r10
@@ -238,6 +257,8 @@ _skip_z_sig_handle:
 	fxrstor	(%rsp)
 	addq	$X86_FXSAVE_SIZE, %rsp
 	popq	%rdi
+	/* rax stash + callee-saved + pad: never restored from the frame */
+	addq	$64, %rsp
 
 #ifdef CONFIG_X86_KPTI
 	/* Lock IRQs as we are using per-cpu memory areas and the
@@ -286,6 +307,32 @@ _bad_syscall:
 	movq	$K_SYSCALL_BAD, %rax
 	jmp	_id_ok
 
+#ifdef CONFIG_PROCESS_VM
+/*
+ * FUNC_NORETURN void z_x86_fork_resume(void *frame)
+ *
+ * Enter user mode resuming a fork-copied syscall frame: the child returns
+ * from the fork syscall its parent made, with RAX = 0. Runs in supervisor
+ * mode; the frame was staged at the top of this thread's privilege stack by
+ * arch_fork_frame_init(), so the common syscall exit path unwinds it exactly
+ * like a live syscall. Callee-saved registers are loaded here because the
+ * exit path never restores them (the C convention preserved them for a real
+ * syscall; a forked child has no such history).
+ */
+.global z_x86_fork_resume
+z_x86_fork_resume:
+	cli
+	movq	%rdi, %rsp
+	movq	__struct_x86_ssf_rbx_OFFSET(%rsp), %rbx
+	movq	__struct_x86_ssf_rbp_OFFSET(%rsp), %rbp
+	movq	__struct_x86_ssf_r12_OFFSET(%rsp), %r12
+	movq	__struct_x86_ssf_r13_OFFSET(%rsp), %r13
+	movq	__struct_x86_ssf_r14_OFFSET(%rsp), %r14
+	movq	__struct_x86_ssf_r15_OFFSET(%rsp), %r15
+	xorq	%rax, %rax	/* the child's fork returns 0 */
+	jmp	z_x86_syscall_exit
+#endif /* CONFIG_PROCESS_VM */
+
 /*
  * size_t arch_user_string_nlen(const char *s, size_t maxsize, int *err_arg)
  *                              ^ RDI          ^ RSI           ^ RDX
diff --git a/arch/x86/core/offsets/ia32_offsets.c b/arch/x86/core/offsets/ia32_offsets.c
index 2dd9ac3e7e9..ea2346c57ba 100644
--- a/arch/x86/core/offsets/ia32_offsets.c
+++ b/arch/x86/core/offsets/ia32_offsets.c
@@ -41,6 +41,9 @@ GEN_OFFSET_SYM(_cpu_arch_t, fpu_owner);
 
 #ifdef CONFIG_USERSPACE
 GEN_OFFSET_SYM(_thread_arch_t, psp);
+#ifdef CONFIG_PROCESS_VM
+GEN_OFFSET_SYM(_thread_arch_t, fork_esi);
+#endif
 #ifdef CONFIG_SIGNAL
 GEN_OFFSET_SYM(_thread_arch_t, sig_eax);
 
diff --git a/arch/x86/core/offsets/intel64_offsets.c b/arch/x86/core/offsets/intel64_offsets.c
index 9d5de9b5460..9bbde261351 100644
--- a/arch/x86/core/offsets/intel64_offsets.c
+++ b/arch/x86/core/offsets/intel64_offsets.c
@@ -67,6 +67,13 @@ GEN_ABSOLUTE_SYM(__X86_CPUBOOT_SIZEOF, sizeof(x86_cpuboot_t));
 #if defined(CONFIG_USERSPACE) && defined(CONFIG_SIGNAL)
 GEN_OFFSET_STRUCT(x86_ssf, rsp);
 GEN_OFFSET_STRUCT(x86_ssf, rdi);
+GEN_OFFSET_STRUCT(x86_ssf, rax);
+GEN_OFFSET_STRUCT(x86_ssf, r15);
+GEN_OFFSET_STRUCT(x86_ssf, r14);
+GEN_OFFSET_STRUCT(x86_ssf, r13);
+GEN_OFFSET_STRUCT(x86_ssf, r12);
+GEN_OFFSET_STRUCT(x86_ssf, rbp);
+GEN_OFFSET_STRUCT(x86_ssf, rbx);
 GEN_ABSOLUTE_SYM(__X86_SSF_SIZEOF, sizeof(struct x86_ssf));
 
 GEN_ABSOLUTE_SYM(__thread_sig_user_s_OFFSET,
diff --git a/arch/x86/include/offsets_short_arch.h b/arch/x86/include/offsets_short_arch.h
index 96054e352b1..28b1a8c6199 100644
--- a/arch/x86/include/offsets_short_arch.h
+++ b/arch/x86/include/offsets_short_arch.h
@@ -19,6 +19,11 @@
 #define _thread_offset_to_psp \
 	(___thread_t_arch_OFFSET + ___thread_arch_t_psp_OFFSET)
 
+#ifdef CONFIG_PROCESS_VM
+#define _thread_offset_to_fork_esi \
+	(___thread_t_arch_OFFSET + ___thread_arch_t_fork_esi_OFFSET)
+#endif /* CONFIG_PROCESS_VM */
+
 #ifdef CONFIG_SIGNAL
 #define _thread_offset_to_sig_eax \
 	(___thread_t_arch_OFFSET + ___thread_arch_t_sig_eax_OFFSET)
diff --git a/include/zephyr/arch/x86/ia32/thread.h b/include/zephyr/arch/x86/ia32/thread.h
index 970b5d8e051..392be2695ff 100644
--- a/include/zephyr/arch/x86/ia32/thread.h
+++ b/include/zephyr/arch/x86/ia32/thread.h
@@ -224,6 +224,10 @@ struct _thread_arch {
 	 * Un-set for supervisor threads.
 	 */
 	char *psp;
+#ifdef CONFIG_PROCESS_VM
+	/* caller's ESI (the syscall id register) captured at syscall entry */
+	uint32_t fork_esi;
+#endif /* CONFIG_PROCESS_VM */
 
 #if defined(CONFIG_SIGNAL)
 	/* Last syscall return value, saved/restored across signal delivery. */
diff --git a/include/zephyr/arch/x86/intel64/exception.h b/include/zephyr/arch/x86/intel64/exception.h
index 669a9e68be6..6639f2803a5 100644
--- a/include/zephyr/arch/x86/intel64/exception.h
+++ b/include/zephyr/arch/x86/intel64/exception.h
@@ -63,6 +63,22 @@ struct x86_ssf {
 	unsigned long rsi;
 	char fxsave[X86_FXSAVE_SIZE];
 	unsigned long rdi;
+	/* Syscall return value stash (signal delivery), zeroed at entry */
+	unsigned long rax;
+	/*
+	 * Callee-saved registers. The C calling convention preserves them
+	 * through the kernel, so the return path never restores them; they
+	 * are captured so fork() can seed the child's resume context with
+	 * the caller's complete register state.
+	 */
+	unsigned long r15;
+	unsigned long r14;
+	unsigned long r13;
+	unsigned long r12;
+	unsigned long rbp;
+	unsigned long rbx;
+	/* keeps the fxsave area 16-byte aligned (even number of slots) */
+	unsigned long pad;
 	unsigned long rsp;
 };
 
