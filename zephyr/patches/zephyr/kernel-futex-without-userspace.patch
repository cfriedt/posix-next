diff --git a/include/zephyr/kernel.h b/include/zephyr/kernel.h
index 6fc535c11ae..7a501ab3790 100644
--- a/include/zephyr/kernel.h
+++ b/include/zephyr/kernel.h
@@ -2650,7 +2650,6 @@ __syscall void *k_queue_peek_tail(struct k_queue *queue);

 /** @} */

-#ifdef CONFIG_USERSPACE
 /**
  * @brief futex structure
  *
@@ -2661,7 +2660,8 @@ __syscall void *k_queue_peek_tail(struct k_queue *queue);
  * permission management mechanism: statically-defined futexes are
  * tracked as kernel objects with a dedicated wait queue, while
  * futexes at other addresses (e.g. stack or heap) share a fixed set
- * of address-hashed wait queues.
+ * of address-hashed wait queues. Futexes do not require
+ * @kconfig{CONFIG_USERSPACE}; without it every futex is unregistered.
  *
  * The kernel maintains @a waiters, the number of threads currently
  * blocked on the futex: it is incremented before a thread blocks and
@@ -2749,7 +2749,6 @@ __syscall int k_futex_wait(struct k_futex *futex, int expected,
 __syscall int k_futex_wake(struct k_futex *futex, bool wake_all);

 /** @} */
-#endif

 /**
  * @defgroup event_apis Event APIs
diff --git a/kernel/CMakeLists.txt b/kernel/CMakeLists.txt
index aea11fa4b06..e9f17373ebe 100644
--- a/kernel/CMakeLists.txt
+++ b/kernel/CMakeLists.txt
@@ -192,6 +192,7 @@ kernel_sources_ifdef(CONFIG_MULTITHREADING
   thread.c
   sched.c
   pipe.c
+  futex.c
   )

 if(CONFIG_MULTITHREADING)
@@ -279,7 +280,6 @@ endif()
 target_sources_ifdef(
   CONFIG_USERSPACE
   kernel PRIVATE
-  futex.c
   mem_domain.c
   userspace_handler.c
   userspace.c
diff --git a/kernel/futex.c b/kernel/futex.c
index 5a61af0dcc6..a81173b3a55 100644
--- a/kernel/futex.c
+++ b/kernel/futex.c
@@ -28,16 +28,35 @@ static struct z_futex_data futex_hash_buckets[FUTEX_HASH_BUCKETS] = {
 	LISTIFY(FUTEX_HASH_BUCKETS, FUTEX_HASH_BUCKET_INIT, (,))
 };

-static struct z_futex_data *k_futex_find_data(struct k_futex *futex)
+#ifdef CONFIG_USERSPACE
+static struct z_futex_data *futex_kobject_data(struct k_futex *futex)
 {
-	struct k_object *obj;
-	uintptr_t addr = (uintptr_t)futex;
+	struct k_object *obj = k_object_find(futex);

-	obj = k_object_find(futex);
 	if ((obj != NULL) && (obj->type == K_OBJ_FUTEX)) {
 		return obj->data.futex_data;
 	}

+	return NULL;
+}
+#else
+static struct z_futex_data *futex_kobject_data(struct k_futex *futex)
+{
+	ARG_UNUSED(futex);
+
+	return NULL;
+}
+#endif
+
+static struct z_futex_data *k_futex_find_data(struct k_futex *futex)
+{
+	uintptr_t addr = (uintptr_t)futex;
+	struct z_futex_data *futex_data = futex_kobject_data(futex);
+
+	if (futex_data != NULL) {
+		return futex_data;
+	}
+
 	return &futex_hash_buckets[(addr / sizeof(struct k_futex)) % FUTEX_HASH_BUCKETS];
 }

@@ -81,6 +100,7 @@ int z_impl_k_futex_wake(struct k_futex *futex, bool wake_all)
 	return woken;
 }

+#ifdef CONFIG_USERSPACE
 static inline int z_vrfy_k_futex_wake(struct k_futex *futex, bool wake_all)
 {
 	if (K_SYSCALL_MEMORY_WRITE(futex, sizeof(struct k_futex)) != 0) {
@@ -90,6 +110,7 @@ static inline int z_vrfy_k_futex_wake(struct k_futex *futex, bool wake_all)
 	return z_impl_k_futex_wake(futex, wake_all);
 }
 #include <zephyr/syscalls/k_futex_wake_mrsh.c>
+#endif

 int z_impl_k_futex_wait(struct k_futex *futex, int expected,
 			k_timeout_t timeout)
@@ -125,6 +146,7 @@ int z_impl_k_futex_wait(struct k_futex *futex, int expected,
 	return ret;
 }

+#ifdef CONFIG_USERSPACE
 static inline int z_vrfy_k_futex_wait(struct k_futex *futex, int expected,
 				      k_timeout_t timeout)
 {
@@ -135,3 +157,4 @@ static inline int z_vrfy_k_futex_wait(struct k_futex *futex, int expected,
 	return z_impl_k_futex_wait(futex, expected, timeout);
 }
 #include <zephyr/syscalls/k_futex_wait_mrsh.c>
+#endif
