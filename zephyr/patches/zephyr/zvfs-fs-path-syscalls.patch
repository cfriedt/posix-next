diff --git a/include/zephyr/fs/fs.h b/include/zephyr/fs/fs.h
index b5c9aecb40f..e75fc131ce0 100644
--- a/include/zephyr/fs/fs.h
+++ b/include/zephyr/fs/fs.h
@@ -677,6 +677,18 @@ int fs_stat(const char *path, struct fs_dirent *entry);
  */
 int fs_statvfs(const char *path, struct fs_statvfs *stat);

+/**
+ * @brief Get the mount flags of the file system containing a path
+ *
+ * @param path Path to the file or directory
+ * @param flags Destination for the mount's FS_MOUNT_FLAG_* bits
+ *
+ * @retval 0 on success;
+ * @retval -EINVAL when a bad file name is given;
+ * @retval -ENOENT when no mount point contains @p path.
+ */
+int fs_mount_flags(const char *path, uint8_t *flags);
+
 /**
  * @brief Set file or directory timestamps
  *
diff --git a/include/zephyr/sys/fdtable.h b/include/zephyr/sys/fdtable.h
index 29f03868806..22b51eaafb5 100644
--- a/include/zephyr/sys/fdtable.h
+++ b/include/zephyr/sys/fdtable.h
@@ -346,6 +346,7 @@ enum {
 	ZFD_IOCTL_STAT,
 	ZFD_IOCTL_TRUNCATE,
 	ZFD_IOCTL_MMAP,
+	ZFD_IOCTL_STATVFS,

 	/* Codes above 0x5400 and below 0x5500 are reserved for termios, FIO, etc */
 	ZFD_IOCTL_FIONREAD = 0x541B,
diff --git a/include/zephyr/sys/zvfs.h b/include/zephyr/sys/zvfs.h
index 7a417c2247c..bbe9075c9d2 100644
--- a/include/zephyr/sys/zvfs.h
+++ b/include/zephyr/sys/zvfs.h
@@ -17,6 +17,7 @@
 #include <stdarg.h>
 #include <stddef.h>
 #include <sys/types.h>
+#include <time.h>

 #include <zephyr/kernel.h>

@@ -27,6 +28,18 @@ extern "C" {
 struct zvfs_stat {
 	off_t size;
 	uint32_t mode;
+	/** Preferred I/O block size, 0 when unknown. */
+	unsigned long blksize;
+	/** Number of 512 B blocks allocated, 0 when unknown. */
+	unsigned long blocks;
+	/** Number of hard links. */
+	unsigned long nlink;
+	/** Time of last access, zero when the backend does not record it. */
+	struct timespec atime;
+	/** Time of last modification, zero when the backend does not record it. */
+	struct timespec mtime;
+	/** Time of last status change, zero when the backend does not record it. */
+	struct timespec ctime;
 };

 /**
diff --git a/include/zephyr/sys/zvfs_fs.h b/include/zephyr/sys/zvfs_fs.h
index b72af97a615..40131922b91 100644
--- a/include/zephyr/sys/zvfs_fs.h
+++ b/include/zephyr/sys/zvfs_fs.h
@@ -16,8 +16,11 @@
  */

 #include <stdbool.h>
+#include <time.h>

 #include <zephyr/fs/fs.h>
+#include <zephyr/sys/zvfs.h>
+#include <zephyr/toolchain.h>

 #ifdef __cplusplus
 extern "C" {
@@ -38,6 +41,39 @@ struct zvfs_fs_desc {
 	bool used: 1;
 };

+/** @name zvfs_access() amode bits */
+/**@{*/
+#define ZVFS_F_OK 0 /**< Existence only */
+#define ZVFS_X_OK 1 /**< Execute or search permission */
+#define ZVFS_W_OK 2 /**< Write permission */
+#define ZVFS_R_OK 4 /**< Read permission */
+/**@}*/
+
+/** @name zvfs_statvfs f_flag bits */
+/**@{*/
+#define ZVFS_ST_RDONLY 0x1 /**< Read-only file system */
+#define ZVFS_ST_NOSUID 0x2 /**< Setuid/setgid bits are ignored */
+/**@}*/
+
+/**
+ * @brief File system statistics for zvfs_statvfs() and zvfs_fstatvfs().
+ */
+struct zvfs_statvfs {
+	unsigned long f_bsize;   /**< File system block size */
+	unsigned long f_frsize;  /**< Fundamental file system block size */
+	unsigned long f_blocks;  /**< Total number of blocks (in f_frsize units) */
+	unsigned long f_bfree;   /**< Number of free blocks */
+	unsigned long f_bavail;  /**< Number of free blocks available to all callers */
+	unsigned long f_files;   /**< Total number of file serial numbers, 0 when untracked */
+	unsigned long f_ffree;   /**< Number of free file serial numbers, 0 when untracked */
+	unsigned long f_favail;  /**< Free file serial numbers available, 0 when untracked */
+	unsigned long f_fsid;    /**< File system ID, 0 when untracked */
+	unsigned long f_flag;    /**< ZVFS_ST_* flag bits */
+	unsigned long f_namemax; /**< Maximum filename length */
+	/** Timestamp resolution in nanoseconds, 0 when unknown. */
+	unsigned long f_timestamp_resolution;
+};
+
 /**
  * @brief Open a path on a mounted Zephyr file system and return an fd.
  *
@@ -47,7 +83,7 @@ struct zvfs_fs_desc {
  *
  * @return File descriptor on success, or -1 on error (errno is set).
  */
-int zvfs_open(const char *name, int flags, int mode);
+__syscall int zvfs_open(const char *name, int flags, int mode);

 /**
  * @brief Allocate a ZVFS FS descriptor slot.
@@ -79,7 +115,7 @@ void zvfs_record_lock_drop_obj(void *obj);
  *
  * @return 0 on success, -1 with errno set on error.
  */
-int zvfs_unlink(const char *path);
+__syscall int zvfs_unlink(const char *path);

 /**
  * @brief Rename file or directory, moving it if necessary.
@@ -89,10 +125,104 @@ int zvfs_unlink(const char *path);
  *
  * @return 0 on success, -1 with errno set on error.
  */
-int zvfs_rename(const char *old, const char *newp);
+__syscall int zvfs_rename(const char *old, const char *newp);
+
+/**
+ * @brief Delete a file, or an empty directory, as ISO C remove() requires.
+ *
+ * @param path Path to remove.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_remove(const char *path);
+
+/**
+ * @brief Get status of a file or directory by path.
+ *
+ * @param path Path to query.
+ * @param buf Destination for the status.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_stat(const char *path, struct zvfs_stat *buf);
+
+/**
+ * @brief Get statistics of the file system containing @p path.
+ *
+ * @param path Any path on the mounted file system.
+ * @param buf Destination for the statistics.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_statvfs(const char *path, struct zvfs_statvfs *buf);
+
+/**
+ * @brief Get statistics of the file system containing the open file @p fd.
+ *
+ * @param fd File descriptor of an open file.
+ * @param buf Destination for the statistics.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_fstatvfs(int fd, struct zvfs_statvfs *buf);
+
+/**
+ * @brief Create a directory.
+ *
+ * @param path Path of the directory to create.
+ * @param mode Permission bits, accepted and ignored (Zephyr file systems
+ *             store no permissions).
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_mkdir(const char *path, uint32_t mode);
+
+/**
+ * @brief Remove a directory.
+ *
+ * @param path Path of the directory to remove.
+ *
+ * @return 0 on success, -1 with errno set on error (ENOTDIR when @p path
+ *         names a regular file).
+ */
+__syscall int zvfs_rmdir(const char *path);
+
+/**
+ * @brief Truncate a file to a given length by path.
+ *
+ * @param path Path of the file.
+ * @param length New length in bytes.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_truncate(const char *path, off_t length);
+
+/**
+ * @brief Check accessibility of a file or directory.
+ *
+ * @param path Path to check.
+ * @param amode ZVFS_F_OK, or any bitwise combination of ZVFS_R_OK,
+ *              ZVFS_W_OK, and ZVFS_X_OK.
+ *
+ * @return 0 when access would be granted, -1 with errno set otherwise.
+ */
+__syscall int zvfs_access(const char *path, int amode);
+
+/**
+ * @brief Set file or directory timestamps.
+ *
+ * @param path Path of the file or directory.
+ * @param times Pointer to two timespecs, access time first and modification
+ *              time second, or NULL to set both to the current time.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+__syscall int zvfs_utime(const char *path, const struct timespec *times);

 #ifdef __cplusplus
 }
 #endif

+#include <zephyr/syscalls/zvfs_fs.h>
+
 #endif /* ZEPHYR_INCLUDE_SYS_ZVFS_FS_H_ */
diff --git a/lib/libc/common/source/stdio/remove.c b/lib/libc/common/source/stdio/remove.c
index 5616920..adb1179 100644
--- a/lib/libc/common/source/stdio/remove.c
+++ b/lib/libc/common/source/stdio/remove.c
@@ -8,6 +8,7 @@
 #include <stdio.h>
 #include <zephyr/fs/fs.h>
 #include <zephyr/sys/util_macro.h>
+#include <zephyr/sys/zvfs_fs.h>
 
 /**
  *
@@ -19,17 +20,22 @@
 
 int remove(const char *path)
 {
-	if (!IS_ENABLED(CONFIG_FILE_SYSTEM)) {
-		errno = ENOTSUP;
-		return -1;
+	if (IS_ENABLED(CONFIG_ZVFS_FS)) {
+		/* zvfs_remove() handles files and empty directories, as ISO C requires */
+		return zvfs_remove(path);
 	}
 
-	int ret = fs_unlink(path);
+	if (IS_ENABLED(CONFIG_FILE_SYSTEM)) {
+		int ret = fs_unlink(path);
 
-	if (ret < 0) {
-		errno = -ret;
-		return -1;
+		if (ret < 0) {
+			errno = -ret;
+			return -1;
+		}
+
+		return 0;
 	}
 
-	return 0;
+	errno = ENOTSUP;
+	return -1;
 }
diff --git a/lib/os/zvfs/Kconfig b/lib/os/zvfs/Kconfig
index bf507eacf04..23484a913a6 100644
--- a/lib/os/zvfs/Kconfig
+++ b/lib/os/zvfs/Kconfig
@@ -58,6 +58,17 @@ config ZVFS_FS
 	  registers a file in the fd table. Required for ISO C fopen() and for POSIX
 	  open() when a mounted file system is present.

+config ZVFS_PATH_MAX
+	int "Maximum ZVFS path length"
+	depends on ZVFS_FS
+	range 16 512
+	default 64
+	help
+	  Longest path, including the terminating NUL, accepted by the ZVFS
+	  path system calls. Each system call verifier places this many bytes
+	  on the privileged stack (twice for zvfs_rename()), so large values
+	  need a matching CONFIG_PRIVILEGED_STACK_SIZE.
+
 config ZVFS_LIBC_FILE
 	bool
 	default y if ZVFS_FS && !MINIMAL_LIBC
diff --git a/lib/os/zvfs/zvfs_fs.c b/lib/os/zvfs/zvfs_fs.c
index 2666923d289..72eb5a5f5bb 100644
--- a/lib/os/zvfs/zvfs_fs.c
+++ b/lib/os/zvfs/zvfs_fs.c
@@ -24,9 +24,12 @@

 #include <zephyr/fs/fs.h>
 #include <zephyr/kernel.h>
+#include <zephyr/internal/syscall_handler.h>
 #include <zephyr/sys/fdtable.h>
 #include <zephyr/sys/zvfs_fs.h>

+#include "zvfs_fs_priv.h"
+
 #define ZVFS_O_ACCMODE (ZVFS_O_RDONLY | ZVFS_O_WRONLY | ZVFS_O_RDWR)

 static struct zvfs_fs_desc desc_array[CONFIG_ZVFS_FS_DESC_MAX];
@@ -83,7 +86,7 @@ static int posix_mode_to_zephyr(int mf)
 	return mode;
 }

-int zvfs_open(const char *name, int flags, int mode)
+int z_impl_zvfs_open(const char *name, int flags, int mode)
 {
 	int rc, fd;
 	struct zvfs_fs_desc *ptr = NULL;
@@ -106,6 +109,19 @@ int zvfs_open(const char *name, int flags, int mode)

 	fs_file_t_init(&ptr->file);

+	if ((flags & (ZVFS_O_CREAT | ZVFS_O_EXCL)) == (ZVFS_O_CREAT | ZVFS_O_EXCL)) {
+		struct fs_dirent entry;
+
+		rc = fs_stat(name, &entry);
+		if (rc == 0) {
+			rc = -EEXIST;
+			goto out_err;
+		}
+		if (rc != -ENOENT) {
+			goto out_err;
+		}
+	}
+
 	if (flags & ZVFS_O_CREAT) {
 		flags &= ~ZVFS_O_CREAT;

@@ -138,6 +154,20 @@ out_err:
 	return -1;
 }

+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_open(const char *name, int flags, int mode)
+{
+	char kname[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kname, name) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_open(kname, flags, mode);
+}
+#include <zephyr/syscalls/zvfs_open_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
 static int fs_close_vmeth(void *obj)
 {
 	struct zvfs_fs_desc *ptr = obj;
@@ -185,6 +215,26 @@ static int fs_ioctl_vmeth(void *obj, unsigned int request, va_list args)
 		}
 		break;
 	}
+	case ZFD_IOCTL_STATVFS: {
+		struct zvfs_statvfs *buf = va_arg(args, struct zvfs_statvfs *);
+		struct fs_statvfs vfs;
+		const struct fs_mount_t *mp = ptr->file.mp;
+
+		if (mp == NULL || mp->mnt_point == NULL) {
+			errno = EBADF;
+			return -1;
+		}
+
+		/* fs_statvfs() rejects a bare "/"; the backend ignores the path */
+		rc = fs_statvfs(mp->mnt_point[1] == '\0' ? "/." : mp->mnt_point, &vfs);
+		if (rc < 0) {
+			break;
+		}
+
+		zvfs_statvfs_from_fs(buf, &vfs,
+				     (mp->flags & FS_MOUNT_FLAG_READ_ONLY) != 0);
+		break;
+	}
 	case ZFD_IOCTL_FSYNC: {
 		rc = fs_sync(&ptr->file);
 		break;
diff --git a/lib/os/zvfs/zvfs_fs_priv.h b/lib/os/zvfs/zvfs_fs_priv.h
new file mode 100644
index 00000000000..151477d1306
--- /dev/null
+++ b/lib/os/zvfs/zvfs_fs_priv.h
@@ -0,0 +1,51 @@
+/*
+ * SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+/**
+ * @file
+ * @brief Internal helpers shared by the ZVFS file system translation units.
+ */
+
+#ifndef ZEPHYR_LIB_OS_ZVFS_ZVFS_FS_PRIV_H_
+#define ZEPHYR_LIB_OS_ZVFS_ZVFS_FS_PRIV_H_
+
+#include <stdbool.h>
+
+#ifdef __cplusplus
+extern "C" {
+#endif
+
+struct fs_statvfs;
+struct zvfs_statvfs;
+
+/**
+ * @brief Map Zephyr file system statistics into ZVFS statistics.
+ *
+ * @param buf Destination.
+ * @param vfs Source, from fs_statvfs().
+ * @param rdonly True when the containing mount is read-only.
+ */
+void zvfs_statvfs_from_fs(struct zvfs_statvfs *buf, const struct fs_statvfs *vfs, bool rdonly);
+
+#ifdef CONFIG_USERSPACE
+/**
+ * @brief Copy a user-supplied path into a kernel buffer of CONFIG_ZVFS_PATH_MAX bytes.
+ *
+ * An over-long path is an error to report (-1 with errno ENAMETOOLONG); an
+ * invalid pointer is a fault to oops on, per the syscall conventions.
+ *
+ * @param dst Kernel destination of at least CONFIG_ZVFS_PATH_MAX bytes.
+ * @param src User-supplied path.
+ *
+ * @return 0 on success, -1 with errno set on error.
+ */
+int zvfs_copy_user_path(char *dst, const char *src);
+#endif /* CONFIG_USERSPACE */
+
+#ifdef __cplusplus
+}
+#endif
+
+#endif /* ZEPHYR_LIB_OS_ZVFS_ZVFS_FS_PRIV_H_ */
diff --git a/lib/os/zvfs/zvfs_path.c b/lib/os/zvfs/zvfs_path.c
index cf386ec505c..d3560ac5d98 100644
--- a/lib/os/zvfs/zvfs_path.c
+++ b/lib/os/zvfs/zvfs_path.c
@@ -11,12 +11,47 @@
 #include <errno.h>

 #include <zephyr/fs/fs.h>
+#include <zephyr/internal/syscall_handler.h>
+#include <zephyr/kernel.h>
+#include <zephyr/sys/fdtable.h>
+#include <zephyr/sys/internal/fdtable_priv.h>
+#include <zephyr/sys/util.h>
 #include <zephyr/sys/zvfs_fs.h>

-int zvfs_unlink(const char *path)
+#include "zvfs_fs_priv.h"
+
+#ifdef CONFIG_USERSPACE
+int zvfs_copy_user_path(char *dst, const char *src)
 {
-	int res = fs_unlink(path);
+	int err;
+
+	if (src == NULL) {
+		errno = EFAULT;
+		return -1;
+	}
+
+	if (k_usermode_string_nlen(src, CONFIG_ZVFS_PATH_MAX, &err) >= CONFIG_ZVFS_PATH_MAX) {
+		K_OOPS(err != 0);
+		errno = ENAMETOOLONG;
+		return -1;
+	}
+	K_OOPS(err != 0);
+	K_OOPS(k_usermode_string_copy(dst, src, CONFIG_ZVFS_PATH_MAX));

+	return 0;
+}
+#endif /* CONFIG_USERSPACE */
+
+/*
+ * Bridge an fs_*() negative-errno return to the -1/errno convention.
+ *
+ * TODO: the ZVFS functions themselves should return a negative error number
+ * directly instead of setting errno, so callers (and the syscall layer) are not
+ * forced through thread-local errno. Until that refactor lands, this helper
+ * converts at the boundary.
+ */
+static int zvfs_err(int res)
+{
 	if (res < 0) {
 		errno = -res;
 		return -1;
@@ -24,13 +59,392 @@ int zvfs_unlink(const char *path)
 	return 0;
 }

-int zvfs_rename(const char *old, const char *newp)
+/* Zephyr's file system subsystem exposes no permission bits; synthesize a fixed mode. */
+static uint32_t zvfs_synth_mode(bool is_dir, bool rdonly)
+{
+	uint32_t mode;
+
+	if (is_dir) {
+		/* directories are always searchable */
+		mode = ZVFS_MODE_IFDIR | ZVFS_S_IRUSR | ZVFS_S_IXUSR | ZVFS_S_IRGRP |
+		       ZVFS_S_IXGRP | ZVFS_S_IROTH | ZVFS_S_IXOTH;
+	} else {
+		mode = ZVFS_MODE_IFREG | ZVFS_S_IRUSR | ZVFS_S_IRGRP | ZVFS_S_IROTH;
+	}
+
+	if (!rdonly) {
+		mode |= ZVFS_S_IWUSR | ZVFS_S_IWGRP | ZVFS_S_IWOTH;
+	}
+
+	return mode;
+}
+
+static bool zvfs_mount_rdonly(const char *path)
+{
+	uint8_t flags = 0;
+
+	if (fs_mount_flags(path, &flags) < 0) {
+		return false;
+	}
+
+	return (flags & FS_MOUNT_FLAG_READ_ONLY) != 0;
+}
+
+int z_impl_zvfs_unlink(const char *path)
 {
-	int res = fs_rename(old, newp);
+	int res;
+	struct fs_dirent entry;
+
+	res = fs_stat(path, &entry);
+	if (res == 0 && entry.type == FS_DIR_ENTRY_DIR) {
+		errno = EISDIR;
+		return -1;
+	}
+
+	return zvfs_err(fs_unlink(path));
+}

+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_unlink(const char *path)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_unlink(kpath);
+}
+#include <zephyr/syscalls/zvfs_unlink_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_rename(const char *old, const char *newp)
+{
+	return zvfs_err(fs_rename(old, newp));
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_rename(const char *old, const char *newp)
+{
+	char kold[CONFIG_ZVFS_PATH_MAX];
+	char knew[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kold, old) < 0 || zvfs_copy_user_path(knew, newp) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_rename(kold, knew);
+}
+#include <zephyr/syscalls/zvfs_rename_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_remove(const char *path)
+{
+	/* Zephyr fs_unlink() removes files and empty directories alike */
+	return zvfs_err(fs_unlink(path));
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_remove(const char *path)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_remove(kpath);
+}
+#include <zephyr/syscalls/zvfs_remove_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_stat(const char *path, struct zvfs_stat *buf)
+{
+	int res;
+	struct fs_dirent entry;
+	struct fs_statvfs vfs;
+	bool is_dir;
+
+	res = fs_stat(path, &entry);
 	if (res < 0) {
-		errno = -res;
+		return zvfs_err(res);
+	}
+
+	is_dir = (entry.type == FS_DIR_ENTRY_DIR);
+
+	*buf = (struct zvfs_stat){0};
+	buf->size = entry.size;
+	buf->mode = zvfs_synth_mode(is_dir, zvfs_mount_rdonly(path));
+	buf->nlink = 1;
+#ifdef CONFIG_FS_DIRENT_EXT
+	buf->atime = entry.atime;
+	buf->mtime = entry.mtime;
+	/* Zephyr file systems track no status-change time */
+	buf->ctime = entry.mtime;
+#endif
+
+	/* block accounting is best-effort: not every backend supports statvfs */
+	if (fs_statvfs(path, &vfs) == 0) {
+		buf->blksize = vfs.f_bsize;
+		buf->blocks = DIV_ROUND_UP(entry.size, 512);
+	}
+
+	return 0;
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_stat(const char *path, struct zvfs_stat *buf)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	K_OOPS(K_SYSCALL_MEMORY_WRITE(buf, sizeof(*buf)));
+	if (zvfs_copy_user_path(kpath, path) < 0) {
 		return -1;
 	}
+
+	return z_impl_zvfs_stat(kpath, buf);
+}
+#include <zephyr/syscalls/zvfs_stat_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+void zvfs_statvfs_from_fs(struct zvfs_statvfs *buf, const struct fs_statvfs *vfs, bool rdonly)
+{
+	*buf = (struct zvfs_statvfs){0};
+	buf->f_bsize = vfs->f_bsize;
+	buf->f_frsize = vfs->f_frsize;
+	buf->f_blocks = vfs->f_blocks;
+	buf->f_bfree = vfs->f_bfree;
+	buf->f_bavail = vfs->f_bfree;
+	buf->f_flag = ZVFS_ST_NOSUID | (rdonly ? ZVFS_ST_RDONLY : 0);
+	buf->f_namemax = MAX_FILE_NAME;
+}
+
+int z_impl_zvfs_statvfs(const char *path, struct zvfs_statvfs *buf)
+{
+	int res;
+	struct fs_statvfs vfs;
+
+	res = fs_statvfs(path, &vfs);
+	if (res < 0) {
+		return zvfs_err(res);
+	}
+
+	zvfs_statvfs_from_fs(buf, &vfs, zvfs_mount_rdonly(path));
+
 	return 0;
 }
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_statvfs(const char *path, struct zvfs_statvfs *buf)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	K_OOPS(K_SYSCALL_MEMORY_WRITE(buf, sizeof(*buf)));
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_statvfs(kpath, buf);
+}
+#include <zephyr/syscalls/zvfs_statvfs_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_fstatvfs(int fd, struct zvfs_statvfs *buf)
+{
+	struct fd_entry *entry = zvfs_fd_entry_get(fd);
+
+	if (entry == NULL) {
+		return -1;
+	}
+
+	return zvfs_fdtable_call_ioctl(entry->vtable, entry->obj, ZFD_IOCTL_STATVFS, buf);
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_fstatvfs(int fd, struct zvfs_statvfs *buf)
+{
+	if (zvfs_fd_syscall_verify(fd) == NULL) {
+		return -1;
+	}
+
+	K_OOPS(K_SYSCALL_MEMORY_WRITE(buf, sizeof(*buf)));
+
+	return z_impl_zvfs_fstatvfs(fd, buf);
+}
+#include <zephyr/syscalls/zvfs_fstatvfs_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_mkdir(const char *path, uint32_t mode)
+{
+	ARG_UNUSED(mode);
+
+	return zvfs_err(fs_mkdir(path));
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_mkdir(const char *path, uint32_t mode)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_mkdir(kpath, mode);
+}
+#include <zephyr/syscalls/zvfs_mkdir_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_rmdir(const char *path)
+{
+	int res;
+	struct fs_dirent entry;
+
+	res = fs_stat(path, &entry);
+	if (res < 0) {
+		return zvfs_err(res);
+	}
+
+	if (entry.type != FS_DIR_ENTRY_DIR) {
+		errno = ENOTDIR;
+		return -1;
+	}
+
+	return zvfs_err(fs_unlink(path));
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_rmdir(const char *path)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_rmdir(kpath);
+}
+#include <zephyr/syscalls/zvfs_rmdir_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_truncate(const char *path, off_t length)
+{
+	int res;
+	int close_res;
+	struct zvfs_fs_desc *ptr;
+
+	if (length < 0) {
+		errno = EINVAL;
+		return -1;
+	}
+
+	/* struct fs_file_t is too large for the privileged stack */
+	ptr = zvfs_fs_desc_alloc(false);
+	if (ptr == NULL) {
+		errno = EMFILE;
+		return -1;
+	}
+
+	fs_file_t_init(&ptr->file);
+
+	res = fs_open(&ptr->file, path, FS_O_WRITE);
+	if (res < 0) {
+		zvfs_fs_desc_free(ptr);
+		return zvfs_err(res);
+	}
+
+	res = fs_truncate(&ptr->file, length);
+	close_res = fs_close(&ptr->file);
+	zvfs_fs_desc_free(ptr);
+
+	if (res == 0) {
+		res = close_res;
+	}
+
+	return zvfs_err(res);
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_truncate(const char *path, off_t length)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_truncate(kpath, length);
+}
+#include <zephyr/syscalls/zvfs_truncate_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_access(const char *path, int amode)
+{
+	struct zvfs_stat st;
+
+	if ((amode & ~(ZVFS_R_OK | ZVFS_W_OK | ZVFS_X_OK)) != 0 && amode != ZVFS_F_OK) {
+		errno = EINVAL;
+		return -1;
+	}
+
+	if (z_impl_zvfs_stat(path, &st) < 0) {
+		return -1;
+	}
+
+	if ((amode & ZVFS_R_OK) != 0 && (st.mode & ZVFS_S_IRUSR) == 0) {
+		errno = EACCES;
+		return -1;
+	}
+
+	if ((amode & ZVFS_W_OK) != 0 && (st.mode & ZVFS_S_IWUSR) == 0) {
+		errno = EROFS;
+		return -1;
+	}
+
+	if ((amode & ZVFS_X_OK) != 0 && (st.mode & ZVFS_S_IXUSR) == 0) {
+		errno = EACCES;
+		return -1;
+	}
+
+	return 0;
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_access(const char *path, int amode)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_access(kpath, amode);
+}
+#include <zephyr/syscalls/zvfs_access_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_zvfs_utime(const char *path, const struct timespec *times)
+{
+	return zvfs_err(fs_utime(path, times));
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_utime(const char *path, const struct timespec *times)
+{
+	char kpath[CONFIG_ZVFS_PATH_MAX];
+	struct timespec ktimes[2];
+	const struct timespec *tp = NULL;
+
+	if (times != NULL) {
+		K_OOPS(k_usermode_from_copy(ktimes, times, sizeof(ktimes)));
+		tp = ktimes;
+	}
+
+	if (zvfs_copy_user_path(kpath, path) < 0) {
+		return -1;
+	}
+
+	return z_impl_zvfs_utime(kpath, tp);
+}
+#include <zephyr/syscalls/zvfs_utime_mrsh.c>
+#endif /* CONFIG_USERSPACE */
diff --git a/subsys/fs/fs.c b/subsys/fs/fs.c
index 8c019a71609..74319ad6d07 100644
--- a/subsys/fs/fs.c
+++ b/subsys/fs/fs.c
@@ -713,6 +713,25 @@ int fs_statvfs(const char *abs_path, struct fs_statvfs *stat)
 	return rc;
 }

+int fs_mount_flags(const char *abs_path, uint8_t *flags)
+{
+	struct fs_mount_t *mp;
+	int rc;
+
+	if ((abs_path == NULL) || (abs_path[0] != '/')) {
+		return -EINVAL;
+	}
+
+	rc = fs_get_mnt_point(&mp, abs_path, NULL);
+	if (rc < 0) {
+		return rc;
+	}
+
+	*flags = mp->flags;
+
+	return 0;
+}
+
 int fs_utime(const char *abs_path, const struct timespec times[2])
 {
 	struct fs_mount_t *mp;
