diff --git a/include/zephyr/kernel/process.h b/include/zephyr/kernel/process.h
index 124c1dc45d7..9b2c4953cdc 100644
--- a/include/zephyr/kernel/process.h
+++ b/include/zephyr/kernel/process.h
@@ -261,6 +261,10 @@ struct k_process {
 	/** Count of process-scoped pending signals (any member may claim) */
 	atomic_t sig_pending;
 #endif /* CONFIG_SIGNAL */
+#ifdef CONFIG_USERSPACE
+	/** Permission-principal index; every member thread holds it */
+	int16_t perm_index;
+#endif /* CONFIG_USERSPACE */
 	/** @endcond */
 };

diff --git a/include/zephyr/sys/internal/kobject_internal.h b/include/zephyr/sys/internal/kobject_internal.h
index ea19d0f9a8d..9c70b9c371e 100644
--- a/include/zephyr/sys/internal/kobject_internal.h
+++ b/include/zephyr/sys/internal/kobject_internal.h
@@ -58,9 +58,16 @@ union k_object_data {
  * @note This is an internal API. Do not use unless you are extending
  *       functionality in the Zephyr tree.
  */
+/* one permission bit per principal: processes when enabled, else threads */
+#ifdef CONFIG_PROCESS
+#define K_OBJ_PERM_BYTES CONFIG_MAX_PROCESS_BYTES
+#else
+#define K_OBJ_PERM_BYTES CONFIG_MAX_THREAD_BYTES
+#endif /* CONFIG_PROCESS */
+
 struct k_object {
 	void *name;
-	uint8_t perms[CONFIG_MAX_THREAD_BYTES];
+	uint8_t perms[K_OBJ_PERM_BYTES];
 	uint8_t type;
 	uint8_t flags;
 	union k_object_data data;
diff --git a/kernel/Kconfig.process b/kernel/Kconfig.process
index b726af39aed..3b6b42a00d8 100644
--- a/kernel/Kconfig.process
+++ b/kernel/Kconfig.process
@@ -19,6 +19,16 @@ config PROCESS

 if PROCESS

+config MAX_PROCESS_BYTES
+	int "Bytes to use to track process permissions to kernel objects"
+	default 1
+	depends on USERSPACE
+	help
+	  Every kernel object stores one permission bit per process (a thread
+	  holds permissions through the process it belongs to). This is the
+	  size of that per-object bitmap in bytes, bounding the number of
+	  simultaneously live processes.
+
 config PROCESS_INIT
 	bool "Start the init process with an initial thread"
 	help
diff --git a/kernel/include/kernel_internal.h b/kernel/include/kernel_internal.h
index b18bb99e8b7..61707c56963 100644
--- a/kernel/include/kernel_internal.h
+++ b/kernel/include/kernel_internal.h
@@ -167,6 +167,13 @@ static inline int z_session_cell_free(struct k_session *session)
 }
 #endif /* CONFIG_SYS_PROCESS */

+#ifdef CONFIG_USERSPACE
+/* permission-principal index management and the reap-time sweep */
+int16_t z_process_perm_index_alloc(void);
+void z_process_perm_index_free(int16_t index);
+void z_process_perms_all_clear(struct k_process *proc);
+#endif /* CONFIG_USERSPACE */
+
 /* validate a user-supplied group handle and every member record (under lock) */
 int z_pgrp_validate(k_pgrp_t pgrp);
 #endif /* CONFIG_PROCESS */
diff --git a/kernel/process.c b/kernel/process.c
index c9934292255..f3c1a277c3f 100644
--- a/kernel/process.c
+++ b/kernel/process.c
@@ -230,6 +230,9 @@ void z_kernel_process_init_one(struct k_process *proc, struct k_process *parent)
 	sys_dlist_init(&proc->threads);
 	z_waitq_init(&proc->waitq);
 	proc->state = K_PROCESS_ACTIVE;
+#ifdef CONFIG_USERSPACE
+	proc->perm_index = z_process_perm_index_alloc();
+#endif /* CONFIG_USERSPACE */
 	k_object_init(proc);
 	sys_dlist_append(&process_active, &proc->active_node);
 	if (parent != NULL) {
@@ -321,6 +324,9 @@ static void process_free_locked(struct k_process *proc)
 #endif /* CONFIG_SIGNAL */
 #ifdef CONFIG_USERSPACE
 	k_object_uninit(proc);
+	z_process_perms_all_clear(proc);
+	z_process_perm_index_free(proc->perm_index);
+	proc->perm_index = -1;
 #endif /* CONFIG_USERSPACE */
 	if ((proc->flags & K_PROCESS_FLAG_LEADER_RELEASE) != 0U) {
 		z_process_leader_release(proc->leader);
@@ -445,6 +451,11 @@ static int process_boot_init(void)
 	for (size_t i = 0; i < RESERVED_NUM; i++) {
 		sys_dlist_append(&session_active, &z_session_reserved[i].cell_node);
 	}
+#ifdef CONFIG_USERSPACE
+	z_proc_idle.perm_index = z_process_perm_index_alloc();
+	z_proc_init.perm_index = z_process_perm_index_alloc();
+	z_proc_kth.perm_index = z_process_perm_index_alloc();
+#endif /* CONFIG_USERSPACE */

 	STRUCT_SECTION_FOREACH(k_process, proc) {
 		z_kernel_process_init_one(proc, &z_proc_init);
diff --git a/kernel/userspace.c b/kernel/userspace.c
index f6106250b98..93177e1e932 100644
--- a/kernel/userspace.c
+++ b/kernel/userspace.c
@@ -77,7 +77,9 @@ static struct k_spinlock obj_lock;         /* kobj struct data */

 #ifdef CONFIG_DYNAMIC_OBJECTS
 extern uint8_t _thread_idx_map[CONFIG_MAX_THREAD_BYTES];
+#ifndef CONFIG_PROCESS
 static void clear_perms_cb(struct k_object *ko, void *ctx_ptr);
+#endif /* CONFIG_PROCESS */
 #endif /* CONFIG_DYNAMIC_OBJECTS */

 const char *otype_to_str(enum k_objects otype)
@@ -282,9 +284,11 @@ static bool thread_idx_alloc(uintptr_t *tidx)
 			 */
 			_thread_idx_map[i] &= ~(BIT(idx - 1));

+#ifndef CONFIG_PROCESS
 			/* Clear permission from all objects */
 			k_object_wordlist_foreach(clear_perms_cb,
 						   (void *)*tidx);
+#endif /* CONFIG_PROCESS */

 			return true;
 		}
@@ -307,8 +311,10 @@ static bool thread_idx_alloc(uintptr_t *tidx)
  **/
 static void thread_idx_free(uintptr_t tidx)
 {
+#ifndef CONFIG_PROCESS
 	/* To prevent leaked permission when index is recycled */
 	k_object_wordlist_foreach(clear_perms_cb, (void *)tidx);
+#endif /* CONFIG_PROCESS */

 	/* Figure out which bits to set in _thread_idx_map[] and set it. */
 	int base = tidx / NUM_BITS(_thread_idx_map[0]);
@@ -384,7 +390,7 @@ static struct k_object *dynamic_object_create(enum k_objects otype, size_t align

 	dyn->kobj.type = otype;
 	dyn->kobj.flags = 0;
-	(void)memset(dyn->kobj.perms, 0, CONFIG_MAX_THREAD_BYTES);
+	(void)memset(dyn->kobj.perms, 0, K_OBJ_PERM_BYTES);

 	k_spinlock_key_t key = k_spin_lock(&lists_lock);

@@ -626,7 +632,7 @@ void *z_impl_k_object_map_size(enum k_objects otype, size_t size)

 	dyn->kobj.type = otype;
 	dyn->kobj.flags = K_OBJ_FLAG_MAP;
-	(void)memset(dyn->kobj.perms, 0, CONFIG_MAX_THREAD_BYTES);
+	(void)memset(dyn->kobj.perms, 0, K_OBJ_PERM_BYTES);

 	K_SPINLOCK(&lists_lock) {
 		sys_dlist_append(&obj_list, &dyn->dobj_list);
@@ -666,6 +672,15 @@ __weak void k_object_wordlist_foreach(_wordlist_cb_func_t func, void *context)

 static unsigned int thread_index_get(struct k_thread *thread)
 {
+#ifdef CONFIG_PROCESS
+	struct k_process *proc = thread->process;
+
+	if ((proc == NULL) || (proc->perm_index < 0)) {
+		return -1;
+	}
+
+	return (unsigned int)proc->perm_index;
+#else
 	struct k_object *ko;

 	ko = k_object_find(thread);
@@ -675,8 +690,46 @@ static unsigned int thread_index_get(struct k_thread *thread)
 	}

 	return ko->data.thread_id;
+#endif /* CONFIG_PROCESS */
+}
+
+#ifdef CONFIG_PROCESS
+static void clear_perms_sched_locked_cb(struct k_object *ko, void *ctx_ptr);
+
+static uint8_t process_idx_map[CONFIG_MAX_PROCESS_BYTES];
+
+int16_t z_process_perm_index_alloc(void)
+{
+	for (int i = 0; i < CONFIG_MAX_PROCESS_BYTES; i++) {
+		int bit = find_lsb_set((uint8_t)~process_idx_map[i]);
+
+		if (bit != 0) {
+			process_idx_map[i] |= BIT(bit - 1);
+			return (int16_t)((i * BITS_PER_BYTE) + (bit - 1));
+		}
+	}
+
+	return -1;
 }

+void z_process_perm_index_free(int16_t index)
+{
+	if (index >= 0) {
+		process_idx_map[index / BITS_PER_BYTE] &=
+			(uint8_t)~BIT((unsigned int)index % BITS_PER_BYTE);
+	}
+}
+
+void z_process_perms_all_clear(struct k_process *proc)
+{
+	uintptr_t index = (uintptr_t)proc->perm_index;
+
+	if (proc->perm_index >= 0) {
+		k_object_wordlist_foreach(clear_perms_sched_locked_cb, (void *)index);
+	}
+}
+#endif /* CONFIG_PROCESS */
+
 static void unref_check(struct k_object *ko, uintptr_t index, bool sched_locked)
 {
 	k_spinlock_key_t key = k_spin_lock(&obj_lock);
@@ -695,7 +748,7 @@ static void unref_check(struct k_object *ko, uintptr_t index, bool sched_locked)

 	__ASSERT(IS_PTR_ALIGNED(dyn, struct dyn_obj), "unaligned z_object");

-	for (int i = 0; i < CONFIG_MAX_THREAD_BYTES; i++) {
+	for (int i = 0; i < K_OBJ_PERM_BYTES; i++) {
 		if (ko->perms[i] != 0U) {
 			goto out;
 		}
@@ -785,12 +838,14 @@ void k_thread_perms_clear(struct k_object *ko, struct k_thread *thread)
 }

 #ifdef CONFIG_DYNAMIC_OBJECTS
+#ifndef CONFIG_PROCESS
 static void clear_perms_cb(struct k_object *ko, void *ctx_ptr)
 {
 	uintptr_t id = (uintptr_t)ctx_ptr;

 	unref_check(ko, id, false);
 }
+#endif /* CONFIG_PROCESS */
 #endif

 static void clear_perms_sched_locked_cb(struct k_object *ko, void *ctx_ptr)
@@ -802,12 +857,17 @@ static void clear_perms_sched_locked_cb(struct k_object *ko, void *ctx_ptr)

 void k_thread_perms_all_clear(struct k_thread *thread)
 {
+#ifdef CONFIG_PROCESS
+	/* permissions belong to the process and are swept at reap */
+	ARG_UNUSED(thread);
+#else
 	uintptr_t index = thread_index_get(thread);

 	if ((int)index != -1) {
 		k_object_wordlist_foreach(clear_perms_sched_locked_cb,
 					 (void *)index);
 	}
+#endif /* CONFIG_PROCESS */
 }

 static int thread_perms_test(struct k_object *ko)
diff --git a/lib/os/process.c b/lib/os/process.c
index b7ac96bc702..2dd35ec6578 100644
--- a/lib/os/process.c
+++ b/lib/os/process.c
@@ -700,6 +700,14 @@ int z_sys_clone_process(const struct sys_clone_args *args, k_pid_t *child)
 	if (pooled) {
 		proc->flags |= K_PROCESS_FLAG_LEADER_RELEASE;
 	}
+#ifdef CONFIG_USERSPACE
+	/* grants made at creation (the leader, its stack, the cloned descriptors) landed on the
+	 * caller's process: the child inherits them, as a copied process does
+	 */
+	z_process_perms_inherit(k_current_get()->process, proc);
+	k_object_access_grant(tid, tid);
+	k_object_access_grant(tid->stack_obj, tid);
+#endif /* CONFIG_USERSPACE */

 #ifdef CONFIG_SIGNAL
 	if (args->sigmask != NULL) {
