diff --git a/include/zephyr/sys/process.h b/include/zephyr/sys/process.h
index f31a9628333..ac14223b9be 100644
--- a/include/zephyr/sys/process.h
+++ b/include/zephyr/sys/process.h
@@ -92,6 +92,15 @@ __syscall k_session_t sys_session_find(int id);
  */
 #define SYS_CLONE_VM_COPY BIT64(0)
 
+/**
+ * @brief Create the child stopped
+ *
+ * The child is fully constructed but its initial thread is not started; the
+ * caller completes setup (process group, signal state, priority) and starts
+ * it with k_thread_start() on the returned child handle.
+ */
+#define SYS_CLONE_PAUSED BIT64(1)
+
 struct sys_clone_args {
 	/** SYS_CLONE_* flags; zero clones nothing from the caller */
 	uint64_t flags;
@@ -109,6 +118,13 @@ struct sys_clone_args {
 	int prio;
 	/** Initial thread options (e.g. K_USER) */
 	uint32_t options;
+	/**
+	 * Initial signal mask of the leader, or NULL for the thread-creation
+	 * inheritance rules; K_SIG_KILL and K_SIG_STOP stay unmaskable.
+	 * Requires CONFIG_SIGNAL and is invalid with SYS_CLONE_VM_COPY (a
+	 * forked child always inherits the caller's mask).
+	 */
+	const struct k_sig_set *sigmask;
 };
 
 /**
diff --git a/lib/os/process.c b/lib/os/process.c
index 7d5dba507c2..c8bca11b772 100644
--- a/lib/os/process.c
+++ b/lib/os/process.c
@@ -709,7 +709,12 @@ int z_impl_sys_clone(const struct sys_clone_args *args, k_pid_t *child)
 	struct k_process *proc;
 	bool pooled = false;
 
-	if ((args == NULL) || ((args->flags & ~SYS_CLONE_VM_COPY) != 0U)) {
+	if ((args == NULL) ||
+	    ((args->flags & ~(SYS_CLONE_VM_COPY | SYS_CLONE_PAUSED)) != 0U)) {
+		return -EINVAL;
+	}
+	if ((args->sigmask != NULL) &&
+	    (!IS_ENABLED(CONFIG_SIGNAL) || ((args->flags & SYS_CLONE_VM_COPY) != 0U))) {
 		return -EINVAL;
 	}
 	if ((args->flags & SYS_CLONE_VM_COPY) != 0U) {
@@ -778,11 +783,22 @@ int z_impl_sys_clone(const struct sys_clone_args *args, k_pid_t *child)
 	}
 #endif /* CONFIG_USERSPACE */
 
+#ifdef CONFIG_SIGNAL
+	if (args->sigmask != NULL) {
+		/* seed the paused leader's mask; scrub as k_sig_mask() does */
+		tid->base.sig.mask = *args->sigmask;
+		tid->base.sig.mask.sig[0] &=
+			~(BIT64(K_SIG_KILL - 1) | BIT64(K_SIG_STOP - 1));
+	}
+#endif /* CONFIG_SIGNAL */
+
 	if (child != NULL) {
 		*child = tid;
 	}
 
-	k_thread_start(tid);
+	if ((args->flags & SYS_CLONE_PAUSED) == 0U) {
+		k_thread_start(tid);
+	}
 
 	return 0;
 }
@@ -793,11 +809,20 @@ static inline int z_vrfy_sys_clone(const struct sys_clone_args *args, k_pid_t *c
 	struct sys_clone_args copy;
 	size_t total_size, stack_obj_size;
 	struct k_object *stack_object;
+#ifdef CONFIG_SIGNAL
+	struct k_sig_set mask_copy;
+#endif /* CONFIG_SIGNAL */
 
 	K_OOPS(k_usermode_from_copy(&copy, args, sizeof(copy)));
 	if (child != NULL) {
 		K_OOPS(K_SYSCALL_MEMORY_WRITE(child, sizeof(*child)));
 	}
+#ifdef CONFIG_SIGNAL
+	if (copy.sigmask != NULL) {
+		K_OOPS(k_usermode_from_copy(&mask_copy, copy.sigmask, sizeof(mask_copy)));
+		copy.sigmask = &mask_copy;
+	}
+#endif /* CONFIG_SIGNAL */
 
 	if (copy.thread == NULL) {
 		/* pool-drawn leader: no caller objects to validate */
