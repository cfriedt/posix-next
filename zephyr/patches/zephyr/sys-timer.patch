diff --git a/include/zephyr/sys/timer.h b/include/zephyr/sys/timer.h
new file mode 100644
index 00000000000..45b788710b7
--- /dev/null
+++ b/include/zephyr/sys/timer.h
@@ -0,0 +1,126 @@
+/*
+ * SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+#ifndef ZEPHYR_INCLUDE_SYS_TIMER_H_
+#define ZEPHYR_INCLUDE_SYS_TIMER_H_
+
+#include <zephyr/kernel.h>
+#include <zephyr/kernel/signal.h>
+#include <zephyr/sys/clock.h>
+
+#ifdef __cplusplus
+extern "C" {
+#endif
+
+/**
+ * @file
+ * @defgroup sys_timer_apis System Timer Pool APIs
+ * @ingroup os_services
+ *
+ * @brief OS-managed pools of kernel timer objects
+ *
+ * The @c sys_timer API manages allocation, deallocation, and the advanced initialization of
+ * @ref k_timer objects from an OS-owned pool, behind system calls. It is the substrate for
+ * POSIX timers (`timer_create()` / `timer_delete()`), but is equally usable by native
+ * applications that want create-and-go timers without static definitions.
+ *
+ * The pool is an elastipool: @c SYS_TIMER_MIN objects (the sum of
+ * @kconfig{CONFIG_SYS_TIMER_MIN} and all `CONFIG_SYS_TIMER_MIN_ADD_*` contributions) are
+ * statically allocated and guaranteed; up to @kconfig{CONFIG_SYS_TIMER_MAX} objects in total
+ * may be allocated, the excess coming from the heap without guarantees. Setting the maximum
+ * equal to the minimum prohibits dynamic allocation entirely.
+ *
+ * Timer arming and queries use the clock-based @ref k_timer_settime and @ref k_timer_gettime
+ * APIs (@kconfig{CONFIG_TIMER_CLOCK}), regardless of how the timer was allocated.
+ *
+ * @{
+ */
+
+/**
+ * @brief Expiry notification description for sys_timer_create()
+ *
+ * Describes the signal generated when the timer expires. Requires
+ * @kconfig{CONFIG_TIMER_SIGNAL}.
+ */
+struct k_timer_notify {
+	/** Thread the signal is delivered to. */
+	k_pid_t target;
+	/** Kernel signal number to queue on expiry */
+	int signo;
+	/** Value accompanying the queued signal */
+	union k_sig_val value;
+};
+
+/**
+ * @brief sys_timer_create() flag: deliver the timer's own address as the notification value
+ *
+ * When set, the created timer's address overrides @c notify->value as the value carried by
+ * expiry signals (as `sival_ptr`). POSIX `timer_create()` uses this for the `evp == NULL`
+ * default, whose notification value is the timer ID - unknowable before creation.
+ * Requires @kconfig{CONFIG_TIMER_SIGNAL}.
+ */
+#define SYS_TIMER_SIGVAL_SELF BIT(0)
+
+#if defined(CONFIG_SYS_TIMER) || defined(__DOXYGEN__)
+
+/**
+ * @brief Allocate and initialize a kernel timer from the OS timer pool
+ *
+ * Allocates a @ref k_timer from the system timer pool, binds it to @a clock_id
+ * (@c SYS_CLOCK_MONOTONIC or @c SYS_CLOCK_REALTIME), optionally attaches signal-based expiry
+ * notification (@a notify, requires @kconfig{CONFIG_TIMER_SIGNAL}), and - if @a value is
+ * non-NULL - arms it atomically at creation, exactly as
+ * `k_timer_settime(*timer, flags, value, interval, NULL, NULL)` would.
+ *
+ * With @a value NULL the timer is created disarmed (POSIX `timer_create()` semantics; arm it
+ * later with @ref k_timer_settime).
+ *
+ * When called from a user thread, the calling thread is granted access to the new timer
+ * object.
+ *
+ * TODO: pool objects become per-process resources, released on process exit.
+ *
+ * @param clock_id  Clock the timer measures against.
+ * @param notify    Optional expiry notification description (NULL for none).
+ * @param flags     0, @c SYS_TIMER_ABSTIME (applies to @a value), and/or
+ *                  @ref SYS_TIMER_SIGVAL_SELF (applies to @a notify).
+ * @param value     Optional initial expiration; NULL to create the timer disarmed.
+ * @param interval  Optional period (used only when @a value is non-NULL).
+ * @param timer     Output: the allocated timer.
+ *
+ * @retval 0 on success
+ * @retval -EAGAIN when the pool is exhausted
+ * @retval -EINVAL on invalid clock, flags, or timespec values
+ * @retval -ENOTSUP when @a notify is non-NULL without @kconfig{CONFIG_TIMER_SIGNAL}
+ */
+__syscall int sys_timer_create(int clock_id, const struct k_timer_notify *notify, uint32_t flags,
+			       const struct timespec *value, const struct timespec *interval,
+			       struct k_timer **timer);
+
+/**
+ * @brief Stop, tear down, and release a kernel timer back to the OS timer pool
+ *
+ * Stops @a timer, detaches any expiry notification (purging queued-but-undelivered signal
+ * instances so that no kernel reference to the timer survives this call), revokes user-mode
+ * grants, and returns the object to the pool.
+ *
+ * @param timer Timer previously allocated with sys_timer_create().
+ *
+ * @retval 0 on success
+ * @retval -EINVAL when @a timer was not allocated from the pool (stale or foreign handle)
+ */
+__syscall int sys_timer_delete(struct k_timer *timer);
+
+#include <zephyr/syscalls/timer.h>
+
+#endif /* defined(CONFIG_SYS_TIMER) || defined(__DOXYGEN__) */
+
+/** @} */
+
+#ifdef __cplusplus
+}
+#endif
+
+#endif /* ZEPHYR_INCLUDE_SYS_TIMER_H_ */
diff --git a/lib/os/CMakeLists.txt b/lib/os/CMakeLists.txt
index df4976f8d53..0a9698bfa44 100644
--- a/lib/os/CMakeLists.txt
+++ b/lib/os/CMakeLists.txt
@@ -95,3 +95,16 @@ if(CONFIG_SYS_THREAD)
     zephyr_compile_definitions(SYS_THREAD_${_pool}_MIN=${_sys_thread_${_pool}_min})
   endforeach()
 endif()
+
+zephyr_sources_ifdef(CONFIG_SYS_TIMER sys_timer.c)
+zephyr_syscall_header_ifdef(CONFIG_SYS_TIMER ${ZEPHYR_BASE}/include/zephyr/sys/timer.h)
+
+if(CONFIG_SYS_TIMER)
+  # Sum CONFIG_SYS_TIMER_MIN with any CONFIG_SYS_TIMER_MIN_ADD_* additions
+  import_kconfig(CONFIG_SYS_TIMER_MIN_ADD_ ${DOTCONFIG} _sys_timer_min_add_keys)
+  set(_sys_timer_min ${CONFIG_SYS_TIMER_MIN})
+  foreach(_add ${_sys_timer_min_add_keys})
+    math(EXPR _sys_timer_min "${_sys_timer_min} + ${${_add}}")
+  endforeach()
+  zephyr_compile_definitions(SYS_TIMER_MIN=${_sys_timer_min})
+endif()
diff --git a/lib/os/Kconfig b/lib/os/Kconfig
index 7c9868d093a..f5decacd38f 100644
--- a/lib/os/Kconfig
+++ b/lib/os/Kconfig
@@ -145,6 +145,8 @@ config POWEROFF
 
 rsource "Kconfig.cbprintf"
 rsource "Kconfig.sys_thread"
+
+rsource "Kconfig.sys_timer"
 rsource "zvfs/Kconfig"
 rsource "cpu_load/Kconfig"
 
diff --git a/lib/os/Kconfig.sys_timer b/lib/os/Kconfig.sys_timer
new file mode 100644
index 00000000000..22ecdbf0aa5
--- /dev/null
+++ b/lib/os/Kconfig.sys_timer
@@ -0,0 +1,50 @@
+# SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
+# SPDX-License-Identifier: Apache-2.0
+
+config SYS_TIMER
+	bool "Support an OS-managed pool of kernel timers"
+	select DYNAMIC_OBJECTS if USERSPACE
+	select SYS_ELASTIPOOL
+	select TIMER_CLOCK
+	# to support heap-based allocation by default, can be disabled
+	imply SYS_HASH_MAP
+	imply SYS_HASH_FUNC32
+	help
+	  Select 'y' here to support an OS-managed pool of k_timer objects,
+	  allocated and released with the sys_timer_create() and
+	  sys_timer_delete() system calls. This is the substrate for POSIX
+	  timers (timer_create()/timer_delete()).
+
+if SYS_TIMER
+
+config SYS_TIMER_MAX
+	int "Maximum size of the system timer pool"
+	default 2147483647
+	help
+	  This value represents the maximum number of timers that may be
+	  allocated from the system timer pool. The first SYS_TIMER_MIN (the
+	  sum of CONFIG_SYS_TIMER_MIN and all CONFIG_SYS_TIMER_MIN_ADD_*
+	  contributions) are statically allocated and guaranteed; the remainder
+	  come from the heap, without guarantees.
+
+	  The default (INT_MAX) effectively does not limit dynamic allocation.
+	  Set this equal to the accumulated minimum to prohibit dynamic
+	  allocation entirely.
+
+	  CONFIG_SYS_TIMER_MAX must be >= the accumulated SYS_TIMER_MIN.
+
+config SYS_TIMER_MIN
+	int "Minimum size of the system timer pool"
+	default 0
+	help
+	  This value represents the minimum number of timers that are
+	  guaranteed to be available from the system timer pool. The minimum
+	  number is guaranteed via static allocation.
+
+	  Applications and libraries contribute additional guaranteed timers by
+	  defining int symbols named CONFIG_SYS_TIMER_MIN_ADD_<NAME>; the build
+	  system sums CONFIG_SYS_TIMER_MIN with all such contributions and, for
+	  technical reasons, provides the result to code as the un-prefixed
+	  SYS_TIMER_MIN compile definition.
+
+endif # SYS_TIMER
diff --git a/lib/os/sys_timer.c b/lib/os/sys_timer.c
new file mode 100644
index 00000000000..f048d0d6e00
--- /dev/null
+++ b/lib/os/sys_timer.c
@@ -0,0 +1,208 @@
+/*
+ * Copyright (c) 2026, Friedt Professional Engineering Services, Inc.
+ *
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+#include <time.h>
+
+#include <zephyr/internal/syscall_handler.h>
+#include <zephyr/kernel.h>
+#include <zephyr/logging/log.h>
+#include <zephyr/sys/elastipool.h>
+#include <zephyr/sys/internal/kobject_internal.h>
+#include <zephyr/sys/timer.h>
+#include <zephyr/toolchain.h>
+
+LOG_MODULE_REGISTER(sys_timer, CONFIG_KERNEL_LOG_LEVEL);
+
+/* For declaring an array of statically-initialized k_timer objects */
+#define K_TIMER_ARRAY_DEFINE_ELEM(idx, name) \
+	Z_TIMER_INITIALIZER_EXT(name[idx], NULL, NULL, SYS_CLOCK_MONOTONIC)
+#define K_TIMER_ARRAY_DEFINE(name, num)                                                            \
+	__maybe_unused struct k_timer name[] = {                                                   \
+		LISTIFY(num, K_TIMER_ARRAY_DEFINE_ELEM, (,), name) }
+
+static void *timer_pool_heap_alloc(const void *ptr, size_t size, size_t align, uint32_t flags)
+{
+	ARG_UNUSED(align);
+	ARG_UNUSED(flags);
+
+	if (size == 0) {
+		/* deallocate */
+#ifdef CONFIG_USERSPACE
+		if (k_object_find((void *)ptr) != NULL) {
+			k_object_free((void *)ptr);
+			return NULL;
+		}
+#endif /* CONFIG_USERSPACE */
+#if defined(K_HEAP_MEM_POOL_SIZE) && (K_HEAP_MEM_POOL_SIZE > 0)
+		k_free((void *)ptr);
+#endif
+		return NULL;
+	}
+
+	if (IS_ENABLED(CONFIG_DYNAMIC_OBJECTS)) {
+		void *timer = k_object_alloc(K_OBJ_TIMER);
+
+#ifdef CONFIG_DYNAMIC_OBJECTS
+		if (timer != NULL) {
+			/* the pool owns the lifetime: opt out of perms-refcount disposal */
+			k_object_find(timer)->flags &= ~K_OBJ_FLAG_ALLOC;
+		}
+#endif /* CONFIG_DYNAMIC_OBJECTS */
+
+		return timer;
+	}
+
+	if (IS_ENABLED(CONFIG_USERSPACE) && k_is_user_context()) {
+		LOG_WRN_ONCE("CONFIG_DYNAMIC_OBJECTS is not enabled");
+		return NULL;
+	}
+
+#if defined(K_HEAP_MEM_POOL_SIZE) && (K_HEAP_MEM_POOL_SIZE > 0)
+	return k_aligned_alloc(__alignof(struct k_timer), sizeof(struct k_timer));
+#else
+	return NULL;
+#endif
+}
+
+BUILD_ASSERT(SYS_TIMER_MIN >= 0);
+BUILD_ASSERT(CONFIG_SYS_TIMER_MAX >= SYS_TIMER_MIN);
+
+static struct k_spinlock timer_pool_lock;
+K_TIMER_ARRAY_DEFINE(sys_timer_pool, SYS_TIMER_MIN);
+SYS_ELASTIPOOL_DEFINE_ADVANCED(timer_pool, sizeof(struct k_timer), __alignof(struct k_timer),
+			       SYS_TIMER_MIN, CONFIG_SYS_TIMER_MAX, timer_pool_heap_alloc,
+			       sys_timer_pool, static);
+
+int z_impl_sys_timer_create(int clock_id, const struct k_timer_notify *notify, uint32_t flags,
+			    const struct timespec *value, const struct timespec *interval,
+			    struct k_timer **timer)
+{
+	int ret;
+	struct k_timer *t = NULL;
+
+	if ((timer == NULL) ||
+	    ((clock_id != SYS_CLOCK_MONOTONIC) && (clock_id != SYS_CLOCK_REALTIME))) {
+		return -EINVAL;
+	}
+
+	if (notify != NULL) {
+		/* signal-based expiry notification arrives with CONFIG_TIMER_SIGNAL */
+		return -ENOTSUP;
+	}
+
+	K_SPINLOCK(&timer_pool_lock) {
+		ret = sys_elastipool_alloc(&timer_pool, (void **)&t);
+	}
+	if (ret < 0) {
+		/* POSIX timer_create() reports resource exhaustion as EAGAIN */
+		return -EAGAIN;
+	}
+
+	k_timer_init(t, NULL, NULL);
+	t->clock_id = (uint8_t)clock_id;
+
+#ifdef CONFIG_USERSPACE
+	if (k_is_in_user_syscall()) {
+		k_object_access_grant(t, _current);
+	}
+#endif /* CONFIG_USERSPACE */
+
+	if (value != NULL) {
+		/* arm atomically at creation; POSIX timer_create() passes value == NULL */
+		ret = k_timer_settime(t, flags, value, interval, NULL, NULL);
+		if (ret < 0) {
+			goto err_release;
+		}
+	}
+
+	*timer = t;
+
+	return 0;
+
+err_release:
+#ifdef CONFIG_USERSPACE
+	if (k_is_in_user_syscall()) {
+		k_object_access_revoke(t, _current);
+	}
+#endif /* CONFIG_USERSPACE */
+	K_SPINLOCK(&timer_pool_lock) {
+		(void)sys_elastipool_free(&timer_pool, t);
+	}
+
+	return ret;
+}
+
+#ifdef CONFIG_USERSPACE
+static inline int z_vrfy_sys_timer_create(int clock_id, const struct k_timer_notify *notify,
+					  uint32_t flags, const struct timespec *value,
+					  const struct timespec *interval, struct k_timer **timer)
+{
+	struct k_timer_notify knotify;
+	struct timespec kvalue;
+	struct timespec kinterval;
+	const struct k_timer_notify *np = NULL;
+	const struct timespec *vp = NULL;
+	const struct timespec *ip = NULL;
+
+	K_OOPS(K_SYSCALL_MEMORY_WRITE(timer, sizeof(*timer)));
+	if (notify != NULL) {
+		/* copy to kernel stack: the target pointer must not change after validation */
+		K_OOPS(k_usermode_from_copy(&knotify, notify, sizeof(knotify)));
+		/* the caller must have been granted access to the target thread */
+		K_OOPS(K_SYSCALL_OBJ(knotify.target, K_OBJ_THREAD));
+		np = &knotify;
+	}
+	if (value != NULL) {
+		K_OOPS(k_usermode_from_copy(&kvalue, value, sizeof(kvalue)));
+		vp = &kvalue;
+	}
+	if (interval != NULL) {
+		K_OOPS(k_usermode_from_copy(&kinterval, interval, sizeof(kinterval)));
+		ip = &kinterval;
+	}
+	return z_impl_sys_timer_create(clock_id, np, flags, vp, ip, timer);
+}
+#include <zephyr/syscalls/sys_timer_create_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+int z_impl_sys_timer_delete(struct k_timer *timer)
+{
+	int ret = 0;
+
+	K_SPINLOCK(&timer_pool_lock) {
+		if (sys_elastipool_check(&timer_pool, timer) != 0) {
+			ret = -EINVAL;
+			K_SPINLOCK_BREAK;
+		}
+	}
+	if (ret < 0) {
+		return ret;
+	}
+
+	/* stops the timer and deregisters it from the realtime re-target registry */
+	k_timer_stop(timer);
+
+#ifdef CONFIG_USERSPACE
+	if (k_is_in_user_syscall()) {
+		k_object_access_revoke(timer, _current);
+	}
+#endif /* CONFIG_USERSPACE */
+
+	K_SPINLOCK(&timer_pool_lock) {
+		ret = sys_elastipool_free(&timer_pool, timer);
+	}
+
+	return ret;
+}
+
+#ifdef CONFIG_USERSPACE
+static inline int z_vrfy_sys_timer_delete(struct k_timer *timer)
+{
+	K_OOPS(K_SYSCALL_OBJ(timer, K_OBJ_TIMER));
+	return z_impl_sys_timer_delete(timer);
+}
+#include <zephyr/syscalls/sys_timer_delete_mrsh.c>
+#endif /* CONFIG_USERSPACE */
diff --git a/tests/lib/sys_timer/CMakeLists.txt b/tests/lib/sys_timer/CMakeLists.txt
new file mode 100644
index 00000000000..38dd4158464
--- /dev/null
+++ b/tests/lib/sys_timer/CMakeLists.txt
@@ -0,0 +1,7 @@
+# SPDX-License-Identifier: Apache-2.0
+
+cmake_minimum_required(VERSION 3.20.0)
+find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
+project(sys_timer)
+
+target_sources(app PRIVATE src/main.c)
diff --git a/tests/lib/sys_timer/Kconfig b/tests/lib/sys_timer/Kconfig
new file mode 100644
index 00000000000..f3332f6d9ba
--- /dev/null
+++ b/tests/lib/sys_timer/Kconfig
@@ -0,0 +1,11 @@
+# SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
+# SPDX-License-Identifier: Apache-2.0
+
+config SYS_TIMER_MIN_ADD_TEST
+	int "Guaranteed system timers contributed by this test"
+	default 2
+	help
+	  Contribution of this test to the accumulated minimum (guaranteed,
+	  statically allocated) size of the system timer pool.
+
+source "Kconfig.zephyr"
diff --git a/tests/lib/sys_timer/prj.conf b/tests/lib/sys_timer/prj.conf
new file mode 100644
index 00000000000..15401b44d48
--- /dev/null
+++ b/tests/lib/sys_timer/prj.conf
@@ -0,0 +1,4 @@
+CONFIG_ZTEST=y
+CONFIG_SYS_TIMER=y
+CONFIG_ASSERT=y
+CONFIG_HEAP_MEM_POOL_SIZE=16384
diff --git a/tests/lib/sys_timer/src/main.c b/tests/lib/sys_timer/src/main.c
new file mode 100644
index 00000000000..9a8b330f639
--- /dev/null
+++ b/tests/lib/sys_timer/src/main.c
@@ -0,0 +1,170 @@
+/*
+ * SPDX-FileCopyrightText: Copyright The Zephyr Project Contributors
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+#include <zephyr/kernel.h>
+#include <zephyr/sys/clock.h>
+#include <zephyr/sys/timer.h>
+#include <zephyr/sys/timeutil.h>
+#include <zephyr/ztest.h>
+#include <zephyr/ztest_error_hook.h>
+
+#define ARM_TICKS 50
+
+static void ts_from_ticks(uint64_t ticks, struct timespec *ts)
+{
+	timespec_from_timeout(K_TICKS((k_ticks_t)ticks), ts);
+}
+
+static void create_invalid(void)
+{
+	struct k_timer *t;
+	struct k_timer_notify notify = {0};
+
+	if (!k_is_user_context()) {
+		/* in user mode a NULL output pointer faults in the syscall layer instead */
+		zassert_equal(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, NULL),
+			      -EINVAL);
+	}
+	zassert_equal(sys_timer_create(0x7f, NULL, 0, NULL, NULL, &t), -EINVAL);
+	/*
+	 * Signal-based notification requires CONFIG_TIMER_SIGNAL. Kernel mode only: in
+	 * user mode the syscall layer faults on the NULL target before the -ENOTSUP.
+	 */
+	if (!IS_ENABLED(CONFIG_TIMER_SIGNAL) && !k_is_user_context()) {
+		zassert_equal(sys_timer_create(SYS_CLOCK_MONOTONIC, &notify, 0, NULL, NULL, &t),
+			      -ENOTSUP);
+	}
+}
+
+static void create_disarmed_delete(void)
+{
+	struct k_timer *t;
+	struct timespec rem;
+
+	zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &t));
+	zassert_equal(k_timer_gettime(t, &rem, NULL), 0, "created timer is not disarmed");
+	zassert_true((rem.tv_sec == 0) && (rem.tv_nsec == 0));
+	zassert_ok(sys_timer_delete(t));
+}
+
+static void create_armed_fires(void)
+{
+	struct k_timer *t;
+	struct timespec value;
+
+	ts_from_ticks(ARM_TICKS, &value);
+
+	zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, &value, NULL, &t));
+	zassert_equal(k_timer_gettime(t, NULL, NULL), 1, "arm-at-create timer is not armed");
+	zassert_true(k_timer_status_sync(t) > 0, "arm-at-create timer did not fire");
+	zassert_equal(k_timer_gettime(t, NULL, NULL), 0, "expired one-shot still armed");
+	zassert_ok(sys_timer_delete(t));
+}
+
+static void delete_while_armed(void)
+{
+	struct k_timer *t;
+	struct timespec value;
+	struct timespec interval;
+
+	ts_from_ticks(ARM_TICKS, &value);
+	ts_from_ticks(ARM_TICKS, &interval);
+
+	zassert_ok(sys_timer_create(SYS_CLOCK_REALTIME, NULL, 0, &value, &interval, &t));
+	zassert_true(k_timer_status_sync(t) > 0);
+	zassert_ok(sys_timer_delete(t), "deleting an armed periodic timer failed");
+}
+
+static void min_guaranteed(void)
+{
+	struct k_timer *timers[2];
+
+	/* this test contributes SYS_TIMER_MIN_ADD_TEST=2 guaranteed (static) timers */
+	for (size_t i = 0; i < ARRAY_SIZE(timers); ++i) {
+		zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &timers[i]),
+			   "guaranteed allocation %zu failed", i);
+	}
+	for (size_t i = 0; i < ARRAY_SIZE(timers); ++i) {
+		zassert_ok(sys_timer_delete(timers[i]));
+	}
+}
+
+static void exhaustion_eagain(void)
+{
+	struct k_timer *timers[16];
+	struct k_timer *extra;
+	const size_t limit = MIN((size_t)CONFIG_SYS_TIMER_MAX, ARRAY_SIZE(timers));
+	size_t n;
+
+	for (n = 0; n < limit; ++n) {
+		zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &timers[n]),
+			   "allocation %zu of %d failed", n, CONFIG_SYS_TIMER_MAX);
+	}
+
+	zassert_equal(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &extra), -EAGAIN,
+		      "exhausted pool did not report -EAGAIN");
+
+	/* releasing one slot makes creation possible again */
+	zassert_ok(sys_timer_delete(timers[0]));
+	zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &timers[0]));
+
+	for (n = 0; n < limit; ++n) {
+		zassert_ok(sys_timer_delete(timers[n]));
+	}
+}
+
+static void delete_stale(void)
+{
+	struct k_timer not_from_pool;
+	struct k_timer *t;
+
+	k_timer_init(&not_from_pool, NULL, NULL);
+	zassert_equal(sys_timer_delete(&not_from_pool), -EINVAL,
+		      "deleting a non-pool timer did not fail");
+
+	zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &t));
+	zassert_ok(sys_timer_delete(t));
+	zassert_equal(sys_timer_delete(t), -EINVAL, "double delete did not fail");
+}
+
+ZTEST_USER(sys_timer, test_sys_timer_create)
+{
+	create_invalid();
+	create_armed_fires();
+	min_guaranteed();
+	if (CONFIG_SYS_TIMER_MAX <= 16) {
+		/* only meaningful with a small, bounded pool (static_only variant) */
+		exhaustion_eagain();
+	}
+}
+
+ZTEST_USER(sys_timer, test_sys_timer_delete)
+{
+	create_disarmed_delete();
+	delete_while_armed();
+
+	if (!IS_ENABLED(CONFIG_USERSPACE)) {
+		/* in user mode a stale handle faults in the syscall layer instead */
+		delete_stale();
+	}
+}
+
+#ifdef CONFIG_USERSPACE
+/* fault-harness exception to one-ZTEST-per-unit: this test point must oops */
+ZTEST_USER(sys_timer, test_sys_timer_delete_fault)
+{
+	struct k_timer *t;
+
+	zassert_ok(sys_timer_create(SYS_CLOCK_MONOTONIC, NULL, 0, NULL, NULL, &t));
+	zassert_ok(sys_timer_delete(t));
+
+	/* access was revoked at delete: any further use must fault, not corrupt */
+	ztest_set_fault_valid(true);
+	(void)k_timer_gettime(t, NULL, NULL);
+	ztest_test_fail(); /* unreachable: the syscall must oops */
+}
+#endif /* CONFIG_USERSPACE */
+
+ZTEST_SUITE(sys_timer, NULL, NULL, NULL, NULL, NULL);
diff --git a/tests/lib/sys_timer/testcase.yaml b/tests/lib/sys_timer/testcase.yaml
new file mode 100644
index 00000000000..75b926e6021
--- /dev/null
+++ b/tests/lib/sys_timer/testcase.yaml
@@ -0,0 +1,24 @@
+common:
+  tags:
+    - os
+    - timer
+tests:
+  libraries.sys_timer:
+    integration_platforms:
+      - native_sim
+      - qemu_x86
+      - qemu_cortex_m3
+  libraries.sys_timer.static_only:
+    integration_platforms:
+      - native_sim
+      - qemu_x86
+    extra_configs:
+      - CONFIG_SYS_TIMER_MAX=2
+  libraries.sys_timer.userspace:
+    filter: CONFIG_ARCH_HAS_USERSPACE
+    integration_platforms:
+      - qemu_x86
+      - qemu_cortex_m3
+    extra_configs:
+      - CONFIG_TEST_USERSPACE=y
+      - CONFIG_ZTEST_FATAL_HOOK=y
