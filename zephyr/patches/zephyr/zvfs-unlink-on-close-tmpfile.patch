diff --git a/include/zephyr/sys/zvfs_fs.h b/include/zephyr/sys/zvfs_fs.h
index f9e515f2d5c..728a8e78083 100644
--- a/include/zephyr/sys/zvfs_fs.h
+++ b/include/zephyr/sys/zvfs_fs.h
@@ -187,6 +187,23 @@ void zvfs_record_lock_drop_obj(void *obj);
  */
 __syscall int zvfs_unlink(const char *path);
 
+/**
+ * @brief Remove @a path on the final close of the description behind @a fd
+ *
+ * Marks the open file description so that @a path is unlinked when its last
+ * descriptor is closed, wherever that close happens - an explicit close or
+ * the descriptor sweep at process exit. The mark is consumed by the close.
+ *
+ * Requires @kconfig{CONFIG_ZVFS_UNLINK_ON_CLOSE}.
+ *
+ * @param fd File descriptor naming the description.
+ * @param path Absolute path to remove; copied, of bounded length.
+ *
+ * @return 0 on success, -1 with errno set on error (EBADF, EINVAL,
+ *         ENAMETOOLONG, ENOSPC when the mark table is full).
+ */
+__syscall int zvfs_unlink_on_close(int fd, const char *path);
+
 /**
  * @brief Rename file or directory, moving it if necessary.
  *
diff --git a/lib/libc/common/Kconfig b/lib/libc/common/Kconfig
index 991d74c1a71..a00540f54aa 100644
--- a/lib/libc/common/Kconfig
+++ b/lib/libc/common/Kconfig
@@ -194,10 +194,13 @@ config COMMON_LIBC_TMPFILE
 	bool "Common C library tmpfile()/tmpnam()"
 	depends on !SEMIHOST
 	depends on ZVFS && FILE_SYSTEM
+	select ZVFS_UNLINK_ON_CLOSE if ZVFS_FS
 	default y
 	help
 	  Common implementation of tmpfile() and tmpnam() for ZVFS-backed C
 	  libraries. tmpfile() opens a generated name under /tmp via zvfs_open()
-	  and binds a FILE to it with zvfs_libc_fdopen(). Deviation from ISO C:
-	  the file is not automatically removed when it is closed or on normal
-	  program termination.
+	  and binds a FILE to it with zvfs_libc_fdopen(). The file is removed on
+	  the final close of its descriptor - fclose(), or the descriptor sweep
+	  at process exit when process support (CONFIG_PROCESS) is enabled.
+	  Without process support a stream that is never closed leaves its file
+	  in place, as there is no process termination at which to remove it.
diff --git a/lib/libc/common/source/stdio/tmpfile.c b/lib/libc/common/source/stdio/tmpfile.c
index ea291b3703d..46f0e64a7f9 100644
--- a/lib/libc/common/source/stdio/tmpfile.c
+++ b/lib/libc/common/source/stdio/tmpfile.c
@@ -51,14 +51,18 @@ FILE *tmpfile(void)
 		return NULL;
 	}
 
+	/*
+	 * The file is removed on the final close of the description - via
+	 * fclose(), or via the descriptor sweep at process exit - as ISO C
+	 * requires. Best effort: with the mark table full the file is simply
+	 * left in place, as before.
+	 */
+	(void)zvfs_unlink_on_close(fd, name);
+
 	fp = zvfs_libc_fdopen(fd, "wb+");
 	if (fp == NULL) {
 		(void)zvfs_close(fd);
 	}
 
-	/*
-	 * Deviation from ISO C: the file is not automatically removed when
-	 * it is closed or on normal program termination.
-	 */
 	return fp;
 }
diff --git a/lib/os/zvfs/Kconfig b/lib/os/zvfs/Kconfig
index ea00807c7b0..6fd4dfc7c34 100644
--- a/lib/os/zvfs/Kconfig
+++ b/lib/os/zvfs/Kconfig
@@ -104,6 +104,24 @@ config ZVFS_FS_RECORD_LOCK_MAX
 	  Maximum POSIX record locks (fcntl F_SETLK/F_GETLK) stored per open file
 	  description backed by zvfs_fs_desc.
 
+config ZVFS_UNLINK_ON_CLOSE
+	bool
+	depends on ZVFS_FS
+	help
+	  Allow marking an open descriptor (zvfs_unlink_on_close()) so that its
+	  path is removed from the file system on the final close of its open
+	  file description, wherever that close happens - an explicit close or
+	  the descriptor sweep at process exit.
+
+config ZVFS_UNLINK_ON_CLOSE_MAX
+	int "Maximum descriptions marked unlink-on-close"
+	depends on ZVFS_UNLINK_ON_CLOSE
+	default 4
+	range 1 64
+	help
+	  Maximum open file descriptions concurrently marked for removal on
+	  their final close.
+
 config ZVFS_OPEN_ADD_SIZE_FS
 	int
 	depends on ZVFS_FS
diff --git a/lib/os/zvfs/zvfs_close.c b/lib/os/zvfs/zvfs_close.c
index 3ca83cadbbd..71387c62e97 100644
--- a/lib/os/zvfs/zvfs_close.c
+++ b/lib/os/zvfs/zvfs_close.c
@@ -4,14 +4,106 @@
  * SPDX-License-Identifier: Apache-2.0
  */
 
+#include <string.h>
+
 #include <zephyr/sys/fdtable.h>
 #include <zephyr/sys/internal/fdtable_priv.h>
+#include <zephyr/sys/zvfs_fs.h>
 #include <zephyr/sys/zvfs_libc.h>
 #include <zephyr/internal/syscall_handler.h>
 
+#ifdef CONFIG_ZVFS_UNLINK_ON_CLOSE
+/* longest absolute path a marked description may carry */
+#define ZVFS_UOC_PATH_LEN 16
+
+/* descriptions whose path is removed on the final close, wherever it happens */
+static struct {
+	struct fd_entry *desc;
+	char path[ZVFS_UOC_PATH_LEN];
+} zvfs_uoc[CONFIG_ZVFS_UNLINK_ON_CLOSE_MAX];
+static struct k_spinlock zvfs_uoc_lock;
+
+int z_impl_zvfs_unlink_on_close(int fd, const char *path)
+{
+	int ret = -ENOSPC;
+	struct fd_entry *entry = zvfs_fd_entry_get(fd);
+
+	if (entry == NULL) {
+		errno = EBADF;
+		return -1;
+	}
+	if ((path == NULL) || (path[0] != '/')) {
+		errno = EINVAL;
+		return -1;
+	}
+	if (strlen(path) >= ZVFS_UOC_PATH_LEN) {
+		errno = ENAMETOOLONG;
+		return -1;
+	}
+
+	K_SPINLOCK(&zvfs_uoc_lock) {
+		ARRAY_FOR_EACH(zvfs_uoc, i) {
+			if (zvfs_uoc[i].desc == NULL) {
+				zvfs_uoc[i].desc = entry;
+				strcpy(zvfs_uoc[i].path, path);
+				ret = 0;
+				/* K_SPINLOCK_BREAK would resume this inner loop */
+				break;
+			}
+		}
+	}
+
+	if (ret < 0) {
+		errno = -ret;
+		return -1;
+	}
+
+	return 0;
+}
+
+#ifdef CONFIG_USERSPACE
+static int z_vrfy_zvfs_unlink_on_close(int fd, const char *path)
+{
+	char kpath[ZVFS_UOC_PATH_LEN];
+
+	if (zvfs_fd_syscall_verify(fd) == NULL) {
+		errno = EBADF;
+		return -1;
+	}
+	if (k_usermode_string_copy(kpath, path, sizeof(kpath)) != 0) {
+		errno = ENAMETOOLONG;
+		return -1;
+	}
+
+	return z_impl_zvfs_unlink_on_close(fd, kpath);
+}
+
+#include <zephyr/syscalls/zvfs_unlink_on_close_mrsh.c>
+#endif /* CONFIG_USERSPACE */
+
+/* consume the mark for @a entry, if any, copying its path into @a path */
+static void zvfs_uoc_consume(struct fd_entry *entry, char *path)
+{
+	path[0] = '\0';
+	K_SPINLOCK(&zvfs_uoc_lock) {
+		ARRAY_FOR_EACH(zvfs_uoc, i) {
+			if (zvfs_uoc[i].desc == entry) {
+				strcpy(path, zvfs_uoc[i].path);
+				zvfs_uoc[i].desc = NULL;
+				break;
+			}
+		}
+	}
+}
+#endif /* CONFIG_ZVFS_UNLINK_ON_CLOSE */
+
 int z_impl_zvfs_close(int fd)
 {
 	int res = 0;
+	bool final = false;
+#ifdef CONFIG_ZVFS_UNLINK_ON_CLOSE
+	char upath[ZVFS_UOC_PATH_LEN] = "";
+#endif /* CONFIG_ZVFS_UNLINK_ON_CLOSE */
 	struct fd_entry *entry = zvfs_fd_entry_get(fd);
 
 	if (entry == NULL) {
@@ -21,14 +113,29 @@ int z_impl_zvfs_close(int fd)
 	(void)k_mutex_lock(&entry->lock, K_FOREVER);
 	if (entry->vtable->close != NULL && entry->obj != NULL &&
 	    zvfs_fd_obj_refcount(entry->obj) <= 1) {
+		final = true;
 		if (entry->mode & ZVFS_MODE_IFSOCK) {
 			res = entry->vtable->close2(entry->obj, fd);
 		} else {
 			res = entry->vtable->close(entry->obj);
 		}
 	}
+#ifdef CONFIG_ZVFS_UNLINK_ON_CLOSE
+	/* consumed before the entry can be recycled; removed after unlock */
+	if (final) {
+		zvfs_uoc_consume(entry, upath);
+	}
+#endif /* CONFIG_ZVFS_UNLINK_ON_CLOSE */
 	k_mutex_unlock(&entry->lock);
 
+#ifdef CONFIG_ZVFS_UNLINK_ON_CLOSE
+	if (upath[0] != '\0') {
+		(void)zvfs_unlink(upath);
+	}
+#else
+	ARG_UNUSED(final);
+#endif /* CONFIG_ZVFS_UNLINK_ON_CLOSE */
+
 #if defined(CONFIG_ZVFS_LIBC_FILE)
 	zvfs_libc_file_free(zvfs_libc_file_from_fd(fd));
 #endif
