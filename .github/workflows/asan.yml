name: ASAN

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  asan:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up workspace
        id: workspace
        uses: ./.github/actions/setup-workspace
        with:
          toolchains: x86_64-zephyr-elf
          ccache-key-prefix: asan

      - name: Run Tests with Twister (ASAN)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          ccache --zero-stats
          "${{ github.workspace }}/${{ env.MANIFEST_PATH }}/scripts/ci/asan.sh"
          ccache --show-stats

      - name: Upload ASAN summary
        if: always() && !cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: asan-summary
          path: |
            ${{ github.workspace }}/asan-summary.json
            ${{ github.workspace }}/asan-findings.json
          if-no-files-found: error

      - name: Save workspace ccache
        if: always() && !cancelled()
        uses: ./.github/actions/save-workspace-ccache
        with:
          cache-primary-key: ${{ steps.workspace.outputs.ccache-primary-key }}
          save: true

  asan-summary-publish:
    if: >-
      always() && !cancelled() &&
      needs.asan.result != 'cancelled' &&
      needs.asan.result != 'skipped'
    needs: [asan]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout main
        uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0

      - name: Download ASAN summary
        uses: actions/download-artifact@v4
        with:
          name: asan-summary
          path: ${{ runner.temp }}/summary

      - name: Check for summary changes
        id: changes
        shell: bash
        run: |
          set -euo pipefail
          fresh="${RUNNER_TEMP}/summary/asan-summary.json"
          dest=doc/metrics/asan-summary.json.sh
          # Ignore provenance-only churn (timestamp/run URL/commit) so an
          # unchanged result set does not open a no-op PR. The committed
          # snapshot is a jsonball shellball; unpack it for the comparison.
          norm() {
            jq -S 'del(.provenance.created_at, .provenance.workflow_run_url,
                       .provenance.commit, .provenance.commit_short)'
          }
          if [ ! -f "$fresh" ]; then
            echo "summary_changed=false" >> "$GITHUB_OUTPUT"
            echo "No fresh ASAN summary; skipping publish PR"
            exit 0
          fi
          if [ -f "$dest" ] && \
             diff <(python3 scripts/ci/jsonball.py unpack "$dest" | norm) \
                  <(norm < "$fresh") >/dev/null; then
            echo "summary_changed=false" >> "$GITHUB_OUTPUT"
            echo "ASAN summary unchanged; skipping publish PR"
            exit 0
          fi
          echo "summary_changed=true" >> "$GITHUB_OUTPUT"
          python3 scripts/ci/jsonball.py pack "$fresh" -o "$dest"

      - name: Create or update ASAN summary PR
        if: steps.changes.outputs.summary_changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: automation/asan-summary
          sign-commits: true
          add-paths: |
            doc/metrics/asan-summary.json.sh
          title: "doc: refresh nightly ASAN summary"
          commit-message: "doc: refresh nightly ASAN summary"
          body: |
            Update the committed AddressSanitizer results summary from the
            ASAN workflow (GitHub Actions run ${{ github.run_id }}).

            This snapshot drives the ASAN badges in the docs.
          labels: |
            automation
            twister
